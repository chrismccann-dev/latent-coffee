# Kickoff brief: `concentration` modifier ship + Concentrated Pour-Over archival

*Synthesis-seam brief from the 2026-08-31 item-56 grill (record: [docs/sprints/grilling-2026-08-31-concentrated-pourover.md](docs/sprints/grilling-2026-08-31-concentrated-pourover.md)). All decisions below are Chris-ratified at the grill - this is planned-execution work; the autonomy rule applies.*

**Problem:** the Concentrated Pour-Over style is locked canon (CONTEXT-brewing headword, 2026-08-31) but has no write path - the `modifiers` jsonb validates against five canonical types, so the style's three reproduced winners cannot be archived, and the AeroPress method card is homeless.

**Goal:** ship the sixth canonical modifier type `concentration`, land the AeroPress immersion-press method card, and verify end-to-end by push_brew'ing the three RP8 winners as the sprint's final step.

## Scope

**In:**

1. **Registry:** add `concentration` as the sixth canonical modifier type in `lib/extraction-modifiers.ts` (mirror wherever the five types are enumerated - grep for `role_based_pulse` to find every surface). Value shape: follow the existing type pattern; free-text scope field carries the mechanism/form note (e.g. "valve form", "immersion-press form"). One value for now (`concentrated`); the headword's promotion trigger governs any future expansion.
2. **MCP:** `push_brew` / `patch_brew` input schema + Tool description updated so the new type validates and is discoverable; run `npm run check:mcp` and confirm the catalog delta; fresh-session catalog note applies (feedback_mcp_catalog_cache_fresh_session).
3. **Cross-system audit (six-actor chain, claude.ai actors retired):** `lib/types.ts` if the modifier type is typed there; any UI surface that renders modifier types by name (grep the modifier type literals in `app/` + `components/`); CONTEXT-brewing already updated at the grill (Modifier entry says "Six canonical types"); no schema migration expected (jsonb - confirm no CHECK constraint exists before assuming).
4. **Method card:** AeroPress immersion-press method card in the **brewing-assistant cluster** (per grill decision f), cross-linked from the equipment-expert AeroPress doc if one exists. Content source: T5 doc § Final output + the cup-1 method-lock amendment (press ≈ 50 s, ~2:00 TOTAL - NOT the scoping doc's mis-transcribed "~2-min press"; see RP8-N17). Register in `lib/mcp/docs.ts` + `npm run check:mcp-bundle` (feedback_register_reference_docs_in_mcp).
5. **Verification = the three push_brews** (exercise-flows-end-to-end): each carries its coffee's existing extraction strategy on Axis 1, the `concentration` modifier on Axis 2, and "Concentrated Pour-Over" vocabulary in `strategy_notes`. These are SECOND archived recipes per coffee - do not touch or supersede the normal-ratio reference brews. Recipes verbatim from the track docs:

   - **Motta (RP8-T2, reproduced Δ0.01% TDS):** Jeferson Motta anaerobic washed Gesha · 1:10 · 15 g / 150 g · EG-1 5.7 · 98°C · Kalita Wave Tsubame 155 + xBloom paper · plain PA tap · bloom 45 g firm swirl 45 s → spiral to ~100 g ~0:45 → early center pour to 150 g ~1:10-1:15, restrained mains · TDS 1.77-1.78%, 1.46× the 1:15 control · profile: punchy candied-strawberry front, mandarin in syrupy body, dark-tea tannin thread (standing Motta character), cool peak. Strategy: Full Expression. Source: `docs/research-projects/concentrated-pourover-t2-motta-abbreviated-ladder.md` § Final output.
   - **Loud Giants (RP8-T3, valve form; reproduced-marginal Δ0.06%, second-coffee confirmed T4):** Picolot Loud Giants Pacamara natural · SWORKS Bottomless + xBloom paper · 1:10 · 15 g / 150 g · EG-1 6.1 (control's own grind) · 95°C kettle-off-after-bloom · plain tap · bloom 45 g Dial 0 closed hold 40-45 s → Dial 5 restrained mains to ~100 g → early final to 150 g ~1:10-1:15 at Dial 5 → open Dial 7 at ~1:45 (operator hindsight: 1:45 preferable to 2:00) → drawdown ~2:00-2:05 · TDS 1.73% (repeat 1.79%), 1.57-1.63× control · profile: creamy concentrated texture, sour-candy watermelon + cherry + blackberry front, back cleanly shortened, tannin thread hot that clears cool, cool peak. Strategy: Full Expression. Source: `docs/research-projects/concentrated-pourover-t3-loud-giants-valve.md` § Final output.
   - **Project One Peach (RP8-T5, press form; profile-reproduced, TDS qualified pass +0.02 yield-explained):** Moonwake Project One Peach Catimor yeast-anaerobic natural (Yunnan) · AeroPress Premium inverted, paper micro-filter, no flow-control cap · 1:10 · 15 g / 150 g · EG-1 7.0 · 89°C · plain tap · stir 2 (one N-S, one E-W, saturation only) → steep to 1:00 → flip ~10 s → very slow press ~50 s → ~2:00-2:05 total · yield ~115-124 g · TDS 1.13-1.20% · zero astringency · profile: fruit punch / peach candy / melon / jasmine milk-tea · note in the row: co-equal serving form = 1:9 concentrate + ~20% bypass to ~1:10-strength (operator preference in direct comparison; "no dilution past optimum"). Strategy: per the coffee's archived control (verify via query_brews at execution; do not guess). Source: `docs/research-projects/concentrated-pourover-t5-aeropress-low-pressure.md` § Final output + § Equipment.

**Out:** any third-axis schema work (promotion trigger not met); staged-scoring/TDS fields (grill decision g: none); Barbie Beans Exp B archival (protocol-doc-only, coffee extinct); the sworks.md rename + CONTEXT-brewing headword (already landed in the grill PR); routine-construction / competition work.

**Entry surface:** fresh Claude Code session, paste this brief ("execute the concentrated pour-over modifier kickoff brief").

**Files likely to touch:** `lib/extraction-modifiers.ts`, `lib/mcp/*` (push_brew/patch_brew schema + descriptions), possibly `lib/types.ts`, brewing-assistant cluster (new method card), `lib/mcp/docs.ts`, `next.config.js` (bundle glob check).

**Verification plan:** `npm run check:mcp` count delta → `npx tsc --noEmit` in main repo → `check:mcp-bundle` → the three push_brews via MCP in a fresh session (catalog cache) → confirm rows render on /brews with the modifier visible → roadmap/shipped.md currency in the same PR.

**Open questions:** none - all interpretive calls were resolved at the grill. (If push_brew's modifier validation turns out to live somewhere unexpected, follow the write path in `lib/brew-import.ts` rather than widening scope.)
