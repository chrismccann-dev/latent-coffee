# Doc-size tripwire registry

**Canonical surface for every doc-size tripwire across all three load surfaces** (Claude Code, claude.ai/projects/brewing, claude.ai/projects/roasting). When a doc crosses its tripwire, fire a **post-tripwire pruning exercise** (the manual operator-led process defined in [Pattern J, ADR-0013](docs/adr/0013-self-improvement-primitives.md) + scoped in [docs/features/doc-pruning-mechanism-brainstorm-2026-06-03.md](docs/features/doc-pruning-mechanism-brainstorm-2026-06-03.md)) — not an autonomous prune. Pattern J was promoted from placeholder to **defined** on 2026-06-03 (light formalization) once all five pruning shapes had a worked example: the **trigger** half is automated (`npm run check:doc-sizes` + daily CI cron); the **response** half (the interpretive prune) stays operator-led by design.

Created 2026-06-03 (Cluster B doc-pruning brainstorm). This table is the single canonical home for tripwire numbers; the prose tripwire in [CLAUDE.md § Standing tripwires](CLAUDE.md) and the cluster tiers in [ADR-0014](docs/adr/0014-pattern-f-threshold-tiers.md) point here rather than re-stating numbers.

## Why this exists

The ADD-shaped self-improvement patterns (A-E in ADR-0013) compound substrate monotonically. Size tripwires catch *gross* bloat, but they only catch one failure mode: a doc growing past a byte budget. They do **not** catch a doc that sits *under* its cap but is full of stale / over-detailed / superseded sections that surface at retrieval time without earning their keep. That second failure mode is what the manual pruning exercise judges; the tripwire is only the *trigger* that schedules the exercise.

## The binding constraint (per ADR-0014)

The limit on doc size is not disk, not read latency, not Claude Code working memory — it is **claude.ai's session-level context window during a live workflow**. claude.ai's MCP client reads docs in full via `read_doc` (no streaming); only `read_doc_section` against an anchor pulls a subset. **Loading profile therefore changes the real cost**, and the tiers below are loading-profile-aware:

- **full-read** docs cost their entire size every session that touches the surface → tighter caps.
- **section-read** docs cost only the pulled anchor → a larger total file is tolerable.

This is why the entry prompts (full-read, 40 KB cap) are capped tighter than the CONTEXT-* glossaries (section-read, 120 KB cap) even though the prompts are smaller files.

## Tripwire policy (static — caps + tiers)

This table is the static **policy** (what's tracked + the caps + loading profile). Live sizes are NOT hand-maintained here — they drifted 3× in 3 days when they were. The live numbers live in the generated block below (`npm run check:doc-sizes`), in exactly one place, never copied by hand.

| Tier | Doc(s) | Cap | Loading profile |
|---|---|---|---|
| **Tier 1 · root + CONTEXT** | CLAUDE.md · PRODUCT.md · docs/product/{roadmap,issues}.md · CONTEXT-{roasting,brewing,shared,taste}.md | 120 KB (approaching 96) | full (CLAUDE) / consulted (PRODUCT) / **section-read** (CONTEXT, claude.ai) |
| **Tier 1 · prompts** | docs/prompts/*.md | **40 KB** (approaching 32) | **full-read** · claude.ai session start |
| **Tier 1 · skills clusters** | docs/skills/*/ | per [ADR-0014](docs/adr/0014-pattern-f-threshold-tiers.md) class tiers (Historian 250/80 · Archivist 200/60 · Equipment-Reference 150/60 · Workflow 100/60 · Coordinator 80/40 · CCIL 150/60) | full via read_doc · claude.ai workflow |
| **Tier 2 · monitor** | docs/architecture/* · docs/reference/* · ARBITER.md · SYNC_V2.md | no hard cap (flag > 150 KB) | on-demand (not every-session) |
| **Auto-memory** | MEMORY.md | 25 KB / 200 lines | governed by the consolidate-memory skill (out of scope for check:doc-sizes) |
| **Excluded** | docs/sprints/* · docs/roasting/archive.md · docs/features/* · docs/taxonomies/* · docs/adr/* · README.md | — | archives + non-session docs: **meant to grow; capping them fights the mechanism** |

### Live sizes

`npm run check:doc-sizes` computes live sizes vs. the caps above and exits non-zero if any Tier-1 surface is over. `-- --write` regenerates this block:

<!-- BEGIN check:doc-sizes (generated — do not edit by hand; run `npm run check:doc-sizes -- --write`) -->

_Live sizes — regenerated by `npm run check:doc-sizes -- --write`. Do not hand-edit; that is exactly what drifted._

**Tier 1 · root + CONTEXT**

| | Doc | Size | Cap | Loading profile |
|---|---|---|---|---|
| ✅ | CLAUDE.md | 48.6 KB | 120 KB | full · every Claude Code session |
| ✅ | PRODUCT.md | 24.2 KB | 120 KB | consulted (product-system index) |
| ✅ | docs/product/roadmap.md | 26.5 KB | 120 KB | consulted (roadmap growth driver) |
| ✅ | docs/product/issues.md | 11.5 KB | 120 KB | consulted |
| ⚠️ | CONTEXT-roasting.md | 104.4 KB | 120 KB | section-read · claude.ai roasting |
| ✅ | CONTEXT-brewing.md | 61.1 KB | 120 KB | section-read · claude.ai brewing |
| ✅ | CONTEXT-shared.md | 52.4 KB | 120 KB | section-read · both |
| ✅ | CONTEXT-taste.md | 19.6 KB | 120 KB | section-read · claude.ai taste |

**Tier 1 · prompts (40KB full-read)**

| | Doc | Size | Cap | Loading profile |
|---|---|---|---|---|
| ✅ | docs/prompts/bundled-brewing-completion.md | 15.9 KB | 40 KB | full-read · claude.ai |
| ⚠️ | docs/prompts/close-lot.md | 32.7 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/log-cupping-stage0-migration.md | 6.9 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/log-cupping.md | 27.4 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/log-roast.md | 29.2 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/one-shot-closeout.md | 27.2 KB | 40 KB | full-read · claude.ai |
| ⚠️ | docs/prompts/one-shot.md | 34.8 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/peer-variant-completion.md | 7.8 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/simulated-pourover.md | 4.8 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/start-brew.md | 7.3 KB | 40 KB | full-read · claude.ai |
| ✅ | docs/prompts/start-lot.md | 15.9 KB | 40 KB | full-read · claude.ai |

**Tier 1 · skills clusters (ADR-0014)**

| | Doc | Size | Cap | Loading profile |
|---|---|---|---|---|
| ✅ | docs/skills/brew-recorder/  [Workflow] | 7.9 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/brewing-assistant/  [Workflow] | 70.6 KB | 100 KB | claude.ai workflow |
| ⚠️ | ↳ largest doc: docs/skills/brewing-assistant/cluster/operational-guide.md | 52.6 KB | 60 KB | single-doc cap 60KB |
| ⚠️ | docs/skills/brewing-equipment-expert/  [EquipmentReference] | 121.5 KB | 150 KB | claude.ai workflow |
| ✅ | docs/skills/brewing-historian/  [Historian] | 182.8 KB | 250 KB | claude.ai workflow |
| ✅ | docs/skills/ccil/  [CCIL] | 35.7 KB | 150 KB | claude.ai workflow |
| ✅ | docs/skills/close-lot-specialist/  [Workflow] | 12.8 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/coordinator/  [Coordinator] | 63.6 KB | 80 KB | claude.ai workflow |
| ✅ | docs/skills/cupping-specialist/  [Workflow] | 33.3 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/peer-learning-roasting-archivist/  [Archivist] | 31.8 KB | 200 KB | claude.ai workflow |
| ✅ | docs/skills/research-assistant/  [Workflow] | 10.7 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/research-coordinator/  [EquipmentReference] | 113.8 KB | 150 KB | claude.ai workflow |
| ✅ | docs/skills/roast-recorder/  [Workflow] | 8.6 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/roasting-assistant/  [Workflow] | 24.5 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/roasting-coordinator/  [Coordinator] | 59.1 KB | 80 KB | claude.ai workflow |
| ⚠️ | docs/skills/roasting-historian/  [Historian] | 208.1 KB | 250 KB | claude.ai workflow |
| ✅ | docs/skills/roest-api-worker/  [Workflow] | 6.7 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/roest-knowledge/  [EquipmentReference] | 61.4 KB | 150 KB | claude.ai workflow |
| ✅ | docs/skills/sourcing-workflow-planner/  [Workflow] | 5.9 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/v-set-assistant/  [Workflow] | 6.7 KB | 100 KB | claude.ai workflow |
| ✅ | docs/skills/wbc-brewing-archivist/  [Archivist] | 121.4 KB | 200 KB | claude.ai workflow |
| ✅ | docs/skills/wbc-roasting-archivist/  [Archivist] | 92.2 KB | 200 KB | claude.ai workflow |
| ⚠️ | ↳ largest doc: docs/skills/wbc-roasting-archivist/cluster/sourcing/strategy.md | 48.6 KB | 60 KB | single-doc cap 60KB |

**Tier 2 · monitor (on-demand)**

| | Doc | Size | Cap | Loading profile |
|---|---|---|---|---|
| ✅ | docs/architecture/data-model.md | 17.4 KB | monitor (flag >150) | on-demand · touching a column |
| ✅ | docs/architecture/page-ia.md | 93.2 KB | monitor (flag >150) | on-demand · touching a surface |
| ✅ | docs/architecture/registries.md | 21.7 KB | monitor (flag >150) | on-demand · touching a registry |
| ✅ | docs/design-system.md | 37.6 KB | monitor (flag >150) | on-demand · UI work |
| ✅ | ARBITER.md | 70.8 KB | monitor (flag >150) | on-demand · process pending arbitration |
| ✅ | SYNC_V2.md | 24.0 KB | monitor (flag >150) | on-demand · MCP work |

<!-- END check:doc-sizes -->

### Prompt cap rationale

The 40 KB prompt cap is deliberately tighter than the file sizes suggest. A 49 KB prompt loaded in full every cupping session costs more context per session than the 115 KB CONTEXT-roasting glossary, which is section-read. The cap may loosen after the manual pruning process gives us lived evidence of what prompt content is genuinely every-session vs. extractable — revisit after 2-3 prompt-side pruning cases.

## Tripwire behavior

- **Over cap** → schedule a post-tripwire pruning exercise (manual, operator + Claude Code, per the scoping doc). The doc keeps working in the meantime; the tripwire is a scheduler, not a hard stop.
- **Approaching (within 20% of cap)** → surface as a watch-item at the next grilling / arbitration session.
- The pruning exercise produces a **structured handoff doc** in [docs/sprints/pruning-cases/](docs/sprints/pruning-cases/) so lessons aggregate across sessions toward eventual systematization.

## Live queue

Human-curated "what to prune next, and why." Live sizes come from the generated block above (`npm run check:doc-sizes`). First populated 2026-06-03; the script's first run surfaced 3 over-cap clusters the ADR-0014 hand-audit had missed. **As of 2026-06-04 the over-cap backlog is fully drained — no Tier-1 surface is over cap.**

**Watch (approaching, no action):**
1. **docs/prompts/one-shot.md (33.9 / 40 KB)** · **docs/skills/brewing-assistant/cluster/operational-guide.md (51.0 / 60, post case-013)** · **CONTEXT-roasting.md (108.1 / 120)** — ⚠️ approaching; the script flags them when touched. one-shot.md is the next prompt-side data point for the "does 40 KB hold?" question.

Cleared:
- docs/skills/brewing-assistant/cluster/operational-guide.md - **pruned 2026-07-08** (54.2 → 51.0 KB / 60 cap; watcher stays in the ≥80% approaching band - every ratified cut executed, the residual IS the considered-and-kept list: Axis-1 strategy table, pivot heuristics, iteration ladder, Step 4 write-contract skeleton), [pruning case 013](docs/sprints/pruning-cases/013-operational-guide-consolidate.md). `consolidate` + provenance-delete, no split: dark-roast override consolidated to rule-stated-once + 3 routes + purchased-lot fallback (grill stamps stripped); Step 4 stale canonical enumerations (15-vs-16 signature-method drift, filter alias history) pointer-ized to `read_canonical` + taxonomy/registry owners, write-contract enums kept inline; apex default + whole-arc rationale reduced to CONTEXT-taste pointers; v8.4/4c changelog tails deleted (owned by bundled-brewing-completion.md); strategy-table corpus counts → brewing-historian pointers; CCIL promotion gate re-homed verbatim to cross-coffee-insights.md § Open Questions. h2 load anchors byte-identical (verified by heading-set diff; `/brew` + bundled-brewing-completion pin them). Chris ratified 2026-07-08. Case-008 2x-discount heuristic re-confirmed: 8.5 KB gross estimate → 3.3 KB actual with kept material respected.
- ~~docs/skills/wbc-roasting-archivist/cluster/sourcing/strategy.md~~ - **pruned 2026-07-08** (48.6 → 37.2 KB / 60 cap; cluster 92.2 → 89.5 / 200; watcher flag cleared), [pruning case 012](docs/sprints/pruning-cases/012-sourcing-strategy-inventory-rehome.md). Preemptive + refresh-burden kill: `re-home` + `split` (case-009 growth-vector isolation) of the inventory snapshot - live per-lot state → the DB (`list_green_inventory`, already authoritative by the doc's own admission), lane-lens + the refresh-on-inventory-change mandate → new sibling [inventory-snapshot.md](docs/skills/wbc-roasting-archivist/cluster/sourcing/inventory-snapshot.md) (the fast half re-trips, if ever, on its own); plus § 7 roster → producers.md pointer, History delete, palate-section apex-voice consolidate (supersession blockquote absorbed), 6 stale in-inventory sentence cuts. § 0 byte-identical. The residual is all slow framework - this doc should never re-trip. Chris ratified 2026-07-08.
- ~~docs/skills/roasting-historian/ cluster (Phase 1)~~ - **pruned 2026-07-08** (cluster 208.1 → ~183 KB / 250 cap), [pruning case 011](docs/sprints/pruning-cases/011-roasting-historian-active-lots-graduation.md). PARTIAL scope: the settled-half active-lots graduation hygiene only - 4 closed-lot active-lots docs stubbed to the Chris-locked ~6-line close-out convention (Bukure / Gesha Clouds / Higuito / Red Plum) + the missing `learnings/redplum-cas-2026.md` capsule created (the only closed lot without one; PR #508 routed its close-out only to the CCIL) + SKILL.md ship-note sediment deleted + stale-status router fixes + 2 resolved open-questions entries deleted per the doc's own delete-on-resolve rule. **Phase 2 (CCIL re-scope + End-of-Lot routing table, ~-22 KB) DEFERRED until AN10 closes** - the CCIL is AN10's named append target. Chris ratified 2026-07-08.
- ~~docs/skills/brewing-equipment-expert/ cluster (second pass)~~ - **pruned 2026-07-08** (cluster 147.0 → 114.0 KB; `operational-reference.md` 43.1 → 22.8 KB; `brewers.md` 19.8 → 9.3 KB), [pruning case 010](docs/sprints/pruning-cases/010-brewing-equipment-expert-second-pass.md). Preemptive at 98% of cap after the RP6 water arc (+25.5 KB/3 weeks). `consolidate`-to-pointer on operational-reference.md's four duplicated blocks (Filter System table → filters.md; Valve Position → sworks.md; Grinder → grinder-eg1.md; Example Outputs cut, canonical format in operational-guide.md Step 2/4) + `split` on the brewers.md ownership axis (13 owned stay; 34 not-owned → `docs/taxonomies/brewers-not-owned-archive.md`, exact case-004 template; `lib/brewer-registry.ts` intact as the full 47-entry validator). Every cut byte already had a newer, measured canonical owner inside the same cluster.
- ~~docs/skills/wbc-brewing-archivist/cluster/wbc-recipes.md~~ — **split 2026-06-29** (73.8 → 35.7 KB; per-recipe rows → new `wbc-recipes-by-family.md` 40.1 KB), [pruning case 009](docs/sprints/pruning-cases/009-wbc-recipes.md). First over-cap fire on an **inherently-growing reference corpus** (gains ~50 routines per WBC year). `split` along the doc's own stated taxonomy↔per-recipe seam: parent keeps distribution + findings + 65 subtype definitions (slow-growing), sibling holds the 154 competitor rows (the fast-growing half). Isolates growth pressure in the sibling so the taxonomy half never re-trips.
- ~~docs/skills/brewing-historian/cluster/patterns/cross-coffee-insights.md~~ — **re-architected 2026-06-04** (80.0 → 44 KB; cluster 157.2 → 135.5), [pruning case 007a](docs/sprints/pruning-cases/007-cross-coffee-insights.md) + 007b. **A new shape: `re-home` / `re-scope`** — not the scoped `split`. Tightened the CCIL's *job* (cross-anchor learnings only) and re-homed single-axis detail OUT to capsules behind routers; 007b drained the cross-cluster holding block (dedupe + back-pointer rewire). First re-scope; first cross-cluster re-home.
- ~~docs/prompts/log-cupping.md~~ — **pruned 2026-06-04** (49.3 → 24.0 KB), [pruning case 006](docs/sprints/pruning-cases/006-log-cupping.md). `consolidate`-to-pointer (duplicated CONTEXT-roasting vocab → pointers, behavior byte-for-byte intact) + `archive` (dead STAGE 0 pre-rewrite migration → sibling break-glass doc; DB-confirmed zero live pre-rewrite lots). First prompt-shape case; confirmed the **40 KB cap holds** (landed at 24 KB with full behavior).
- ~~docs/skills/brewing-equipment-expert/ cluster + filters.md~~ — **pruned 2026-06-03** (cluster 157.8 → 109.9 KB; `filters.md` 70.4 → 22.4 KB), [pruning case 004](docs/sprints/pruning-cases/004-filters-md.md). `split` on the ownership axis: owned (23) stay in `filters.md`, not-owned (44) → `docs/taxonomies/filters-not-owned-archive.md`; research blocks → appendix pointer. `lib/filter-registry.ts` kept intact as the full 67-entry validator. **Cleared the cluster cap too, so case 005 (cluster decomposition, Pattern F) is MOOT — never needed a session.** First `split` on an ownership axis + first split shaped by a validation-mirror constraint.
- ~~PRODUCT.md~~ — **refactored + split 2026-06-03** (127.3 → 20.9 KB), [pruning case 003](docs/sprints/pruning-cases/003-product-md.md). Operator-led prototype + Claude Code synthesis: roadmap → `docs/product/roadmap.md`, issues → `docs/product/issues.md`, Lessons Learned → `docs/sprints/lessons-learned-archive.md`, closed roadmap detail ceased-duplication (already in shipped.md). **First standalone `archive` worked example — all five shapes now covered.**
- ~~CONTEXT-roasting.md~~ — **pruned 2026-06-03** (115.8 → 108.2 KB), [pruning case 002](docs/sprints/pruning-cases/002-context-roasting.md). Operator-led reorganization (cluster regrouping + agent navigation scaffolding) + Claude Code prune (`consolidate` + `delete` + one `extract` of FC operational detail to fc-marking.md). First worked examples of `consolidate` and `delete`.

## Maintenance

Live sizes are **script-generated, not hand-maintained** (the hand-maintained column drifted 3× in 3 days). Cadence for `npm run check:doc-sizes`:
- **claude.ai grilling review** (the recurring 3-part protocol) + **arbiter sessions** — run `-- --write` to refresh the generated block.
- **Daily CI cron** (`.github/workflows/doc-sizes-check.yml`, mirrors `migrations-check`) — the catch-all; exits non-zero when a Tier-1 surface creeps over cap, so a doc that drifts over gets caught within a day.
- **Any sprint that materially edits a tracked doc** — run it before the cross-system audit.

Only the **static policy** above (caps / tiers / loading profile / scope) is hand-edited — when a new tracked surface appears or a cap changes. The `## Live queue` narrative ("what to prune next, and why") stays human-curated; the sizes that feed it come from the generated block.
