# Shipped Sprints

Compact reverse-chronological index of every shipped sprint. Per-sprint retrospectives with full narrative + lessons live in `memory/project_*.md` (one file per sprint). Migrations live in `supabase/migrations/*.sql` — each filename + header comment documents what changed.

Moved out of PRODUCT.md as part of the May 2026 doc cleanup PR (when PRODUCT.md crossed the 120KB tripwire). The pointer remains at PRODUCT.md § Where shipped sprint history lives.

| Date | Sprint | Landmark |
|---|---|---|
| 2026-05-25 | **Sprint 3.3 — Auto-supersede paired sprint (explicit `supersede_ids` + implicit-intent arbiter detection + MCP tool-list cache visibility)** | First sprint off the post-architecture Queued list; predecessor was the load-orchestration arc (PRs [#250](https://github.com/chrismccann-dev/latent-coffee/pull/250)-[#253](https://github.com/chrismccann-dev/latent-coffee/pull/253) merged 2026-05-25). 3 items, ~2-3h. **#R88 — `supersede_ids` param on `propose_doc_changes`**: [lib/mcp/propose-doc-changes.ts](lib/mcp/propose-doc-changes.ts) gains an optional `supersede_ids: string[]` parameter. Existing `(target_doc, section_anchor)` overlap auto-supersede fires first; the new parameter is additive so claude.ai can explicitly flip older proposals when the corrected fix lives at a different anchor or different `target_doc`. Same RLS scope + `status='pending'` filter as the per-citation supersede UPDATE; idempotent on already-superseded UUIDs. Older row's `notes` reads `Explicitly superseded by <new_id> on <timestamp> via supersede_ids` so explicit-vs-overlap supersessions are distinguishable in `SELECT notes` audits. **#R89 — implicit-intent supersession detection (arbiter-walked)**: new subsection under [ARBITER.md § Auto-supersede semantics](ARBITER.md#auto-supersede-semantics). Two scan patterns: (a) summary-parse for `CORRECTED:` / `SUPERSEDES:` / `(supersedes <UUID>)` / `(replaces <UUID>)` variants; (b) source-id-stem detection for V2-vs-V1 proposals on the same lot/coffee (matching `source.kind` + canonical-stem-overlap on `source.id`). Both produce SUPERSESSION CANDIDATES the arbiter confirms before flipping (not auto-applied); SQL recipes + worked examples included for both. Explicit `supersede_ids` is positioned as the preferred channel; implicit detection is the safety net. **#R90 — MCP tool-list cache visibility**: [scripts/check-mcp-tools.ts](scripts/check-mcp-tools.ts) now prints each registered tool's source-file mtime, a `TOTAL: N tools registered` summary line, and `lib/mcp/server.ts last modified: YYYY-MM-DD` so the count delta is visible at a glance after a registration edit. CLAUDE.md § Dev notes adds a one-liner pointing at `npm run check:mcp` + reminder that claude.ai's MCP client may cache its tool catalog (force a fresh session or explicit `tool_search(query: "<name>")` to bust the cache). **Verification**: `npx tsc --noEmit` exit 0 (via worktree node_modules symlink); `npm run check:mcp-bundle` clean (22 DOC_FILES paths / 14 bundle globs); `npm run check:mcp` fails only on pre-existing `patch_brew` discoverability issue ("Edit / amend / fix / mutate" lacks 2+ action verbs from the allow-list) — orthogonal to this sprint, spawned as a separate follow-up. **Out of scope**: no schema migration; no retroactive UPDATE on pre-2.4.1 proposals; no auto-application of implicit-intent detections (human-in-the-loop only); no `propose_canonical_addition` extension (canonical queue uses promote/alias/reject/duplicate, not superseded). **Next sprint**: Roadmap re-session — re-prioritize PRODUCT.md § Queued post-architecture (3.4 / 3.5 / 3.6 / 3.7) given the architecture arc + load-orchestration arc are closed. Some entries (3.6 / 3.7) flagged "likely subsumed by architecture" need re-evaluation. **PR**: [#254](https://github.com/chrismccann-dev/latent-coffee/pull/254) (merged 2026-05-26 UTC, squash, main commit `1725bdab`). **Branch**: `claude/jovial-merkle-0ebede`. |
| 2026-05-21 | **Architecture Wave 4 PR 4b — Master-doc residual migration + redirect stubs + CLAUDE.md compaction; architecture implementation arc CLOSED** | Closes the 4-wave composable sub-skills architecture implementation per [ADR-0011](docs/adr/0011-composable-sub-skills-architecture.md) / [ADR-0012](docs/adr/0012-master-coordinator-pattern.md) / [ADR-0013](docs/adr/0013-self-improvement-primitives.md). **Master doc shrink:** BREWING.md 124KB → ~3KB redirect-stub pointer table; ROASTING.md 72KB → ~6KB redirect-stub pointer table. **Cumulative architecture-arc master-doc shrink ~358KB** across Waves 2-4 (BREWING 213KB → ~3KB; ROASTING 147KB → ~6KB). **~196KB operational content migrated** to sub-skill clusters: new [coordinator/operator-guide.md](docs/skills/coordinator/operator-guide.md) (cross-domain operator guide — canonical lookups + MCP server how-to + per-coffee threads + Session Debrief paste template); expanded [coordinator/catalog.md § brewing-domain-principles + § roasting-domain-principles](docs/skills/coordinator/catalog.md) (Two-Axis framing + championship-mode + Roasting Philosophy + What Good Looks Like + What I Am Not Trying To Do + V-set methodology); new [brewing-assistant/cluster/operational-guide.md](docs/skills/brewing-assistant/cluster/operational-guide.md) (BREW PROMPT Steps 1-4 + Coffee Brief + Recipe Output + Iteration Loop + Resolved Brew Output Format); new [brewing-equipment-expert/cluster/operational-reference.md](docs/skills/brewing-equipment-expert/cluster/operational-reference.md) (Location Constraints + Equipment Reference + Valve Position + Filter System + Example Outputs + Brewer Rotation Framework); new [roasting-assistant/cluster/onboarding-protocol.md](docs/skills/roasting-assistant/cluster/onboarding-protocol.md) (New Coffee Onboarding Steps 1-4 + Naming Conventions + Parallel Experiment Considerations); 2 new [roasting-historian/cluster/patterns/by-process/{natural,honey}.md](docs/skills/roasting-historian/cluster/patterns/by-process/) (Naturals + Honey roast-direction frameworks — per Wave 3 PR 1 Chris-lock cleanup pass per PR 4a scope decision 3); 6 new [roasting-historian/cluster/active-lots/<lot>.md](docs/skills/roasting-historian/cluster/active-lots/) + README (CGLE Sudan Rume Natural V5 + CGLE Gesha Clouds + COS Higuito + BRA Fazenda Um Wush Wush + RWA Bukure Natural Lot 21 + REDPLUM Castillo); [roasting-historian/cluster/one-shot-calibrations/README.md](docs/skills/roasting-historian/cluster/one-shot-calibrations/) (currently-empty in-process state); 2 new [roest-knowledge/cluster/protocols/{between-batch-protocol,green-storage}.md](docs/skills/roest-knowledge/cluster/protocols/); Process/Variety Signal Table appended to [brewing-historian/cluster/patterns/cross-coffee-insights.md](docs/skills/brewing-historian/cluster/patterns/cross-coffee-insights.md). **CLAUDE.md sub-skills section extracted** to new [docs/architecture/sub-skills-status.md](docs/architecture/sub-skills-status.md) per PR 4a scope decision 4 — CLAUDE.md 125,750 → 114,960 bytes (back under the 120KB tripwire). **ARBITER.md target_doc allow-list** marks `brewing.md` / `roasting.md` as deprecated post-Wave-4-PR-4b (redirect-stub-only); proposals now route to `skills/{path}.md` cluster paths. **14 new MCP Resources** registered in `lib/mcp/docs.ts` (listDocs count 100 → 114). **4 scope decisions Chris-locked at session start via AskUserQuestion, all recommended defaults accepted**: (1) BREW PROMPT operational content destination = brewing-assistant cluster `operational-guide.md`. (2) Lot Knowledge migration = per-lot directories under roasting-historian/cluster/{active-lots,one-shot-calibrations}/ mirroring existing learnings/<lot>.md precedent. (3) Session Debrief Template destination = keep in prompts (recommended) — pre-flight grep surfaced it was NOT actually duplicated in any prompt; substantive content folded into coordinator/operator-guide.md as operator-convenience appendix. (4) PR commit strategy = single PR single commit per Wave 2/3 precedent. **Verification**: `npm run check:mcp-bundle` clean (14 DOC_FILES / 9 bundle globs); `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync; `npx tsc --noEmit` exit 0 via worktree node_modules symlink. **Retro key findings**: (1) Pre-flight grep + prior-lock surfacing pattern held cleanly AGAIN — 11th consecutive architecture-sprint where AskUserQuestion at session start was load-bearing. Brewing Assistant Wave 3 PR 2 "no cluster" lock surfaced via grep before drafting AskUserQuestion; recommendation framework interpreted the lock's Pattern-F escape clause as supporting brief default (b) cluster destination. (2) Pre-flight verification of brief assumptions — brief said Session Debrief Template was duplicated in log-cupping.md / bundled-brewing-completion.md; grep proved otherwise; substantive operator-convenience content folded into coordinator/operator-guide.md as appendix rather than dropped. (3) Master-doc redirect stubs are larger than the brief's ~500-byte target (BREWING.md 3.3KB / ROASTING.md 5.7KB) because a structured per-section pointer table is more useful than a minimal redirect; the substantive win is the ~196KB → ~9KB master-doc shrink. (4) Architecture implementation arc closes after 9 PRs spanning 4 waves (Wave 1 + Wave 2 PR 1/2/3 + Wave 3 PR 1/2/3 + Wave 4 PR 4a/4b). Sprint R's 4-phase Option 1 sequence (audit cluster → architecture brainstorm → architecture implementation → roadmap re-session) reaches its final phase: roadmap re-session promotes from queued → ACTIVE. **Out of scope**: Roadmap re-session (next sprint; re-assesses Queued post-architecture items 3.3-3.7 + deferred candidates against the new architectural baseline); brewing-side `/add?type=purchased` deprecation per `feedback_mcp_only_input.md` (future sprint); prompt subsumption sprint (deferred to a future sprint once sub-skills exercised in lived practice ≥3 times); Learning Knowledge promotion (≥2 completed research tracks); POD-1 follow-up (4 trigger conditions per cluster/pod-1-routing.md). **Branch**: assigned by worktree harness. |
| 2026-05-21 | **Architecture Wave 4 PR 4a — CCIL skeleton + Sudan Rume seed pattern (Chain 6 activated)** | First Wave 4 PR; opens the architecture arc's closing wave. CCIL ([docs/skills/ccil/](docs/skills/ccil/)) flipped PLACEHOLDER → ACTIVE with two cluster files authored: (1) [`cluster/coffee/sudan-rume/across-roasting-and-brewing.md`](docs/skills/ccil/cluster/coffee/sudan-rume/across-roasting-and-brewing.md) — proof-of-pattern seed (~10KB) synthesizing the cross-domain Sudan Rume substrate (N=3: CGLE Sudan Rume Hybrid Washed closed + CGLE Sudan Rume Natural V5 active + externally-roasted CGLE Las Margaritas Sudan Rume Natural brewing-only); surfaces variety throughline (transparency-driven aromatic landrace, light brown-tea body, herbal-spicy register) + roasting layer patterns (FC 200-205°C window, WB-Ground delta directional interpretation by lot family, hopper pre-load 125°C, fruit-layer-needs-washed-energy caveat from Dongzhe) + brewing layer patterns (April Brewer Glass + April Paper vehicle-correction for naturals, 91°C temperature ceiling, body-intuition-not-grind-signal rule); flags a cross-domain tension (process-scoped vs variety-scoped vehicle-dependency: the Latent-roasted SR Hybrid Washed reference brew runs on UFO Ceramic + Sibarist Fast Cone + Melodrip and produces integrated cup, contradicting the brewing-historian's "fast-cone phase-separates" rule); proposes planner-consumable recommendations for Roasting Assistant + Brewing Assistant + Learning Assistant. (2) [`cluster/decomposition-log.md`](docs/skills/ccil/cluster/decomposition-log.md) — Pattern F audit trail starter; append-only log of CCIL self-decomposition events triggered by 120KB total cluster / 60KB single doc / dispatch-accuracy degradation / cross-pattern density tripwires per [ADR-0013](docs/adr/0013-self-improvement-primitives.md). **Chain 6 (Wölfl cross-pollination brew)** in [coordinator/handoff-rules.md](docs/skills/coordinator/handoff-rules.md) promoted PLACEHOLDER → ACTIVE-pending-lived-practice (every sub-skill in the chain exists on paper; chain not yet exercised against a real cross-pollination lot). Master Coordinator [catalog.md](docs/skills/coordinator/catalog.md): CCIL entry flipped → ACTIVE; wave-status updated to mark Wave 4 PR 4a shipped + PR 4b pending; Learning Assistant entry updated to reference CCIL as live (not "when Wave 4"). **4 scope decisions Chris-locked at session start via AskUserQuestion**, all recommended defaults accepted: (1) **CCIL content depth** = skeleton + Sudan Rume seed pattern, per CCIL SKILL.md placeholder line 56 lock ("Seed corpus for Wave 4: start with 1-2 cross-domain pattern docs as proof-of-pattern (e.g. Sudan Rume across brewing + roasting)") surfaced via pre-flight grep per `feedback_recommend_prior_lock_as_default.md`. (2) **PR scoping** = split Wave 4 into PR 4a (CCIL) + PR 4b (master-doc residual migration + redirect stubs + CLAUDE.md compaction + cleanup pass). Pre-flight surfaced that BREWING.md still has 124KB of substantial residual operational content (BREW PROMPT Steps 1-4 + Two-Axis Framework + Equipment Reference + Valve Position + Filter System + Example Outputs) and ROASTING.md still has 72KB (Roasting Philosophy + Standard Workflow + Naturals + Honey + Data Capture + New Coffee Onboarding + Lot Knowledge + Session Debrief + Storage); single-PR sizing (brief default) would have been L-XL, not M. Split matches Wave 2/3 multi-PR cadence and gives clean architectural-arc-closing rhythm. (3) **Cleanup pass disposition** = fold Naturals + Honey + 3 ambiguous ROASTING.md sections into Wave 4 PR 4b redirect-stub migration; Wave 4 PR 4b is touching ROASTING.md anyway. (4) **CLAUDE.md compaction timing** = held for PR 4b alongside extraction to a new `docs/architecture/sub-skills-status.md` doc; PR 4a adds the Wave 4 marker line only (CLAUDE.md still over 120KB tripwire post PR 4a; PR 4b lands the compaction). **MCP Resource registration** — `lib/mcp/docs.ts` adds 3 SKILL_FILES entries (CCIL SKILL.md + sudan-rume seed + decomposition-log) + 3 corresponding DOC_DESCRIPTIONS + 3 listDocs entries; existing `./docs/skills/**/*.md` glob in next.config.js covers all new paths (Wave 1 work, no glob change needed). listDocs entry count 97 → 100 (+3 net). **Cross-system propagation**: CCIL SKILL.md flipped + "Wave 4 implementation status" section rewritten; coordinator/catalog.md wave-status + CCIL row + Learning Assistant row updated; coordinator/handoff-rules.md Chain 6 promoted + Wave-status extended; CLAUDE.md sub-skills section header + main paragraph extended with Wave 4 PR 4a details + Wave 4 PR 4b pending marker; PRODUCT.md § Active Sprints Wave 3 entry extended with Wave 4 PR 4a shipped + Wave 4 PR 4b pending; PRODUCT.md § 2 renamed "Wave 4 PR 4b" with full PR 4b scope; this shipped.md row added. **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 1 lib edit (mcp/docs.ts +3 SKILL_FILES + +3 DOC_DESCRIPTIONS + +3 listDocs); Actor 4 (MCP) — 3 new Resources + Master Coordinator catalog + handoff-rules updates; Actor 5 (Claude Code) — CLAUDE.md + PRODUCT.md + this shipped.md row + 2 coordinator/ doc edits + 1 SKILL.md flip + 2 cluster docs authored; Actor 2 (prompts) — zero updates (prompts continue as claude.ai entry surface; CCIL is consumed by planner sub-skills which the prompts already invoke); Actor 3 (claude.ai) — picks up 3 new Resources + Master Coordinator catalog + handoff updates on next session start; Actor 1 (Chris) — reads merged PR + reviews cross-domain CCIL behavior in next claude.ai session. **Verification**: `npm run check:mcp-bundle` clean; `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync (no registry changes); `npx tsc --noEmit` exit 0 via worktree node_modules symlink; listDocs entry count 97 → 100 as expected. Skip preview per `<when_to_verify>` — pure-doc + MCP Resource registration; not browser-observable. **Retro key findings**: (1) Pre-flight grep + prior-lock surfacing pattern from Wave 3 PR 1/2/3 held cleanly AGAIN — CCIL placeholder SKILL.md line 56 was the load-bearing prior lock for scope decision 1 (Sudan Rume seed). 10th consecutive architecture-sprint where AskUserQuestion at session start was load-bearing. `feedback_recommend_prior_lock_as_default.md` keeps paying for itself. (2) Pre-flight READ of BREWING.md + ROASTING.md residual content surfaced that brief's "M — likely 1 session" sizing was unrealistic — single-PR Wave 4 would have been L-XL given 196KB combined residual. Split into PR 4a + PR 4b matches Wave 2/3 multi-PR cadence and keeps each session sized M. Pattern: when a kickoff brief estimates sizing, pre-flight verify with actual file sizes before accepting the size. (3) First CCIL seed pattern authored — Sudan Rume substrate is rich on both domains (closed + active Latent lots + externally-roasted brewing data point). Cross-domain tension (vehicle-dependency rule's process-scoped vs variety-scoped) surfaces naturally — this is exactly the kind of insight CCIL is positioned to hold (neither Historian alone would have flagged it). (4) Skip preview verification per `<when_to_verify>` — pure-doc + MCP Resource registration sprint, not browser-observable. **Out of scope**: Wave 4 PR 4b (master-doc residual migration + ~500-byte redirect stubs + CLAUDE.md compaction via extraction to docs/architecture/sub-skills-status.md + Naturals + Honey + 3 ambiguous ROASTING.md sections cleanup pass); Pattern A refresh events (CCIL re-synthesis when Historians' patterns drift; will fire as the SR Natural V5 lot closes); future cross-domain seed patterns (Gesha + Mandela XO candidates if their substrate evolves; Pattern F decomposition once cluster exceeds 120KB total / 60KB single doc); Learning Knowledge promotion (≥2 completed research tracks); POD-1 follow-up sprint (gated on 4 trigger conditions per `cluster/pod-1-routing.md`). **Branch**: assigned by worktree harness. |
| 2026-05-26 | **Architecture Wave 3 PR 3 — Workflow Executing tier (5 sub-skills: Roast Recorder + Brew Recorder + Cupping Specialist + Roest API Worker + Close-Lot Specialist); Wave 3 closed** | Third Wave 3 PR; closes the Workflow tier of the composable sub-skills architecture. 5 SKILL.md files flipped PLACEHOLDER → ACTIVE — substrate-writer executors that wrap existing `push_*` / `patch_*` MCP Tools (no new Tools; Tool count stays at 35). Each owns 1-5 Tools + canonical-validation discipline + cross-link verification: Roast Recorder (`push_roast` + `push_roast_recipe` when not pre-pushed by Roasting Assistant + `patch_roast` + `patch_roast_recipe`; owns slot→recipe_id map resolution incl. pre-rewrite-lot fallback (a) + missing-recipe inline backfill (b) per log-roast.md STAGE 1; sets `fc_audibility` 4-value enum per Sprint 11 RO-CP-3); Brew Recorder (`push_brew` + `patch_brew` + downstream `propose_doc_changes` into Brewing Historian cluster; per-coffee terminal write because brewing iterates IN-THREAD ONLY per ADR-0011 § iteration-depth asymmetry — only optimized brew persists; validates `fermentation_qualifiers` Anoxic cue per Sprint T3 / CR-5); Cupping Specialist (`push_cupping` + `patch_cupping` + `patch_experiment` + `patch_roast` is_reference_candidate flag + `propose_doc_changes`; executes Day-7 xBloom cupping + V-set Path A/B/C-1/C-2 routing); Roest API Worker (`push_roast_profile` + `patch_roast_recipe` linkage; symmetry-split from Roest Knowledge per ADR-0011; validation gap acknowledged today — returns success on API acceptance, not machine confirmation; forward investment for a future `verify_roast_profile_landed` Tool); Close-Lot Specialist (`push_roast_learnings` + `patch_roast_learnings` + `patch_roast` is_reference + worth_repeating + `patch_inventory` archive + `propose_doc_changes`; handles both V-set + one-shot close-out incl. 7 lever-attribution fields schema-rejected on one-shots per migration 054 + Outcome A reference-quality vs Outcome B "Closed without reference" via `why_this_roast_won: NULL` routing; cross-link verification end-to-end). **4 scope decisions Chris-locked at session start via AskUserQuestion, all recommended defaults accepted**: (1) **POD-1 integration depth** = summary inline in Cupping Specialist SKILL.md + author `docs/skills/cupping-specialist/cluster/pod-1-routing.md` reference doc; respects the scoping draft's own "Decision deferred. More cross-thread observation needed" lock per `feedback_recommend_prior_lock_as_default.md`. Cupping Specialist is the only Wave 3 PR 3 sub-skill with a `cluster/`. The full Path-C rewrite + simulated-pourover schema scoping (`cuppings.eval_method='Simulated Pourover'` vs `brews.is_simulated_pourover` flag vs hybrid) is gated on 4 lived-practice trigger conditions: 2-3 more V-set Path A lots + at least one one-shot close-out + Stefano Um / Bukure / Higuito decisions + C-2 disambiguation cases. (2) **`lib/agents/` helper authoring** = skip per ADR-0011 + ADR-0012 defer-code-until-needed pattern; future authoring gated on lived-practice friction. (3) **Chain status promotion** = promote Chains 1 + 3 + 4 from PARTIAL → ACTIVE at PR 3 ship time per "all hops ACTIVE → chain ACTIVE" convention; Chain 2 → ACTIVE-pending-Learning-Knowledge (archival hop deferred per ADR-0011 trigger of ≥2 completed research tracks); Chain 5 already ACTIVE single-step; Chain 6 placeholder for Wave 4 CCIL. (4) **Tool description ownership pointers** = append single-line "Owned by <sub-skill> per ADR-0011" to each affected push/patch Tool description (13 Tools touched; co-ownership pointers on multi-owner cases like `patch_roast` co-owned by Roast Recorder + Cupping Specialist + Close-Lot Specialist; `patch_experiment` co-owned by Cupping Specialist + Roast Recorder; `patch_roast_recipe` co-owned by Roast Recorder + Roest API Worker; `push_roast_recipe` co-owned with Roest API Worker for linkage path). **Pre-flight grep load-bearing AGAIN** — found POD-1 scoping draft's explicit "DRAFT" status + "Decision deferred" prose + 4 unmet trigger conditions, which conflicted with brief's default (a) full integration; recommended (b) summary + bookmark per `feedback_recommend_prior_lock_as_default.md`. Chris accepted recommendation. **Master Coordinator updates**: catalog.md 5 entries flipped placeholder → ACTIVE + wave-status updated to mark Wave 3 closed; dispatch-rules.md gains 10 new intent → executor mappings (post-roast batch logging / pre-rewrite-lot inline backfill / optimized brew completion / in-thread brew finalization / Day-7 cupping + Path A/B/C routing / is_reference_candidate flag-setting / Roest API profile push / V-set close-out / one-shot close-out / Chain 3 Path C close-without-reference); handoff-rules.md promoted Chains 1 + 3 + 4 PARTIAL → ACTIVE, Chain 2 → ACTIVE-pending-Learning-Knowledge, Wave-status updated to mark Wave 3 closed. **MCP Resource registration** — `lib/mcp/docs.ts` adds 6 SKILL_FILES entries (5 SKILL.md + 1 cluster/pod-1-routing.md) + 6 corresponding DOC_DESCRIPTIONS + 6 listDocs entries; existing `./docs/skills/**/*.md` glob in next.config.js covers all new paths (Wave 1 work, no glob change needed). listDocs entry count 91 → 97 (+6 net). **Tool description updates** — 13 lib/mcp/push-*.ts + patch-*.ts files edited to append single-line ownership pointers (push_roast / push_roast_recipe / push_brew / push_cupping / push_roast_learnings / push_roast_profile / patch_roast / patch_roast_recipe / patch_brew / patch_cupping / patch_experiment / patch_roast_learnings / patch_inventory). **Cross-system propagation**: CLAUDE.md sub-skills section header line updated + main paragraph extended with Wave 3 PR 3 ship details + Wave 3 closed marker; PRODUCT.md § Active Sprints Wave 3 entry updated to mark Wave 3 closed + Wave 4 marked as next active sprint; PRODUCT.md POD-1 entry rewritten to reflect "absorbed at SKILL.md level + bookmarked at cluster/pod-1-routing.md pending 4 trigger conditions" framing; this shipped.md row added. **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 14 lib edits (mcp/docs.ts +6 SKILL_FILES + +6 DOC_DESCRIPTIONS + +6 listDocs; 13 Tool description edits in lib/mcp/push-*.ts + patch-*.ts); Actor 4 (MCP) — 6 new Resources + 13 Tool description ownership pointers; Actor 5 (Claude Code) — CLAUDE.md + PRODUCT.md + this shipped.md row + 3 coordinator/ doc edits + 5 SKILL.md flips + 1 cluster doc authored; Actor 2 (prompts) — zero updates per scope decision (prompts continue as claude.ai entry surface; sub-skills become canonical fetch target the prompts compose over); Actor 3 (claude.ai) — picks up 6 new Resources + Master Coordinator catalog + dispatch + handoff updates + 13 Tool description ownership pointers on next session start; Actor 1 (Chris) — reads merged PR + dispatch behavior in next claude.ai session. **Verification**: `npm run check:mcp-bundle` clean; `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync; `npx tsc --noEmit` exit 0 via worktree node_modules symlink; sibling-cluster sanity check via listDocs count (91 → 97 = +6 as expected). **Retro key findings**: (1) Pre-flight grep + prior-lock surfacing pattern from Wave 3 PR 1 retro held cleanly AGAIN — POD-1 scoping draft's "Decision deferred" prose was the load-bearing prior lock; reframed brief default (a) → recommendation (b). 9th consecutive architecture-sprint where AskUserQuestion at session start was load-bearing. (2) Substrate-writer executors are richer SKILL.md authoring than reads-only planners (PR 2) — own Tool surface + validation discipline + substrate-write semantics; ~100-180 lines each vs PR 2's ~60. POD-1 absorption + cluster authoring added a 6th file to author this PR. (3) Tool description ownership pointer pattern is the right shape for sub-skill-as-architectural-overlay — single-line append; 13 Tools touched cheaply; gives claude.ai dispatch-time signal without disturbing the 2026-05-08 audit work. Multi-owner cases (`patch_roast` × 3, `patch_experiment` × 2, `patch_roast_recipe` × 2, `push_roast_recipe` × 2) documented inline in the pointer. (4) CLAUDE.md tripwire **crossed**: 119,938 → ~125KB after Wave 3 PR 3 paragraph append. Flag for compaction follow-up — sub-skills section is now ~12KB of CLAUDE.md; consider whether to extract to its own doc or compact the per-PR ship narratives into a single summary table. (5) Skip preview verification per `<when_to_verify>` — pure-doc + MCP Resource registration + Tool description tweaks; not browser-observable. **Out of scope**: Wave 4 (CCIL skeleton + BREWING.md + ROASTING.md redirect-stub rewrite to ~500 bytes each); POD-1 follow-up sprint (gated on 4 trigger conditions per `cluster/pod-1-routing.md`); Learning Knowledge promotion (≥2 completed research tracks); Naturals + Honey + 3 ambiguous sections cleanup pass (fold into Wave 4 OR run as small follow-up sprint); prompt subsumption / deprecation (deferred to a future sprint once sub-skills exercised in lived practice ≥3 times); CLAUDE.md compaction (sub-skills section consider extract). **Branch**: assigned by worktree harness. |
| 2026-05-26 | **Architecture Wave 3 PR 2 — Workflow Planning tier (4 sub-skills: Roasting Assistant + Brewing Assistant + Learning Assistant + Sourcing Workflow Planner)** | Second Wave 3 PR. Reads-only composition over Wave 1+2 Knowledge clusters; no master-doc shrink (Wave 3 PR 3 + Wave 4 are the remaining shrink events). 4 SKILL.md files flipped PLACEHOLDER → ACTIVE, each with explicit dependency lists matching the Knowledge clusters they compose over: Roasting Assistant reads Roasting Historian + WBC Roasting Archivist + Roest Knowledge + Peer-Learning Roasting Archivist (V-set + one-shot recipe design pre-push); Brewing Assistant reads Brewing Historian + WBC Brewing Archivist + Brewing Equipment Expert (3-phase: Phase 1 initial recipe construction, Phase 2 in-thread iteration absorbing Palate Evaluator per ADR-0011 § iteration-depth asymmetry, Phase 3 handoff to Brew Recorder); Learning Assistant reads both Historians + Brewing Equipment Expert + WBC Roasting Archivist § sourcing/ + CCIL (Wave 4 dependency, degrades gracefully) + direct green_beans (the only cross-domain planner; constructs research tracks distinct from per-lot experiments table rows); Sourcing Workflow Planner reads WBC Roasting Archivist § sourcing/ + Roasting Historian closed-lot retros + direct green_beans (buy/hold/pass + lane-fit assessment; physical-world decision, no substrate write). **4 scope decisions Chris-locked at session start via AskUserQuestion, all recommended defaults accepted**: (1) cluster shape = SKILL.md only, no cluster/ subdirectories — templates accrue under Pattern F if they emerge in lived use; (2) prompt subsumption = leave prompts unchanged, sub-skills become canonical fetch target via MCP Resource read (start-lot.md / log-roast.md / one-shot.md / start-brew.md / log-brew.md continue as claude.ai entry surface); (3) handoff chains = author now in handoff-rules.md (extends precedent of Wave 1's pre-authored Chain 1 + Chain 2); (4) PR sequencing = PR 2 alone per Wave 3 plan, PR 3 substrate-writers in separate session. Learning Knowledge stays deferred per prior lock in learning-knowledge/SKILL.md (≥2 completed research tracks triggers promotion; today zero) — surfaced in opening message per `feedback_recommend_prior_lock_as_default.md`. **Master Coordinator updates**: catalog.md 4 entries flipped placeholder → ACTIVE (PR 2) + wave-status paragraph updated; dispatch-rules.md gains a Wave 3 PR 2 section with 8 new intent → planner mappings (new lot intake / one-shot lot design / V_(n+1) recipe design / new brew session / in-thread brew iteration / optimized brew dial-in / research-track design / sourcing opportunity); handoff-rules.md restructured into 6 numbered chains with explicit status (ACTIVE / PARTIAL / PLACEHOLDER) — Chains 1-4 PARTIAL pending Wave 3 PR 3 substrate-writer activation (Cupping Specialist + Brew Recorder + Roast Recorder + Roest API Worker + Close-Lot Specialist), Chain 5 sourcing eval ACTIVE (single-step), Chain 6 Wölfl cross-pollination PLACEHOLDER pending Wave 4 CCIL. **MCP Resource registration** — `lib/mcp/docs.ts` adds 4 SKILL_FILES entries (one per Workflow Planning sub-skill) + 4 corresponding DOC_DESCRIPTIONS + 4 listDocs entries; existing `./docs/skills/**/*.md` glob in next.config.js covers all new paths (Wave 1 work, no glob change needed). check:mcp-bundle 14/9 clean. **Cross-system propagation**: CLAUDE.md sub-skills section appended with Wave 3 PR 2 paragraph naming all 4 sub-skills + their composition + scope decisions + handoff-rules chain enumeration; PRODUCT.md § Active Sprints Wave 3 entry updated to mark PR 2 shipped + scope decisions + remaining PR 3 work; this shipped.md row added. **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 1 lib edit (mcp/docs.ts +4 SKILL_FILES + +4 DOC_DESCRIPTIONS + +4 listDocs); Actor 4 (MCP) — 4 new Resources; Actor 5 (Claude Code) — CLAUDE.md + PRODUCT.md + this shipped.md row + 3 coordinator/ doc edits + 4 SKILL.md flips; Actor 2 (prompts) — zero updates per scope decision 2; Actor 3 (claude.ai) — picks up 4 new Resources + Master Coordinator catalog + dispatch + handoff updates on next session start; Actor 1 (Chris) — reads merged PR + dispatch behavior in next claude.ai session. **Verification**: `npm run check:mcp-bundle` 14/9 clean (no new globs needed); `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync (no registry changes); `npx tsc --noEmit` exit 0 via worktree node_modules symlink. **Retro key findings**: (1) Wave 3 PR 1's procedural rule (feedback_recommend_prior_lock_as_default.md) was load-bearing this session for Learning Knowledge — present prior lock as the default + recommended option, then proceed unless overridden. Learning Knowledge stays deferred per its SKILL.md lock; surfaced via opening message rather than AskUserQuestion since the lock was strong. (2) All 4 scope decisions surfaced at session start before any file edits — same pattern as Wave 3 PR 1, consistently load-bearing. (3) PR 2 is the lightest implementation sprint of the architecture arc — pure SKILL.md authoring + 3 coordinator/ doc edits + 1 lib/mcp/docs.ts edit + 3 master-doc-list edits. No cluster files, no master-doc surgery, no DB scans. Wave 3 PR 1 retro #5 "single-PR-per-session pattern is stable" held cleanly. (4) Skip preview verification per `<when_to_verify>` — pure-doc + MCP Resource registration sprint, not browser-observable. **Out of scope**: Wave 3 PR 3 (5 Workflow Executing tier sub-skills + POD-1 absorption into Cupping Specialist + potential lib/agents/ helper authoring); Wave 4 (CCIL skeleton + master-doc redirect-stub rewrite); Learning Knowledge promotion (≥2 completed tracks); Naturals + Honey + 3 ambiguous sections cleanup pass (deferred to Wave 3 closing or Wave 4); prompt subsumption / deprecation (deferred to a future sprint once sub-skills exercised in lived practice). |
| 2026-05-26 | **Architecture Wave 3 PR 1 — Operator-stub Knowledge pair (Peer-Learning Roasting Archivist + Roest Knowledge)** | First Wave 3 PR. Operator-stub-then-integrate pattern lands per Pattern I — Chris-stubbed clusters (existing prose from ROASTING.md + Dongzhe livestream doc) integrated into cluster shape by Claude Code. **ROASTING.md shrunk from 103,668 → 72,234 bytes (~31KB shrink, within the ~25-40KB target after Chris-lock kept Naturals + Honey + 3 ambiguous sections in ROASTING.md for Wave 3 cleanup pass).** 14 new cluster files: (a) Peer-Learning Roasting Archivist — cluster/per-peer/dongzhe.md merging Yunnan-Hatchi 2026-05-17 livestream extraction + ROASTING.md § Reference Roast Target #249 + § Peer Insights into single peer profile; cluster/cross-peer/patterns.md N<3 placeholder; cluster/source-index.md provenance + freshness log. (b) Roest Knowledge — 10 files spanning cluster/machine/{l200-ultra,counterflow-observations}.md + cluster/protocols/{evaluation,fan-strategy,fc-marking}.md (bundles § Standard Inlet Curve Template + § Fan Strategy) + cluster/api/{read-surface,write-surface,quirks}.md + cluster/firmware/README.md + cluster/observed-quirks.md. (c) Roasting Historian extension — cluster/patterns/roast-to-brew-translation.md absorbing ROASTING.md § Roast-to-Brew Translation (override of prior roasting-historian/SKILL.md:51 Chris-lock that kept R-to-B in ROASTING.md until Wave 4; re-locked to Roasting Historian via AskUserQuestion in this session because content shape is cross-coffee pattern aggregation, not Roest-machine-specific). **3 scope decisions Chris-locked at session start via AskUserQuestion**: (1) PR sequencing = PR 1 only this session (4-PR Wave 3 sequencing); (2) R-to-B destination = extract but to Roasting Historian (not Roest Knowledge per kickoff brief) with my recommendation honored; (3) Counterflow Observations + Naturals Framework + Honey Process Direction Fork classification — Counterflow Observations to Roest Knowledge (header self-declares Machine-Specific); Naturals + Honey stay in ROASTING.md for Wave 3 cleanup pass deferred. **Procedural rule extracted**: [feedback_recommend_prior_lock_as_default.md](../../~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_recommend_prior_lock_as_default.md) — when kickoff brief deviates from a prior Chris-locked decision in substrate, present the lock as Option 1 + (Recommended) in AskUserQuestion; don't frame deviation as neutral peer. Chris noted post-question "I didn't realize this deviated that much from what we talked about" — confirms the rule was load-bearing. **Operator-stub Chris-stubs-Claude-integrates pattern landed for first time** — both Peer-Learning Roasting Archivist + Roest Knowledge SKILL.md files explicitly note this is the canonical workflow per Round 2 lock; Wave 3 PR 1 confirms what "integrating Chris's existing prose into cluster shape" looks like in practice. **Bash surgery for big-block deletion**: head + cat pointer + tail bottom-up pattern reused from Wave 2 PR 3; 9 sections removed in bottom-up order (preserving line numbers for later edits). 9 pointer blocks written to /tmp first, then spliced. **ROASTING.md self-refs updated**: 3 in-doc references (line 47 R-to-B prelude cross-ref + line 160 + line 260 dongzhe-livestream cross-refs) updated to point at cluster files directly. **MCP Resource registration** — `lib/mcp/docs.ts` adds 16 SKILL_FILES entries + 16 DOC_DESCRIPTIONS + 29 listDocs entries (13 of which repair a Wave 2 PR 3 oversight where roasting-historian cluster files were added to SKILL_FILES + DOC_DESCRIPTIONS but missed in listDocs — Wave 2 PR 3 retro note added). 2 existing descriptions updated (docs://roasting.md + docs://roasting/dongzhe-livestream-2026-05.md → redirect-stub note). **docs/roasting/dongzhe-livestream-2026-05.md → redirect stub** per Wave 2 redirect-stub pattern; cluster file is canonical. **SKILL.md flipped** for both operator-stub clusters from PLACEHOLDER → ACTIVE; roasting-historian/SKILL.md:51 lock annotated with OVERRIDDEN note pointing to procedural-rule memory. **Cross-system propagation**: CLAUDE.md ROASTING.md description (rewritten enumerating retained + migrated sections + cluster URIs) + sub-skills section (Wave 3 PR 1 added) + docs/roasting/ entry; PRODUCT.md § Active Sprints Wave 3 status + § Standing tripwires (ROASTING.md 103KB → 72KB); 6 prompts updated (start-lot.md + log-cupping.md + log-roast.md + close-lot.md + one-shot.md + one-shot-closeout.md) — read-side bullets + write-side propose_doc_changes routing both updated to point at cluster URIs; 3 cluster doc refs updated (wbc-roasting.md 4 refs + gua-libertad-aurelio-del-cerro.md 1 ref); push-roast-profile.ts 2 code comments updated (Standard Inlet Curve Template + Fan Strategy cluster URIs). **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 1 lib edit (mcp/docs.ts +16 SKILL_FILES + +16 DOC_DESCRIPTIONS + +29 listDocs = 13 Wave 2 PR 3 retroactive + 16 net-new); 1 push-roast-profile.ts comment edit; Actor 4 (MCP) — 16 new Resources + 13 retroactive Wave 2 PR 3 fixes; existing `skills/{path}.md` propose_doc_changes routing covers all new cluster paths via isKnownDoc validator; Actor 5 (Claude Code) — CLAUDE.md + PRODUCT.md + this shipped.md row + ROASTING.md self-refs; Actor 2 (prompts) — 6 prompt updates + 3 cluster doc updates; Actor 3 (claude.ai) — picks up 16 new Resources + 13 retroactive listDocs + 2 updated descriptions on next session start; Actor 1 (Chris) — reads merged PR + reviews cluster behavior in next roasting session. **Verification**: `npm run check:mcp-bundle` 14/9 clean (no new globs needed); `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync (no registry changes); `npx tsc --noEmit` exit 0 via worktree node_modules symlink; ROASTING.md size delta lands at -31KB. **Retro key findings**: (1) Pre-flight grep was load-bearing AGAIN — found roasting-historian/SKILL.md:51 R-to-B lock that conflicted with kickoff brief BEFORE any edits; surfaced as Chris-override decision. New procedural rule captured the lesson. (2) Pre-flight grep also found 17 prompt + cluster refs to migrated sections that needed updating — agents/sampling would have missed these. Wave 1+2 retro lesson confirmed for 3rd time. (3) Wave 2 PR 3 listDocs() oversight discovered + fixed in same PR (13 roasting-historian entries were in SKILL_FILES + DOC_DESCRIPTIONS but missing from listDocs). Pattern: when adding new sub-skill cluster entries, grep the listDocs function for existing sibling clusters to catch missed registrations from prior waves. (4) Operator-stub Chris-stubs-Claude-integrates pattern shipped for first time — both SKILL.md files explicitly say "Chris does research + initial format proposal + stub authoring; Claude Code integrates." In practice this meant migrating Chris's existing ROASTING.md prose + Dongzhe livestream doc into cluster shape rather than authoring novel content. **Out of scope**: Wave 3 PR 2 (4 workflow planning tier sub-skills); Wave 3 PR 3 (5 workflow executing tier sub-skills + POD-1 inside Cupping Specialist); Wave 4 (CCIL + cross-system sync rewriting BREWING.md + ROASTING.md as redirect stubs); Naturals + Honey + 3 ambiguous sections cleanup pass (deferred to Wave 3 closing or Wave 4). **Branch**: `claude/pedantic-curie-1240b1`. |
| 2026-05-26 | **Architecture Wave 2 PR 3 — Roasting Historian (ROASTING.md § Cross-Coffee Insight Layer + § Open Questions extraction); Wave 2 closed** | Third and final Wave 2 PR — closes the wave. Parallel to PR 2's Brewing Historian extraction; same Bash-surgery pattern and AskUserQuestion scope-decision pattern reused. **ROASTING.md shrunk from 147,083 → 103,315 bytes (~43KB shrink)** — slightly under the 60-80KB target band, but that's the cost of Chris's locked decision to keep `# Roast-to-Brew Translation` in ROASTING.md until Wave 4 (the section is cross-domain bridge content, not Historian content). 12 new cluster files under [docs/skills/roasting-historian/cluster/](../skills/roasting-historian/cluster/): patterns/cross-coffee-insights.md (verbatim CCIL extraction: Confirmed Patterns + FC Floor/Ceiling + WB-to-Ground Delta Norms + Session Position Effect + Green Spec→Starting Hypothesis with Additive + Precedence tables + Varietal Aromatic Fingerprints + Rest Behavior Patterns + FC-Temp Architectural Constraint + xbloom Misranking + Working Hypotheses) + patterns/open-questions.md (12 research questions) + patterns/general.md (intentionally-sparse non-axis catch-all) + patterns/by-cultivar/gesha.md (N=3 stub: Oma+Surma+Clouds) + patterns/by-process/washed.md (N=6 stub: SR Washed + Oma + Surma + El Socorro + Libertad + Rancho Tio) + 7 per-lot pointer files in learnings/ (cgle-mandela-xo / cgle-sudan-rume-hybrid-washed / gv-oma-lot-25-035 / gv-surma-lot-25-039 / gua-el-socorro-java / gua-libertad-aurelio-del-cerro / rancho-tio-emilio). **4 scope decisions Chris-locked via AskUserQuestion at session start**: (1) CCIL boundary = move CCIL + Open Questions only; keep Roast-to-Brew Translation in ROASTING.md through Wave 4; (2) Per-lot learning depth = pointer + brief synthesis (kickoff brief recommendation) — each cluster/learnings/<lot>.md links to the roast_learnings DB row + /green/[id] resolved-view + archive.md + ROASTING.md reference summary, plus 1-paragraph cross-lot framing; avoids two-home drift on the 14-field carry-forward block; (3) Stub seeding source — pre-flight DB scan found all 4 scope_tags columns are empty (Sprint 12 / ADR-0009 forward-only population, no historical backfill across the 7 closed lots) so FK-based seeding from green_beans substituted: Gesha (N=3) + Washed (N=6) are the only axes at threshold; (4) Pointer style = single-line per PR 2 precedent + defer by-density-moisture buckets (boundaries not yet locked; Dongzhe-livestream framing future). **ROASTING.md residual back-compat**: h1 `# Cross-Coffee Insight Layer` + `# Open Questions` retained at the original anchor positions with single-line pointer blocks routing to the cluster files — preserves both `ROASTING.md#cross-coffee-insight-layer` and `ROASTING.md#open-questions` anchors for any session still referencing the old paths. **ROASTING.md self-refs updated**: 3 in-doc references (lot-knowledge 5-tier framing at lines 494-495 + Reference Roasts + Brews closing pointer at line 630) updated to point at cluster files directly. **MCP Resource registration** — `lib/mcp/docs.ts` adds 12 new SKILL_FILES entries (1 SKILL.md + 3 patterns/{cross-coffee-insights,open-questions,general}.md + 2 by-{cultivar,process}/<axis>.md + 7 per-lot learnings/) + 12 matching DOC_DESCRIPTIONS; existing `./docs/skills/**/*.md` glob covers all new paths. docs://roasting.md description updated to note CCIL + Open Questions migration. `check:mcp-bundle` 14/9 clean. **SKILL.md flipped** from PLACEHOLDER → "Wave 2 PR 3 shipped 2026-05-26" with realized cluster contents enumerated and the scope_tags-empty finding documented. **Cross-system propagation**: CLAUDE.md ROASTING.md description + sub-skills section (Wave 2 marked complete; Wave 3 promoted to #1); CONTEXT.md § Cross-coffee insight layer (Wave 2 PR 3 status); PRODUCT.md § Active Sprints (Wave 2 closed + section renumber so Wave 3 = #1) + § Standing tripwires (ROASTING.md 147KB → 103KB under tripwire); ARBITER.md § CCIL consolidation pass (3 references updated to point at cluster files); this shipped.md row added. Roasting-side prompt update deferred — back-compat anchors resolve, and the existing prompt text references "Cross-Coffee Insight Layer" by name (still valid; the cluster file is the new home). **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 1 lib edit (mcp/docs.ts +12 SKILL_FILES + +12 DOC_DESCRIPTIONS); Actor 4 (MCP) — 12 new Resources; existing `skills/{path}.md` propose_doc_changes routing covers roasting-historian via isKnownDoc validator; Actor 5 (Claude Code) — CLAUDE.md + CONTEXT.md + PRODUCT.md + ARBITER.md + this shipped.md row + ROASTING.md self-refs; Actor 2 (prompts) — no prompt updates (back-compat anchors stable); Actor 3 (claude.ai) — picks up 12 new Resources + 1 updated roasting.md description on next session start; Actor 1 (Chris) — reads merged PR + reviews cluster behavior in next roasting session. **Verification**: `npm run check:mcp-bundle` 14/9 clean (no new globs needed); `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync (no registry changes); `npx tsc --noEmit` zero errors via worktree node_modules symlink; ROASTING.md size delta lands at -43KB. **Retro key findings**: (1) Pre-flight DB scan was load-bearing — discovered all 4 scope_tags arrays are empty (forward-only population per Sprint 12 / ADR-0009; no historical backfill on 7 closed lots). Substituted FK-based seeding from green_beans; only by-cultivar/gesha.md + by-process/washed.md cross the N≥3 threshold. (2) AskUserQuestion at session start surfaced 4 scope decisions — boundary + lot depth + seeding source + buckets — before any file edits. Same pattern as PR 2, load-bearing again. (3) Bash surgery (head + pointer + tail) reused from PR 2 — `head -n 775 + cat /tmp/roasting-pointers.md + tail -n +1014` cleanly sliced the file. (4) ROASTING.md shrink at 43KB lands slightly under the 60-80KB target band; the cost is paid for the Roast-to-Brew Translation residual (~15KB). Net Wave 2 cumulative master-doc shrink: ~131KB (BREWING 88KB + ROASTING 43KB). **Out of scope**: Wave 3 (operator-stub clusters Peer-Learning Roasting Archivist + Roest Knowledge + 9 workflow tier sub-skills with POD-1 inside Cupping Specialist); Wave 4 (CCIL + cross-system sync rewriting BREWING.md + ROASTING.md as redirect stubs); per-density-moisture bucket stubs (deferred until bucket boundaries lock); roasting-side prompt rewrites (back-compat anchors resolve, deferred to Wave 4 redirect-stub rewrite). **Branch**: `claude/vibrant-jennings-91263c`. |
| 2026-05-26 | **Architecture Wave 2 PR 2 — Brewing Historian (BREWING.md § Cross-Coffee Insight Layer extraction)** | Second Wave 2 PR (3-PR sequencing: PR 1 WBC Archivists shipped same day, PR 3 Roasting Historian remaining). First substantive master-doc shrink event of the entire architecture arc. **BREWING.md shrunk from 213,831 → 124,107 bytes (~88KB shrink, within 60-80KB target band)** by extracting the Cross-Coffee Insight Layer into 21 new cluster files under [docs/skills/brewing-historian/cluster/patterns/](../skills/brewing-historian/cluster/patterns/). **CCIL boundary** confirmed via grep: lines 585-934 of pre-PR BREWING.md = `# Cross-Coffee Insight Layer` h1 + intro + § How to Use This Section + § By Extraction Strategy (6 per-strategy sub-sections) + § By Modifier (Axis 2) + § By Process + § By Variety + § Cooling Behavior Observations + § Office Brewing Notes (Palo Alto) + § Open Questions + § End-of-Coffee Workflow. Cleanly bounded; SECTION 4 - WBC REFERENCE at line 935 unaffected. **Per-strategy split decision (Chris-locked at session start)**: 6 "Coffees That Confirmed X" sub-sections moved to `by-strategy/<strategy>.md` (clarity-first / suppression / balanced-intensity / full-expression / extraction-push / hybrid); BREWING.md § Axis 1 canonical strategy definitions stay in BREWING.md (residual through Wave 4 per scope-out: Two-Axis Framework intro stays). Each by-strategy file cross-links back to the Axis 1 definition. **cross-coffee-insights.md** absorbs the remaining CCIL content verbatim (~50KB). **Stub-heavy seeding (Chris-locked)**: 7 N≥3 cultivar stubs created (Gesha N=34, 74158 N=5, Sidra N=5, Ethiopian landrace population N=4, Mejorado N=3, Pacamara N=3, Sudan Rume N=3) + 6 N≥3 base+fermentation-modifier coffee-family stubs (Anaerobic Natural N=10, Anaerobic Washed N=5, Yeast-Inoculated Washed N=4, Yeast-Inoculated Natural N=3, Double Anaerobic Washed N=3, Thermal Shock Washed N=3) sourced from Supabase MCP queries against brews table. Each stub points back to the cross-coffee-insights.md rollup + relevant per-strategy entries; per-cluster deep-dive patterns accrue as the corpus grows. **BREWING.md residual back-compat**: h1 `# Cross-Coffee Insight Layer` retained at line 585 with pointer block enumerating where each former sub-section now lives — preserves `BREWING.md#cross-coffee-insight-layer` anchor for any session still referencing the old path. **BREWING.md self-refs updated**: 2 in-doc references (Hybrid pointer at line ~115, End-of-Coffee Workflow doc-review at line ~406) point at cluster files instead of `#cross-coffee-insight-layer` anchor. **Single-line pointer style (Chris-locked)**: per former sub-section, one line per pointer (maximizes shrink, clean residual). **MCP Resource registration** — `lib/mcp/docs.ts` adds 21 new SKILL_FILES entries (1 SKILL.md + 1 cross-coffee-insights.md + 6 by-strategy + 7 by-cultivar + 6 by-coffee-family) + 21 matching DOC_DESCRIPTIONS + 21 listDocs entries; existing `./docs/skills/**/*.md` glob covers all new paths (Wave 1 work). docs://brewing.md description updated to note CCIL migration. `check:mcp-bundle` 14/9 clean. **SKILL.md flipped** from PLACEHOLDER → "Wave 2 PR 2 shipped 2026-05-26" with realized cluster contents enumerated. **Cross-system propagation**: README.md doc index, CLAUDE.md (BREWING.md description + sub-skills section + Documentation Index), CONTEXT.md § Cross-coffee insight layer entry, PRODUCT.md § Active Sprints Wave 2 (PR 2 marked shipped, PR 3 remaining) + § Standing tripwires (BREWING.md size updated 213KB → 124KB) + § Scaling watch-items (BREWING.md balloon mitigation marked shipped). Brewing-side prompts updated: bundled-brewing-completion.md STEP 2 + propose-doc-changes-from-brew.md updated `target_doc` discipline to route cross-coffee content at `skills/brewing-historian/cluster/patterns/<file>.md`. **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 1 lib edit (mcp/docs.ts +21 SKILL_FILES + +21 DOC_DESCRIPTIONS + +21 listDocs); Actor 4 (MCP) — 21 new Resources; existing `skills/{path}.md` propose_doc_changes routing (shipped Wave 2 PR 1) covers brewing-historian paths via isKnownDoc validator; Actor 5 (Claude Code) — CLAUDE.md + CONTEXT.md + PRODUCT.md + README.md + this shipped.md row; Actor 2 (prompts) — 2 brewing-side prompt updates; Actor 3 (claude.ai) — picks up 21 new Resources + 1 updated brewing.md description on next session start; Actor 1 (Chris) — reads merged PR + reviews cluster behavior in next brewing session. **Verification**: `npm run check:mcp-bundle` 14/9 clean (no new globs needed); `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` 20 axes in sync (no registry changes); BREWING.md size delta lands at -88KB (within 60-80KB target band, slightly over). **Retro key findings**: (1) Three scope decisions surfaced via AskUserQuestion at session start (per-strategy split direction + pointer style + stub seeding depth) — Chris-locked Split-CCIL-with-Axis-1-staying + single-line pointers + stub-heavy-seed-only-N≥3. (2) `Edit` tool's exact-string requirement made the 350-line CCIL deletion impractical inline — used `head -n 584 + tail -n +935 + cat /tmp/pointer.md` Bash surgery to slice the file cleanly. (3) The Wave 1 + Wave 2 PR 1 retro lesson "grep first before authoring" caught 2 BREWING.md self-refs (Hybrid pointer at line 115 + End-of-Coffee doc-review at line 406) that needed updating beyond the bulk CCIL deletion. (4) Single-line pointer style preserved 100% of the master-doc shrink while keeping the back-compat anchor intact — the residual block is ~20 lines total. **Out of scope**: Wave 2 PR 3 (Roasting Historian — parallel ROASTING.md § Cross-Coffee Insight Layer extraction; same 60-80KB shrink target); Wave 3 (operator-stub clusters + 9 workflow tier sub-skills with POD-1 inside Cupping Specialist); Wave 4 (CCIL + BREWING.md / ROASTING.md redirect-stub rewrite). **Branch**: `claude/eloquent-booth-d8f402`. |
| 2026-05-26 | **Architecture Wave 2 PR 1 — WBC Brewing + WBC Roasting Archivists paired ship** | First Wave 2 PR (3-PR sequencing locked at kickoff: this WBC Archivists pair, then Brewing Historian PR 2, then Roasting Historian PR 3). Mechanical-moves PR — no master-doc shrink yet (that comes in PR 2 + PR 3 when Cross-Coffee Insight Layer + per-strategy patterns extract out of BREWING.md / ROASTING.md). **4 file migrations via `git mv`**: `docs/brewing/wbc-reference.md` + `docs/brewing/wbc-recipes.md` → `docs/skills/wbc-brewing-archivist/cluster/`; `docs/roasting/wbc-roasting.md` → `docs/skills/wbc-roasting-archivist/cluster/wbc-roasting.md`; `docs/roasting/wbc-sourcing.md` → `docs/skills/wbc-roasting-archivist/cluster/sourcing/strategy.md` (sub-dir reserved for future Sourcing-Knowledge split). Relative-path-depth fixes (2-levels-up → 4 or 5 levels-up) applied to all 4 files. **4 redirect stubs** (~500 bytes each) at the original paths preserve back-compat. **9 cluster sub-resources seeded** (Wave 2 stubs): WBC Brewing Archivist gets 6 per-strategy stubs (suppression / clarity-first / balanced-intensity / full-expression / extraction-push / hybrid) + 1 canonical stub (wbc-tested-recipes.md); WBC Roasting Archivist gets 2 sourcing nav stubs (portfolio-lanes.md / priority-targets.md pointing at sections of strategy.md) + 1 per-competitor README placeholder + 1 canonical stub (wbc-tested-cultivars.md). **2 SKILL.md flipped** from PLACEHOLDER → "Wave 2 PR 1 shipped 2026-05-26" with realized cluster contents enumerated. **MCP Resource registration** — `lib/mcp/docs.ts` SKILL_FILES adds 16 new entries (2 SKILL.md + 4 migrated cluster files + 6 per-strategy stubs + 2 canonical stubs + 2 sourcing sub-resource stubs) + 16 corresponding DOC_DESCRIPTIONS + 16 listDocs entries; 4 old DOC_DESCRIPTIONS for the migrated URIs flipped to "[MIGRATED in Wave 2 PR 1]" stub framing. `check:mcp-bundle` 14/9 clean. **propose_doc_changes target_doc allow-list extended** with `skills/{path}.md` shape ([lib/mcp/propose-doc-changes.ts](../../lib/mcp/propose-doc-changes.ts) — validates against `isKnownDoc(\`docs://skills/${path}.md\`)` so only registered SKILL_FILES paths resolve; Zod descriptions + error messages + 2 inline comments updated). **ARBITER.md routing** gains 5th target_doc table row + narrative paragraph documenting "WBC content edits land in the WBC Archivist clusters, not BREWING.md / ROASTING.md" per [master-doc-transition-plan.md § Risk: BREWING.md / ROASTING.md keep growing during migration](../architecture/master-doc-transition-plan.md). **Cross-system propagation** — CONTEXT.md (8 refs across 4 paths), BREWING.md (7 refs), ROASTING.md (3 refs), CLAUDE.md (2 doc-index sections), SYNC_V2.md (3 refs across Resources table + doc-storage section), [docs/adr/0001-two-axis-brewing-framework.md](../adr/0001-two-axis-brewing-framework.md), [docs/features/importer-exporter-scoping.md](../features/importer-exporter-scoping.md), [lib/extraction-modifiers.ts](../../lib/extraction-modifiers.ts) (2 comment refs), [lib/mcp/doc-tools.ts](../../lib/mcp/doc-tools.ts) (Tool description), [lib/hybrid-subform.ts](../../lib/hybrid-subform.ts) (1 comment ref). Historical sprint docs (shipped.md prior rows, kickoff briefs, grilling followups, dogfood-polish-pr-reconciliation-summary.md) intentionally NOT updated — point-in-time artifacts per Wave 1 precedent; redirect stubs preserve link resolution. **Prompts**: start-brew.md prepends WBC Brewing Archivist dispatch instruction for Step 1d Named Consideration lookups; bundled-brewing-completion.md adds WBC Brewing Archivist dispatch + `skills/{path}.md` target_doc shape in STEP 2; start-lot.md STAGE 2 adds WBC Roasting Archivist read paths for Tier 1/2/3 sourcing framing + same-green dev ladder. **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI, zero schema, 4 lib edits (propose-doc-changes.ts validator + targetDocToUri + 2 comments + extraction-modifiers.ts + doc-tools.ts + hybrid-subform.ts comments + mcp/docs.ts 16 SKILL_FILES additions); Actor 4 (MCP) — 16 new Resources + 4 stub-flagged descriptions + `skills/` target_doc shape; Actor 5 (Claude Code) — ARBITER.md + CLAUDE.md + CONTEXT.md + PRODUCT.md + SYNC_V2.md + 2 ADR/feature refs + this shipped.md row; Actor 2 (prompts) — 3 prompt updates; Actor 3 (claude.ai) — picks up 16 new Resources + 4 updated stub descriptions + `skills/{path}.md` target_doc support on next session start; Actor 1 (Chris) — reads merged PR + arbiter procedure surfaces new routing path. **Verification**: `npm run check:mcp-bundle` 14/9 clean; `npx tsx scripts/check-registry-md-sync.ts` 20 axes in sync (no registry changes); `npx tsc --noEmit` zero errors via worktree node_modules symlink. **Retro key surprises**: (1) Wave 1 retro's "brief was missing 5 ref sites" pattern recurred — initial grep surfaced 24 distinct files referencing the 4 source paths; pruning historical artifacts left 11 substantive update sites + 4 redirect stubs. The grep-first discipline is now standard practice. (2) Path-depth fixes on the migrated `sourcing/strategy.md` (depth 5) vs the other 3 (depth 4) added a per-file branching in the relative-link rewrites; this asymmetry will recur in any future "cluster sub-dir" migrations and is worth documenting in the master-doc-transition-plan. (3) propose_doc_changes target_doc validator extension was the load-bearing code change — without it, arbiter routing via ARBITER.md prose is advisory only. The `isKnownDoc(\`docs://${trimmed}\`)` gate keeps the validator strict without requiring per-cluster allow-listing. (4) Sourcing tentatively-merged-with-WBC-Roasting decision (Q4 from kickoff) preserved as ADR-0011 future-split trigger — `cluster/sourcing/` sub-dir is forward-investment for the day dedicated sourcing research grows beyond ~3-5 docs. **Out of scope**: Wave 2 PR 2 (Brewing Historian — extracts BREWING.md "Cross-Coffee Insight Layer" + per-strategy patterns into cluster; ~60-80KB BREWING.md shrink); Wave 2 PR 3 (Roasting Historian — parallel extraction from ROASTING.md); Wave 3 (operator-stub clusters + 9 workflow tier sub-skills; POD-1 inside Cupping Specialist); Wave 4 (CCIL + cross-system sync rewriting BREWING.md + ROASTING.md as redirect stubs). **Branch**: `claude/jolly-archimedes-db89cb`. |
| 2026-05-26 | **Architecture Wave 1 — Master Coordinator + Brewing Equipment Expert paired ship** | First implementation sprint of the composable sub-skills architecture per [ADR-0011](../adr/0011-composable-sub-skills-architecture.md) / [ADR-0012](../adr/0012-master-coordinator-pattern.md) / [ADR-0013](../adr/0013-self-improvement-primitives.md). Single PR; markdown + cluster migration only (no schema / Tool count / UI surface changes). **Master Coordinator catalog** — 4 files under [`docs/skills/coordinator/`](../skills/coordinator/) (SKILL + 18-sub-skill catalog + dispatch rules + handoff rules) form the lazy-loading dispatch surface for claude.ai sessions. **Brewing Equipment Expert cluster** — consolidates 8 existing assets under [`docs/skills/brewing-equipment-expert/`](../skills/brewing-equipment-expert/): SKILL.md + 4 `cluster/{brewers,filters,grinder-eg1,sworks}.md` migrated verbatim from `docs/taxonomies/` (with relative-path-depth updates 2-levels-up → 4-levels-up) + 4 `resources/{brewer,filter,grinder,sworks}-registry.md` pointer docs to the `lib/*-registry.ts` validation mirrors (which stay as code in `lib/`, not migrated). **Redirect stubs** (~200 bytes each) at the 4 original `docs/taxonomies/{brewers,filters,grinders,sworks}.md` paths preserve back-compat for any session referencing the old URIs. **MCP Resource registration** — `lib/mcp/docs.ts` adds 9 new entries (4 coordinator + 1 BEE SKILL + 4 cluster files) + `next.config.js` `outputFileTracingIncludes` extended with `./docs/skills/**/*.md` glob; `check:mcp-bundle` 14 paths / 9 globs (was 14/8). **Cross-system propagation**: `lib/{brewer,filter,grinder,sworks}-registry.ts` header comments + `scripts/check-registry-md-sync.ts` mdPath fields + `ARBITER.md` registries table + `PRODUCT.md` equipment + canonical-registries tables + `ROASTING.md` grinder reference + `CONTEXT.md` SWORKS resolution line + `CLAUDE.md` (new architecture section after Documentation Index + 3 taxonomy-section pointer updates) all updated. **Prompts**: `start-brew.md` + `bundled-brewing-completion.md` prepend a session-start instruction to fetch `docs://skills/coordinator/catalog.md` and dispatch equipment-knowledge questions to the Brewing Equipment Expert cluster. **Brief-flagged surfaces and the 5 additional touch points found at execution time** (registry comments + sync-check script + ARBITER.md + PRODUCT.md + ROASTING.md + CONTEXT.md) all closed in the same PR — propagation discipline per CLAUDE.md sprint cadence #4. **6-actor matrix walk**: Actor 6 (UI + schema + lib) — zero UI change, zero schema change, 5 lib edits (mcp/docs.ts + 4 registry header comments); Actor 4 (MCP) — 9 new Resources registered + 4 redirect-stub URIs updated to "[MIGRATED] use docs://skills/..." descriptions; Actor 5 (Claude Code) — CLAUDE.md architecture section + 3 taxonomy section pointers + CONTEXT.md SWORKS line + ARBITER.md table + PRODUCT.md 3 table-rows + 3 reference links + this shipped.md row; Actor 2 (prompts) — start-brew.md + bundled-brewing-completion.md prepend instructions; Actor 3 (claude.ai) — picks up 9 new Resources + 4 updated stub descriptions on next session start, dispatch rules now route equipment intent to BEE cluster; Actor 1 (Chris) — reads merged PR + reviews catalog/dispatch behavior in next brewing session. **Verification**: `npm run check:mcp-bundle` 14/9 clean; `npx -y -p tsx@4 tsx scripts/check-registry-md-sync.ts` all 20 axes in sync (brewers / filters / grinders / sworks resolving the new cluster paths); `npx tsc --noEmit` zero errors via the worktree node_modules symlink trick (CLAUDE.md build-hygiene rule). **Retro key surprises**: (1) The brief listed ~25-30 files but grep at pre-execution time surfaced 5 additional reference sites not in the brief (`scripts/check-registry-md-sync.ts` mdPath, `lib/{4}-registry.ts` header comments, `ARBITER.md` table, `PRODUCT.md` 6 links, `ROASTING.md` grinder link, `CONTEXT.md` SWORKS line). Closing them in the same PR honored the cross-system audit rule rather than leaving them as drift. (2) `git mv` preserved rename history on all 4 taxonomy files; content-only edits then updated the `../../lib/...` (2-levels-up) relative paths to `../../../../lib/...` (4-levels-up) without disrupting the rename detection. (3) Q1 from the brief (does taxonomy content fit cleanly under cluster/) answered empirically: yes — 4 files are self-contained authored taxonomies; only relative-path updates needed, no content restructuring. Q2-Q4 recommendations locked as stated. **Out of scope**: Wave 2 (4 consolidation ships — Brewing Historian + Roasting Historian + WBC Brewing Archivist + WBC Roasting Archivist; biggest BREWING.md / ROASTING.md shrink event); Wave 3 (operator-stub clusters + workflow tier; POD-1 lands inside Cupping Specialist here); Wave 4 (CCIL + cross-system sync rewriting BREWING.md + ROASTING.md as redirect stubs). The Surface 1 follow-up (Immersion modifier + 2 audit-prep typos) remains parked as separate tiny sprint. |
| 2026-05-26 | **Architecture brainstorm cluster — composable sub-skills + self-improving + cross-corpus (ADRs 0011 / 0012 / 0013)** | First architecture-sequencing-era sprint per Sprint R's Option 1 phase-2 lock. 4-round plan-mode brainstorm session covering 3 tightly-coupled questions (Q1 self-improving definition · Q2 cross-corpus joined learning · Q3 sub-skill decomposition per Chris's audio note). Shipped: **3 ADRs** ([0011 — Composable sub-skills architecture replacing monolithic BREWING.md/ROASTING.md](../adr/0011-composable-sub-skills-architecture.md) · [0012 — Master Coordinator pattern + lazy-loading dispatch](../adr/0012-master-coordinator-pattern.md) · [0013 — Self-improvement primitives: 9 patterns + 3-stage autonomy ladder + bloat tripwires](../adr/0013-self-improvement-primitives.md)); **18 sub-skill decomposition spec stubs** under `docs/skills/<sub-skill>/` (Wave-1 pair = Master Coordinator + Brewing Equipment Expert authored in full; 16 placeholders for later waves); [**master-doc transition plan**](../architecture/master-doc-transition-plan.md) wave-by-wave shrink schedule; [**Wave 1 implementation kickoff brief**](architecture-wave-1-kickoff-2026-05-26.md) paste-ready. **18-sub-skill enumeration**: 7 active knowledge (Roasting Historian / Brewing Historian / WBC Roasting Archivist [tentatively absorbs Sourcing Knowledge] / WBC Brewing Archivist / Brewing Equipment Expert / Peer-Learning Roasting Archivist / Roest Knowledge) + 9 workflow (4 planning: Roasting Assistant / Brewing Assistant / Learning Assistant / Sourcing Workflow Planner; 5 executing: Roast Recorder / Brew Recorder / Cupping Specialist / Roest API Worker / Close-Lot Specialist) + 2 special (CCIL / Master Coordinator) + 1 deferred (Learning Knowledge — promote when Learning Assistant has run ≥2 research tracks). **3-tier framing locked**: Knowledge (authors+maintains docs+supporting registries; output is the "knowledge cluster" — top doc + registries + sub-docs) / Workflow with plan-execute axis (planning constructs work, executing writes substrate; Chris's "all my things split between planning and executing — similar to claude code") / Cross-Coffee Insight Layer (CCIL — cross-domain synthesis above the two domain Historians' internal cross-coffee synthesis). **Master Coordinator outside the tiers** — single coordinator across both brewing+roasting (NOT per-domain coordinators because cross-domain handoffs are real: POD-1's roasting→brewing→roasting handoff is Chain 1 in coordinator/handoff-rules.md). **9 self-improvement patterns** codified in ADR-0013: A substrate-event refresh (push_* triggers) / B external-event refresh (WBC year, firmware) / C registry-expansion refresh (taxonomy_overrides_queue events) / D tier-threshold refresh (early→emerging→established→mature corpus crossings per CONTEXT.md § Corpus tier) / E workflow-execution refresh (sub-skill output observed) / F bloat-tripwire decomposition (cluster >120KB OR single doc >60KB — CCIL is the archetypal Pattern F user, self-decomposes via prompt-revision) / G catalog-update refresh (Master Coordinator only; cross-system audit at every sub-skill ship) / H dispatch-accuracy refresh (Master Coordinator only; override-log review) / I operator-initiated resource integration (Pattern I cross-cutting; e.g. Dongzhe livestream → Peer-Learning cluster via Claude Code session). **3-stage autonomy ladder** monotonic-with-rollback: Stage 1 in-loop (default Waves 1-2; every refresh arbited via propose_doc_changes) → Stage 2 override (N=5 default / N=10 substrate-writers / N=3 CCIL consecutive auto-approvals advance) → Stage 3 autonomous (override rate <10% across 3 quarters); auto-demote when override rate ≥10% Stage 3→2 / ≥25% Stage 2→1 — built-in immune system. **Architecture-level Pattern F tripwires**: catalog.md >30KB → propose domain-shard re-introduction; sub-skill cluster >60KB → self-decomposition; total docs/skills/ >300KB → Wave 5 architectural retro. **POD-1 absorbed into Cupping Specialist Wave 3** — sequencing collapse, no longer a free-standing sprint. POD-1 scoping draft preserved as historical artifact; PRODUCT.md POD-1 entry rewritten as "ABSORBED 2026-05-26 into Cupping Specialist" with pointer to docs/skills/cupping-specialist/SKILL.md. **Wave plan**: Wave 1 = Master Coordinator + Brewing Equipment Expert paired ship (proof-of-pattern; consolidates 8 existing files: 4 lib/{brewer,filter,grinder,sworks}-registry.ts + 4 docs/taxonomies/{brewers,filters,grinders,sworks}.md); Wave 2 = 4 consolidation ships (Historians + WBC Archivists; biggest BREWING.md/ROASTING.md shrink event ~60-80KB each); Wave 3 = 2 operator-stub clusters (Peer-Learning + Roest Knowledge; Chris stubs first per the taxonomy-sprint operator-stubs-then-integration handoff pattern) + 9 workflow tier sub-skills with POD-1 inside Cupping Specialist; Wave 4 = CCIL + cross-system sync (BREWING.md + ROASTING.md become ~500-byte redirect stubs). **5 PRs landed during this brainstorm session** (#196 producer taxonomy queue · #197 roaster taxonomy queue · #198 BREWING.md +7KB · #199 Picolot card · #200 ROASTING.md +12KB) — textbook live demo of Patterns A + C in the existing arbiter pipeline; the new architecture formalizes their structural homes (per-sub-skill clusters, per-pattern signals, autonomy graduation paths) rather than replacing them. **BREWING.md + ROASTING.md grew ~19KB DURING the brainstorm itself** — real-world reinforcement of architectural urgency; the bloat-tripwire failure mode is still accelerating at ~19KB per arbiter batch. **8 architectural primitives surfaced** + cataloged across the 3 ADRs: 3-tier framing / plan-execute axis / lazy-loading / self-decomposition / 3-stage autonomy ladder / two-level synthesis (internal-to-domain Historians → cross-domain CCIL) / operator-stubs-then-integration handoff / iteration-depth asymmetry (roasting iterates at substrate level via V-sets; brewing iterates in-thread only; explains why Cupping Specialist + Close-Lot Specialist exist but no brewing-side Palate Evaluator — Brewing Assistant absorbs the iteration-helper sub-prompt). **`docs/skills/<sub-skill>/` is canonical home** — `.claude/skills/` stays as-is (no individual sub-skill slash commands; Chris's entry surface is `docs/prompts/*.md` via claude.ai, NOT `/slash-skill` in Claude Code); `lib/agents/` adds code helpers only WHEN automation needed. **6-actor matrix walked**: Actor 6 — 22 new files (3 ADRs + retro + transition plan + Wave-1 kickoff + 4 coordinator/* files + 18 sub-skill SKILL.md stubs); Actor 4 — no MCP Tool/Resource changes (Wave 1 implementation registers the new Resources); Actor 5 — CLAUDE.md unchanged this PR (Wave 1 updates references); Actor 2 — no prompt changes (Wave 1+2 updates start-brew.md / bundled-brewing-completion.md to dispatch via Master Coordinator); Actor 3 — claude.ai picks up new docs/skills/ Resources on next session start (Wave 1 onward); Actor 1 — Chris reviews ADRs + Wave-1 kickoff brief. **Pure document + spec output; zero substrate writes; zero schema/code/migration/Tool changes** — this brainstorm is the architectural decision; implementation lands in Waves 1-4. **Surface 1 follow-up (Immersion modifier + 2 audit-prep typos)** parked as separate tiny sprint post-Wave-1. **Branch:** `claude/architecture-brainstorm-2026-05-26`. |
| 2026-05-25 | **Sprint R — PRODUCT.md roadmap restructure + audit cluster prep + architecture brainstorm kickoff** | First post-grilling-sequencing-era sprint. Triggered by Chris's input-collection turn (2026-05-25) that brought 9 new big-picture / tactical items into Sprint R's original 3-part scope (PRODUCT.md currency / deferred candidate prioritization / next-sprint kickoff brief). Walked the 9 inputs holistically against the brief's deferred candidates (POD-1 / DF-SCH-CAND-5 / CR-7-promote / CR-9 / BCP-3..8 / BR-6-9 / MCP-6 / DF-OPS1-2) plus the still-queued Sprint 3.3 (Dropbox handoff from earlier session — verified not shipped, all 3 items intact: no `supersede_ids` param, no summary-parse path, no SYNC_V2 cache doc). **Structural observation surfaced + locked**: BREWING.md (204KB) and ROASTING.md (132KB) are both past the 120KB tripwire codified in CLAUDE.md sprint cadence; Chris's input #7 audio note proposes decomposing both into composable sub-skills with a master coordinator (roasting assistant / roast recorder / Roest expert / cupping specialist / roasting historian / peer-learning archivist / WBC champion archivist / sourcing assistant / learning assistant / master coordinator). The tripwire already fired; this is the architectural response. **Chris-locked Option 1 sequence** via AskUserQuestion: audit cluster → architecture brainstorm → architecture implementation → roadmap re-session. POD-1 explicitly deferred behind architecture work ("I can delay POD-1 because I can deal with the cycle within my current claude.ai thread so not having the feature inside latent is not blocking my actual workflow"). **Deliverables**: (1) PRODUCT.md § Active Sprints completely restructured around the 4-phase sequence — removed stale "Per-entity directed synthesis" item #8 (shipped 2026-05-08), demoted 3.3-3.7 + POD-1 + General cleanup Track 3 to new "Queued post-architecture" section, added new "Deferred candidates" section enumerating 14 deferred items from post-grilling-sequencing.md; (2) 3 audit prep artifacts under `docs/sprints/sprint-r-audit-prep-*-2026-05-25.md` — claude.ai memory diff (7 sections covering canonical taxonomies / 35 Tools / Resources / ADRs 0001-0010 / recent substrate / resolved ambiguities / standing principles), roasting workflow baseline (5-phase breakdown from intake through close-out + one-shot variant, pulled literally from 6 roasting prompts), brewing workflow baseline (5-phase breakdown + asymmetry-vs-roasting section); (3) 2 kickoff briefs under `docs/sprints/*-kickoff-2026-05-25.md` — audit cluster execution (uses Sprint R prep artifacts, ~1-2h Chris-side) + architecture rethink cluster (Q1 self-improving / Q2 cross-corpus / Q3 sub-skill decomposition framed as one coherent brainstorm with 4-round shape + expected 2-3 ADR output + decomposition spec); (4) PRODUCT.md Side Quests addition for input #4 (generic feedback handoff queue scoping); (5) PRODUCT.md Longer Term updates for inputs #8 (design system merge ready, delayed) + #9 (page-by-page polish should precede design merge); (6) PRODUCT.md Scaling Watch-Items tripwire annotated with architecture brainstorm cluster as the response. **9 Chris-inputs mapped to roadmap**: #1 (claude.ai memory audit) → audit cluster sprint #1; #2 (POD-1) → Queued post-architecture; #3 (Chris workflow audit) → audit cluster sprint #1; #4 (generic feedback queue) → Side Quests + folded into #7 sub-skill brainstorm; #5 (cross-brewing/roasting joined learning) → architecture brainstorm Q2; #6 (self-improving definition) → architecture brainstorm Q1; #7 (sub-skill decomposition audio note) → architecture brainstorm Q3 PRIMARY; #8 (design system merge) → Longer Term Redesign update; #9 (page polish first) → Longer Term Redesign update + General cleanup Track 3 expansion. **Spawn agent pattern used for parallel research**: 2 Explore agents in parallel pulled the roasting + brewing workflow baselines from the 11 prompt files + master refs + CONTEXT.md while Sprint R's main thread restructured PRODUCT.md inline. Brewing agent hit a constraint violation mid-run but recovered with a slimmer-but-usable output; roasting agent returned a comprehensive 5-phase + one-shot-variant breakdown with per-phase MCP Tool list + vocabulary + hand-off points + 6 "notable substrate gap" candidates Chris may surface in the audit. **Substrate change minimal**: zero schema / code / migration / Tool changes; PRODUCT.md restructure + 5 new doc files + shipped.md row. 6-actor matrix walk per CLAUDE.md cadence #4 — Actor 1 (Chris): rendered prose in PRODUCT.md / new audit prep doc set / kickoff briefs; Actor 2 (prompts): zero changes; Actor 3 (claude.ai catalog): zero Tool / Resource changes (new docs are session-scoped, intentionally not registered as Resources per the audit-trail convention from `docs/audits/2026-05-18/*`); Actor 4 (MCP): zero changes; Actor 5 (Claude Code docs): PRODUCT.md restructured; Actor 6 (app schema + UI): zero changes. Per `feedback_sprint_closeout_roadmap_currency.md`, this row + the memory retro at `memory/project_sprint_r_roadmap_restructure.md` ship in the same PR as the PRODUCT.md edit. **Open questions surfaced but deferred**: ADR-0006 promotion check (Q3 from kickoff brief) deferred per Chris-locked "wait for 3rd cross-party audit" trigger — Sprint F + Sprint M may count as patterns but the original framing was cross-party grilling specifically. **Handoff to audit cluster**: audit-execution sprint kickoff brief is paste-ready; Chris reads claude.ai memory diff + 2 workflow baselines, walks both surfaces, logs findings into `memory/feedback_mcp_continuous_log.md` Batch N+1 + bundles retro into `memory/project_audit_cluster_2026-05-XX.md`. Architecture brainstorm cluster opens after audit cluster closes. **Branch**: `claude/sprint-r-roadmap-review-2026-05-25`. |
| 2026-05-24 | **Sprint F — Final cross-system sync (Sprints 0-14 audit + 4 shipped.md row backfills)** | Audit-only sprint closing the post-grilling sequencing era. Read-only 6-actor matrix walk against Sprints 0-14 (11 schema migrations 055-065, 35 MCP Tools, 4 new ADRs 0007-0010, 7 prompt files touched, 3 SynthesisCard responsive surfaces); audit found one drift — `docs/sprints/shipped.md` missing 4 of 13 sprint-era rows (Sprint 0 / Sprint 9 / Sprint M / Sprint 14) — fixed inline as Chris-decision ride-along 2026-05-24. **Substrate inventory + full per-actor walk** locked to [docs/sprints/sprint-f-audit-2026-05-24.md](sprint-f-audit-2026-05-24.md). **All hops clean except the shipped.md drift**: Actor 6 (schema + lib + UI) — every migration column lands in `lib/types.ts` with provenance comments; ResolvedView reads `pourover.aromatic_behavior` + `pourover.structural_behavior` (Sprint 11 character relocation); SynthesisCard renders short-form below `md:` (Sprint 13 SYN-3). Actor 4 (MCP) — Tool count 35; every Sprint 10-14 schema column has matching Zod field with migration + ADR cited in `.describe()`; `check:mcp-bundle` 14/8 clean. Actor 5 (Claude Code) — CONTEXT.md glossary entries present for every Sprint 10-14 substrate change with explicit Sprint-N + migration + ADR back-links; ADR-0006 gap is intentional (reserved for RO-CP-9 substrate-practice gap audit candidate, deferred per Chris-locked "wait for a 3rd cross-party audit" trigger). Actor 2 (prompts) — zero stale-vocab hits across the 7 roasting-side + 4 brewing-side prompt files. Actor 3 (claude.ai catalog) — `lib/mcp/docs.ts` registers all 14 doc Resources including sworks.md + importer-exporter-scoping.md + redesign.md; TAXONOMY_AXES = 11; `docs/audits/2026-05-18/` files intentionally not Resources (internal audit trail, not catalog-served). Actor 1 (Chris UI) — render paths surface every Sprint 10-14 schema column coherently. **5 shipped.md rows inserted in this PR**: Sprint F above Sprint 13 row; Sprint 14 at 2026-05-23; Sprint 9 + Sprint M below Sprint 10 at 2026-05-19; Sprint 0 below the 2026-05-18 cluster. Row content for backfills derived from PR titles, commits, retros, and the audit substrate inventory. **Retro key surprise**: Sprints 10-14 each documented their 6-actor matrix walk in their own shipped.md rows + retros, so Sprint F's job collapsed from "re-derive from scratch" to "spot-check assertions" — the per-sprint discipline pays compounding audit dividends. The one drift caught (4 missing shipped.md rows) is the same drift Sprint 10's retro had already flagged — the "separate follow-up" deferral pattern compounds when not closed promptly. **Handoff to Sprint R**: POD-1 (pour-over discriminator gate + optimized brew lifecycle states) tops deferred sprint candidates per [post-grilling-sequencing.md § Deferred sprints](post-grilling-sequencing.md); DF-SCH-CAND-5 enumeration decision (4 of 5 already shipped via Schema sprints + Sprints 11-12); CR-7-promote SWORKS structured-pour-structure column; CR-9 / WBC-4-large water taxonomy bootstrap. **Branch**: `claude/sprint-f-final-cross-system-sync-2026-05-24`. |
| 2026-05-23 | **Sprint 14 — Roasting prompt vocab + recipe-noun canonicalization (RO-CP-6 + RO-4 + DF-ARB1)** | Final roasting-cluster sprint in the post-grilling sequencing series. Three bundled items; zero schema / MCP / UI change — 5-file diff covering 3 prompt edits + CONTEXT.md L1552 closure + 3 new glossary entries. **RO-CP-6** (canonical-noun asymmetry locked): **recipe** is the canonical aggregate noun for the complete roast specification before machine execution — the `roast_recipes` schema entity. **Roest profile** names the machine artifact (JSON pushed to the tablet via `push_roast_profile`); one recipe row produces one or more Roest profile pushes. **Anchor profile** / **reference roast profile** / **CF-Light profile** / **washed profile** / **natural profile** / **working profile** preserved verbatim as **curve-shape names** — distinct from both the recipe aggregate and the Roest profile machine artifact (curve-shape names describe transferable starting frameworks at V1 design time, not rows in `roast_recipes` and not JSON pushes to the Roest tablet). Drift cases scrubbed: `one-shot.md` STAGE 2 title ("Design the single profile" → "Design the single recipe") + `<Z spec>` placeholders in primary_question / rationale templates → `<Z recipe>` + "key recipe specs one-liner" in close-lot.md + one-shot-closeout.md confirmation outputs → "key recipe summary one-liner". The Green Spec → Starting Hypothesis table preserves "spec" because it means physical bean specification (density / moisture / altitude) — a distinct meaning. **RO-4** (final pass of prompt vocab audit): three drift scrubs above; remaining RO-4 surface from prior Sprints 6-13 already complete (zero `elasticity\b` hits outside the historical-rename comment in close-lot.md; zero "Hybrid Washed" hits). **DF-ARB1** (CCIL consolidation arbiter task): ARBITER.md procedure validated against the CCIL Round 4 #11 consolidation pass (shipped 2026-05-18 PR #178) — promote 8 to Confirmed Patterns, relocate 10 Lows to Working Hypotheses. **CONTEXT.md** L1552 strikethrough resolution ("Resolved 2026-05-23 Sprint 14 / RO-CP-6"). Three new glossary entries: **Recipe** (aggregate noun for design intent at L34), **Roest profile** (machine artifact at L37), **Anchor profile / reference roast profile / curve-shape names** (preserved canonical concepts at L41). **6-actor matrix walked**: Actor 6 (UI + schema + lib) — n/a (zero code change); Actor 4 (MCP) — n/a (zero Tool / Resource change); Actor 5 (Claude Code) — CONTEXT.md L1552 + 3 new glossary entries; Actor 2 (prompts) — 3 prompt edits (`one-shot.md`, `close-lot.md`, `one-shot-closeout.md`); Actor 3 (claude.ai) — picks up updated CONTEXT.md + prompt vocabulary on next session start; Actor 1 (Chris) — no UI render impact (vocabulary lock is doc-only). **Verification**: 3 prompt-side greps post-edit (zero hits for "Design the single profile" / "<Z spec>" / "key recipe specs one-liner"); CONTEXT.md cross-references intact. **Backfilled in Sprint F 2026-05-24** — original PR (#191, merged 2026-05-19) did not include a shipped.md row at ship time; this row reconstructs the substrate change from PR title + commit + CONTEXT.md L1552/L1564 narrative. **Branch**: `claude/sad-jackson-1fb14c`. |
| 2026-05-22 | **Sprint 13 — Cross-source synthesis + resynthesize trigger + mobile short-form (SYN-6 + SYN-7 + SYN-3 + ADR-0010)** | Third post-grilling-sequencing sprint and the first synthesis-pipeline-touching sprint since Sprint T4. Three bundled items + ADR; migration 065 (8 column adds on 4 cache surfaces — `short_form_capsule text` + `synthesis_input_max_updated_at timestamptz` on `terroirs` / `cultivars` / `roaster_syntheses` / `process_aggregation_syntheses`). **SYN-6 — cross-source brewing + roasting unified capsule** (largest substrate gap from 2026-05-16 grilling): terroir / cultivar / roaster adapters now read both `brews` AND `roast_learnings` rows joined through `green_beans.{terroir_id,cultivar_id}` (Latent path on the roaster adapter pulls all roast_learnings unconditionally). New optional `EntityAdapter.formatRoastLearningRow` companion + `BuildPromptInput.roastLearnings` field carry the cross-source corpus through to a second JSON block in the prompt. Three new SHARED_RULES directives at the top of the rules block: (1) unified-narrative bridging (anti-pattern: appended brewing/roasting sections), (2) `is_process_dominant` flagging per Q5 (heavy-process brews — 6/82 brews including Mandela XO + Janson Green-Tip + El Paraiso Luna — surface with explicit process attribution rather than generalizing to anchor), (3) roasting-vocab preservation. Tier classification (`classifyTier`) now counts the combined brews + roast_learnings corpus. **Process adapter deferred per ADR-0010 Q2** — `green_beans` has no `signature_method` column and the 2 brews with `brews.signature_method` populated both have `green_bean_id=NULL`, so the signature-join path surfaces zero rows; modifier-level cross-source through reference-roast brews is structurally heavier and not warranted at current corpus size. Pre-flight DB scan confirmed cross-source lift is thin today (best case: Bench Sheko terroir +2; Gesha cultivar +2; Latent roaster +7) — wiring is forward investment as much as present payoff. **SYN-7 — resynthesize trigger gap closed**: every cache surface persists `synthesis_input_max_updated_at` (max `updated_at` across the corpus rows at synthesis time); page-side, new helper `computeInputMaxUpdatedAt` in [lib/synthesis/inputUpdatedAt.ts](../../lib/synthesis/inputUpdatedAt.ts) recomputes the same value from the rendered corpus + roast_learnings and passes to SynthesisCard. Trigger is belt-and-suspenders — either the brew-count delta OR the updated_at delta fires regen. Closes content-change scenarios 2 + 3 (rewritten `what_i_learned`, newly-filled `roast_learnings`); scenarios 1 + 4 (anchor FK reassignment, registry rich-anchor code edits) stay manual-Regenerate per ADR-0010 Q4. **SYN-3 — mobile short-form variant**: new [`lib/synthesis/buildShortFormPrompt.ts`](../../lib/synthesis/buildShortFormPrompt.ts) builds the 3rd-call prompt (1-2 paragraphs + 2-3 takeaways, vocabulary preservation enforced); `runSynthesis` now sequences 3 Sonnet 4.6 calls (raw → humanizer → short-form digest); SynthesisCard renders short-form below `md:` breakpoint via `<div className="md:hidden">` + long-form at `md:` and up via `<div className="hidden md:block">`. Long-form fallback when `short_form_capsule` is NULL (post-migration / pre-regeneration / best-effort 3rd-call failure). **ADR-0010** locks the 3-call cost shape (~3x latency + credit per regeneration vs ADR-0002's 2-call baseline, bounded by lazy-regeneration trigger) + Q2 process-side deferral rationale + Q3 unified-narrative design + Q5 prompt-level flag. **Humanizer `USER_OVERRIDE`** extended with one categorical bullet covering roasting vocabulary (primary lever / brewing tolerance / acceptable roast window / underdevelopment signal / overdevelopment signal / aromatic behavior / structural behavior / rest behavior / scope tags). **8 consumer pages wired** — terroirs/[id] / cultivars/[id] / roasters/[slug] / processes/[base] / processes/[base]/[subprocess] / processes/[base]/modifiers/[combo] / processes/modifiers/[modifier] / processes/signatures/[name]. Each: cache select extended with `short_form_capsule, synthesis_input_max_updated_at`; SynthesisCard receives 3 new props. Terroir + cultivar pages add a `roast_learnings`-side `updated_at` query joined through `green_beans.{terroir_id,cultivar_id}`; roaster page adds it only on the Latent path. **Sprint 12's `*_scope_tags` arrays** flow through the new `formatRoastLearningRow` adapters as forward-compat reads — all 7 closed lots have empty `[]` arrays today (Sprint 12 shipped columns, no claude.ai workflow has populated them yet); rendering tolerates empty. **6-actor matrix walked**: Actor 6 (lib + UI + schema) — migration 065 + 5 lib/synthesis/ files (types.ts + buildPrompt.ts + new buildShortFormPrompt.ts + runSynthesis.ts + humanizer.ts) + new inputUpdatedAt.ts helper + 3 adapter extensions + 4 synthesize route updates + SynthesisCard responsive render + 8 consumer page wires + lib/types.ts (Terroir + Cultivar 2 fields each); Actor 5 (Claude Code) — CONTEXT.md 5 strikethroughs (Cross-source intent / Known gap / process_dominant wiring closed / Mobile short-form resolved / Brewing-only resolved) + CLAUDE.md AI Synthesis paragraph rewritten + ADR-0010 + this shipped.md row; Actor 4 (MCP) — zero new Tool / Resource; check:mcp-bundle 14 paths / 8 globs clean; Actor 2 (prompts) — zero `docs/prompts/*.md` edits (synthesis prompts live in lib/synthesis/, not docs/); Actor 3 (claude.ai) — picks up new short-form + cross-source capsule reads on next page render; no MCP catalog impact; Actor 1 (Chris) — rendered terroir/cultivar/roaster pages now incorporate roasting-side learnings in unified narrative; mobile readers see short-form below md:. **Verification**: `npx tsc --noEmit` zero errors via worktree symlinked node_modules; `check:mcp-bundle` 14/8 clean; `check:registry-sync` all 20 axes in sync (no registry changes). **Out of scope**: scenario 1 + 4 SYN-7 coverage (manual Regenerate territory); process adapter SYN-6 path (deferred per ADR-0010 Q2 until either `green_beans.signature_method` lands or modifier-via-reference-roast join earns its keep); is_process_dominant promotion to adapter-level weight tier (prompt-level flag first; promote if signal warrants). **Branch:** `claude/sprint-13-cross-source-synthesis-2026-05-22`. |
| 2026-05-21 | **Sprint 12 — Roasting scope-tag + MCP queue + arbiter extensions (RO-CP-5 + MCP-1 + CR-4 + CR-10 + ADR-0009)** | First non-schema-only sprint of the post-grilling sequencing series. Four bundled items + ADR; touches the largest cross-actor surface to date. **RO-CP-5** (migration 064 + ADR-0009): 4 new `text[]` `*_scope_tags` columns on `roast_learnings` — `cultivar_takeaway_scope_tags` / `terroir_takeaway_scope_tags` / `general_takeaway_scope_tags` / `starting_hypothesis_scope_tags`. NOT NULL DEFAULT `'{}'::text[]`; no historical backfill (7 closed lots stay with empty arrays). **Loose-canonical prefix convention**: prompts describe (`close-lot.md` STAGE 3 + `one-shot-closeout.md` STAGE 4 capture); write paths do NOT enforce. Conventional prefixes locked in CONTEXT.md § Scope-tag prefix convention: `process:washed` / `variety:sudan-rume` / `country:colombia` / `altitude:high` / `evaluation_method:day-7-pourover` / `density:high` / `general`. Sudan Rume Washed→Natural transition is the case study locked in ADR-0009 (FC-temp-architectural-constraint hypothesis emerged because washed-anchor carry-forwards didn't transfer cleanly; cross-lot queries unreliable via grep when 4/7 lots had implicit-only scope). Pre-flight on 7 closed lots before plan-mode confirmed scope IS rich + INCONSISTENT in prose today: 3/7 explicit "For washed Colombians" / "For XO-fermented" prose, 4/7 unscoped general principles. **MCP-1** (migration 063): widen `taxonomy_overrides_queue.axis` CHECK enum 7→8 by adding `signature_method`. `push_brew` gains `signature_method_override: boolean` mirroring producer/roaster pattern; new `findOrCreateSignatureMethod` helper in `lib/brew-import.ts` joins the existing 5-axis canonical-text validation pattern. `validateBrewPayload` strict signature_method check replaced by the override-aware helper in `persistBrew`. Pre-flight confirmed signature_method lives on `brews` only (not `green_beans`) — push_green_bean wiring NOT needed; simplified scope. 2 existing brews carry signatures (Moonshadow / TyOxidator), both canonical, no data conflicts. Queue trio (`propose_canonical_addition` / `list_taxonomy_queue` / `resolve_queue_entry`) AXIS_VALUES enums all widened. **CR-4** (new MCP Tool `list_skeleton_entries`): Tool count 34→35 below the 50-Tool tripwire. Third arbiter queue type alongside `doc_proposals` + `taxonomy_overrides_queue`. Reads pure module-level data from `lib/producer-registry.ts` + `lib/roaster-registry.ts` (no DB). Returns `{ axis, name, country, location, populated_fields[] }` for arbiter promote-vs-alias-vs-reject judgment. ARBITER.md gains new § Skeleton review section (S1–S5 procedure) — registry-file edit IS the state transition (no DB row, no status flip; mirrors `resolve_queue_entry`'s edit pattern). **CR-10**: `RoasterEntry.skeleton?: true` type field added; Latent backfilled as the inaugural roaster skeleton. Audit-pass for the 21 sprint 1h.1 net-new roasters deferred to incremental arbiter sessions per Sprint 12 plan. Helper exports `listSkeletonRoasters()` mirroring `listSkeletonProducers()` extracted from `PRODUCERS.filter` pattern. **ADR-0009**: locks per-field scope_tags shape + loose-canonical prefix convention + the Sudan Rume Washed→Natural case study + rejection of 3 alternatives (per-lot `is_process_dominant`-style flag — too coarse; extend ADR-0005 precedence framework — over-engineered for 7-lot corpus; accept prose-only with mandated "For X:" prefix — text-matching against free vocabulary drifts faster than structured tags). Reversal cost low (text[] is additive). **MCP Tool surface** (8 files): `push_roast_learnings` + `patch_roast_learnings` add 4 `*_scope_tags: z.array(z.string()).optional()` Zod fields with describe carrying the prefix convention + Sprint 12 schema-notes paragraph; `push_brew` adds `signature_method_override` Zod field + describe + extends `overridable[]` array + validation-hint regex updated to include `signature_method`; queue trio AXIS_VALUES enums widened; new `list_skeleton_entries.ts` written + registered in `server.ts`. **lib/types.ts** `RoastLearning` interface gains 4 `string[]` fields (non-nullable, default `[]`). **lib/roast-import.ts** `RoastLearningsPayload` + `persistRoastLearnings` insert payload + `ROAST_LEARNINGS_PATCH_FIELDS` all extended. **lib/brew-import.ts** adds `BrewPayload.signature_method_override`, exports `findOrCreateSignatureMethod` helper + `FindOrCreateSignatureMethodResult` type, wires through `persistBrew` (added to error aggregation + narrowing + structured override + `fireQueueInserts` axis list). **Living docs**: ARBITER.md gets Skeleton review section + signature_method row in T2 axis table; `docs/features/taxonomy-overrides-queue.md` axis count 7→8 + table row + axis enum CHECK + D3 description softened; CONTEXT.md L728 axis-count update + L742 strictness-tier update + L983 enumeration update + L1521 Sprint T1 trailing-sentence update + L1522 + L1538 + L1539 + L1546 (RO-CP-5 carry-forward scope-tag flagged ambiguity) all struck-through with "Resolved 2026-05-21 Sprint 12" resolution language; 3 new CONTEXT.md entries — **Scope tags** (Roasting cluster), **Scope-tag prefix convention** (Roasting cluster), Skeleton entry queue extended in-place on existing **Skeleton entry** entry. CLAUDE.md MCP server status + Tool count tripwire + ROAST LEARNINGS block all updated to 35 Tools / 14+4 scope-tag fields. **Prompts** (2 files): close-lot.md STAGE 3 carry-forward section gets full prefix-convention preamble + per-field scope_tags capture guidance (each of 4 fields); one-shot-closeout.md STAGE 4 gets parallel preamble (scope_tags independent of confidence prefix) + per-field guidance (emphasizing aggressive tagging on `starting_hypothesis` to compensate for N=1 confidence floor). **Substrate audit (6-actor matrix)**: Actor 6 (UI + schema + lib) — migrations 063 + 064 + `lib/types.ts` + `lib/roast-import.ts` (3 spots) + `lib/brew-import.ts` (BrewPayload + helper + persistBrew); Actor 4 (MCP) — 8 Tool files + new Tool + server registration + Tool descriptions document Sprint 12 schema notes; Actor 5 (Claude Code) — CONTEXT.md 7 strikethroughs + 2 new entries + 1 extended entry + CLAUDE.md + ARBITER.md + ADR-0009 + docs/features/taxonomy-overrides-queue.md + this shipped.md row; Actor 2 (prompts) — close-lot.md + one-shot-closeout.md; Actor 3 (claude.ai) — picks up widened AXIS enum + new override field + 4 scope_tags fields + new `list_skeleton_entries` Tool on next session start; Actor 1 (Chris) — arbiter procedure now surfaces 3 queue types; no rendered UI change this sprint (scope_tags capture happens at MCP write time, UI surfacing deferred). **Verification**: `npx tsc --noEmit` zero errors via worktree symlinked node_modules per CLAUDE.md guidance; `npm run check:mcp-bundle` + `check:registry-sync` clean; migrations applied via Supabase MCP `apply_migration` + post-apply `execute_sql` round-trip confirmed queue CHECK widened to 8 + 4 scope_tags columns landed with `'{}'::text[]` defaults; 7 closed lots verified with empty arrays. **Retro key surprises**: (1) Pre-flight DB query on `signature_method` column locations ruled out push_green_bean wiring early — saved scoping effort that the kickoff brief allowed for. (2) Pre-flight scope-prose analysis on 7 closed lots was the load-bearing input to Q1 — prose IS rich (7/7 carry-forwards populated) but scope IS inconsistent (3/7 explicit + 4/7 implicit), making the "structured tag" decision clean rather than borderline. The "not urgent at ~5-10 closed lots" caveat from the source brief was overridden by the queryability argument, and the case study (Sudan Rume) made the gap concrete. (3) `list_skeleton_entries` as a new Tool felt right at design time and remained right at implementation — extending `list_taxonomy_queue` would have conflated two distinct lifecycles (queue rows = pending decisions; skeleton flags = committed state). The Tool count cost (34→35) is below the tripwire. (4) `findOrCreateSignatureMethod` slots into the existing 5-axis `validateCanonicalText` pattern with no special-case logic — Sprint T1's `SIGNATURE_LOOKUP` (15 canonicals) was already shape-compatible with `CanonicalLookup`, so the wiring is mechanical. (5) Loose-canonical prefix convention vs strict-canonical enum was the right call at 7-lot corpus — prompts capture the convention, ADR-0009 locks the examples + reversal cost, and `makeCanonicalLookup` wrappers can ride along later if drift bites. (6) Scope_tags UI surfacing deferred — the data substrate exists for cross-lot SQL queries today; rendering on `/green/[id]` is a future ride-along once tags accumulate on new lots. **Out of scope**: Sprint 13 (cross-source synthesis + resynthesize trigger — SYN-6 + SYN-7 + SYN-3 mobile short-form; depends on Sprint 11's cuppings schema being stable); patch_brew Tool surface (does not exist today; signature_method correction on existing brews routes through `PATCH /api/brews/[id]` or defers); `canonicalizeOverridablePatch` extension for signature_method on the PATCH route (deferred follow-up — signature is rarely re-canonicalized on existing brews); scope_tags rendering in `/green/[id]` ResolvedView (deferred — present surface is the prose carry-forward, tags are for query-side at this sprint); audit-pass for 21 sprint 1h.1 net-new roasters (incremental arbiter sessions). **Branch**: `claude/optimistic-sutherland-ef128d`. |
| 2026-05-20 | **Sprint 11 — Roasting schema migrations bundle 2 (RO-CP-3 + RO-5 + RO-6 + ADR-0008)** | Second schema-touching sprint after the dogfood-merge gate opened. Three bundled items + ADR landing on `roast_learnings` + `cuppings` + `roasts`. **RO-CP-3** (migration 061): add `roasts.fc_audibility` text CHECK enum (`audible` / `subtle` / `silent` / `ambiguous`) — surfaced in 2026-05-17 cross-party grilling round 4; structures the audibility state that was free-text in roast prose. No backfill: 135 historical roasts left NULL (only 6 had `fc_total_cracks` populated, too sparse to backfill reliably). **RO-5 ≡ RO-CP-8**: prompts-only enforcement on underdev/overdev cup-side strictness. Pre-flight audit reframed the sprint — all 3 of 7 closed lots with populated `underdevelopment_signal` / `overdevelopment_signal` (Mandela XO / Sudan Rume Hybrid Washed / Surma) already follow a clean **cup-first + labeled-roast-correlate pattern** (cup observation first, then optional `Roast-level signal: Agtron WB above 77 / FC below 200°C / Maillard above 50%` correlate). No data move needed; close-lot.md + one-shot-closeout.md formalize the pattern for future writes. No new `roast_anomalies` column ([feedback Q4](https://github.com/anthropics/apps): prompts-only over column-add). **RO-6** (migration 062): relocate `aromatic_behavior` + `structural_behavior` from `roast_learnings` to `cuppings` per ADR-0008 / Option A (single canonical cup per lot). Migration's data move: per closed lot, write prose to the canonical pourover cupping on `best_roast_id` (priority `eval_method ILIKE '%pourover%'` DESC, then `cupping_date` DESC, then `created_at` DESC, LIMIT 1). All 7 closed lots backfilled deterministically (preflight confirmed 1:1 target match). Columns dropped from `roast_learnings`. **ADR-0008**: codifies the per-cup vs lot-aggregate framing distinction; cites Option B (write to all cuppings on best_roast_id — redundant) + Option C (keep on both — two-home ambiguity) as alternatives; relationship to ADR-0001 (Two-Axis Brewing) since these are cup-side observation primitives the brewing-side framework consumes; reversal cost noted as substrate-wide unwind. **MCP Tool surface** (6 files): `push_roast` + `patch_roast` add `fc_audibility` Zod field + describe; `push_cupping` + `patch_cupping` add `aromatic_behavior` + `structural_behavior` Zod fields + describe with relocation provenance; `push_roast_learnings` + `patch_roast_learnings` REMOVE both fields from Zod + Tool descriptions document the move + add prose pointer ("push them via push_cupping"). `CUPPING_PATCH_FIELDS` + `ROAST_PATCH_FIELDS` + `ROAST_LEARNINGS_PATCH_FIELDS` updated. `lib/types.ts` Roast interface gains `fc_audibility`; Cupping gains both prose fields; RoastLearning drops both. `lib/roast-import.ts` payload + persist + patch field lists updated. **UI**: `app/(app)/green/[id]/page.tsx` ResolvedView drops the two `<LearningRow>`s from ROASTING LEARNINGS card; REFERENCE CUP pourover sub-card renders `aromatic_behavior` + `structural_behavior` as labeled blocks below `overall` (or after the CupRow ladder in the fallback branch). `components/PerRoastReflections.tsx` extends the populated-filter + adds an FC audibility row inside the collapsed `<details>` block (per-roast drill-down rather than 10th column on RoastLogTable per kickoff Q3). **Prompts** (5 files): `log-roast.md` adds `fc_audibility` capture instructions in the STAGE 2 silent-FC bullet + adds to STAGE 3 numeric-fields-list; `log-cupping.md` STAGE 2 push_cupping section adds `aromatic_behavior` + `structural_behavior` to the prose fields list (8 → 10); `close-lot.md` STAGE 3 drops the aromatic/structural bullets + tightens the underdev/overdev paragraph to encode the cup-first + labeled-roast-correlate pattern + reframes the rest section as "Lot-level rest behavior" with a relocation note; `one-shot.md` STAGE 3 adds fc_audibility to the silent-FC bullet + numeric-fields-list AND STAGE 4 adds aromatic/structural to the 10 prose fields; `one-shot-closeout.md` STAGE 4 strikes the aromatic/structural bullet + redirects to one-shot.md STAGE 4 push_cupping. **CONTEXT.md retirement**: L1511 (underdev/overdev intermix flag) + L1512 (aromatic/structural relocation flag) struck through with `Resolved 2026-05-20 via Sprint 11 / migration 062 / ADR-0008` resolution language; § Cup character header drops "(currently misplaced in schema)" parenthetical; § FC audibility state appends schema column note; § Aromatic behavior + Structural behavior body text updated to cite cuppings storage + Sprint 11 / ADR-0008 provenance. **CLAUDE.md**: two new sibling entries — `roasts.fc_audibility` (next to is_reference_candidate) + `cuppings.aromatic_behavior + structural_behavior` (next to cuppings.sweetness + temperature_behavior). **docs/roasting/redesign.md L182** schema-snapshot paragraph extended with Sprint 11 ride-along note; **ROASTING.md** grep clean (no edits needed). **Substrate audit (6-actor matrix)**: Actor 6 (UI + schema + lib) — migrations 061 + 062 + `lib/types.ts` (3 interfaces) + `lib/roast-import.ts` (multiple spots) + ResolvedView + PerRoastReflections; Actor 4 (MCP) — 6 Tool files + Tool descriptions document the move; Actor 5 (Claude Code) — CONTEXT.md L1511 + L1512 + § Cup character + § FC audibility state + CLAUDE.md + ADR-0008 + redesign.md; Actor 2 (prompts) — 5 prompt files (log-roast + log-cupping + close-lot + one-shot + one-shot-closeout); Actor 3 (claude.ai) — picks up new + dropped Tool fields on next session start; Actor 1 (Chris) — reads relocated aromatic/structural prose in REFERENCE CUP card + fc_audibility in PerRoastReflections on ResolvedView. **Verification**: 7 lots' aromatic/structural prose confirmed landed on expected pourover cupping IDs (1:1 with preflight); columns confirmed dropped from roast_learnings + added to cuppings + added to roasts; both migration applies succeeded clean. **Retro key surprise**: RO-5 collapsed from "data audit + possible new column" to "prompts-only enforcement" because the pre-flight read of the 3 populated cells found Chris was ALREADY following the right pattern. The audit step proved the column-add was unnecessary; the prompt sharpening just formalizes the existing pattern. Pattern: pre-flight DB scans on "data audit" items routinely collapse scope. **Out of scope**: Sprint 12 (RO-CP-5 carry-forward scope-tag + MCP-1 signature_method queue + CR-4 skeleton-entry arbiter + CR-10 roaster skeleton-flag). |
| 2026-05-19 | **Sprint 10 — Roasting schema migrations bundle 1 (RO-1 + RO-2 + RO-3 + RO-4 + ADR-0007)** | First schema-touching sprint after the dogfood-merge gate opened. Four bundled items + ADR all landing on `roast_learnings`. **RO-1**: rename `elasticity` → `brewing_tolerance` (migration 060) — physics metaphor with ambiguous polarity replaced by workflow-language column. **RO-2**: relabel `roast_window_width` CharacterCard UI from "Roast Window" → "Acceptable Roast Window" (UI-only; column name unchanged). **RO-3 ≡ RO-CP-7**: add `terroir_takeaway text NULL` to close the 4th carry-forward axis (cultivar / terroir / general / starting-hypothesis) — populatable on one-shot lots (terroir attribution does not require cross-batch evidence). **RO-4**: prompts sweep retires the "fold the terroir lesson into general_takeaway until the column lands" workaround language in `docs/prompts/close-lot.md` + renames `elasticity` references in `close-lot.md` + `one-shot-closeout.md`. **ADR-0007**: codifies the broader elasticity → (acceptable_roast_window + brewing_tolerance) concept split per CONTEXT.md L1508 + the controls-both-sides posture rationale (Chris's audio framing during kickoff Q1). **Motto rename**: Latent's `houseStyle` + `bmrHouseStyle` in `lib/roaster-registry.ts` rewritten as "Roast for brewing tolerance, brew for intensity..." (mechanical follow-through of the column rename). **CONTEXT.md retirement**: L105 Avoid note updated; L242 Carry-forward learnings entry updated to reflect all 4 axes in schema; L1508 + L1510 (the two open follow-ups the master plan named) struck through with `Resolved 2026-05-19 via Sprint 10 / migration 060 / ADR-0007` resolution language. **MCP Tool surface**: `push_roast_learnings` + `patch_roast_learnings` Zod field `elasticity` → `brewing_tolerance` + describe rewritten; `terroir_takeaway` Zod field added with describe noting one-shot populatability. Both Tool top-level descriptions updated (one-shot NULL list rename + carry-forward list extended + schema-notes block documenting the migration). `push_green_bean.is_one_shot` describe + `docs.ts` one-shot-closeout description prose mentions updated. **UI**: `app/(app)/green/[id]/page.tsx` ResolvedView CharacterCard labels updated (Elasticity → Brewing Tolerance; Roast Window → Acceptable Roast Window); TO CARRY FORWARD card gains a 4th LearningRow for `terroir_takeaway` between Cultivar and General per kickoff Q4. **Living docs**: `ROASTING.md:370` one-shot field list + L760 "elasticity signal" → "brewing-tolerance signal"; `docs/taxonomies/roasters.md` motto rename × 2; `docs/roasting/redesign.md:182` field list update; `docs/features/reference-roast-and-guide.md` thesis-sentence rename × 3 with historical-context callout; `PRODUCT.md:247` Roasting Intent description rephrased. **Substrate audit (6-actor matrix)**: Actor 6 (UI + schema + lib) — migration 060 + `lib/types.ts` + `lib/roast-import.ts` (4 spots) + ResolvedView; Actor 4 (MCP) — 4 Tool/Resource files (push + patch + push-green-bean is_one_shot describe + docs.ts one-shot-closeout describe); Actor 5 (Claude Code) — CONTEXT.md retirement + ADR-0007; Actor 2 (prompts) — close-lot.md + one-shot-closeout.md; Actor 3 (claude.ai) — picks up renamed Tool fields + new field on next session start; Actor 1 (Chris) — reads relabeled CharacterCards + new TO CARRY FORWARD row on ResolvedView. **Verification**: `npm run check:mcp-bundle` 14/8 clean; `check:registry-sync` 20 axes in sync; `npx tsc --noEmit` zero errors (worktree symlinked to main repo node_modules per CLAUDE.md guidance; symlink removed pre-commit); pre/post-migration DB queries via `mcp__supabase__execute_sql` confirm `brewing_tolerance` column with 6 populated rows preserved + `terroir_takeaway` column added (0 populated, NULL across all 7 rows correctly); `check:migrations` pre-existing 046 prefix collision flagged (not introduced). Local browser-preview verification blocked by missing `.env.local` in worktree per CLAUDE.md Local Verification Fallbacks rule — fell back to MCP execute_sql for DB confirmation + Vercel preview for end-to-end UI verification. **Pre-existing shipped.md drift**: Sprint M (PR #185) + Sprint 9 (PR #186) merged 2026-05-19 without updating shipped.md rows; separate follow-up should backfill those entries (not in this PR's scope). **Retro key surprise**: Q1 (motto handling) was the only interpretive open question Chris's audio reply explicitly reframed — the controls-both-sides posture (Chris roasts AND brews) is what makes brewing tolerance the load-bearing axis worth naming. The original "elasticity" was a physics metaphor borrowed from materials science; the rename gives the column a workflow-language name that matches the operational meaning. Captured this rationale in ADR-0007 verbatim. **Out of scope**: Sprint 11 (RO-CP-3 + RO-5 + RO-6 schema migrations bundle 2 — fc_audibility enum, underdev/overdev audit, aromatic_behavior + structural_behavior relocation to cuppings; lands ADR-0008); 7 historical sprint/audit docs still referencing "elasticity" (pre-existing artifact of the term's working-vocabulary period; not load-bearing); roaster-side prompt `start-lot.md` / `log-roast.md` / `log-cupping.md` had no `elasticity` references requiring sweep (grep confirmed zero hits). **Branch**: `claude/infallible-black-f97ce5`. |
| 2026-05-19 | **Sprint 9 — Roasting cluster doc edits (precedence table split + WB→Gnd polarity reframe + BREWING.md CCIL rename)** | First sprint after the dogfood merge gate opened — opens Phase 2 of post-grilling-sequencing with the roasting-side cluster work mirrored on brewing. Three bundled items; doc-only sprint (zero schema / MCP / UI change). **RO-CP-1** (ROASTING.md precedence table split): per CONTEXT.md L1568, the Green-Spec → Starting-Hypothesis table was additive-only and failed on parameters where multiple signals conflict (low density + natural process net ~0 additive; precedence says use full natural-anchor energy) AND parameters where one signal categorically rules out a regime (silent-FC categorically forces bean-temp end condition). Splits the additive table from a new precedence table: process-family → anchor-selection, FC-audibility-expectation → end-condition, fruit-layer-presence → energy-direction-inversion. Categorical-precedence now legible for future-Chris + claude.ai-the-author rather than implicit in anchor selection. **RO-CP-2** (WB→Ground polarity reframe): roasting-side polarity reframed away from the "small delta = better" oversimplification toward the "delta sign + magnitude in context of underdev / overdev signals" framing; ROASTING.md prose updated to reflect that the WB→Gnd delta is a diagnostic signal cross-referenced with cup-side observations rather than a standalone quality target. **SYN-8 both sides** (BREWING.md ↔ ROASTING.md cross-coffee section naming asymmetry): BREWING.md's `SECTION 3 — Archive Patterns` renamed to `Cross-Coffee Insight Layer` to match ROASTING.md's existing name (chosen over a third umbrella name — Chris-locked 2026-05-19 as the lighter-touch option per CONTEXT.md L1571). The "be a little bit more structured at exactly what I'm trying to pull out here that is the most helpful for creating new recipes" framing flagged 2026-05-16 NOT addressed by Sprint 9 — that's a separate queued sprint (BREWING.md structural pass on the renamed section; tracked in PRODUCT.md). **6-actor matrix walked**: Actor 6 (UI + schema + lib) — n/a; Actor 4 (MCP) — n/a (BREWING.md + ROASTING.md served via existing `docs://brewing.md` + `docs://roasting.md` Resources; auto-pickup on next session start); Actor 5 (Claude Code) — CONTEXT.md L1571 updated to note the BREWING.md structural-pass deferral; Actor 2 (prompts) — n/a; Actor 3 (claude.ai) — picks up renamed BREWING.md section + ROASTING.md precedence table on next Resource read; Actor 1 (Chris) — no UI render impact. **Verification**: ROASTING.md + BREWING.md spot-checks confirmed table split + rename landed; `check:mcp-bundle` clean (no Resource catalog change). **Out of scope**: BREWING.md Cross-Coffee Insight Layer structural pass (queued separately per CONTEXT.md L1571). **Backfilled in Sprint F 2026-05-24** — original PR (#186, merged 2026-05-19) did not include a shipped.md row at ship time; this row reconstructs the substrate change from PR title + commit + CONTEXT.md L1568/L1571 narrative. Sprint 10's retro had already flagged the missing row ("Sprint M (#185) + Sprint 9 (#186) merged 2026-05-19 without updating shipped.md rows; separate follow-up should backfill"). **Branch**: `claude/sprint-9-roasting-doc-edits-2026-05-19`. |
| 2026-05-19 | **Sprint M — Mid-sequence cross-system sync (T1-T5 + parallel work)** | Audit-only sprint between Phase 1 (Sprints T1-T5 pre-dogfood cluster) and Phase 2 (Sprints 9+ post-dogfood). Read-only 6-actor matrix walk against Sprints 1-5 substrate delta + the parallel CCIL consolidation pass (PR #178) + Schema sprint (PR #176) + Roasting dogfooding cleanup (PR #174). Mirrored Sprint F's eventual shape but smaller-scope (5 sprints + 3 parallel PRs, not 14 sprints). No code change in this PR; findings folded into Sprint 9+ planning. Master-plan slot at [post-grilling-sequencing.md L540](post-grilling-sequencing.md) ("Sprint M: Mid-sequence cross-system sync against Sprints 1-5"). **Outcomes**: no drift surfaced post-audit; substrate-practice gap audit ADR-0006 candidate deferred per Chris-confirmed "wait for a 3rd cross-party audit" — Sprint M is the second audit after #167's roasting cross-party + #168 grilling-cleanup, and warranted a sit-and-watch posture rather than ADR promotion. Confirmed Phase 2 could open with Sprint 9 cleanly. **6-actor matrix walked**: Actor 6 — n/a (audit only); Actor 4 — n/a; Actor 5 — sprint-summary doc + this row; Actor 2 — n/a; Actor 3 — n/a; Actor 1 — n/a. **Verification**: `check:mcp-bundle` + `check:registry-sync` clean at audit time. **Backfilled in Sprint F 2026-05-24** — original PR (#185, merged 2026-05-19) did not include a shipped.md row; Sprint 10's retro had already flagged this drift as "separate follow-up". Sprint F is that separate follow-up, executed as ride-along per Chris-decision 2026-05-24. **Branch**: `claude/sprint-m-sync-audit-2026-05-18`. |
| 2026-05-18 | **Sprint T5 — Pre-safe scoping + audit docs (11 items + DF-SCH enumeration)** | Fifth and final pre-dogfood sprint of the post-grilling sequencing plan. Single PR with 11 audit/scoping decision docs in [docs/audits/2026-05-18/](../audits/2026-05-18/) + one shipped canonical-surface promotion (SWORKS valve flow). Pre-flight DB queries reframed three items at kickoff. **BR-3 WBC systematic review** — confirmed Latent's 6 strategies + 4 modifiers + 5 Hybrid sub-forms + 5 cross-cutting doc-layer patterns is the right starting point for the 102-recipe WBC corpus. 1 minor cooling-curve modifier candidate considered + deferred (population too low). **BR-4 extraction_confirmed** — keep + reframe (NOT retire). Pre-flight showed 30/82 populated (37%, not "fires rarely" as the brief implied); 3 Hybrid→Balanced-Intensity divergence captures + 7 strategy-divergence callouts validate the field. Reframe narrows scope: populate ONLY when affirmation has recipe-decision context OR brew tasted as different strategy than planned. **BR-5 is_process_dominant** — keep as-is. 6/82 populated (7.3%), all 6 are extreme co-ferment / heavy-process coffees (Mandela XO / Janson Green-Tip 1010 / El Paraiso Luna / etc.); used exactly as designed. Synthesis adapter wiring deferred to Sprint 13. **WBC-2 Time Distribution Playbook** — stay deferred at MODIFIER_TYPES=4. Promote `time_distribution_pattern` modifier (5 templates: fixed_pulse / drain_based_pulse / double_bloom / short_long_short + the already-promoted role_based_pulse) ONLY when ≥3 brews use a non-role_based_pulse template AND a cross-coffee learning question can't be answered by prose `strategy_notes`. **CR-6 strategy tag ↔ extraction strategy coherence** — DON'T force-align. The two vocabularies describe distinct lifecycle stages (roaster-default signal vs brew-resolved outcome); 3 brew-side "missing" tags (Suppression / Extraction Push / Hybrid) + 4 roaster-side "extra" tags (SYSTEM / VARIES / BALANCED→X / SELF-ROASTED) are all legitimate. Case-style divergence (UPPERCASE-DASH vs Title Case) encodes side-of-schema, preserved. Future descriptive cross-mapping table in BREWING.md / wbc-reference.md is a ride-along for Sprint 9. **CR-7 SWORKS valve flow — SCOPE + SHIP** (Chris-confirmed expansion at kickoff). New [`docs/taxonomies/sworks.md`](../taxonomies/sworks.md) (authoritative authored content with `## Canonical list` + per-dial entries + adjustment logic + 5 canonical recipe patterns) + [`lib/sworks-registry.ts`](../../lib/sworks-registry.ts) validation mirror (`SworksEntry` rich shape with `validDials: SworksDialEntry[]`, single canonical SWORKS Bottomless Dripper; `SWORKS_LOOKUP` for dripper-name canonicalize + 4-alias map; `SWORKS_DIAL_STATE_LOOKUP` for per-dial state vocabulary). Registered as `docs://taxonomies/sworks.md` in [lib/mcp/docs.ts](../../lib/mcp/docs.ts) (11th canonical axis). `scripts/check-registry-md-sync.ts` adds the sworks row alongside grinders. Status enum: 'normal' / 'dead_zone' (1-4) / 'recovery_only' (past-7). Vocabulary anchor only — no `brews` schema column references the registry (per-pour valve-dial persists in `brews.pour_structure` free-text); future structured-pour-structure sprint may extract. **CR-11 saturation audit** — 95-96% target MET on every axis. 100% on cultivar / process / brewer / filter / grinder / roast_level / signature_method; 98.8% producer (single-row drift `Asefa Dukamo` vs canonical `Asefa & Mulugeta Dukamo`); 95.1% terroir (4 auto_created back-compat rows from pre-canonical-registries imports); ≥95% flavor base via ALIAS_MAP. Override queue: 3 producer + 1 roaster pending. Doc proposals: 8 pending (5 roasting + 2 brewing + 1 roaster card). **CR-13 + MCP-3 strictness mapping (bundled per kickoff)** — per-axis 4-tier framework locked: 8 strict-static / 1 closed-objective / 2 self-owned / 1 self-only / 4 open-loose axes. Per-tier behavior documented inline (registry shape · MCP enforcement · override path · arbiter role · net-new expectation). Non-uniform queue support is principled, not drift — flavor + roast_level + process_modifiers legitimately don't have queue support; the `signature_method` strictness gap is the one mismatch, resolved by Sprint 12 MCP-1. **MCP-4 xBloom API** — no public API exists as of 2026-05-18. Search trail: 2 web searches + WebFetch on community xRecipe site. No developer docs / GitHub org / API mention. Integration value is marginal anyway (xBloom runs one fixed recipe). Re-test if xBloom publishes developer docs OR Chris adds a 2nd brewer with public API. **RO-7 rest_behavior** — keep as-is. Pre-flight reframed the "currently unused" framing: 3/7 closed lots populated (43%) with rich operational prose (Day-N evaluation drift / cross-cup vehicle comparison / storage + temperature observations). Reframe the Tool description in `push_roast_learnings` + `close-lot.md` prompt to encode the three-thread scope; ride-along with Sprint 10. **DF-SCH enumeration** — 4 of 5 candidates already shipped via Schema sprints S1-S4 earlier today. CAND-1 (`cuppings.wb_to_ground_delta`) / CAND-2 (`roasts.is_reference_candidate`) / CAND-3 (`cuppings.sweetness` + `temperature_behavior`) / CAND-4 (`roast_recipes.was_backfilled` + `backfill_notes`) all live. CAND-5 (`experiments.taste_for_validation_*`) defers — Sprint 6 DF-A13 prompt-level alternative is the default path; promote to schema only if dogfood signal warrants. **CONTEXT.md 7 flagged-ambiguity closes** inline (process_dominant / canonical-strictness 4-tier / xBloom API / cross-axis vocab coherence / SWORKS valve flow taxonomy / WBC subtypes systematic review / rest_behavior). **CLAUDE.md** adds SWORKS taxonomy paragraph mirroring the grinder paragraph (11th canonical axis). **Six-actor matrix walk**: Actor 6 (UI) — no render surface; Actor 4 (MCP) — new `docs://taxonomies/sworks.md` Resource registered + described, no new Tool, bundle covered by pre-existing `./docs/taxonomies/*.md` glob; Actor 5 (Claude Code) — CLAUDE.md + CONTEXT.md updated; Actor 2 (prompts) — none edited (4 ride-along reframes flagged for future); Actor 3 (claude.ai) — picks up new sworks Resource on next session start; Actor 1 (Chris) — reads 11 audit docs in PR review. **Verification**: `npx tsc --noEmit` clean; `check:registry-sync` 20 axes in sync (sworks added at ts: 1, md: 1); `check:mcp-bundle` 15/8 (was 14/8 — sworks adds to TAXONOMY_AXES); `check:migrations` pre-existing 046 prefix collision flagged (not introduced). **Retro key surprises**: (1) Pre-flight DB queries reframed BR-4 + RO-7 entirely — the brief's "fires rarely" / "currently unused" framings were both wrong, and the column name in the brief (`process_dominant` / `roast_window`) drifted from actual schema (`is_process_dominant` / `roast_window_width`). Repeat of T3's pre-flight-reframes-scope pattern. (2) 4 of 5 DF-SCH candidates already shipped earlier today via Schema sprints S1-S4 — the master plan was authored before Schema sprints; T5 inherited a much smaller decision surface than scoped. Direct `information_schema.columns` query was the cheap-and-decisive check vs trusting the master plan's "still pending" framing (this is `feedback_audit_completeness_verification_grep.md` applied). (3) CR-7's scope expansion at kickoff (Chris chose ship-the-promotion over scope-only) added an M-effort net-new canonical axis to a doc-heavy sprint; clean to ship because the EG-1 grinder taxonomy is the structural precedent. **Out of scope**: Sprint M mid-sequence sync (separate sprint immediately after T5); blocking dogfood-merge gate (sits between Sprint M and Sprint 6); Sprints 6-14 (post-dogfood execution); 4 prompt/Tool-description reframes (BR-4 + RO-7 + CR-6 cross-mapping table + SWORKS — ride-alongs for Sprint 6 / Sprint 10 / Sprint 9 respectively); CAND-5 schema promotion (alternative-only path; promote post-Sprint-6 if dogfood signal warrants); 4 pending taxonomy_overrides_queue entries (arbiter-run, separate from audit sprint); 4 auto_created brews.terroir_id back-compat provenance cleanup (future data-sanity ride-along). **Branch:** `claude/funny-tereshkova-353345`. |
| 2026-05-18 | **Sprint T4 — Synthesis pipeline pre-safe edits (SYN-2 + SYN-4 + SYN-5)** | Fourth sprint of the post-grilling sequencing plan; second-to-last pre-dogfood sprint. Single PR; sizing landed S (was S-M; SYN-5 collapsed to a 1-line categorical bullet rather than a refactor). Three items in [lib/synthesis/](../../lib/synthesis/) closing 3 flagged-ambiguity follow-ups from the 2026-05-16 synthesis grilling. **SYN-2 — 4-tier classifier replaces binary earlyData flag**: new `classifyTier(rowCount): TierConfig` in [buildPrompt.ts](../../lib/synthesis/buildPrompt.ts) returns a gradient (`early` <3 / `emerging` 3-7 / `established` 8-15 / `mature` 16+) carrying paragraph count (2-3 → 4-6), takeaway count, `max_tokens` budget (700 → 1500), and confidence-language hint per tier. The hint interpolates the actual rowCount ("Early data: only 1 lot is archived..."). `BuildPromptResult` exposes the resolved `TierConfig`; `runSynthesis` consumes `tier.maxTokens` and forwards `tier.tier` on `SynthesisOutcome`. Adapter contracts unchanged — no per-adapter ripple (no adapter touches tier or count logic). **SYN-4 — Re-synthesis directive + anti-pattern as top SHARED_RULES**: aspirational "Do not summarize each coffee one by one" line replaced by two top-of-list entries — (1) directive: "This is a re-synthesis, not a summary. ... The output must NOT have the shape of a list of coffees with one observation per coffee. New data points get pulled in multiplicatively, not appended..."; (2) anti-pattern: concrete worked example showing list-shape vs synthesized-shape. Mirrors the multiplicative-not-additive design philosophy in CONTEXT.md § Knowledge capsule. **SYN-5 — Humanizer spot-check + categorical preservation expansion**: pre-flight spot-check on 5 cached capsules across all 4 axes (Volcán Barú Highlands N=24, Ethiopian landrace N=3, Moonwake N=17, Washed-base N=40, Anaerobic-modifier N=16) showed **>95% canonical-term survival** — terms NOT in the hardcoded `USER_OVERRIDE` allowlist ("anoxic", "hermetic tank", "white tea / black tea / orange pekoe") all survived verbatim. Humanizer respects domain vocabulary categorically. Option C-modified landed — kept the enumeration, added one categorical bullet covering signature methods (15 canonicals post Sprint T1), fermentation qualifiers ("Anoxic" post Sprint T3), modifier-axis names, honey color tiers, + catch-all clarifier for any cultivar / terroir / producer / farm / roaster proper noun. Option A (build-time CONTEXT.md extraction) and Option B (humanizer tool-use) explicitly rejected as over-engineering given the >95% baseline. **CONTEXT.md edits**: 3 flagged-ambiguity follow-ups closed (lines 1524-1527 → resolution language inlined); 1 new glossary entry **Corpus tier** under § Synthesis Pipeline locking the 4-tier shape canonically; Knowledge capsule entry gains a cross-reference noting the directive is now codified in SHARED_RULES; Synthesis pipeline entry verb-updated (early-data flag → corpus-tier hint). **CLAUDE.md edits**: synthesis architecture blurb L53 refreshed from hardcoded "4-6 paragraphs + 4-6 takeaways" → tier-aware sizing + humanizer USER_OVERRIDE recap; L102 terroirs synthesis blurb tier-aware; L145 roasters prompt framing reframed (early-data flag → corpus-tier sizing). **6-actor matrix walk**: Actor 6 (UI) — no schema change; cached capsules regenerate with new tier-aware shape on next count-mismatch or manual Regenerate. Actor 4 (MCP) — zero Tool surface change; no new Resources; no description edits. Actor 5 (Claude Code) — CONTEXT.md + CLAUDE.md synced same PR. Actor 2 (prompts) — zero `docs/prompts/*.md` edits (synthesis prompts live in `lib/synthesis/`, not docs/). Actor 3 (claude.ai) — zero impact (read-side, consumes capsules via Resources / page renders, doesn't write them). Actor 1 (Chris) — tier-aware confidence + length renders on aggregation pages after cache turnover. **Verification**: `npx tsc --noEmit` clean (worktree symlinked node_modules per CLAUDE.md guidance; symlink removed pre-commit). One-shot `scripts/verify-synthesis-tiers.ts` harness exercised `classifyTier` + `buildSynthesisPrompt` at corpus sizes 1/5/10/24 + boundary tests 0/2/3/7/8/15/16/50 — all 4 tiers fire correctly, SHARED_RULES top-2 read as directive + anti-pattern, output-format counts match the tier. Script deleted post-verification. Real-world LLM capsule re-synthesis deferred to natural next-visit flow (Sonnet 4.6 is highly compliant with explicit "Write N paragraphs" instructions; prompt-shape correctness implies output-shape correctness with high confidence). **Pre-flight spot-check was the load-bearing step** — it collapsed SYN-5 from "investigation + possible refactor" to "1-line categorical bullet + glossary update," saving an estimated sprint worth of work that Option A or B branches would have triggered. Pattern for future scoping-flagged items: run the cheapest possible audit first, let findings reframe the work. **Out of scope**: SYN-3 (mobile short-form, deferred — needs 2nd LLM-call shape + schema column), SYN-6 (cross-source brewing+roasting unified capsule, Sprint 13), SYN-7 (resynthesize content-change gap, bundled with SYN-6), SYN-8 (BREWING/ROASTING cross-coffee naming asymmetry, Sprint 9). **Branch:** `claude/lucid-banach-4487fb`. |
| 2026-05-18 | **Sprint T3 — Canonical-registries cluster batch (CR-1 + CR-2 + CR-3 + CR-5)** | Third sprint of the post-grilling sequencing plan; largest pre-dogfood sprint and the first one to ship in two PRs ([#181](https://github.com/chrismccann-dev/latent-coffee/pull/181) + [#182](https://github.com/chrismccann-dev/latent-coffee/pull/182)) per the kickoff sizing call. 4 follow-up items from the 2026-05-16 canonical-registries grilling bundled by surface affinity: 3 substrate corrections (docs + alias map + scoping doc) in PR 3a, schema + MCP + render in PR 3b. **CR-1 — processes.md qualifier-strategy corrective** ([#181](https://github.com/chrismccann-dev/latent-coffee/pull/181)): Round 9 grilling locked "aggregation level stays at the modifier (Anaerobic), not the qualifier (Anoxic)." Reframed in three spots (Qualifiers paragraph + inline Anaerobic modifier description + Modifier Index entry) as "record when known, doesn't dictate strategy." CONTEXT.md § Qualifier pre-locked the corrective at the 2026-05-16 grilling; processes.md now matches the glossary. **CR-2 — Nordic Approach alias removal** ([#181](https://github.com/chrismccann-dev/latent-coffee/pull/181)): pre-flight DB scan reframed the sprint scope entirely. The migration-031 collapse (sprint 1l, April 2026) had already renamed every `producer = "Nordic Approach"` row to `Mekuria Mergia & Elias Rooba`; today's DB has zero rows under the importer name AND the one row attributed to Mekuria is the legitimate producer (per registry, Nordic Approach IS one of Mekuria's importers). Re-identification work was zero. Removed the `PRODUCER_ALIASES` map entry + the alias-table row in producers.md; replaced with a load-bearing comment block. Future writes of `producer = "Nordic Approach"` now fail canonical resolution and surface in `taxonomy_overrides_queue` rather than silently re-collapsing. **CR-3 — Importer/exporter scoping doc** ([#181](https://github.com/chrismccann-dev/latent-coffee/pull/181)): new [docs/features/importer-exporter-scoping.md](../features/importer-exporter-scoping.md) weighing three options (A — full new canonical axis with registry + schema columns + Tool wiring · B — elevate `ProducerEntry.importers/exporters` to typed canonicals · C — stay unmodeled, deferred). Recommendation + Chris-confirmed call: Option C. Four forward triggers documented for re-opening the question: sourcing-model shift / override-queue drift ≥5/yr / WBC sourcing structured-data need / cross-system audit catching quiet drift. Registered as `docs://features/importer-exporter-scoping.md` MCP Resource — first time `docs/features/` surfaces in `DOC_FILES`; `next.config.js outputFileTracingIncludes` gets a `./docs/features/*.md` glob alongside (caught at audit time by `check:mcp-bundle`). **CR-5 — `fermentation_qualifiers` schema column + MCP wiring + read-only render** ([#182](https://github.com/chrismccann-dev/latent-coffee/pull/182)): migration 059 adds `brews.fermentation_qualifiers text[] NOT NULL DEFAULT '{}'::text[]` + targeted `UPDATE` backfilling the one historical row carrying qualifier intent (Anoxic Natural, Rosado, id fd346045). Pre-flight scan confirmed `Anoxic` appears in exactly one brew across the 82-row corpus — the substrate gap was tightly bounded. `lib/process-registry.ts`: `FERMENTATION_QUALIFIERS = ['Anoxic']` + `FermentationQualifier` type + `FERMENTATION_QUALIFIER_ALIASES` (`No Oxygen` / `Zero O2` / `Oxygen Free` → `Anoxic`) + `FERMENTATION_QUALIFIER_LOOKUP` via `makeCanonicalLookup`; `StructuredProcess` gains the readonly array field; `LEGACY_DECOMPOSITIONS['Anoxic Natural']` populates the qualifier; `structuredProcessColumns` + `isProcessResolvable` updated. **`composeProcess` deliberately unchanged** — qualifier is record-when-known column-only, NOT in the display string (matches the Round 9 aggregation-level lock; if it went into the legacy `brews.process` string, the `/processes` page would have started grouping "Anoxic Natural" as a separate aggregation cell — undoing the lock). MCP write surface: `push_brew` + `patch_brew` Zod schemas + `canonicals://` processes axis exposure + `lib/brew-import.ts` BrewPayload / validate / seedStructuredProcess / `PATCH_BREW_EDITABLE_FIELDS` / patchBrew validation-loop / PROCESS_FIELDS recompose trigger / recompose-merged literal. Read surface: `/brews/[id]` Process row renders the qualifier inline as `[Anaerobic Natural] QUALIFIER [Anoxic]`, verified locally on the Rosado brew. ProcessPicker / `/add` / `/edit` deliberately unchanged per `feedback_mcp_only_input.md` (brewing-side picker on the eventual deprecation list; claude.ai writes the field directly). Downstream consumers updated: `lib/types.ts` Brew interface, `lib/process-aggregation.ts` brewToStructured, `lib/process-routing.ts` parseModifierComboSlug, `scripts/data-sanity-audit.ts` BrewRow + BREW_COLUMNS query string + buildStructuredProcess. **6-actor matrix walked on both PRs**: UI updated (CR-5 verified via preview); MCP gains 1 new `docs://` Resource (3a) + 1 new Zod field on push_brew/patch_brew + canonicals axis (3b); Claude Code docs synced (CONTEXT.md three flagged-ambiguity lines marked Resolved; CLAUDE.md Process Data Model 8 → 9 columns); prompts grep clean for both Nordic Approach (3a) and Anoxic/qualifier (3b); claude.ai picks up new Resource + Tool changes on next catalog refresh; Chris sees qualifier inline on /brews/[id] + flagged-ambiguity Resolved entries in CONTEXT.md. **Verification**: PR 3a — `check:mcp-bundle` 14/8 (was 13/7); `check:registry-sync` all axes; `check:migrations` 046 collision pre-existing; tsc clean. PR 3b — `check:mcp-bundle` 13/7 (base off main, will become 14/8 post-3a-merge); `check:registry-sync` all 19 axes including new fermentation qualifier sub-axis; tsc clean after fixing two downstream consumers (brew-import patchBrew recompose path + scripts/data-sanity-audit BrewRow shape — caught the StructuredProcess-shape ripple cleanly); preview verified on Rosado brew (`Process: [Anaerobic Natural] QUALIFIER [Anoxic]` renders inline, no console errors). **Pre-flight DB scan was the single highest-leverage step of the sprint** — reframed two items entirely. Brief said CR-2 needed "brew-by-brew producer re-identification for affected rows"; pre-flight found zero affected rows + one legitimate-attribution row → re-ID work was zero. CR-5 brief implied historical-backfill scope unknown; pre-flight found exactly one brew with Anoxic anywhere → migration was ALTER + targeted UPDATE WHERE id, not batch backfill. **Pattern for future "DB audit needed" sprints: run the pre-flight query first; the count + actual data shape often reframes the sprint** (this is `feedback_verify_handoff_premise.md` applied to its load-bearing case). **Two-PR split earned its keep** — single-PR would have bundled substantively different items (substrate corrections vs schema/MCP/UI); split kept review depth manageable + lets CR-5 revert independently if issues surface post-merge. **Trivial merge conflicts expected** between 3a and 3b on `processes.md` § Qualifiers + CONTEXT.md fermentation_qualifiers entry; PR 3b's schema-landed text supersedes the schema-pending text in 3a. **Branch:** PR 3a `claude/charming-elion-89793b`; PR 3b `claude/charming-elion-89793b-3b`. |
| 2026-05-18 | **Sprint T2 — wbc-sourcing.md doc-edit bundle** | Second sprint of the post-grilling sequencing plan; smallest sprint in the sequence (single doc touched, 3 items in one PR). Folds the 2026-05-16 WBC-cluster grilling locks from CONTEXT.md into the authored [docs/roasting/wbc-sourcing.md](../roasting/wbc-sourcing.md). **WBC-6 — § 10 portfolio 4 → 5 lanes**: `Value / roast-practice` promoted from gap-analysis-only to authored frame; new table row + rationale paragraph naming the lane's role (sub-$30/kg cheap experiment substrate for 3-batch dev ladders + rest-curve experiments + green-curve calibration without burning expensive lots). Inventory gap analysis line 392 "four-lane" → "five-lane". Bone-stock 2026-05-09 doc had 4 authored lanes but the gap-analysis treated a 5th lane (Value / roast-practice) as load-bearing since the original snapshot; this sprint closes the substrate-practice gap. **WBC-8 — § 11 Sourcing channels section authored from scratch**: 6-channel table (Generalist retailer / Specialty importer / Competition-grade importer / Direct-from-farm / Auction-sample channel / Direct-from-auction awareness-only) with Volume window + Access pattern + Competition-grade fit + Examples columns. 4 narrative subsections: Direct-from-farm sub-pattern (1kg-ish sample bags) + Geisha Village classification correction + Direct-from-auction awareness-only framing + Channel mix today. The doc had no Sourcing channels section before this sprint despite CONTEXT.md (lines 1185-1207) carrying the canonical 6-channel vocabulary since the 2026-05-16 grilling — finding revealed at kickoff: WBC-8 wasn't "add a 6th channel" but "author the whole § from scratch with all 6 channels present." **WBC-7 — Geisha Village classification correction**: rendered inside § 11 as a dedicated subsection; the table row also classifies GV correctly under Direct-from-farm with the 1kg-sample-bag sub-pattern. Geisha Village's website-as-storefront masks the structural relationship (the farm itself sells direct, no importer between farm and buyer). Locked GV's sub-pattern as 1kg-sample-bag direct only (NOT WhatsApp post-relationship like CGLE — different sub-pattern). **CONTEXT.md follow-up close**: Portfolio lanes entry rewritten from "4-lane authored frame + implicit 5th via gap analysis" to "5-lane authored frame locked Sprint T2"; the "Implicit 5th lane" follow-up paragraph removed (the open follow-up is now closed). History entry on wbc-sourcing.md adds a 2026-05-18 (Sprint T2) row documenting the 3 doc edits. **Six-actor matrix walk** (doc-only sprint): Actor 6 (UI) — n/a; Actor 4 (MCP) — wbc-sourcing.md is the `docs://roasting/wbc-sourcing.md` Resource (registered at [lib/mcp/docs.ts:47](../../lib/mcp/docs.ts)) so the new content auto-flows on next session read; no Tool description changes; Actor 5 (Claude Code) — CONTEXT.md Portfolio lanes entry updated; CLAUDE.md / ROASTING.md / BREWING.md / PRODUCT.md reference the doc generically (no 4-vs-5 count drift); Actor 2 (prompts) — grep clean (no SR/brewing prompts reference the 4-lane shape or the GV-generalist-retailer classification); Actor 3 (claude.ai) — picks up new § 11 + updated § 10 on next Resource refresh; Actor 1 (Chris) — doc-only, no UI render impact. **Verification**: `npm run check:registry-sync` all 19 axes in sync (sprint touches no registries); `npm run check:mcp-bundle` 13 DOC_FILES paths covered by 7 bundle globs (`docs/roasting/**` glob includes wbc-sourcing.md, no entry change needed); `npm run check:migrations` no new migrations (pre-existing 046 prefix collision flagged in trigger-bound follow-ups, not introduced by this sprint). **/simplify pass**: re-read the new § 11 end-to-end; GV mention duplicates across table + sub-pattern paragraph + correction subsection are purposeful narrative (each serves a different reading purpose — quick scan vs sub-pattern emphasis vs correction context) rather than technical duplication; no extractions warranted. **Retro**: surprise was finding § 11 needed to be authored from scratch (CONTEXT.md was ahead of wbc-sourcing.md); pattern flagged for next post-grilling-sequencing sprint is that when CONTEXT.md is locked first, the followup file's "add X alongside existing 5" framing can mislead about scope — verify the "existing 5" actually exist in the target doc before sizing. Pattern for future sprints folding CONTEXT.md vocabulary into living docs: always re-grep the target doc, not just trust the followup file's "current state" framing. **Branch:** `claude/vigorous-banach-7cff13`. |
| 2026-05-18 | **Sprint T1 — Brewing-cluster doc edits + canonical surface** | First sprint of the post-grilling sequencing plan. **Three brewing-cluster items bundled, zero roasting-side surface** (master plan's blocking dogfood-merge gate held). **BR-1 — signature method registry expansion**: `lib/process-registry.ts` `SIGNATURE_METHODS` 3 → 15 entries to match Chris's working canonical list (CONTEXT.md L426). The 14 working-list signatures plus Wave Hybrid as the 15th. Hybrid Washed deprecated because it fails the "mechanically opaque" criterion (CGLE publicly decomposes it as Anaerobic + Aerobic Washed); [migration 058](../../supabase/migrations/058_signature_method_hybrid_washed_deprecation.sql) re-maps the one affected brew row (CGLE Sudan Rume Hybrid Washed) to `signature_method=NULL + fermentation_modifiers=['Anaerobic','Aerobic'] + process='Aerobic Anaerobic Washed'`. `SignatureEntry` interface: `producer` + `country` now optional (skeleton-pending-verification for 11 new entries without confirmed attribution) + `experimental_modifiers?` field added (Enzyflow / Sous-vide / Amazake consume it). Non-canonical descriptors in CONTEXT.md L426 parentheticals (Dynamic Cherry / Biological Fermentation / Thermal Control / Zeolite Drying) intentionally omitted from modifier arrays rather than approximated. **4 null-handling sites** updated: signatures page subtitle uses `filter(Boolean).join(' · ')`, processes index uses a flat `if (entry)` block, synthesis adapter conditionally pushes producer line + surfaces `experimental_modifiers`, ProcessPicker hint conditionally appends `at ${producer}`. [docs/taxonomies/processes.md](../taxonomies/processes.md) Signature methods section expanded (heading `(3)` → `(15)`) + Hybrid Washed deprecation note in aliases. [scripts/check-registry-md-sync.ts](../../scripts/check-registry-md-sync.ts) anchor updated. `CONTEXT.md L1465` strikethrough-Resolved; the MCP-1 follow-up (signature_method as override-eligible axis on `taxonomy_overrides_queue`) remains queued for Sprint 12. **BR-2 — BREWING.md Phase-Mapped + Role-Based Pulse vocabulary split**: rewrote both definitions to use the formal split locked in CONTEXT.md L407-412 (mechanical role + cup-side target as two parallel labels). Cup-side targets explicitly draw from the canonical 5-attribute set `aroma / attack / mid-palate / body / finish`; mechanical roles named as open-ended WBC-axis engagement. Modifier checklist for Role-Based Pulse also updated. RBP definition references Phase-Mapped Hybrid's split rather than re-stating (avoids drift). Retired the abstract `saturation / body / clarity / finish` vocabulary everywhere it was a definition; concrete Tamarind re-framing example sentences (L144 + L693 + L900) preserved as-is because they describe specific mechanical roles, not abstract canonical vocab. **WBC-1 — BREWING.md Step 3 single-variable orthodoxy softening**: added scale-dependent adjustment rule mirroring the roasting-side Adjustment entry (CONTEXT.md § Adjustment). Brew 1 (and often Brew 2) = wide-variance multi-variable exploratory (geography analogy: country, then state, then city). Brew 2 → Brew 3 = narrow on leading direction, usually single-variable (classical Motta rule in convergent regime). Brew 3+ = probe a held-constant variable or replicate as control. Preserves Motta-prevention value by making single-variable the default once a directional signal lands, not before. **CLAUDE.md updates** (3 spots): L89 signature_method enumeration refreshed to 15 + Hybrid Washed deprecation note; L112 + L124 Signature Methods nav surface counts updated. **Verification**: `npx tsc --noEmit` zero errors; `npm run check:registry-sync` passes 19 axes including `process — signature  (ts: 15, md: 15)`; `npm run check:mcp-bundle` passes; migration 058 applied to live DB via `mcp__supabase__apply_migration` + post-condition SQL confirms the one row updated cleanly; preview server confirmed `/processes/signatures/[name]`, `/processes`, `/brews/[id]/edit` all compile (full UI verification blocked by worktree's missing Supabase env vars per CLAUDE.md Local Verification Fallbacks rule, fell back to MCP execute_sql for DB confirmation). **/simplify pass** caught 5 cleanups: flattened nested ternary in `/processes` index, fixed double-separator whitespace bug in signatures page subtitle via `.filter(Boolean).join(' · ')`, trimmed two narrating comment blocks in `lib/process-registry.ts`, removed two inline entry comments (XO + Wave Hybrid) duplicated in processes.md, collapsed the duplicated "two parallel labels" explanation in BREWING.md RBP. Efficiency review found zero issues. **Out of scope**: zero roasting-side surface touched (master plan's blocking dogfood-merge gate); MCP-1 deferred to Sprint 12 (taxonomy_overrides_queue addition for signature_method); SYN-8 BREWING/ROASTING cross-coffee rename deferred to Sprint 9 (unified rename both sides post-dogfood). Branch: `claude/distracted-chaum-a34d30`. |
| 2026-05-18 | **CCIL consolidation pass (first application of the operational ladder)** | Doc-maintenance sprint closing Round 4 #11 from the dogfooding queue. The Cross-Coffee Insight Layer in [ROASTING.md](../../ROASTING.md) had grown append-only with each V-set close-out; 8 cross-lot-validated patterns were buried among 10 single-lot Low-confidence "Working hypothesis added" paragraphs (Round 4 #11 surfaced this on 2026-05-15 with the prediction that by V3 cupping the lot would carry 6+ working hypotheses; today is the consolidation pass). **ROASTING.md restructure (3 tiers):** (1) **`## Confirmed Patterns`** subsection added at top - short bullet index of 8 promoted patterns each linking to the detail section that holds the evidence (drop-temp ceiling 205-208°C across counterflow lots; first-roast-of-session late-and-hot; Day 7 pourover as universal evaluation gate; xbloom under-extracts delicate aromatic washed coffees; WB-to-ground delta tightening as profile-dialing signal; Sudan Rume aromatic vocabulary shared across washed and natural processing; high-density washed Colombian ≥800 g/L energy-tolerant; producer-notes-lemongrass-jasmine-bergamot predict eval-recipe under-extraction). The subsection is a navigable index - detail sections stay in place, no duplication. Promotion criterion stated inline: High confidence AND repeated across ≥2 lots OR strong cross-lot corroboration. (2) **Body unchanged** - 6 reference tables (FC Floor & Ceiling / WB Delta Norms / Session Position / Green Spec / Varietal Aromatic / Rest Behavior) + 2 Medium/Medium-High `## H2` subsections (FC-Temp Architectural Constraint on Naturals + the newly-promoted xbloom Evaluation Gate Misranking on Anaerobic Naturals - M moved from orphan-paragraph at line 896 to its own H2 subsection). (3) **`## Working Hypotheses (Single-Lot, Low Confidence)`** subsection added at bottom - holding pen for 10 pre-ladder Low-confidence single-lot entries relocated from inline positions (under WB Delta + Green Spec + Rest Behavior tables and the K + L standalone subsections). Each entry carries originating lot + observation date (e.g. "RWA-NOVA-NAT21-RB-2026 V1, observed 2026-05") for future age-out passes. Intro explicitly frames it as a holding pen that should drain over time, NOT an append target for new Low entries - per [CONTEXT.md § Key-insight confidence ladder](../../CONTEXT.md) (finalized 2026-05-18 in the cleanup-actions PR), Low does NOT route to CCIL going forward. 2 Open Questions backlinks updated to new section anchors (the slugified anchor changed when K + L moved from standalone `## H2` to `### H3` under Working Hypotheses). **No retirements this pass** - all 10 Low entries are dated 2026-04 to 2026-05 (too recent to clear the 90-day age-out window OR the 3-similar-lots-without-corroboration threshold). The retire window first applies at the 2026-08 consolidation pass. **ARBITER.md addition - `## CCIL consolidation pass`** playbook (7 steps): CCIL-1 audit / CCIL-2 promote / CCIL-3 retire / CCIL-4 restructure check / CCIL-5 commit + PR + merge / CCIL-6 six-actor cross-system audit (verifies `roasting.md` Resource still resolves the top-level `Cross-Coffee Insight Layer` anchor after restructure - H1 unchanged, so prompts continue to work; subsection-specific anchors checked for any prompt that links them) / CCIL-7 worked example documenting this 2026-05-18 first pass as shape template. Trigger phrases: "consolidate cross-coffee insights" / "process pending CCIL consolidation" / "do a CCIL pass". Cadence: Chris-driven; two natural triggers (Working Hypotheses subsection accumulates 6+ entries OR a High-confidence cross-lot pattern emerges that deserves explicit promotion). **Per-session prompts NOT updated** - `log-cupping.md` / `close-lot.md` / `one-shot.md` / `one-shot-closeout.md` / `start-lot.md` all reference CCIL by top-level H1 anchor "Cross-Coffee Insight Layer"; that anchor is unchanged. Per-session appends continue to target body subsections (table extensions, Medium/Medium-High named subsections); promotion to Confirmed Patterns is the arbiter's job at consolidation time, not the per-session author's. The operational-ladder rule (Low → `additional_notes`, not CCIL) is already in CONTEXT.md + log-cupping.md STAGE 6 skip-and-report language from the cleanup-actions PR. **Six-actor cross-system audit walked**: Actor 6 (UI) - ROASTING.md edits; Actor 4 (MCP) - `roasting.md` Resource auto-reflects file content, no code change; Actor 5 (Claude Code) - ARBITER.md playbook addition + shipped.md row + this entry; Actor 2 (prompts) - no edits, anchors stable; Actor 3 (claude.ai) - next session pulls fresh content via `docs://roasting.md` Resource; Actor 1 (Chris) - Confirmed Patterns subsection navigable from `# Cross-Coffee Insight Layer` heading; section-anchor links in the bullet list use GitHub-style slug form. **Feedback-log entry updated** - Round 4 #11 in `feedback_mcp_continuous_log.md` flipped from `parked for separate sprint` to `shipped` with PR reference. **Branch:** `claude/exciting-hamilton-4eb1bd`. |
| 2026-05-18 | **Schema sprint (S1 + S2 + S4 migrations + S3 MCP-exposure fold-in)** | First post-#174 schema sprint. 3 additive migrations + 1 cross-cutting MCP+UI gap-close in one PR. **Migration 055 (S1 — `cuppings.wb_agtron + wb_to_ground_delta`)**: snapshot `roasts.agtron` onto cuppings at push time + Postgres-generated `wb_to_ground_delta numeric GENERATED ALWAYS AS (wb_agtron - ground_agtron) STORED`. Solves Round 3 #6 SQL-gymnastics friction. Backfilled 99/99 existing cuppings via `roasts` JOIN; 59 have populated delta (matches `ground_agtron` coverage). Cross-lot delta range now queryable: **-4.3 (most anomalous sign-inversion, exceeds Fazenda Um v1b's -0.8 surfaced in dogfood) to +11.9**. `persistCupping` joins `roasts.agtron` by `roast_id` at insert; `patch_cupping(wb_agtron=...)` exposed for the rare post-hoc Agtron re-measurement realignment case (documented as override-only in the Tool description). **Migration 056 (S2 — `roasts.is_reference_candidate boolean DEFAULT false`)**: forward-looking quality flag during V-set iteration, distinct from `is_reference` (lot-level final at close-out) AND `worth_repeating` (recipe-replay judgment). Resolves Round 3 #8 ("Fazenda Um V1B is leading but best-of-the-worst, not a reference example" — that state now queryable distinct from "no winner yet" and from "leading slot IS reference quality"). Net-new semantic forward; no historical backfill. Set via `patch_roast(is_reference_candidate: true|false)` in `log-cupping.md` STAGE 3 on the leading slot. Close-out flow promotes via separate explicit `patch_roast(is_reference: true)` — the candidate flag does NOT auto-flip. Partial-index `idx_roasts_reference_candidate (green_bean_id, is_reference_candidate) WHERE is_reference_candidate = true`. **S3 fold-in (`cuppings.sweetness + temperature_behavior` MCP+UI exposure, no migration)**: schema landed in migration 046 (2026-05-07) but Zod schemas + persistCupping payload + ResolvedView render had never been updated — 10-day substrate-gap. push_cupping + patch_cupping Zod fields added; `CuppingPayload` type extended; `persistCupping` passes through; `CUPPING_PATCH_FIELDS` allowlist extended. UI render: REFERENCE CUP card adds `<CupRow label="Sweetness" />` + `<CupRow label="Temperature behavior" />` rows alongside the existing 6 prose fields; All Cuppings collapsible appends sweetness to the joined descriptors line + renders temperature_behavior as a muted Temp-behavior callout + renders the new `wb_to_ground_delta` column below cupping cards. Closes Round 4 #12 friction. **Migration 057 (S4 — `roast_recipes.was_backfilled boolean + backfill_notes text`)**: provenance for design-intent recovery. Backfilled the 129 migration-052 legacy shells (112 still match the NULL-bezier + NULL-experiment_id signature post-enrichment) with `was_backfilled=true` + canonical note. push_roast_recipe + patch_roast_recipe Zod fields added; `log-cupping.md` STAGE 0 + `log-roast.md` STAGE 1(b) inline-backfill paths now set both fields. Resolves Round 5 #5 (Rancho Tio one-shot backfill) provenance gap. Future UI badge render deferred to follow-up. Distinguishes design-time pushes (`was_backfilled=false`, default) from STAGE-0 inline-backfilled rows (true + prose source) from migration-052 legacy shells (true + canonical note). **S5 confirmed closed prompt-level in #174**, no schema row needed. **Cross-system audit walked the six-actor chain**: Actor 6 (schema/UI) — 3 migrations + persistence layer + UI render. Actor 4 (MCP) — 6 Zod-schema files extended (push_cupping / patch_cupping / push_roast / patch_roast / push_roast_recipe / patch_roast_recipe) + persistCupping `roasts.agtron` JOIN. Actor 5 (Claude Code) — CLAUDE.md Data Model section adds explicit notes on `roast_recipes.was_backfilled` + new `roasts.is_reference_candidate` + new `cuppings.wb_agtron`/`wb_to_ground_delta` + `cuppings.sweetness`/`temperature_behavior` MCP-reachable. Actor 2 (prompts) — 5 prompt files updated: log-cupping.md STAGE 0/2/3 (sweetness + temperature_behavior + candidate marking + was_backfilled on backfill paths); log-roast.md STAGE 1(b) (was_backfilled on inline-backfill push_roast_recipe); close-lot.md STAGE 2 (candidate→reference promotion explicit, no auto-flip); one-shot.md STAGE 4 (sweetness + temperature_behavior in cupping push). Actor 3 (claude.ai) — catalog auto-refresh on next session picks up new Zod fields + descriptions. Actor 1 (Chris) — UI renders both prose axes + wb_to_ground_delta on cupping cards in ResolvedView. **CONTEXT.md additions — 2 glossary entries**: "Reference candidate" (Forward design section) distinguishing mid-flight quality flag from final reference + worth_repeating; "Backfilled recipe" (after Pre-V_n calibration gate) documenting 3 operational sources (migration 052 shells / STAGE-0 inline / STAGE 1(b) inline) + queryability + deferred badge render. **Locked design choices at sprint kickoff (AskUserQuestion)**: S1 = denormalize wb_agtron onto cuppings + generated column (cross-table delta can't be a Postgres generated column directly); S2 = roasts-scoped not experiments-scoped (mirrors `is_reference` placement, batch-grain matches the actual decision); S4 = both boolean + text (mirrors `worth_repeating + why_this_roast_won` and `key_insight_confidence + key_insight` pattern); S3 = fold MCP-exposure fix into this sprint (small footprint, closes 10-day drift). **Verification**: idempotent migrations applied to live DB via `apply_migration` MCP after pre-flight `information_schema.columns` verified prerequisites exist + targets absent. Post-apply checks: cuppings(99 total / 99 wb_agtron / 59 delta), roasts(135 total / 0 candidate baseline / 8 is_reference baseline), roast_recipes(138 total / 112 backfilled / 26 design-time). Migration drift baseline noted but not addressed: many pre-046 migrations live in `supabase/migrations/` but are missing from `supabase_migrations.schema_migrations` tracking — visible via `list_migrations` MCP but doesn't block this sprint since prerequisites verified via `information_schema.columns` instead. Tool count unchanged at 32 (additive field surface, no new tools). |
| 2026-05-18 | **Roasting dogfooding - cleanup actions (Sprints 6-8 of post-grilling-sequencing.md)** | 28-item polish PR closing out the V-set + one-shot prompt-rewrite cycle (PRs #157 + #158 + Rounds 0-7 dogfood loop ending 2026-05-17 with Higuito V3 cupping). Parallel-track sprint per the master-plan cadence Chris shared 2026-05-18 (one-PR, ship+merge from the vigilant-aryabhata worktree; sequential master-plan tracks T0-T4+ proceed independently in the goofy-liskov worktree). **Sub-PR A — 13 prompt items across 5 files** (`docs/prompts/log-cupping.md` / `log-roast.md` / `close-lot.md` / `one-shot.md` / `one-shot-closeout.md`): (1) **log-cupping.md STAGE 0 state-shape migration** — pre-rewrite lot detection + inline backfill of `roast_recipes.predicted_cup` + `experiments.updated_cup_prediction_*` + `taste_for_*` with worked examples; closes Round 6 #4. Detection fires when V_n batch_ids populated + updated_cup_prediction_a NULL + recipe rows missing predicted_cup. Halt-and-report when expected_outcomes too generic to split per-slot. (2) **log-cupping.md STAGE 4 Path C expansion to two variants** (closes Rounds 3 #1 + 7 #7): Path C-1 (missing peer-roasted reference cup — Fazenda Um/Untold canonical case) and Path C-2 (missing cup-side discriminator: recipe_variant inversions — Higuito V3 canonical case). Both halt V_(n+1) design; state stays Waiting for next cupping. Distinct from control experiment (Path B narrowing). (3) **log-cupping.md STAGE 3 prose-field disambiguation** — `what_changes_going_forward` (forward + actionable) vs `open_questions` (interrogative) vs `additional_notes` (descriptive, not directive); quick disambiguation rule added at end of STAGE 3 (closes Round 3 #9). (4) **log-cupping.md STAGE 3 winner format strictness** — `"V<n><letter> (Batch <Roest#>)"` format; everything past the slot id goes to additional_notes (closes Round 3 #7). (5) **log-cupping.md STAGE 3 + 4 + 6 + STAGE 0 stage-write directive checklists** ("This STAGE writes: ...") at top of every STAGE on log-cupping.md + log-roast.md + close-lot.md + one-shot.md + one-shot-closeout.md (closes Round 6 #5). (6) **log-cupping.md STAGE 3 key_insight_confidence ladder operationalization** — Low / Medium / Medium-High / High with operational definitions (single-V-set / 1-2 prior data points / strong evidence / ready-to-promote-to-protocol) + tie-breaking rule "pick the lower" (closes Round 7 #8). Ladder language inlined into log-cupping STAGE 3 + log-roast STAGE 5 + one-shot.md STAGE 4 + the MCP schema descriptions on `key_insight_confidence` for both push_experiment + patch_experiment (mirrors across the 4 surfaces so it doesn't drift). (7) **log-cupping.md STAGE 6 skip-and-report language** — defer ROASTING.md proposal when (a) Path C-2 fired (provisional leading slot), (b) V_(n+1) imminent will directly test the insight, (c) key_insight_confidence is Low; print `STAGE 6: skipped - <reason>` (closes Round 7 #9). (8) **log-cupping.md STAGE 3 delta_from_cup_* extension** — walk the three taste_for_* reference points (producer notes / prior V_(n-1) memory / specific adjustment tested) noting which materialized as expected vs not (closes Round 7 #10, prompt-level path chosen over S5 schema migration). (9) **log-cupping.md STAGE 2 rest_days flag + date-drift handling** — out-of-window cupping prefixed with `REST_DAYS_DRIFT: cupped Day <N>, off the Day 7 gate by <delta>` (closes Round 3 #4 + #10). (10) **log-roast.md STAGE 1 (a) pre-rewrite batch_slot fallback** — infer slot from `roast_recipes.roest_profile_name` or `recipe_name` regex match against `<LOT-PREFIX>[-_ ]v<n><letter>` patterns; opportunistic patch_roast_recipe to backfill batch_slot + experiment_id so future passes skip the fallback (closes Round 3 #2). (11) **log-roast.md STAGE 1 (b) halt-and-report relaxation** — allow inline `push_roast_recipe` backfill when design intent is reconstructable from session memory (curve + predictions + drop rules + rationale), halt only when genuinely unrecoverable (closes Round 1 #1). (12) **one-shot.md + one-shot-closeout.md STAGE 2 is_reference / worth_repeating decoupling** — `is_reference: true` is now set UNCONDITIONALLY at one-shot close-out regardless of Outcome A/B (single batch IS structurally the reference roast for the lot); A/B distinction lives on `worth_repeating` + `why_this_roast_won` (the latter set NULL on Outcome B, which is what the Sprint 3.2 #18 "Closed without reference" ResolvedView sub-card triggers on). Closes Rounds 5 #4 + #6. (13) **close-lot.md STAGE 2 decoupling clarifier** — same axis-1 vs axis-2 framing on V-set lots; `is_reference` is structural axis, `worth_repeating` is judgment axis. **Sub-PR B — 9 MCP items across 7 files** (`lib/mcp/patch-experiment.ts` / `push-experiment.ts` / `push-roast-recipe.ts` / `push-roast-profile.ts` / `push-roast.ts` / `patch-roast.ts` / `propose-doc-changes.ts`): (14) **patch-experiment.ts behavior fix** — omit `canonical_values: {}` from response when empty (echoes were previously always-emitted-even-if-empty); the response shape is now `{ experiment_pk, updated_fields, canonical_values? }` with canonical_values optional. Closes Round 1 #7. (15) **patch-experiment.ts winner echo + format warning** — when `winner` is supplied, echo it in `canonical_values.winner` plus a `canonical_values.winner_format_warning` field when the value diverges from the `"V<n><letter> (Batch <Roest#>)"` regex (case-insensitive on V/letter). Wired into the SAME omit-empty logic from (14) so the response stays shape-stable. Closes Rounds 3 #7 + 6 #9. (16) **push_experiment + patch_experiment delta_from_cup_* description fix** — "vs `updated_cup_prediction_*` if populated, else vs design-time `roast_recipes.predicted_cup` for the matching recipe row" (closes Round 4 #8). (17) **push_experiment + patch_experiment batch_ids description fix** — "Omit field entirely to leave NULL at design time — do NOT pass the string 'null' or 'NULL'" (closes Round 6 #6) + 16-cross-batch-fields enumeration restructured in the tool-level description to be more readable (four numbered families, closes Round 6 #7). (18) **push_experiment + patch_experiment key_insight_confidence description** — operational ladder language inlined verbatim (matches the prompt-side language so claude.ai sees the same definitions whether it's reading the prompt or the MCP schema). Closes Round 7 #8 (B-half). (19) **push_roast_recipe parent_recipe_id semantics relax** — directional-ancestor framing (set when v3a was directly informed by v2b's design intent, including a SHIFTED SPREAD anchored on v2b, not only strict replication). Closes Round 4 #9. (20) **push_roast_profile + push_roast end_condition_type case discipline notes** — BEAN_TEMP (ALL-CAPS, Roest API enum integer mapping NONE=0/TOTAL_TIME=1/DEV_TIME=2/DTR=3/BEAN_TEMP=4) vs bean_temp (LOWERCASE, DB column enum bean_temp/dev_time/manual) — case discipline call-out added to both descriptions with "don't cross-pollinate" warning. Closes Round 4 #10. (21) **push_roast + patch_roast is_reference description decoupling** — full semantic call-out documenting the structural-vs-judgment axis split, including the one-shot case where is_reference: true is set unconditionally regardless of Outcome A/B. Closes Round 5 #4 (MCP half). (22) **propose_doc_changes ASCII↔Unicode normalization documentation** — current_text matching is byte-for-byte via String.includes(); NO automatic ASCII→Unicode normalization. Common offenders enumerated (en-dash vs hyphen / curly quotes vs straight / NBSP vs space / NFC vs NFD). Tool-level description gets a one-liner pointer to the current_text field. Closes Round 6 #8. **Sub-PR C — 3 page items + helpers** in `app/(app)/green/[id]/page.tsx`: (23) **WaitingForNextRoastView section reorder** — header → ROASTS · V_n SectionCard (Primary Question + Roast Hypothesis + Drop Rules) → GreenBeanInfoCard (promoted from slot 4 to slot 3) → ExperimentFrameCard (demoted from slot 2 to slot 4) → RoastLogTable → PerRoastReflections → Additional Information. Reflects load-bearing hierarchy: the page's primary job is "what's queued to roast", so the active V_n surface comes first, identity next, design-time context after. Closes Round 1 #12. (24) **WaitingForNextCuppingView section reorder** — header → Cupping Hypothesis · V_n → Roast Actuals · V_n → GreenBeanInfoCard → ExperimentFrameCard (demoted from slot 2 to slot 5, skipControlBaseline preserved) → RoastLogTable → CrossBatchNotesBlock (demoted from slot 5 to the deep-archive group with PerRoastReflections, per the 6.7 framing "reflective context, not foreground signal") → PerRoastReflections → Additional Information. Recommended Option-2 placement (cleanup-actions PR session, after Chris asked for a recommendation on the TBD slot): if Round-8 dogfood shows frequent consultation of CrossBatch at cupping prep time, promote back to slot 3 (one-line move). Closes Round 1 #13. (25) **Drop temp design→achieved diff row** on RoastActualsTable — new row labeled `Drop temp` shows `<recipe.end_condition_target>°C → <roast.drop_temp>°C` per slot; achieved cell amber-tinted (`text-latent-roast-emphasis font-semibold`) when divergence > 0.5°C. Two new helpers (`isDesignDropTempApplicable` + `formatDropTempDiff`) — design value pulled only when `end_condition_type` is `bean_temp` (other types leave the design half blank since the target isn't in °C). Surfaces operator-override visually at cupping prep time. Closes Round 3 #2. **CONTEXT.md additions — 3 entries**: (26) **Pre-V_n calibration gate** (new entry under § Forward design, next to Tolerance-anchored design) — names both Path C variants (C-1 peer-cup gate / C-2 cup-side discriminator gate), distinguishes from Pre-V1 risk reduction (which is a bundle of pre-V1 practices the gate flows through) and control experiment (which is a new V-set under Path B). Open follow-up flagged: if Path C fires often enough, schema/page may want a dedicated lifecycle state distinct from Waiting for next cupping. (27) **`is_reference: true` can coexist with `why_this_roast_won = NULL` on one-shot Outcome B lots** — new paragraph added inside the existing **Closed without reference** entry. Documents the structural-axis vs verdict-prose-axis decoupling and what the Sprint 3.2 #18 ResolvedView sub-card actually triggers on. (28) **Key-insight confidence ladder** — new entry under § V-set close synthesis. 4-level enum with operational definitions + tie-breaking rule "pick the lower" + cross-reference to the CCIL marker vocabulary in ROASTING.md (single source of truth so the schema enum + this entry + the MCP descriptions don't drift). **Verification**: `npx tsc --noEmit` zero errors (worktree symlinks node_modules from main repo per CLAUDE.md guidance; symlink removed before commit). `npm run check:registry-sync` all 19 axes in sync. `npm run check:mcp-bundle` 13 DOC_FILES paths all covered by 7 bundle globs (Sprint-0 script, first real consumer). `npm run check:migrations` — no new migrations in this PR, no drift introduced. UI preview verification deferred to Vercel preview (local `.env.local` not present in worktree per CLAUDE.md Local Verification Fallback rule; not a sprint blocker). **Cross-system audit (six-actor chain per Sprint 0's strengthened rule)**: Actor 6 (UI) — page reorder + Drop temp diff row land in page.tsx; no schema changes. Actor 4 (MCP) — 9 schema-description updates + 1 behavior fix; no new Tool surface; Resource catalog unchanged. Actor 5 (Claude Code) — CONTEXT.md additions, CLAUDE.md unchanged (the 4-state lifecycle description still applies; Path C halts within "Waiting for next cupping" rather than introducing a new state). Actor 2 (prompts) — 5 prompt files rewritten with consistent cross-references; ladder + decoupling vocabulary mirrors verbatim across MCP descriptions + CONTEXT.md + prompts. Actor 3 (claude.ai) — picks up new prompts + Tool descriptions at next session start; no Resource catalog refresh needed. Actor 1 (Chris) — UI changes observable in browser preview (Drop temp row + reorders); CONTEXT.md additions surface via the `read_doc` MCP Resource. **Out of scope this PR** (per the brief): schema-migration candidates S1-S5 (deferred to a separate sprint per the brief's "schema sprint separate" framing); A #13 routed through the prompt-level path (option chosen at kickoff via AskUserQuestion); pour-over discriminator + optimized brew lifecycle states sprint (post-Sprint-R per master plan); grill-with-docs followups (separate sequential-track sprints per post-grilling-sequencing.md); CCIL consolidation arbiter task + patch_roast/read_doc_section intermittent failure logs investigation (queued elsewhere); CGLE Sudan Rume Natural V5 missing recipe rows + historical end_condition_target backfill (operational backfills, Chris-driven). **Branch:** `claude/vigilant-aryabhata-2ec30a`. |
| 2026-05-18 | **Sprint 0 — Strengthened cross-system sync rule + check-mcp-bundle script + 6-actor matrix in CLAUDE.md** | Foundation sprint kicking off the post-grilling-sequencing era. Three substrate adds, doc + script only (zero schema / MCP / UI change). **CLAUDE.md § Sprint cadence #4** strengthened: the cross-system audit rule rewritten with the **6-actor matrix template** (Chris / docs/prompts / claude.ai project instructions / MCP server / Claude Code / Latent app) — every substrate change must be traced through the chain at sprint-close time, not deferred to monthly cleanup. **`scripts/check-mcp-bundle.ts`** new — enforces that every `docs://` Resource in `lib/mcp/docs.ts` `DOC_FILES` is covered by a glob in `next.config.js` `outputFileTracingIncludes['/api/mcp/**']`. Closes the recurring "Resource bundle miss" pattern (PR #65 humanizer-skill miss + PR #164 CONTEXT.md + docs/roasting/*.md miss) where MCP-served docs went un-bundled at Vercel deploy time. Wired as `npm run check:mcp-bundle`; exits non-zero with per-miss report on mismatch. **6-actor matrix** addition was the gate decision: prose-alone insufficient for the recurring Resource bundle misses, so the script is the enforcement — but the broader principle (substrate change = trace through the 6-actor chain) needed to be locked in CLAUDE.md so future Claude Code sessions absorb it on session start. **6-actor matrix walked**: Actor 6 — n/a (no UI / schema change); Actor 4 — n/a (no Tool / Resource change); Actor 5 — CLAUDE.md edits + `scripts/check-mcp-bundle.ts`; Actor 2 — n/a; Actor 3 — n/a (script is build-time, not runtime catalog); Actor 1 — n/a. **Verification**: `npm run check:mcp-bundle` returns clean post-script (13 paths / 7 globs at Sprint 0 ship time; now 14/8 post-Sprint-T5's sworks.md addition). **Backfilled in Sprint F 2026-05-24** — original PR (#173, merged 2026-05-18) did not include a shipped.md row at ship time; this row reconstructs the substrate change from PR title + commit message + the script + CLAUDE.md § Sprint cadence #4 final form. **Branch**: `claude/sprint-0-cross-system-sync`. |
| 2026-05-13 | **Sprint 3.2 — Cleanup-A bundle (code + migration)** | First architectural-queue sprint post-Sub-Pages-6 close-out. 20 low-risk items bundled into one PR. **Items shipped:** (1) `adminRegionMatches` token-set comparison in [lib/brew-import.ts](../../lib/brew-import.ts) replaces lowercase strict-equality at both `matchTerroir` candidate-lookup and `findOrCreateTerroir` drift-warning sites — "Tolima Department" now matches "Tolima"; "Antioquia" matches the multi-dept canonical "Antioquia / Caldas / Quindío / Tolima". (2) Top-level try/catch wrappers on [app/api/mcp/token/route.ts](../../app/api/mcp/token/route.ts) + [app/api/mcp/authorize/route.ts](../../app/api/mcp/authorize/route.ts) with inner-error logging; two prior silent catches in token route now log via `console.error('[mcp/token] ...')`. (3) `checkEndConditionBounds` helper in [lib/mcp/end-condition-bounds.ts](../../lib/mcp/end-condition-bounds.ts) — single source of truth for bean_temp/dev_time/manual×value cross-field bounds; wired into `push_roast_profile` + `push_roast` + `patch_roast` + `push_roast_recipe` + `patch_roast_recipe` handlers. (4) `power_bezier` refinement on `push_roast_profile` + `push_roast_recipe` + `patch_roast_recipe`: rejects non-null/non-empty power_bezier on INLET_TEMP profiles (Chris's exclusive mode; profile_type=5 hardcoded). (5) `withToolErrorLogging('<tool_name>', handler)` shared wrapper at [lib/mcp/tool-wrapper.ts](../../lib/mcp/tool-wrapper.ts) — applied to **34 MCP tool handlers** across 27 files via sub-agent sweep; logs `[tool_name] <err>` on unhandled throws then re-throws preserving the SDK's JSON-RPC error path. (6) Item #6 (provenance `*_override` backfill) verified-only — pre-flight DB query confirmed all 79 brews + 13 green_beans rows have non-NULL `terroir_provenance` / `cultivar_provenance` (work was done silently during Phase 3 impl); no migration needed. Also corrected the brief's column-name mismatch: actual schema is `*_provenance` (text enum `'canonical' | 'auto_created'`), not `*_override` (boolean). (7) Item #7 provenance write-path audit — confirmed `push_green_bean` ([lib/roast-import.ts:228-229](../../lib/roast-import.ts)) + `push_brew` ([lib/brew-import.ts:955-956](../../lib/brew-import.ts)) both correctly source provenance from `FindOrCreateResult.created`. Flagged for future: `patch_green_bean` bumps `canonicals_updated_at` on FK change but doesn't refresh `*_provenance` — inconsistency, out of Cleanup-A scope. (8) **Migration 053** at [supabase/migrations/053_green_beans_cleanup_backfill.sql](../../supabase/migrations/053_green_beans_cleanup_backfill.sql) bundles three small backfills: green_beans.origin from terroir.country (3 rows, skip Surma's richer compound), variety from cultivar.cultivar_name (4 rows), Rancho Tio lot name (drop `(Taza Dorada 2024 #6)` suffix). Applied to live DB via Supabase MCP `apply_migration`; post-condition verified — origin_mismatch 4→1 (Surma preserved), variety_null_with_cultivar 4→0, Rancho Tio name clean. (9) `npm run check:migrations` script at [scripts/check-migrations.ts](../../scripts/check-migrations.ts) + entry in package.json + [.github/PULL_REQUEST_TEMPLATE.md](../../.github/PULL_REQUEST_TEMPLATE.md) checklist line + [.github/workflows/migrations-check.yml](../../.github/workflows/migrations-check.yml) CI gate (skips gracefully when `SUPABASE_SERVICE_ROLE_KEY` secret unset; runs the diff when configured). Script detects 3 drift modes: pending (local file not applied), orphan (applied row without local file), prefix collision (two `NNN_` files share the same prefix). Already caught the pre-existing `046_cupping_sweetness_temperature_behavior.sql` + `046_extraction_strategy_v8_4.sql` collision on first run — exits 1 as designed. Pending/orphan checks require the service-role key; collision check runs unconditionally. (10) `<GreenBeanInfoCard>` ([components/GreenBeanInfoCard.tsx](../../components/GreenBeanInfoCard.tsx)) surfaces a muted footer when `canonicals_updated_at` is set or when `terroir_provenance` / `cultivar_provenance = 'auto_created'`. Today's 13 lots all carry `canonical` provenance; the render is forward-investment for future MCP-pushed lots that materialize new canonical rows. (11) `composeLotMeta(bean)` helper in [app/(app)/green/[id]/page.tsx](../../app/(app)/green/%5Bid%5D/page.tsx) replaces 4 inline `[bean.origin, bean.variety, bean.process].filter(Boolean).join(' · ')` sites with FK-fallback (`bean.terroir?.country` for origin / `bean.cultivar?.cultivar_name` for variety) — belt-and-suspenders against future inserts with NULL columns. (12) **"Closed without identifying a reference roast"** disambiguator card on `ResolvedView` — fires when `learnings.why_this_roast_won` is NULL (today: Oma / Surma / Libertad / El Socorro; rich-prose lots Sudan Rume Hybrid Washed + Mandela XO skip the card). Sits above the Reference Roast section so the reader doesn't mistake em-dashes for missing data. (13) Removed the "The resolved view is the densest..." placeholder `<details>` block on `ResolvedView` — empty forward-investment that didn't materialize content. (14) Text-overflow fix on `ResolvedView`'s 2 two-col grids — `min-w-0` on grid children (Recipe Design/Achieved + Cupping/Optimized Brew sub-cards) + `break-words` on the two long-prose blocks (cupping `overall` + optimized brew descriptors). Eliminates Mandela XO's mid-word truncation Chris flagged in the 6.7 spot-check. (15) `.env.local` removed from git tracking via `git rm --cached`; the .gitignore patterns (`/.env.local`, `/.env*.local`) were already there but the file had been historically tracked. (16) Documented lot-naming convention in [CLAUDE.md § Green](../../CLAUDE.md) — `${PRODUCER_OR_FARM_NAME} - ${CULTIVAR_NAME}, ${PROCESS_NAME}`; auction-tier suffixes belong in `lot_id` not `name`. (17) Documented one-shot lot framework in CLAUDE.md + [docs/roasting/redesign.md § 3.1](../../docs/roasting/redesign.md) — single-batch lots flow through the same experiment → roast → cupping → learnings pipeline as a 1-batch V-set; no schema flag, the pattern is implicit via `experiments.batch_ids` cardinality. (18) Memory file [feedback_audit_completeness_verification_grep.md](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_audit_completeness_verification_grep.md) — already shipped during Sub Pages 6.7 close-out (originSessionId hungry-lamport-cce250); referenced in MEMORY.md index. No new memory work needed. **Verification**: `npx tsc --noEmit` zero errors; `npm run check:registry-sync` all axes in sync; `npm run check:mcp` 34 tools pass discoverability; `npm run check:migrations` flags only the pre-existing 046 collision; preview server end-to-end DOM-verified the 3 ResolvedView changes (Oma renders "Closed without identifying a reference" card; Mandela XO suppresses the card and has 2 `break-words` + 5 `min-w-0` elements; "Additional Information" placeholder text absent from both pages); `composeLotMeta` confirmed via Oma subtitle "Ethiopia · Gesha 1931 · Washed" and Mandela XO "Colombia · Mandela · XO Fermentation". **Out of Cleanup-A scope** (flagged for follow-up): pre-existing `046_*` migration prefix collision (renaming would touch already-applied migration; defer); `patch_green_bean` provenance refresh inconsistency (item-7 audit finding); Track B recipe backfill content for Higuito V3 / Gesha Clouds / Sudan Rume Natural / Fazenda Um / Surma / Libertad / El Socorro (Chris fires via `patch_roast_recipe` + `patch_roast_learnings` from claude.ai sessions). Branch: `claude/sprint-3-2-cleanup-a`. |
| 2026-05-13 | **Green-bean close-out: substrate gap rendering (Sub Pages 6.7)** | Seventh and final sub-sprint of the Sub Pages 6 roasting-side rebuild — closes the 7-part series. **Substrate-gap rendering, not schema work** — surfaces 3 field families that were 100/99/91% populated in the DB but invisible on every page shape since 6.3-6.5 deferred them to "follow-up surface" calls. **3 new shared components** in `components/`: (1) [`<ExperimentFrameCard>`](../../components/ExperimentFrameCard.tsx) renders 6 frame fields (`context` / `control_baseline` / `shared_constants` / `levels_tested` / `expected_outcomes` / `failure_boundary`) as label-prefixed prose rows in a visible `<SectionCard>` placed between the lot header and the ROASTS·V_n / CUPPING HYPOTHESIS·V_n card on both waiting views. `skipControlBaseline` prop hides the field on the cupping view because `<ReferenceSignalsCard>` already surfaces it as "Anchor cup". Auto-hides null rows; auto-hides the whole card when all 6 rows null. (2) [`<CrossBatchNotesBlock>`](../../components/CrossBatchNotesBlock.tsx) renders the V_(n-1) prior experiment's `observed_outcome_a/b/c/d` slots as a collapsed `<details>` block on the cupping view — reflective context for the cupping table, not foreground signal. The CURRENT experiment's `observed_outcome_*` are still NULL during the cupping window (synthesis hasn't happened yet) per redesign § 4.2, so reading from V_(n-1) is the right source. Auto-hides when `priorExp` is null or all 4 slots are NULL. (3) [`<PerRoastReflections>`](../../components/PerRoastReflections.tsx) renders per-roast `what_worked` / `what_didnt` / `what_to_change` as a collapsed drill-down below `RoastLogTable` on all 3 lifecycle states. Per-roast drill-down rather than per-row expansion keeps the dense 9-column table compact (per the 6.3 retro: "reflection prose belongs better on the resolved view or as a per-roast drill-down"). **4th component extracted in /simplify pass**: [`<CollapsibleSection>`](../../components/CollapsibleSection.tsx) — card-chrome `<details>` primitive. Hit rule-of-5 across the 3 new components + `RoastLogTable`'s `defaultCollapsed` branch + `page.tsx`'s All Cuppings + Experiment Journey blocks (the exact-match duplicated string was the longest in the repo, per the code-reuse review agent). Refactor preserves identical render output on all 3 views — verified post-refactor via preview snapshot equality. **Lib extraction**: `SLOT_LETTERS` + `SlotLetter` type + new `PriorExperimentShape` type all moved out of `page.tsx` to `lib/lifecycle-state.ts`. `pickPriorExperiment`'s return type tightened from `{ ..., [k:string]:any } | null` to `PriorExperimentShape | null` — drops the index-signature leakage into `CrossBatchNotesBlock`'s prop contract (per code-quality review agent). **ExperimentFrameCard `title` prop** changed from optional with dead-default to required. **Wiring in `page.tsx`** (3 view functions): WaitingForNextRoastView gets `ExperimentFrameCard` after lot header + `PerRoastReflections` after `RoastLogTable`. WaitingForNextCuppingView gets `ExperimentFrameCard skipControlBaseline` after lot header + `CrossBatchNotesBlock` after Roast Actuals + `PerRoastReflections` after `RoastLogTable`. ResolvedView gets `PerRoastReflections` between collapsed `RoastLogTable` and All Cuppings details. **Tracks A + C + D shipped this PR; Track B rides separately** (recipe backfill data — Chris fires the paste-ready `patch_roast_recipe` templates via MCP in a separate workflow session; templates at [docs/sprints/sub-pages-6-7-recipe-backfill-templates.md](sub-pages-6-7-recipe-backfill-templates.md); bezier curves deferred to Sprint 3.5 Roest parity Phase 3). **Track C** (MCP Tool descriptions + `docs/prompts/*.md` consistency audit) originally scoped separate per the kickoff slicing decision but bundled in after the audit returned all-clear-cut, no interpretive items, ~6 LOC total. Audit report at [docs/sprints/sub-pages-6-7-audit.md](sub-pages-6-7-audit.md). 4 clear-cut fixes applied: (1) `lib/mcp/push-green-bean.ts:73` stale "use the app /add or /edit UI" → `patch_green_bean`; (2) `lib/mcp/push-cupping.ts:36` same stale ref → `patch_cupping` (the /add path never wrote cuppings anyway); (3) `lib/mcp/docs.ts:73` "6-sub-sprint roasting rebuild (6.1-6.6)" → "7-sub-sprint (6.1-6.7)"; (4) `docs/prompts/closed-bean-full-fill.md:135` leads with stale `best_batch_id` → leads with `best_roast_id` (Sub Pages 6.1 typed FK to roasts.id; preferred), `best_batch_id` demoted to legacy back-compat parenthetical. The initial Explore-agent pass under-sampled MCP at 39% and missed 3 of the 4 items; verification grep across the full surface caught them. Brewing-side prompts will need a parallel sweep when brewing-side `/add?type=purchased` deprecation lands (future sprint per `feedback_mcp_only_input.md`). **CLAUDE.md drift fixes** (Track D bundled into this PR): rewrote the stale "8 fields populated on 100% of rows are still invisible" line to credit 6.7's surfacing via the 3 new components; dropped the "13 unresolved experiments remain blocked on 5 missing-bean backfills" line (all 5 named beans are now in the DB: Mandela XO + Sudan Rume + Forrest + Higuito present; the line was stale post-Sub-Pages-6 series); promoted the "Sub Pages 6.3 will surface" forward-pointer on the ROAST LOG paragraph to a 6.7 past-tense reference; updated the Sub Pages 6 series summary from "6-sub-sprint" to "7-sub-sprint" with 6.7's substrate-gap framing. **Pre-flight expectations from Chris's brief** (verified post-ship): experiment frame fields 40/40 on `context` + 5 siblings; observed_outcome_a/b/c 37/38/36; observed_outcome_d 6 (the 4-batch lots); `what_worked` 128/129; `what_didnt` 119/129; `what_to_change` 118/129. **Verified end-to-end** via preview on all 4 lifecycle states: Gesha Clouds V2 (waiting-for-next-roast, 6 frame fields render in the new card; 6 reflections populated), El Paraiso Red Plum V1 (waiting-for-next-cupping, 5 frame fields with control_baseline correctly skipped, anchor cup still in Reference Signals, 3 reflections, CrossBatchNotesBlock auto-hides since V1 has no prior), Bukure + Fazenda (also V1 cupping, same auto-hide), Mandela XO (resolved, 13 reflections populated, existing collapsed All Cuppings + Experiment Journey route through CollapsibleSection unchanged). `npx tsc --noEmit` zero errors. No console warnings. **Not in 6.7**: Track B data writes (Chris fires those via MCP separately), Track C audit + clear-cut PR (separate followups), `LabelledProseRow` extraction (rule-of-25 across page.tsx but wider blast radius than this close-out warrants — promote in Cleanup-A), bezier-curve enrichment on the 13 recipes (Sprint 3.5 Roest parity Phase 3). |
| 2026-05-13 | **/add?type=self-roasted deprecation (Sub Pages 6.6)** | Final sub-sprint of the Sub Pages 6 roasting-side rebuild; closes the 6-part series. **Removal sprint, not a build sprint** — strips the full self-roasted branch from the app so claude.ai via the Latent MCP server is the sole writer for every roasting-side entity. Surfaces stripped: (1) `app/(app)/add/page.tsx` — entire 9-step SR wizard (~700 lines) + step-1 chooser screen + `handleSaveSelfRoasted` + 2 local parsers (`parseGreenBeanSpreadsheet` + `parseRoastLogSpreadsheet` + the shared `parseSpreadsheet` helper) + 18 SR-specific state hooks + `ParsedTerroir` / `ParsedCultivar` types + `GreenBeanHint` component. Purchased flow steps renumbered 2-6 → 1-5 (chooser deletion absorbed step 1); `sourceType` state narrowed to a constant; `<StepHeader>` ROASTED badge variant removed. Legacy `/add?type=self-roasted` URL now `router.replace('/green')` on mount as a courtesy bounce (page is `'use client'`, so `next/navigation` `redirect()` doesn't apply — `useEffect` + `router.replace` is the right pattern). (2) `components/Header.tsx` `+ ADD` button → `/add?type=purchased` direct (skips the now-pointless 1-option chooser). (3) `app/(app)/green/page.tsx` empty state — drops the 🌱 tile + CTA button + the 6.6-pending inline comment, replaces with copy-only block ("NO GREEN BEANS YET" + "Lots land here once a roasting session in claude.ai writes via the Latent MCP server. Open a roasting session to add your first lot."). (4) `app/(app)/brews/page.tsx` empty state CTA retargeted to `?type=purchased` for explicitness (functionally equivalent since `/add` with no type defaults to purchased post-strip). **Deliberately NOT in 6.6**: `EditBrewForm.tsx` for `source='self-roasted'` brews — stays per [feedback_mcp_only_input.md](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_mcp_only_input.md) ("brewing-side deprecation is a future sprint"); PATCH `/api/brews/[id]` route — stays for the same reason; all `lib/brew-import.ts` shared helpers (`findOrCreateTerroir` / `findOrCreateCultivar` / `seedStructuredProcess` / `seedStructuredGrind` / `composeGrind` / `composeProcess` / `structuredProcessColumns` / `structuredGrindColumns`) — all still used by purchased flow + PATCH route + MCP Tool stack, leave untouched; purchased `/add` flow — fully functional post-renumber; optional cleanups (moisture/density bare-numeric rollout, Phase 3 `expected_outcomes` / `observed_outcome_*` handling, 5-bean backfill to unblock 13 unresolved experiments) stay deferred per series-scope discipline. **AskUserQuestion-locked decisions at kickoff** (4 questions, all answered): (1) `/add?type=self-roasted` route fate = redirect to /green; (2) Header `+ ADD` = route to `/add?type=purchased`; (3) SR brew `/edit` = leave alone (brewing-side, future sprint); (4) `/green` empty state = drop button, update copy. **Pre-flight DB findings**: 6 self-roasted brews + 73 purchased + 79 total — every SR brew already FK-links to a `green_bean` row; 4 of 6 carry `roast_id = NULL` (legacy pre-FK uploads), 2 FK-matched (Mandela XO #139 + Sudan Rume Hybrid Washed #133); roasting substrate (13 green_beans · 129 roasts · 129 roast_recipes · 40 experiments · 86 cuppings · 6 roast_learnings) lives entirely outside `/add` and is MCP-written. **Audit findings** (Explore agent): `parseGreenBeanSpreadsheet` / `parseRoastLogSpreadsheet` were local-only — die with the SR branch; no tests in repo to update; no SR-specific API routes; no MEMORY.md files describe SR `/add` as current operating procedure. **Doc updates same PR** (per doc-touch checkpoint + roadmap-currency rule): CLAUDE.md — 4 spots updated (the "Add brew (self-roasted)" bullet rewritten as past-tense MCP-only pointer; ProcessPicker "self-roasted save path does NOT surface" sentence stripped; "purchased step 6" → "purchased step 5" throughout; "Sub Pages 6 series in progress" rolled up to "shipped — closed 2026-05-13"; Grinder section SR `/add` insert path note dropped); PRODUCT.md — 6.5 marker flipped to ✅ (was already shipped but PRODUCT.md drifted), 6.6 marker added with full landmark prose, parent Sub Pages 6 series flipped to "series closed 2026-05-13", 6.2 inline empty-state-preserved-through-6.6 phrase corrected; `docs/sprints/shipped.md` — this row. **Verification**: `npx tsc --noEmit` zero errors; `npm run build` clean; preview server end-to-end checked (`/add` no-type renders purchased step 1 directly; `/add?type=self-roasted` `router.replace`s to `/green`; Header `+ ADD` lands on purchased step 1; `/green` index renders unchanged for all 13 lots; `/green/[id]` resolved + waiting-for-roast + waiting-for-cupping all unchanged from 6.5; `/brews/{id}/edit` for one purchased + one self-roasted brew loads correctly with all fields populated). Empty-state visual verification skipped — Chris's dogfood DB has 13 lots, so `totalSurfaced === 0` never fires; the code-path correctness is the load-bearing check. **After 6.6**: claude.ai via the Latent MCP server is the sole writer for every roasting-side entity. Brewing-side deprecation (`/add?type=purchased` + `EditBrewForm.tsx`) is a future sprint per the standing feedback principle (likely trigger: claude.ai brewing-write workflow stable for a month+). |
| 2026-05-13 | **/green/[id] resolved-lot view + LegacyDetailRender retirement (Sub Pages 6.5)** | Fifth sub-sprint of the Sub Pages 6 roasting-side rebuild. Adds the third lifecycle-driven shape on `/green/[id]` — `resolved` now routes to a new `ResolvedView` — and retires the pre-6.3 `LegacyDetailRender` fall-through. The only remaining state, `in_inventory`, is filtered out of the `/green` index per scope doc § 5.1 and reachable only via direct URL — routes to a minimal `InventoryPlaceholder` (back-link + lot header + single SectionCard with "design V1 in claude.ai" empty-state messaging + `<GreenBeanInfoCard>`). **New shape, top to bottom:** Hero (green `latent-resolved-emphasis` tile — first real consumer of the 6.4 forward-invested token + inline "Resolved" badge) → `REFERENCE ROAST · BATCH #N` SectionCard with **Why this roast won** verdict on a `latent-resolved-emphasis-surface` card (NULL fallback prompts `patch_roast_learnings`) + **Reference Roast Recipe** 2-col grid (LEFT "Design" from `roast_recipes` linked via `roasts.recipe_id`: Peak inlet / Drop temp / End condition / Charge + Hopper / Fan curve; RIGHT "Achieved" from the `roasts` row matched by `best_roast_id`: FC time / FC temp / Drop time / Drop temp / Agtron WB + inline WB→Gnd Δ from primary cupping's `ground_agtron`). All Design rows except Charge/Hopper render em-dashes today since backfilled recipes have NULL beziers (Phase 3 enrichment is intentionally empty per redesign doc § 7) → `REFERENCE CUP` SectionCard with **Best cup synthesis** prose (sources `optimizedBrew.what_i_learned` → pourover `overall` fallback) + two side-by-side sub-cards: LEFT "Cupping · #N" surfaces the latest pourover-eval cupping on the ref roast (composed `overall` or labeled aroma/flavor/acidity/body/finish rows), RIGHT "Optimized Brew · #N retasted" surfaces the brew row joined via `green_bean_id` (prefer `roast_id = best_roast_id`, fallback to first; populated on Sudan Rume/Mandela XO via FK, on Surma/Oma/Libertad/El Socorro via fallback) with compact recipe line composed by `composeBrewRecipeLine` (brewer · filter · dose:water (ratio) · temp · grinder + setting) + `<StrategyPill variant="row">` + `<ModifierBadges>` + descriptor prose. Bottom-line italicized `bean.producer_tasting_notes` for comparison → `ROASTING LEARNINGS · {lot}` SectionCard with **three character mini-cards** (Primary Lever / Roast Window / Elasticity in a `grid-cols-1 sm:grid-cols-3 gap-4` of white-bordered `<CharacterCard>` sub-blocks; "Not populated" empty-state when NULL) + 7 detail rows in `space-y-4` (Secondary levers / Underdev signal / Overdev signal / What didn't matter / Aromatic behavior / Structural behavior / Rest behavior; skip nulls) → `ROASTING LEARNINGS · TO CARRY FORWARD` SectionCard (Cultivar takeaway / General takeaway / Starting hypothesis for similar lots; cross-cutting "Contributed to practice" punted per scope § 6) → `<GreenBeanInfoCard>` (shared, third consumer ratifies the 6.4 rule-of-3 extraction) → `<RoastLogTable defaultCollapsed highlightedBatchIds={[batchNumber]}>` (the `defaultCollapsed` prop's first real consumer — 6.4 forward investment now load-bearing) → **All Cuppings · N evaluations** collapsed `<details>` with one row per cupping chronologically (`Batch #N · {rest_days}d rest · {eval_method}` mono meta + composed descriptor prose; reference batch row gets `font-semibold` + ⭐ marker) → **Experiment Journey · N sets** collapsed `<details>` with per-set summary cards in ascending chronological order (experiment_id mono + `Batches:` + `primary_question` + highlighted `winner` + `key_insight` with optional confidence suffix — curated narrative per scope § 5.4, NOT full transposed tables) → Additional Information `<details>` placeholder noting the resolved view is the densest of the four shapes. **Top-level fetch update**: replaced legacy `relatedBrews` `id, coffee_name`-only projection with a richer `brewsForLot` query (28 fields: source / extraction_strategy / hybrid_subform / modifiers / strategy_notes / cooling_curve_target / brewer / filter / dose_g / water_g / ratio / grind / grinder / grind_setting / temp_c / bloom / pour_structure / total_time / 6 descriptor fields / flavor_notes / flavors / structure_tags / key_takeaways / what_i_learned / created_at) so `ResolvedView` consumes the brew without a second round-trip. **Inline helpers** (rule-of-2 not yet hit for extraction): `extractBatchNumber(raw)` (strips `Batch ` / `#` prefix), `pickPourover(cuppings, refRoastId)` (latest pourover eval_method on ref roast, fallback to any cupping), `pickOptimizedBrew(brewsForLot, refRoastId)` (FK match preferred, fallback to first), `composeBrewRecipeLine(brew)` (compact `·`-joined summary), 5 recipe-side render helpers (`renderPeakInlet` / `renderDropTemp` / `renderEndCondition` / `renderChargeHopper` / `renderFanCurve` — all em-dash safe), 4 row primitives (`<RecipeRow>` / `<CupRow>` / `<CharacterCard>` / `<LearningRow>`). **Removed in same diff**: pre-6.3 `LegacyDetailRender` function (~270 lines) + `LearningField` helper + legacy `relatedBrews` thin fetch — all dead post-dispatch. Diff is net-negative on `page.tsx`. **Locked decisions from kickoff `AskUserQuestion` round**: (1) Reference Cup data sources = pourover cupping + brews row joined via green_bean_id with `roast_id = best_roast_id` preference, (2) 3 character cards = SectionCard-style sub-cards in 1→3 col grid (not GridField pattern, not border-l inline), (3) LegacyDetailRender = delete entirely + tiny InventoryPlaceholder for direct-URL in_inventory hits, (4) All cuppings = one row per cupping chronological with ref-batch ⭐ marker. **Pre-flight DB findings confirmed**: all 6 resolved lots have typed `best_roast_id` (clean Phase-3 backfill); 2/6 have rich `why_this_roast_won` (Sudan Rume + Mandela XO), 4/6 NULL; 2 brews link via FK, 4 via fallback; Surma's reference roast (#25) has 1 Cupping (Day 4) + 2 Pourover (Day 10 + 13) — `eval_method` is the distinguisher between the 2 sub-cards, NOT `recipe_variant` (always NULL on observed). **Verified end-to-end** via preview server on all 6 resolved lots (all 200, `Resolved` badge present, `Why this roast won` rendered, `Optimized Brew` sub-card populated) + non-regression on 3 waiting-for-next-roast lots (Higuito / Forest / Sudan Rume Natural) + 3 waiting-for-next-cupping lots (Red Plum / Bukure / Fazenda Um) + Rancho Tio (pre-framework legacy edge case still routes to 6.3 view with "No experiments designed yet" hint) + `/green` index (3 sections, all section titles present, no errors). `in_inventory` not exercised at runtime — no bean in DB matches that state today (every bean has ≥1 roast), so `InventoryPlaceholder` is a defensive route the data doesn't reach; tsc compiles its imports clean. `npx tsc --noEmit` zero errors. |
| 2026-05-13 | **/green/[id] waiting-for-next-cupping view + shared components + semantic color tokens (Sub Pages 6.4)** | Fourth sub-sprint of the Sub Pages 6 roasting-side rebuild. Adds the second dispatch branch on `/green/[id]`: `waiting_for_next_cupping` routes to new `WaitingForNextCuppingView`. **New shape, top to bottom:** Hero (sage tile, same as 6.3) → `CUPPING HYPOTHESIS · V_n` SectionCard with Primary Question prose + Summary transposed table (3 rows × N batches: Original prediction from `recipe.predicted_cup` muted, Updated prediction from `experiments.updated_cup_prediction_*` purple-tinted with `border-l-2 border-latent-cup-emphasis`, Taste for from `experiments.taste_for_*` purple-tinted) + Reference signals sub-card (purple-tinted `bg-latent-cup-emphasis-surface border border-latent-cup-emphasis`, 3 rows: Producer notes / V_(n-1) winner cup derived from prior exp's `winner` + matching `observed_outcome_*` / Anchor cup from `experiments.control_baseline`) → `ROAST ACTUALS · V_n` SectionCard (6 rows × N batches: FC / Drop / vs expected total amber from `delta_from_roast_*` / Dev time / Maillard / Agtron WB; column headers `slot · #batch_id`; rows auto-hide when all batches NULL) → Green Bean Info → Roast Log → Additional Information `<details>` placeholder. **Anchor cup reuses existing `experiments.control_baseline`** (no migration; Chris locked the decision over a new `anchor_cup_reference` field after weighing the semantic overlap). **3 chrome token families added** (Chris's pushback drove semantic naming — "name by meaning, not color value, so the underlying palette can shift without a rename"): `latent-cup-emphasis` + `-surface` for cupping-related (#6B4D75 muted purple + #F4EDF3 surface), `latent-roast-emphasis` + `-surface` for roast-related (#8B5E1F coffee-brown amber + #F8EFE0 surface), `latent-resolved-emphasis` + `-surface` for resolved lots (#2C3E2D + #F8FFF0, aliasing existing accent/highlight values as forward investment for 6.5). Pairing convention documented inline: `bg-latent-X-emphasis-surface border border-latent-X-emphasis` for colored cards; `text-latent-X-emphasis` or `border-l-2 border-latent-X-emphasis` for accents inside neutral cards. **2 shared components extracted** (rule-of-3: 6.3/6.4/6.5 all consume the same shapes): `<GreenBeanInfoCard bean>` (prop-driven, parent has the bean row — no duplicate Supabase round-trip) + `<RoastLogTable roasts cuppings highlightedBatchIds? defaultCollapsed?>` (the `defaultCollapsed` prop is 6.5 forward investment for the resolved-view archival log; renders natively via `<details>` rather than React state to stay server-component-compatible). **6.3 promotions in same PR** (Chris's pushback identified rule-of-3 amber usage triggering the token add early): Drop Rules card surface `bg-latent-highlight` → `bg-latent-roast-emphasis-surface border border-latent-roast-emphasis`; HypothesisTable's Drop temp + Peak inlet rows now amber-highlight every cell when their values vary across batches (scope doc § 5.5 "highlighted amber on the cell(s) where it varies across batches" — Drop temp + Peak inlet are levers; other rows like Expected FC / Total / End Condition / Agtron / Hypothesis are descriptive and stay neutral). Variance detection via a `rawValue` extractor on the RowSpec + `new Set(values).size > 1` check at top of `HypothesisTable`. **Today's data shape** (load-bearing): 3 lots route to this state (Bukure / Red Plum / Fazenda). None have recipes linked to their latest experiment (`experiment_id = NULL` on all 129 backfilled recipes per scope doc § 7), and none have `updated_cup_prediction_*` / `taste_for_*` / `delta_from_roast_*` populated yet (those fields landed in migration 052 with NULL backfill). So today's render is mostly empty-state explainers prompting `patch_experiment` / `push_roast_recipe` from claude.ai — page is built for the new workflow. The Reference signals card has the most populated content today (producer notes + anchor cup both populated on Bukure; Red Plum + Fazenda have producer notes only). **Verified end-to-end** on all 7 lot routes via preview server: (1) `/green` index — 3 lots in cupping section, 4 in roast section, 6 resolved; (2) Bukure (V1 cupping, 3 roasts #176/#177/#178, anchor cup populated) — full shape renders; (3) Red Plum (V1 cupping, synthesized winner) — same shape, routes via the cupping_count==0 disjunction; (4) Fazenda (V1 cupping) — same shape; (5) Higuito (V3 waiting-for-roast) — 6.3 view via shared components, no regression; (6) Rancho Tio (pre-framework legacy edge case) — 6.3 view, no V-label, shared components render; (7) Mandela XO (resolved) — LegacyDetailRender fall-through preserving GREEN BEAN DETAILS / BEST ROAST / EXPERIMENTS / ROAST LEARNINGS / CUPPING HISTORY. Console clean across all routes. **New helpers in page.tsx** (inline, defer extraction until 6.5 hits rule-of-3): `parseDeclaredBatchOrder` (ordered array variant of the 6.3 batch-ids parser — first declared batch maps to slot a, second to b, etc., when recipes don't link), `computeSlotInfos` (slot a/b/c/d → {recipe, roast, declaredBatchId} for cupping/actuals tables; matches roast via `recipe_id` FK first, falls back to declared batch_ids position), `pickPriorExperiment` (most recent experiment with `created_at < pivot.created_at`, stable tie-break by id). V_(n-1) winner cup derivation: parse `priorExp.winner` for a trailing slot letter (a/b/c/d), then read `priorExp.observed_outcome_<slot>`; fallback to `priorExp.key_insight` if winner doesn't resolve to a slot. **Out of scope (deferred to 6.5)**: resolved-view dispatch branch, LegacyDetailRender fold into shared components (third consumer is 6.5), Phase 3 legacy field cleanup, Tabler `ti-leaf` icon swap, `/add` deprecation (6.6). `npx tsc --noEmit` + `npm run build` both pass. |
| 2026-05-13 | **/green/[id] waiting-for-next-roast view (Sub Pages 6.3)** | Third sub-sprint of the Sub Pages 6 roasting-side rebuild. `/green/[id]` is now **state-driven** — `computeLifecycleState(bean)` (the 6.2 helper) dispatches to one of N page shapes at the same URL. 6.3 ships the **waiting-for-next-roast** shape via a new `WaitingForNextRoastView` component inside `app/(app)/green/[id]/page.tsx`; lots in other states fall through to a preserved `LegacyDetailRender` (the pre-6.3 single-shape page, copied verbatim) until 6.4 + 6.5 add the cupping + resolved shapes. **New shape, top to bottom:** Hero (sage `bg-latent-accent-light` tile matching the /green index "active" state) + 🌱 emoji (Tabler icon swap still deferred — no icon library installed) → `ROASTS · V_n` SectionCard with Primary Question prose + Roast Hypothesis sub-block. **Transposed batch table** (rows = attributes / cols = batches): Drop temp / Peak inlet / Expected FC / Expected Total / End Condition (mono) / Predicted Agtron WB / Hypothesis (`rationale` field, not `notes`). Auto-hides any row where ALL recipes have the field NULL — table tracks what's actually being designed rather than a sea of em-dashes. Empty-state explainer prompts `push_roast_recipe` from claude.ai when 0 recipes link to the experiment. **Drop Rules card** renders only when ≥1 recipe has `drop_rule_if_fast` or `drop_rule_if_slow`; 2 rows × N batches inside a `bg-latent-highlight` (pale green) surface. **GREEN BEAN INFO card** (trimmed 6-field grid + producer's tasting notes prose). **ROAST LOG** preserves the 9-col table from the legacy render + adds current-experiment row highlighting: rows whose `batch_id` matches a parsed token from `latestExp.batch_ids` get `class="highlight"` row tint + a `●` marker after the batch number. `parseBatchIdsForHighlight(raw)` mirrors the lifecycle-state helper's batch_ids parsing inline (rule-of-2 not yet hit; extract if 6.4 needs the same parse). **Additional Information** `<details>` collapsed block with a placeholder note that deep surface (cupping history / all experiments / roast learnings / related brews) renders on the resolved-lot shape in 6.5. **Helpers**: `formatVLabel(experimentId)` extracts the trailing V-segment ("COS-HIG-BOR-2026-V3" → "V3") via `/[vV](\d+)(?!.*[vV]\d)/` with fallback to the full label; useful when experiment_id has a V-suffix convention. **Today's data shape** (load-bearing): 0 of 129 backfilled recipes link to any experiment (backfill set `experiment_id = NULL` per scope doc § 7 "no aggressive dedupe"), so the transposed table renders the empty-state explainer for all 4 current waiting-for-next-roast lots until claude.ai pushes recipes via the new MCP Tool surface. Page is built for the new workflow — it'll light up the moment recipes start landing. **Verified end-to-end** on all 4 lot patterns: (1) Higuito V3 (TBD batch_ids, V3 designed but no roasts logged → empty-state explainer + 0 highlights), (2) Gesha Clouds V2 (synthesized, batch_ids "170, 171, 172" → 3 of 6 roast log rows highlighted with `●` markers + correct row tint), (3) Rancho Tio (no experiments + 1 roast = pre-framework legacy edge case → section title drops V-label "ROASTS" + "design the first experiment" fallback), (4) Sudan Rume Hybrid Washed (resolved → falls through cleanly to `LegacyDetailRender` preserving GREEN BEAN DETAILS / BEST ROAST / EXPERIMENTS / ROAST LEARNINGS / CUPPING HISTORY). **Scope doc § 5.5 amber chrome deferred to 6.4** — Drop Rules card uses existing `latent-highlight` (pale green) token; amber promotion waits for sibling uses (varying-cell highlight + "+5s overran" delta callouts in 6.4's Roast Actuals card) to accumulate to rule-of-3 token-add justification. Mid-implementation gotcha I avoided: the existing legacy render is 270+ lines and tempting to refactor; instead preserved verbatim as a fallback function so the dispatch is a 5-line change at the top of the page component (rather than a whole-file rewrite with all 3 shapes inline today). The /simplify pass extracting shared primitives (Hero, GreenBeanInfoCard, RoastLogTable) waits until 6.4 + 6.5 land and the rule-of-3 trigger fires. `npx tsc --noEmit` + `npm run build` both pass; build emits all expected routes. |
| 2026-05-13 | **/green index lifecycle sections (Sub Pages 6.2)** | Second sub-sprint of the Sub Pages 6 roasting-side rebuild. Replaces the flat green-bean card grid with 3 lifecycle-state-grouped sections (Waiting for next roast / Waiting for next cupping / Resolved). **State derivation lives in `lib/lifecycle-state.ts`** — `computeLifecycleState(bean)` returns one of 4 states (`in_inventory` / `waiting_for_next_roast` / `waiting_for_next_cupping` / `resolved`) computed on read from the joined experiment + roast + cupping + roast_learnings shape. Resolution order codifies scope-doc § 8 verbatim with two edge cases the doc doesn't name explicitly: (1) pre-framework legacy lots with roasts but no experiments route to `waiting_for_next_roast` (Rancho Tio today — 1 roast, 0 experiments); (2) synthesis-done-but-V_(n+1)-not-designed also routes to `waiting_for_next_roast` per the loop framing in § 3 ("the user moves from waiting-for-next-roast → waiting-for-next-cupping → back to waiting-for-next-roast for V_(n+1)"). **Cupping gate is the scope-doc disjunction** — either `cupping_count == 0` OR `winner == null` routes to `waiting_for_next_cupping` (so Red Plum / Fazenda / Bukure surface there even though their latest experiments have `winner` populated — the page tells the user "go log the cuppings" not "design V2"). Two display helpers ship alongside: `lifecycleSectionTitle(state)` (sentence case per § 5.1) + `lifecycleStageLabel(state, referenceBatchLabel?)` (right-column status indicator: "Next roast" / "Next cupping" / "Batch #N" or "Reference"). **Index rewrite**: flat sectioned list (no `<SectionCard>` chrome — index is intentionally airier than the prior card grid). Each section: title + right-aligned column header ("Stage" for active / "Reference" for resolved) + `border-b`-separated rows. Each row: 12x12 color tile + lot name + metadata line (`origin · variety · process` with empties filtered) + right-aligned stage label. **Tile color signals lifecycle state** — `bg-latent-accent-light` (sage) for active lots, `bg-latent-fg` (near-black) for resolved (hue shift = green coffee → roasted coffee, fits design-conventions "hue-not-lightness" rule; both are existing chrome tokens, no new tokens added). Resolved rows surface `Batch #${best_batch_id}` with `^Batch\s*#?` regex strip so legacy values like `"Batch 139"` render as `Batch #139` not double-prefixed. In-inventory lots are computed but intentionally not rendered on the index (waiting for the eventual inventory page per § 5.1 punt). **All 13 live lots route correctly**: 4 waiting-for-next-roast (Rancho Tio / Higuito / Gesha Clouds / Sudan Rume Natural) + 3 waiting-for-next-cupping (Red Plum / Bukure / Fazenda Um) + 6 resolved (Sudan Rume Hybrid Washed #133 / Mandela XO #139 / Surma #25 / Oma #52 / Libertad #94 / El Socorro #88). Tabler `ti-leaf` icon swap **deferred to 6.3** — not load-bearing for the index (no leaf icon in the index mockup), lands with the detail-page hero rewrite where the emoji is currently most visible. Empty state preserved through 6.6 — still links to `/add?type=self-roasted` (deprecation lands in 6.6). Empty state CTA inline-commented as deprecation-pending so the next session reading the file doesn't fix-and-ship the link removal. Verified end-to-end via preview server: desktop (all 13 lots in correct sections, tile colors correct, Batch # labels correct) + mobile 375px (truncation clean, no horizontal scroll). `npx tsc --noEmit` + `npm run build` both pass; build emits all expected static + dynamic routes including `/green`. One TS hiccup mid-implementation: `[...Set<string>]` spread requires higher TS target than the repo configures; switched to `Array.from(out)` (same pattern as existing helpers in the repo). |
| 2026-05-13 | **Roasting redesign schema + MCP Tool surface (Sub Pages 6.1)** | First sub-sprint of the 6-part Sub Pages 6 roasting-side rebuild (scope doc: [docs/roasting/redesign.md](../roasting/redesign.md), 530 lines, Chris-authored hand-off). Phase 1 of the doc's 3-phase migration plan — purely additive schema + MCP Tool surface, no page changes, no /add deprecation. **Migration 052** created `roast_recipes` table (~30 fields with curves / drop rules / design-time predictions / Roest linkage / `rationale` for per-batch Hypothesis prose + `notes` free catch-all; RLS + 3 indexes), added `roasts.recipe_id` FK + index, added 16 cross-batch fields to `experiments` (`updated_cup_prediction_a/b/c/d` + `taste_for_a/b/c/d` + `delta_from_roast_a/b/c/d` + `delta_from_cup_a/b/c/d` per scope doc § 4.2 four-write-moments rationale), added `roast_learnings.best_roast_id` typed UUID FK + index. **Backfill** (all clean): 129 roast_recipes rows 1:1 from existing roasts (one per execution, no replication relationships expressed yet per scope doc § 7 "no aggressive dedupe"), 129 `roasts.recipe_id` linked via copied `created_at`, 6 `roast_learnings.best_roast_id` resolved via `regexp_replace + JOIN ON batch_id` from legacy text values ("133" / "Batch 139" / "25" / "52" / "94" / "88"), 2 `is_reference=true` drift flips on Mandela XO #139 + Surma #25 (Chris's Sub Pages 6 round-1 bundle opt-in). All smoke counts match expectations. **MCP Tool surface 32→34**: 2 new (`push_roast_recipe` + `patch_roast_recipe`) with full ~30-field Zod schemas, V-set UPSERT key on (experiment_id, batch_slot) when both supplied otherwise (green_bean_id, recipe_name) for one-off recipes, 4-part description convention compliant (verb cluster / WHEN / PAYLOAD / RETURNS). 6 existing Tools extended: push/patch_experiment got 16 new optional fields with per-temporal-moment describe() text; push/patch_roast got `recipe_id` FK; push/patch_roast_learnings got `best_roast_id` FK (`best_batch_id` text stays through Phase 3 for back-compat). **Resource surface**: new `roast_recipes://by-experiment/{experiment_id}` template (returns `{ experiment_id, recipes[] }` ordered by `batch_slot`); existing `roasts://by-bean/{green_bean_id}` Resource now joins `roast_recipes[]` so claude.ai reads design intent + as-recorded execution in one fetch. **Types** (hand-maintained in `lib/types.ts`, no CLI regen): new `RoastRecipe` interface, extended `Roast`/`Experiment`/`RoastLearning` with new fields, new `InsertRoastRecipe` type. **Doc updates**: copied 530-line scope doc into `docs/roasting/redesign.md` + registered in `lib/mcp/docs.ts`; CLAUDE.md § Data Model added `roast_recipes` entity bullet + 3 relationship-pattern lines + Green section lifecycle-state derivation rules + 4→13 lot count refresh; PRODUCT.md § Side Quests Sub Pages 6 converted from single-entry placeholder to 6.1-6.6 sequenced series with per-sub-sprint scope; ROASTING.md gained "Schema model" pointer to the redesign doc. **Pre-flight DB findings confirmed** during planning: 13 lots (not 4 — CLAUDE.md was stale), 6 closed / 7 active per derived state, all 6 `best_batch_id` text values resolve cleanly via regex (no orphans). **NOT in 6.1**: page changes (6.2-6.5), /add deprecation (6.6), prompt updates in `docs/prompts/*.md` (6.6 cleanup), Phase 3 legacy `expected_outcomes` / `observed_outcome_*` cleanup. |
| 2026-05-12 | **Sprint 3.1 — Architectural-queue brainstorm** | Plan-mode session producing the next-iteration sprint queue. Same shape as Sprint 2.2's V2-architecture brainstorm: 3 AskUserQuestion rounds, 11 locked decisions, ~30 architectural-queue items categorized across 4 input sources (`feedback_v2_mcp_feedback_log.md` Batches 1-20 + Sprint 2.7.5 retro + Phase 3 PR #105 deviations + Sprint 3.0 OAuth retro). **Output:** 6-sprint build queue 3.2-3.7 — Cleanup-A bundle (code + migration, ~12 items, ships next) → Auto-supersede paired sprint (#R88 + #R89 + tool-list cache visibility, ~2-3h) → Per-batch failure_boundary breach record (plan-mode + impl, #R67 + #R68) → Roest API parity Phase 3 (7 pull-side schema gaps including R65 HIGH-IMPACT timezone-drift bug) → BREWING/ROASTING doc reconciliation (subprocess spec / STAGE 8 routing / Active Lots sub-anchoring / one-shot framing / schema-describe micro-fixes) → Prompt v5 rewrites (9 incremental-sync-v4 + new-bean-intake updates). **2 scoping docs land** in `docs/features/`: `roest-api-parity-phase-3.md` (Sprint 3.5) + `experiments-breach-record.md` (Sprint 3.4 placeholder, awaits plan-mode session). **3 promotions out of debt**: defensive try/catch + console.error sweep → Cleanup-A; MCP tool-list cache visibility → Auto-supersede; Active Lots sub-anchoring → BREWING/ROASTING fixes. **R65 timezone-drift bug rides along in Roest parity** — not carved out as hotfix; symmetric with the other 6 pull-schema items. **`/green/[id]` design refresh stays Chris-owned** (Side Quests, not queued, mockup-then-scope pattern). **Parked / triggered**: recipe_variant canonical promotion (5+ values across 3+ beans), parse_cupping_transcript voice-memo pipeline, push_cupping body-length error instrumentation, #R38 dual-namespace axis confusion, #R46 tool_search ranking. **PRODUCT.md § Active Sprints** renumbered: 3.1 entry replaced by 3.2-3.7 (items 1-6); General cleanup sprint (was #2) → #7; Per-entity directed synthesis (was #4) → #8. No code, no migrations, no schema changes — all implementation lives in the future sub-sprints. Retro: `memory/project_v3_1_brainstorm.md`. |
| 2026-05-12 | **Sprint 2.7.5 — Workflow walkthrough + Roest API write dogfood** | 7-round dogfood loop across 12 PRs (#107 / #109 / #110 / #119 / #130 / #132 / #134 / #137 / #138 / #139 / #143 / #144). Bundled the Roest API write integration build (Phase 1 + 2: `push_roast_profile` / `push_inventory` / `patch_inventory`; Tool count 29 → 32) with iterative friction polish across the full roasting workflow — green bean intake → profile design → roast → cup → sync → forward design → close-out — exercised end-to-end on Higuito + REDPLUM and other lots. **Schema additions:** migration 044 on `roasts` (roest_notes / end_condition_type / end_condition_target / fc_total_cracks + worth_repeating type promotion boolean → text tristate) + migration 050 on `experiments` (additional_notes / open_questions / key_insight_confidence). **MCP-surface improvements:** `updated_fields` echo symmetry sweep across all 5 patch_* Tools (Round-5) + `canonical_values` echo on patch_experiment / patch_roast / patch_inventory for enum-validated fields (Round-7) + composite_key echo on push_cupping (Round-4) + `canonicals_updated_at` actually-writes fix in patchGreenBean (Round-7) + `hopper_load_temp: null` correction in pull_roest_log (different signal from Roest's preheat_temperature, Round-5) + corrected `mapEndConditionEnum` decoder (BEAN_TEMP=4 not 1; latent Sprint-2.5 bug surfaced via Roest UI screenshot in Round-2). **Prompt evolution:** new-bean-intake.md (tools list + routing + V1-spread default), in-process-bean-incremental-sync.md (FIRST-OF-LIKELY-TWO recipe_variant rule + operator-override check + FC Floor & Ceiling per-lot append routing + additional_notes framing + STAGE 8 forward design + cross-references), closed-bean-full-fill.md (symmetric updates), new `design-next-experiment-set.md` for V_N → V_{N+1} forward design with V1-brackets / V2-narrows / V3-confirms discipline codified. **Two standing rules extracted:** `feedback_external_drift_ok_latent_canonical_required.md` (Roest can drift; Latent DB must resolve canonical) + `feedback_migration_drift_pattern.md` (recurring class: migrations land in `supabase/migrations/` via PR but don't auto-run in Supabase; diagnostic SQL pattern + idempotent fix recipe; confirmed twice in this sprint). **Continuous-feedback channel** opens at `memory/feedback_mcp_continuous_log.md` for post-sprint friction polish + drift control. **Outstanding follow-ups (11 items)** categorized in retro: cleanup-sprint bundle (5), plan-mode sprint (1, breach record), triggered-by-evidence (3), fold-into-doc-touch (1), separate sprint (1, /green/[id] design refresh). See `memory/project_v2_7_5_workflow_walkthrough.md` for full retro + lessons. |
| 2026-05-11 | **/roasters detail rethink (Sub Pages 5)** | Re-ordered around three jobs in priority order: (1) brew-guide / typical brew style for a random coffee from this roaster, (2) general roasting + resting info, (3) recall a specific coffee. Surfaced **24 previously-invisible `RoasterEntry` fields** that flowed only into the synthesis prompt before — roastStyle / developmentBias / restCurve / primaryBrewer / filterType / extractionIntent / brewAdjustmentMethod / overExtractionTolerance / processSensitivity / failureMode / Baseline Recipe (composed inline from tempC + ratio + typicalBrewTime + agitationLevel) now render on the page UI in parallel with the prompt anchor. 9-section layout top-to-bottom: Hero (family swatch + name + family/coffee-count/location; strategy-tag pill + URL link dropped) → Brewing Philosophy (Brew Guide as anchor or "No official brew guide" + House Style + Brewing Intent) → Roasting Philosophy → Generalized Brewing Recipe (2-col grid w/ 6 fields + 3 full-width below) → Resting Info → Coffees I Have Brewed From This Roaster → **What I've Learned (synthesis card deliberately demoted** below the coffees list per Chris's three-jobs framing — synthesis is nice-to-have for roasters vs front-and-center on terroir/cultivar/processes) → Additional Information (`<CollapsibleBlock>` w/ 5 sub-blocks: Roaster Metadata label-prose rows for leftover fields including strategyTag / primaryDriver / brewGuideSource+Type / doseG / waterG / calibrationRole / confidenceLevel / bmrHouseStyle when distinct from houseStyle / bmrNotes; Common Flavor Notes via `<FlavorNotesByFamily bare>`; Cultivars / Terroirs / Processes Explored via `<TagLinkList bare>`) → Confidence (HIGH ≥5 / MEDIUM ≥2 / LOW = 1). New local `<LabelledField label value>` helper for inline `**label:** value` rows (mirror of cultivar's `<GridField>` pattern, not extracted — rule-of-one). **No adapter change, no schema change, no migration** — the rich registry was already populated; only the page UI was missing. Sparse-registry roasters degrade gracefully via auto-hiding empty sections — Latent (3-field entry) renders Brewing Philosophy w/ houseStyle row only, Roasting Philosophy + Generalized Recipe + Resting Info sections all hide. Pre-flight DB query: 24 roasters / 79 brews; top 5 = 66% of brews; 11 of 24 single-brew tail; 6 of 24 have cached synthesis (all current, no regen wave). Per-roaster archive page directed-prompt enhancement stays as a separate Longer Term Items entry — Sub Pages 5 surfaced the rich-field substrate the synthesis prompt already used; the adapter retune is a future sprint. Verified end-to-end on Shoebox (mockup match) + Hydrangea (HIGH, fully populated) + Latent (sparse, 3 sections auto-hide) + Bean & Bean (1 brew, LOW). Mobile 375px: 2-col grid stacks, CollapsibleBlock renders `<details>` collapsed, no horizontal scroll. `npx tsc --noEmit` + `npm run build` both pass; console clean. |
| 2026-05-11 | **/processes first-principles regrouping (Sub Pages 4)** | Replaced the flat text-keyed `/processes/{slug}` aggregation (28 distinct values, 19 of them 1-brew thin pages out of 79 brews total — 68% of the surface) with a 3-tier composable architecture grounded in the existing `base_process` + modifier-axis registry. Subsumed the deferred 1e.4 facet redesign in the same sprint. **Index restructured** into 3 navigation surfaces: Core Process Portals (4 base hubs sorted by brew count desc — Washed 39 / Natural 32 / Honey 8; Wet-hulled hidden until first brew lands), Modifier Index (8 entries: Anaerobic 16 / Yeast Inoculated 9 / Dark Room Dried 6 / Cold Fermentation 4 / Raised Bed 4 / Slow Dry 4 / Double Anaerobic 3 / Thermal Shock 3 — driven by `eligibleModifierIndexEntries(brews)` so the list grows as new modifiers cross the threshold), Signature Methods (Moonshadow / Hybrid Washed / TyOxidator). **Sub-page eligibility** codified per Chris's brainstorm Rules 1-3: Rule 1 (≥3 brews) earns `/processes/{base}/modifiers/{combo}`; Rule 2 (signature exemption) always earns `/processes/signatures/{name}` even at 1 brew; Rule 3 (Honey color tier structural exemption) earns `/processes/honey/{subprocess}`. ~21 page surfaces total. **5 new page kinds**: index · base hub (Hero / Process Summary / Observed Variants & Modifiers chip clusters / Brew Archetypes 5-field grid / synthesis / coffees list / Additional Information CollapsibleBlock / Confidence) · Honey subprocess sub-page (Process Breakdown chip rows / synthesis / coffees / cross-link sections) · per-base modifier-combo mini-page (mockup 2 shape) · cross-base Modifier Index page (Modifier Overview prose / By Base 4-col grid / Common Stacks list / synthesis / coffees / Additional Information) · signature method page (Process Overview prose / Process Breakdown / Observed Cup Profile bullets / synthesis ≥2 brews / coffees). **New lib infrastructure**: `lib/process-routing.ts` (deterministic kebab-case slug helpers + reverse parsers + `SUB_PAGE_THRESHOLD = 3` constant + URL builders for cross-link convenience) · `lib/process-aggregation.ts` (single source of truth for grouping brews by aggregation kind — `aggregateBaseHub` returns pure/modified split + modifierCombos[] + honeySubprocesses[] + signatures[]; `aggregateModifierIndex` returns all + byBase + commonStacks[]) · `lib/process-confidence.ts` (Rule 5 confidence labels). **Display dedupe** in new `composeProcessDisplay` helper: strips leading `"Anaerobic "` from drying modifiers when fermentation already contains Anaerobic — fixes "Anaerobic Anaerobic Slow Dry Raised Bed Natural" render bug. Modifiers joined with " + " separator; comma before base. Same dedupe applied in `structuredLabel` (aggregation chip labels). **Schema**: migration 051 created `process_aggregation_syntheses` table keyed on `(user_id, aggregation_kind, aggregation_key)` with CHECK constraint on `aggregation_kind IN ('base', 'honey_subprocess', 'modifier_combo', 'modifier_index', 'signature')`; legacy `process_syntheses` dropped (3 rows lost, regen on visit). **Adapter dispatch**: `lib/synthesis/adapters/process.ts` rewritten as `getProcessAdapter(kind)` factory exporting 5 per-kind adapters; each shapes its anchor differently (base reads `BaseProcessEntry.summary + brewArchetype`; modifier_index reads `ModifierEntry.overview + byBase breakdown`; signature reads `SignatureEntry.overview + cupProfile + producer/country/protocol facts`); 9-tier weighting + 5-step outputFormat + formatLearningRow preserved verbatim across all kinds. API route accepts `{kind, key}` body. **Registry extension**: `lib/process-registry.ts` adds `BaseProcessEntry` (4 entries with `summary` + 5-field `brewArchetype`) + `ModifierEntry` (8 entries with `overview` for the cross-base index pages) + `SignatureEntry.overview` + `SignatureEntry.observedCupProfile`; all fields optional with empty-state messaging until Phase A content authoring lands. **Cross-link migration**: 3 sites (cultivars/[id] / terroirs/[id] / roasters/[slug] "PROCESSES EXPLORED" TagLinkList) updated to link to `/processes/{baseSlug}` and dedupe by `base_process` instead of composed `process` text. **Tier B content scope** (~37 authored cells) shipped with empty-state placeholders pending Chris's Phase A authoring. **/simplify pass** extracted 3 shared component primitives: `<ProcessBreakdownRow>` (3 inline copies → import) · `<ProcessConfidenceCard>` (6 inline copies → import) · `<ProcessCoffeesList>` (6 inline copies → import). Verified end-to-end via preview server: /processes index 3 sections + live counts · /processes/washed hub all 8 sections · /processes/washed/modifiers/double-anaerobic-thermal-shock-yeast-inoculated mockup 2 verbatim · /processes/modifiers/anaerobic cross-base aggregation 16 brews/3 bases · /processes/honey/white 3 brews · /processes/signatures/moonshadow 1 brew · /processes/wet-hulled returns 404 · Anaerobic+Slow Dry+Raised Bed Natural display dedupe ✓ · mobile 375px responsive ✓ · npx tsc --noEmit + npm run build both pass. |
| 2026-05-11 | **/cultivars first-principles regrouping (Sub Pages 3)** | Scope expanded mid-kickoff from a Light terroir-shape port to a full data-model inversion when Chris's first-principles question surfaced that `mergeLineageCharacteristics`'s first-non-null + array-union across sibling cultivars was producing semantically garbled aggregate pages: Bourbon Mutation Lineage's "Acidity Style" was whichever of Bourbon Aji / Caturra / Laurina / Pacas / Purple Caturra / Villa Sarchi came first alphabetically with the field populated, even though those mutations have totally different cup behavior. Lineage deleted as a destination; cultivar is the only leaf. **Index restructured** Species (header) → Family (header w/ swatch + visible-lineage count) → Lineage (sub-header, non-link) → Cultivar (linked row w/ brew count). 0-brew cultivars hidden — 30 → 28 visible (Bourbon + Wush Wush filtered). Family cascades hide when all its lineages hide; lineage cascades hide when all its leaves hide. **Detail page rewritten** around Chris's three-act mental model (General Cultivar Info → What I've Learned → My Experiences) — 9 sections: Hero (cultivar_name h1 + species→family→lineage breadcrumb subtitle, sibling Tags row dropped) → Genetic Background (prose) → Cultivar Context (2-col grid, 4 fields: Typical Origins / Altitude Sensitivity / Limiting Factors / Market Context) → Brewing & Cup Profile (2-col grid, 8 fields: Common Processing Methods / Typical Flavor Notes / Acidity Style / Body Style / Aromatics / Terroir Transparency / Extraction Sensitivity / Brewing Tendencies) → Roasting Characteristics (2-col grid, 4 fields: Roast Tolerance / Roast Behavior / Resting Behavior / Common Pitfalls) → What I've Learned About This Cultivar (synthesis, **conditional on brewList.length >= 2** — 18/30 cultivars hide it) → Coffees I Have Brewed In This Cultivar (filtered by `cultivar_id` not `.in(siblings)`) → Additional Information (CollapsibleBlock w/ bare-prop FlavorNotesByFamily + 3 TagLinkLists for terroirs / processes / roasters) → Confidence. Local `<GridField label value>` helper renders each grid cell (label above value). **Mokka + Mandela rich content backfilled** from `docs/taxonomies/varieties.md` so no thin pages ship — only 2/30 cultivars were skeletons and both had ≥1 brew. **29 stale lineage-aggregated synthesis rows invalidated** to NULL via `UPDATE cultivars SET synthesis = NULL, synthesis_brew_count = NULL WHERE synthesis IS NOT NULL` — re-generation per-cultivar on first ≥2-brew visit via SynthesisCard's brewCount-changed trigger. Synthesis route caller now sends `[singleId]` instead of lineage siblings; `entityName` = `primary.cultivar_name`. Cultivar adapter dropped the dead `cultivarNamesInLineage` field. Cross-link audit clean — 4 sites (brews / terroirs / processes / roasters detail) already pass `cultivar.id` correctly; click "Laurina" on a brew page now lands on Laurina (semantically correct), not Bourbon Mutation Lineage. No new design tokens; reused canonical primitives (SectionCard / CollapsibleBlock / FlavorNotesByFamily bare / TagLinkList bare). Verified end-to-end on Laurina (1 brew, synthesis hidden) + Sidra (5 brews, synthesis renders) + Mokka (1 brew with rich backfilled content rendering across all 4 structured grids) + /cultivars index (28 visible, 0-brew filter working) + mobile 375px (grids collapse to 1-col, CollapsibleBlock collapsed-by-default). |
| 2026-05-10 | **/terroirs/[id] detail page rethink (Sub Pages 2)** | Reordered the terroir detail surface around Chris's three jobs: (1) quick reference about the region, (2) aggregation of what I've learned, (3) coffees list. New section order: Hero (unchanged) → `HIGH LEVEL SUMMARY` (NEW SectionCard, promoted `context` field) → MESO TERROIRS EXPLORED → TERROIR CONTEXT (now 3 fields: soil / cup_profile / why_it_stands_out) → TERROIR CHARACTER → TYPICAL PRODUCTION → WHAT I'VE LEARNED ABOUT THIS TERROIR (synthesis, the load-bearing aggregation surface) → COFFEES BREWED FROM THIS REGION (promoted up from position-bottom; label renamed from "COFFEES FROM {MACRO}") → ADDITIONAL INFORMATION (`<CollapsibleBlock>` mobile-collapsed wrapper containing Flavor Notes by Family + Cultivars + Processes + Roasters) → CONFIDENCE. Two adjacent fixes bundled: cultivar tag labels simplified from `${lineage} / ${name}` → just `${name}` ("Rosado" / "Gesha" / "Sudan Rume" / "Sidra" / "Pink Bourbon" / "Purple Caturra" / "Aruzi"); /terroirs index now hides macro_terroir groups with 0 brews (Mantiqueira Highlands + Central Plateau Highlands gone, 22 → 20 visible macros). New shared primitive: `bare`-prop on `<FlavorNotesByFamily>` + `<TagLinkList>` strips the SectionCard wrapper for nesting inside CollapsibleBlock — forward investment for Sub Pages 3-6. No directed-prompt adapter changes (existing terroir synthesis already produces the 4-6 paragraph + 4-6 practical-takeaway shape). No new design tokens. Pre-flight DB query confirmed: 36 terroirs, 22 fully described (all 9 description fields populate together), Huila Highlands `context` DB value matches mockup byte-for-byte, 2 zero-brew macro groups confirmed. Verified end-to-end on Huila Highlands (mockup match) + Northern Andean Highlands (skeleton row surfacing fully-described sibling content via macro-group aggregation) + /terroirs index + mobile 375px disclosure expand. |
| 2026-05-09 | **/brews/[id] detail page rethink** | Re-ordered the primary brew surface around the user job ("about to brew/serve, what do I do?") per Chris's design doc + mockup. New section order: Header → Reference Brew Recipe (page anchor with table + Bloom + executable pour bullets + Drawdown + Strategy/Modifier pills + Modifier Detail) → Presentation Overview → Peak Expression (dark) → Coffee Overview (4 leaf-level tags with breadcrumbs) → What I Learned (dark, promoted up the page) → Full Brew Notes (renamed from "Additional Information"; mobile-collapsed via pure-CSS `<details>` dual-tree). New `lib/pour-structure.ts` parser with composable strategy cascade (newline / semicolon / middle-dot / period+marker / arrow-chain) — handles Pepe Jijón "Pour 1: ... → ...g. Pour 2: ..." + Apricoast semicolon + Ngoma newline + Picolot/Loud Giants arrow-chain shapes. ParsedPourStep carries optional structured fields (label / time / amount_g / method) as forward investment for a future `brews.pours` jsonb migration — render layer stays the same. New components: `<RecipeTable>` (6-row 2-col anchor), `<PourStructureList>` (parsed bullets + drawdown filtered out so it surfaces in its own footer), `<CollapsibleBlock>` (pure-CSS responsive disclosure, no client component). New `splitModifierLabel(m)` helper in `lib/extraction-modifiers.ts` splits the em-dash so type/form renders as a pill while detail renders separately. Orphan field policy: render conditionally in Full Brew Notes if non-empty (strategy_notes + cooling_curve_target + terroir_connection + cultivar_connection + classification); `roast_connection` dropped from the read view (0/79 brews populate it). View + edit stay split. Brew count corrected 55 → 79 in CLAUDE.md drift. Verified across 4 brews (Pepe Jijón mockup match, Loud Giants Picolot arrow-chain → 7 bullets, CGLE Mandela XO single-pour, Gesha Village Surma self-roasted with all orphan fields populated). |
| 2026-05-09 | **WBC roasting + sourcing reference docs** | PR #128. One-off doc sprint, not from PRODUCT.md Active Sprint queue. Two new open-ideas docs in `docs/roasting/`: `wbc-roasting.md` (6 generalizable lessons from the ~100-row WBC competitor archive 2022-2025 + 4 Roest L200 hypotheses + top-level Blending experiments section with 3-batch dev ladder protocol gated by Roest `quantity_g` inventory check + top-level Rest as a structured variable section with 4-portion rest-curve protocol that fits onto existing cupping schema with no new entity + Tier 1/2/3 experiment queue) and `wbc-sourcing.md` (sourcing strategy by producer / origin / variety / process / elevation + 4-lane portfolio model + Tier 1/2/3 priority targets + Latent inventory mapped to tiers scoreboard snapshot 2026-05-09 + Latent's actual current palate vs the WBC sweet spot section reframing Tier 1 as roasting-skill upkeep rather than urgent buy given Chris's VERY-expressive + Suppression preference). Parallel to existing brewing-side WBC reference layer (`docs/brewing/wbc-reference.md` + `docs/brewing/wbc-recipes.md`) but content shape differs - sparse roasting CSV means hypothesis / experiment doc, not recipe lookup; useful competitor citations folded inline rather than maintained as a separate competitor archive. Both docs registered in `lib/mcp/docs.ts` for `read_doc` access. ROASTING.md gained 3 cross-refs (top brewing-side cross-link, New Coffee Onboarding Step 1 - Intake, Lot Knowledge intro). CLAUDE.md Documentation Index `docs/roasting/` entry expanded to list all three roasting docs. Persistent palate refinement saved to `user_taste_evolution.md` § Palate refinement (2026-05-09). |
| 2026-05-08 | **Per-entity directed synthesis** | New `lib/synthesis/` scaffolding (`types.ts` / `buildPrompt.ts` / `runSynthesis.ts` / `humanizer.ts` + vendored `humanizer-skill.md` from blader/humanizer MIT) + 4 per-entity adapters under `lib/synthesis/adapters/` (terroir / cultivar / process / roaster). Each adapter ports Chris's authored prompt verbatim (4-6 paragraphs + 4-6 practical-takeaway bullets, weighted-input table from his sample-output files, registry anchor) so a 24-brew Volcán Barú synthesis pulls on terroir-specific climate / processing / variety context instead of repeating "taste the coffee cooler." 4 synthesize routes refactored from ~120 → ~50 lines each, calling shared `runSynthesis(adapter, entity, brews)` orchestrator. Two-call pipeline: Sonnet generates raw → Sonnet humanizer pass strips AI-tells (em-dash overuse / inflated significance / rule-of-three / promotional language / etc.) before save to cache. Domain vocabulary preserved verbatim via system-prompt override (Suppression / Clarity-First / SWORKS / Picolot / etc.). Cache write surface unchanged: `terroirs.synthesis` + `cultivars.synthesis` + `process_syntheses` + `roaster_syntheses` (all already TEXT, no migration). New `<SynthesisRenderer>` parses `\n\n` paragraphs + `* ` bullet lists without a markdown lib. /simplify pass extracted shared `<SynthesisCard>` (rule-of-4 across the 4 entity wrappers) and deleted `TerroirSynthesis.tsx` + `CultivarSynthesis.tsx` + `ProcessSynthesis.tsx` + `RoasterSynthesis.tsx`. `next.config.js` `outputFileTracingIncludes` extended to bundle `humanizer-skill.md` for the 4 synthesize routes. |
| 2026-05-08 | **Track 2 — Data sanity audit** | New `scripts/data-sanity-audit.ts` (12 audit dimensions: orphan rows / childless pairs / process+grind drift between legacy text and structured columns / canonical drift on text-only columns cross-referenced against `taxonomy_overrides_queue` / Phase 3 queue health + provenance markers / roast→cupping pair completeness / modifier shape via `cleanModifiers` / flavor + structure_tags via `cleanFlavors` + `cleanStructureTags` / SR-brews-need-green_bean_id / doc_proposals staleness / synthesis cache staleness across `terroirs.synthesis_brew_count` + `cultivars.synthesis_brew_count` + `process_syntheses` + `roaster_syntheses`). Run via `npm run audit:data` (one-shot, requires `SUPABASE_SERVICE_ROLE_KEY` in `.env.local`). Dry-run report committed at `docs/audits/track-2-2026-05-08.md` — 159 findings: 11 Bucket A (process recompose ×9, provenance flag flip ×1, Rwanda admin_region orphan ×1), 64 Bucket B (orphan terroirs ×2, producer drift ×5, stale-cupping roasts ×57 most likely C after row-by-row triage), 50 info (mid-iteration roasts <7d ×18, cache staleness ×32). Substrate gap surfaced: `SUPABASE_SERVICE_ROLE_KEY` not in either local `.env.local` so v1 dry-run ran via Supabase MCP `execute_sql` — script is canonical for repeat runs once Chris adds the key. Bucket A fixes split to follow-up PR per round-1 decision. Track 2 of the 3-track General cleanup sprint (Track 1 shipped 2026-05-07 PR #119; Track 3 bundled with Claude-Design redesign). |
| 2026-05-08 | **WBC corpus active consultation (Brewing v8.5)** | PR #117. Section 4 of BREWING.md reframed from "reference-only, not consulted during a normal Coffee Brief" to actively-pulled during Step 1d (new "WBC corpus + cross-cutting control patterns check" Named Consideration alongside Cooling-Curve Design). MODIFIER_TYPES grows 3 → 4 — `role_based_pulse` promoted from the wbc-reference.md "consciously not pursuing" list to a 4th canonical Axis 2 modifier (free-text `roles` sub-field) for percolation-only per-pour role discipline. OUTPUT_SELECTION_FORMS grows 3 → 4 — `dilution` form added with optional `dilution_g` sub-field on `OutputSelectionModifier` for post-brew water addition. Brewer rotation discipline gains 3 WBC anchor cross-refs (Switch ↔ Ferket Selective Bloom; Orea v4 + Sibarist FAST + Melodrip ↔ Wölfl Extraction Push; SWORKS ↔ Carlos Medina Suppression analog). docs/brewing/wbc-reference.md gains 7 new sections per Chris's critical review: Counting Note (family vs axis), Cross-Cutting Control Patterns (Time Distribution playbook / Water Strength / Agitation Taper / Filter Behavior / Pre-Brew Conditioning / Output Selection extension), 3-tier Practical Experiment Queue, Competition-to-Home Translation Rules. Cross-cutting calibration axes deliberately stay in the doc layer rather than promoting to canonical modifiers — "keep compact strategy set, add separate doc layer" framing. Read-side dog-food validated same-day on a Newbery Khun Lao brief (claude.ai pulled wbc-recipes.md + wbc-reference.md by name, Andreas Harestad 2025 fingerprint match identified, Water Strength surfaced as cross-cutting load-bearing axis, RBP fired with agitation-taper framing). |
| 2026-05-08 | **ROASTING.md Lot Knowledge reorg + Active Lots refresh** | PR #116. 5-tier restructure of ROASTING.md's lot knowledge surface (Active Lots / Reference Roasts+Brews / Cross-Coffee Insights / Open Questions / Archive) replacing the prior scattered "How This Project Is Structured" + "Active Lots" + "One-Shot Calibrations" + "Recently Closed Lots" + "Cross-Coffee Insight Layer" + "Reference Brew Recipes by Lot" + "Per-lot generalized learnings" sections. Active Lots refresh bundled in same PR — currency check on the 5 listed lots, mid-iteration lots added. |
| 2026-05-08 | **MCP Tool descriptions audit** | PR #114 + spot-check follow-up PR #115. Description-only audit of all 32 MCP Tools against the post-#111 doc-description pattern (when / payload / returns). 22 Tools at 3/3 kept as-is; 10 Tools at 2/3 (missing explicit Returns shape) gained a `Returns { ... }` sentence — full patch_* family (6 Tools) plus push_roast_learnings, resolve_queue_entry, list_doc_sections, read_doc, read_doc_section, read_canonical, propose_doc_changes, propose_canonical_addition. Stretch goal: DESCRIPTION CONVENTION block added to top of `lib/mcp/discoverability-check.ts` (next to the existing ACTION_SYNONYMS lint guard) so future Tool authors hit the 4-part pattern. PR #115 follow-up added `fix` / `mark` / `correct` to the patch_brew + patch_roast verb clusters in ACTION_SYNONYMS after a spot-check turned up missing entries. Zero behavior or schema changes. `npm run check:mcp` 32/32 + `npx tsc --noEmit` clean. |
| 2026-05-08 | **Registry-md sync script — drift guard for 10 canonical taxonomies** | PR #112. New `scripts/check-registry-sync.ts` — for each of the 10 canonical taxonomy axes (cultivars / terroirs / processes / roasters / producers / brewers / filters / flavors / roast-levels / grinders), parse the authored `docs/taxonomies/*.md` headers + the validation-mirror `lib/*-registry.ts` canonical array and fail when they diverge. Permanent low-maintenance guard for the "2-step deliberate edit" pattern that was previously only enforced socially. Wired into `npm run check:registry-sync` so the existing CI surface picks it up without a workflow edit. |
| 2026-05-07 | **May 2026 doc cleanup PR** | Track 1 of the 3-track General cleanup sprint. Walks the full root-doc surface: PRODUCT.md restructure (7-section roadmap collapses 4 prior sections + relocates "What's Missing"), CLAUDE.md (Documentation Index + Sprint cadence 4→6 items), README.md refresh, SYNC_V2.md restructure (~555→~250 lines), ARBITER.md drift fixes, BREWING.md (April Brewer registry fix in 3 files), ROASTING.md (13 edits incl. new Roast Philosophy paragraph), 3 prompts updated, lib/mcp/docs.ts adds descriptions + registers docs/roasting/archive.md, migration 046 (cuppings.sweetness + cuppings.temperature_behavior), SYNC.md + design-brief.md deleted. |
| 2026-05-06 | **Roest API write integration Phase 1+2 (bundled)** | PR #110. 3 new MCP Tools: `push_roast_profile` + `push_inventory` + `patch_inventory`. Tool count 29 → 32. Phase 2 promoted into the same sprint as Phase 1 — bidirectional inventory sync matters for the new-bean onboarding loop now that writes flow both directions. Substrate: scope=read+write + authedWrite + getRoestCustomerInfo + 3-read retrofit. Empirical end_condition wins over OpenAPI (BEAN_TEMP=1 not 4). |
| 2026-05-06 | **Brewing v8.4 — Hybrid promotion + WBC docs refresh** | PR #108. 6th strategy promoted (Hybrid as extraction structure vs the 5 intensity strategies). 102-recipe WBC archive (`docs/brewing/wbc-recipes.md`) ships alongside refreshed `docs/brewing/wbc-reference.md`. 3 brew reclassifications from Balanced+Immersion → Hybrid (Sequential). Cooling-Curve Design as new Step 1d named consideration with `cooling_curve_target` free-text column. |
| 2026-05-05 | **Phase 3 — taxonomy_overrides_queue + provenance + 3 MCP Tools** | PRs #103 (design) + #104 (doc proposals batch) + #105 (impl). Migration 045 adds `taxonomy_overrides_queue` (single shape across 7 axes; EXCLUDE constraint on `(user_id, axis, lower(raw_value)) WHERE status='pending'` auto-collapses duplicates) + `terroir_provenance` / `cultivar_provenance` enum on `green_beans` + `brews` + `green_beans.canonicals_updated_at`. 3 new Tools (`list_taxonomy_queue` + `propose_canonical_addition` + `resolve_queue_entry`) lift count 26 → 29. ARBITER.md gained "Taxonomy queue arbitration" section + T1-T6 procedure. |
| 2026-05-05 | **Phase 2 — substrate gaps from 16 roasting + 5 brewing dog-food sessions** | PR #101 + follow-up #102 (cross-system audit findings post-#101 merge: `lib/types.ts` + `/add` page drift caught only because Chris asked — origin of `feedback_cross_system_audit.md` standing rule). |
| 2026-05-05 | **Phase 1 — prompts foundation cleanup** | PR #100. Port 7 prompts to `docs/prompts/`, MCP-served via `lib/mcp/docs.ts` `DOC_FILES` + `listDocs()`. Single source of truth that auto-updates on every Vercel deploy, replacing manual paste-into-claude.ai-project-files cycle. Cluster F prompt-v5 rewrites + Cluster A namespace updates included. |
| 2026-05-04 | **Iteration round-2 fixes + doc proposals batches** | PRs #95 / #96 / #97 / #98 / #99. Rwanda Northern Province / Central Plateau Highlands terroir registry add (#99); BREWING + ROASTING canonicals reframe (#96 + #97); doc proposals batch from May 4 brewing dog-food (#98); BREWING.md `canonicals://` Tool fallback note for clients that don't enumerate URI templates (#95). |
| 2026-05-03 | **V2 Sprint 3.0.5 — promote brews Resources to Tools** | PR #94. `brews://recent` + `brews://by-id` Resources promoted to `list_recent_brews` + `get_brew` Tools (architectural rule: claude.ai surfaces Tools to the model, Resources only as catalog). Tool count 24 → 26. |
| 2026-05-03 | **V2 Sprint 3.0 — OAuth 2.1 + PKCE for claude.ai web MCP** | PR #93. Discovery endpoints (`/.well-known/oauth-protected-resource` + `/.well-known/oauth-authorization-server`), `/api/mcp/authorize`, `/api/mcp/token`, WWW-Authenticate header on `/api/mcp` 401. Migration 043 for auth-code storage. Static client credentials via Vercel env. claude.ai web becomes the main brewing surface post-3.0 + post-2.7. |
| 2026-05-03 | **V2 Sprint 2.7 — Brewing prompt v9 + canonicalization + MCP-as-source-of-truth flip** | PR #92 — last build sprint in V2 queue. Bidirectional canonicalization: BREWING.md +173 lines, ROASTING.md -3 lines. Two new subdocs: `docs/brewing/wbc-reference.md` + `docs/roasting/archive.md`. After deploy + MCP registration, claude.ai retires 12 uploaded files (-14,614 lines from project context). Em-dash sweep (103 → 0 in BREWING) shipped same PR. |
| 2026-05-02 | **Post-2.6 cleanup mini-session** | PRs #87 (cherry-pick of #86) + #88 (registry edits) + #89 (recompose_process flag) + #90 (migration 042: terroirs UNIQUE includes meso_terroir). 17 patches + 8 orphan deletions via Sprint 2.6 patch_* Tools. |
| 2026-05-02 | **V2 Sprint 2.6 PR2 — 6 patch_* Tools** | PR #87 (cherry-pick of #86). 6 new Tools: `patch_brew` / `patch_green_bean` / `patch_roast` / `patch_cupping` / `patch_experiment` / `patch_roast_learnings`. Tool count 18 → 24. `patchBrew` helper extracted from `app/api/brews/[id]/route.ts` as single source of truth. |
| 2026-05-02 | **V2 Sprint 2.6 PR1 — strict find-or-create on FK tables** | PR #85. Closes 4 silent-failure modes on terroir / cultivar find-or-create (R7 / R8 / R12 / R23) via strict-canonical fail-fast + meso in match key + (country, macro) pair validation. |
| 2026-05-01 | **Roasting MCP feedback batches 1-9** | PRs #75 - #84. Tool discoverability sweep + workflow context + push_green_bean UPSERT + push_roast UPSERT + brews[] in get_bean_pipeline + push_cupping UPSERT + Tolima registry gap + push_cupping recipe_variant + get_bean_pipeline desc surfaces patch_cupping. PR #84 doc proposals batch from May 1-2 roasting dog-food. |
| 2026-04-30 | **V2 Sprint 2.5 — Roasting MCP tools + ROASTING.md** | PR #73 + follow-up #74. 7 new Tools (5 push + 2 Roest pull). ROASTING.md ~770-line verbatim port. Migration 039 (14 cols on roasts + cuppings + experiments + green_beans). Roest API integration via `/o/token/`. CGLE Mandela XO end-to-end dog-food. 2 substrate gaps fixed in #74 (numeric weights + preflight). |
| 2026-04-29 | **V2 Sprint 2.4 — propose_doc_changes + arbiter playbook** | PRs #66 + #67 + #68 + feedback batches #69 / #70 / #71. Polymorphic `propose_doc_changes` Tool + `doc_proposals` queue (migration 037) + ARBITER.md + per-citation `target_doc` + section-anchor reads + 4 doc-introspection + 2 canonical-introspection Tools. First prose round-trip = Dongzhe 5th brew. |
| 2026-04-28 | **V2 Sprint 2.3 — MCP scaffold + push_brew + read Resources** | First V2 build sprint — sync infrastructure live. `@modelcontextprotocol/sdk` 1.29.0 + `WebStandardStreamableHTTPServerTransport` on Vercel Node serverless. Bearer auth via `api_keys` table + migration 036. 12 canonical Resources at `canonicals://{axis}` + `brews://recent` + `brews://by-id/{uuid}` + `docs://brewing.md`. `push_brew` Tool wrapping `persistBrew`. `SUPABASE_SERVICE_ROLE_KEY` substrate gap closed mid-sprint. |
| 2026-04-28 | **V2 architecture brainstorm (Sprint 2.2)** | Architecture doc shipped at SYNC_V2.md. Five locked decisions: MCP transport / API key auth / bidirectional with Resources / repo-file source-of-truth + DB proposal queue / asymmetric write trust. Build queue locked. |
| 2026-04-27 | **V1-brews dog-food (Sprint 2.1)** | First end-to-end SYNC pipeline run. Picolot Emerald PL#015 (Mokka Natural) + Janson Green-Tip Gesha Natural Anaerobic 1010 landed. Mokka cultivar instantiated. `immersion` shipped as 4th canonical Modifier type (later absorbed into Hybrid in v8.4). |
| 2026-04-27 | **Extraction Strategy v2** | Migration 034. `EXTRACTION_STRATEGIES` extended 3 → 5 (Suppression + Extraction Push promoted) + new `brews.modifiers jsonb` array (3 canonical types: output_selection / inverted_temperature_staging / aroma_capture). Hue-separated 5-pill palette. ModifierComposer + ModifierBadges. BREWING.md gains SECTION 4 = WBC Reference. **8-registry taxonomy batch complete** post this sprint. |
| 2026-04-26 | **Brewer + Filter taxonomy adoption (sprint 1f)** | Migration 032. Two new registries from one CSV pair: 46 brewers (12 owned, 24 aliases) + 64 filters (22 owned post-dedupe, 34 aliases). Schema Option A. Material axis dropped. Orea v3/v4 ambiguity defaults to v4. xBloom net-new dripper. "Espro Bloom" relabel → xBloom Premium Paper Filters. |
| 2026-04-26 | **Producer taxonomy adoption (sprint 1l)** | Migration 031. 49-name flat → 120-entry rich registry. Schema Option A. allowOverride pattern. 6 collapses. /simplify rule-of-three hit on `canonicalizeOverridable` PATCH helper. |
| 2026-04-25 | **Grinder taxonomy adoption + V2 rework (sprint 1k)** | Migrations 029 + 030. V2 same-session rework: single EG-1 with burrs/burrShape/burrSize fields; setting axis flipped to enumerated strict (51 valid values 3.0-8.0 in 0.1 steps; 16 with rich content). New `components/GrindSettingInput.tsx`. |
| 2026-04-24 | **Roast level taxonomy + roaster /add enforcement (1m + 1h.2)** | Migration 028. 8 Agtron-anchored canonical buckets + 22 aliases. New `canonicalize()` method on CanonicalLookup fixes case-drift. CanonicalTextInput gains `allowOverride` + "Use anyway" link. |
| 2026-04-24 | **Roaster taxonomy structural port (1h.1)** | PR #52. Migration 027. 70 canonical roasters across 6 families (added SYSTEM as 6th). 4 strategy reclassifications (Rose/Noma/Picky Chemist → SYSTEM, TM Coffee → CLARITY-FIRST). |
| 2026-04-24 | **/add SR canonical rebuild (1i)** | SR steps 7+8 rebuilt to canonical pickers; `handleSaveSelfRoasted` routes through findOrCreateTerroir + findOrCreateCultivar via Promise.all. |
| 2026-04-23 | **Process enforcement (1e.3)** | PR #46. ProcessPicker (shared builder) + `/add` purchased + `/add` SR + `/edit` + persistBrew + PATCH route + save-gate. |
| 2026-04-23 | **Process schema migration + decomposition (1e.2)** | Migrations 025 + 026 (post-ship correction). 8 structured columns added to brews + 55-row backfill. |
| 2026-04-23 | **Process structural port (1e.1)** | First Phylum A2 port. Composable taxonomy. `lib/process-families.ts` → `lib/process-registry.ts`. composeProcess / decomposeProcess + LEGACY_DECOMPOSITIONS for 20 DB values. Zero DB/UI change. |
| 2026-04-22 | **Region enforcement (1d.3)** | CanonicalTextInput on `/add` + `/brews/[id]/edit` bound to `TERROIR_MACRO_LOOKUP`. `findOrCreateCultivar` + `findOrCreateTerroir` extracted to `lib/brew-import.ts`. Admin_region auto-populates from registry. |
| 2026-04-22 | **Region content backfill (1d.2)** | Migration 024. 22 UPDATEs populating 11 content columns from regions.md. climate_stress → free-text; typical_processing Title Case. Overwrites migration 009. |
| 2026-04-22 | **Region taxonomy adoption (1d.1)** | Migration 023. 121 canonical macros across 38 countries + rich `lib/terroir-registry.ts`. Meso/micro demoted to free-text. Dual-registry drift killed. 11 DB-row renames preserving 55 brew FKs. |
| 2026-04-22 | **Variety enforcement (1b)** | CanonicalTextInput on `/add` purchased review + `/brews/[id]/edit`. Edit picker flips from 26-DB-rows to 63-canonical registry with lazy find-or-create; aliases resolve on save; `isResolvable` added to `makeCanonicalLookup`. |
| 2026-04-22 | **Variety content backfill (1a.2)** | PR #36 + migration 022. 26 existing DB cultivar rows populated with 18 attribute fields from varieties.md. |
| 2026-04-22 | **Variety taxonomy adoption (1a.1)** | PR #34 + migration 021. `docs/taxonomies/varieties.md` + rich `lib/cultivar-registry.ts` (63 canonicals / 20 lineages / 48 aliases). Dual-registry drift killed. Mokka pre-req for SYNC V1 resolved. |
| 2026-04-22 | **Producer + roaster canonicalization** | PR #23 + migration 018. Mirrors migration 013's flavor-notes pattern. Audit found 2 drifted producer clusters + 5 verbose strings. New `lib/producer-registry.ts` + `lib/canonical-registry.ts` factory. New `components/CanonicalTextInput.tsx`. |
| 2026-04-21 | **SYNC.md playbook (V1-brews step c)** | `SYNC.md` at repo root — validator-table-driven. Deprecated 2026-05-07 in May doc cleanup PR (V2 MCP path is canonical). |
| 2026-04-21 | **Terroir + cultivar canonical registries (V1-brews step b)** | PR #30 — `lib/terroir-registry.ts` + `lib/cultivar-registry.ts` + factory alias tier. |
| 2026-04-21 | **BMR v7.1 → BREWING.md (V1-brews step a)** | PR #27 — `BREWING.md` at repo root. |
| 2026-04-21 | **Reference Roasts + Roasting Guide — brainstorm + feature doc** | `docs/features/reference-roast-and-guide.md`. |
| 2026-04-21 | **Mobile polish on /brews, /brews/[id], filter bar** | — |
| 2026-04-21 | **Cross-dim filters on /brews** | — |
| 2026-04-20 | **Self-roasted flow audit + /add hardening** | PR #25 + migration 020. |
| 2026-04-20 | **Green detail render polish** | — |
| 2026-04-20 | **Data-depth backfill (terroirs + cultivars)** | Migrations 015 / 016 / 017. |
| 2026-04-19 | **Experiments import + render polish** | Migration 019. |
| 2026-04-19 | **Design-system standardization** | — |
| 2026-04-19 | **Cross-link backfill across aggregation detail pages** | — |
| 2026-04-18 | **Roasters aggregation** | Migration 014. |
| 2026-04-17 | **Design polish + mobile pass** | PR #14. |
| 2026-04-17 | **Flavor canonicalization + edit UI** | Migration 013. |
| 2026-04-16 | **Processes aggregation** | Migration 012. |
| 2026-04-16 | **Producer column + backfill** | PR #12 + migration 011. |
| 2026-04-16 | **Extraction strategy sprint** | PRs #10 / #11. |
| 2026-04-15 | **Terroir macro redesign** | PR #5. |
| 2026-04-14 | **Cultivar lineage redesign** | PR #4. |
| (earlier) | **Purchased-coffee import flow, build hygiene, initial schema + backfills** | PR #9 + migrations 001-010. |

---

## See also

- **Per-sprint retros:** `memory/project_*.md` — full narrative + lessons per sprint, indexed in `memory/MEMORY.md`.
- **Migrations:** `supabase/migrations/*.sql` — each filename + header comment is the authoritative record of what changed.
- **Active and queued sprints:** [PRODUCT.md § Roadmap](../../PRODUCT.md#roadmap).
