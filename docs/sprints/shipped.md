# Shipped Sprints

Compact reverse-chronological index of every shipped sprint. Per-sprint retrospectives with full narrative + lessons live in `memory/project_*.md` (one file per sprint). Migrations live in `supabase/migrations/*.sql` — each filename + header comment documents what changed.

Moved out of PRODUCT.md as part of the May 2026 doc cleanup PR (when PRODUCT.md crossed the 120KB tripwire). The pointer remains at PRODUCT.md § Where shipped sprint history lives.

| Date | Sprint | Landmark |
|---|---|---|
| 2026-05-13 | **/green/[id] waiting-for-next-roast view (Sub Pages 6.3)** | Third sub-sprint of the Sub Pages 6 roasting-side rebuild. `/green/[id]` is now **state-driven** — `computeLifecycleState(bean)` (the 6.2 helper) dispatches to one of N page shapes at the same URL. 6.3 ships the **waiting-for-next-roast** shape via a new `WaitingForNextRoastView` component inside `app/(app)/green/[id]/page.tsx`; lots in other states fall through to a preserved `LegacyDetailRender` (the pre-6.3 single-shape page, copied verbatim) until 6.4 + 6.5 add the cupping + resolved shapes. **New shape, top to bottom:** Hero (sage `bg-latent-accent-light` tile matching the /green index "active" state) + 🌱 emoji (Tabler icon swap still deferred — no icon library installed) → `ROASTS · V_n` SectionCard with Primary Question prose + Roast Hypothesis sub-block. **Transposed batch table** (rows = attributes / cols = batches): Drop temp / Peak inlet / Expected FC / Expected Total / End Condition (mono) / Predicted Agtron WB / Hypothesis (`rationale` field, not `notes`). Auto-hides any row where ALL recipes have the field NULL — table tracks what's actually being designed rather than a sea of em-dashes. Empty-state explainer prompts `push_roast_recipe` from claude.ai when 0 recipes link to the experiment. **Drop Rules card** renders only when ≥1 recipe has `drop_rule_if_fast` or `drop_rule_if_slow`; 2 rows × N batches inside a `bg-latent-highlight` (pale green) surface. **GREEN BEAN INFO card** (trimmed 6-field grid + producer's tasting notes prose). **ROAST LOG** preserves the 9-col table from the legacy render + adds current-experiment row highlighting: rows whose `batch_id` matches a parsed token from `latestExp.batch_ids` get `class="highlight"` row tint + a `●` marker after the batch number. `parseBatchIdsForHighlight(raw)` mirrors the lifecycle-state helper's batch_ids parsing inline (rule-of-2 not yet hit; extract if 6.4 needs the same parse). **Additional Information** `<details>` collapsed block with a placeholder note that deep surface (cupping history / all experiments / roast learnings / related brews) renders on the resolved-lot shape in 6.5. **Helpers**: `formatVLabel(experimentId)` extracts the trailing V-segment ("COS-HIG-BOR-2026-V3" → "V3") via `/[vV](\d+)(?!.*[vV]\d)/` with fallback to the full label; useful when experiment_id has a V-suffix convention. **Today's data shape** (load-bearing): 0 of 129 backfilled recipes link to any experiment (backfill set `experiment_id = NULL` per scope doc § 7 "no aggressive dedupe"), so the transposed table renders the empty-state explainer for all 4 current waiting-for-next-roast lots until claude.ai pushes recipes via the new MCP Tool surface. Page is built for the new workflow — it'll light up the moment recipes start landing. **Verified end-to-end** on all 4 lot patterns: (1) Higuito V3 (TBD batch_ids, V3 designed but no roasts logged → empty-state explainer + 0 highlights), (2) Gesha Clouds V2 (synthesized, batch_ids "170, 171, 172" → 3 of 6 roast log rows highlighted with `●` markers + correct row tint), (3) Rancho Tio (no experiments + 1 roast = pre-framework legacy edge case → section title drops V-label "ROASTS" + "design the first experiment" fallback), (4) Sudan Rume Hybrid Washed (resolved → falls through cleanly to `LegacyDetailRender` preserving GREEN BEAN DETAILS / BEST ROAST / EXPERIMENTS / ROAST LEARNINGS / CUPPING HISTORY). **Scope doc § 5.5 amber chrome deferred to 6.4** — Drop Rules card uses existing `latent-highlight` (pale green) token; amber promotion waits for sibling uses (varying-cell highlight + "+5s overran" delta callouts in 6.4's Roast Actuals card) to accumulate to rule-of-3 token-add justification. Mid-implementation gotcha I avoided: the existing legacy render is 270+ lines and tempting to refactor; instead preserved verbatim as a fallback function so the dispatch is a 5-line change at the top of the page component (rather than a whole-file rewrite with all 3 shapes inline today). The /simplify pass extracting shared primitives (Hero, GreenBeanInfoCard, RoastLogTable) waits until 6.4 + 6.5 land and the rule-of-3 trigger fires. `npx tsc --noEmit` + `npm run build` both pass; build emits all expected routes. |
| 2026-05-13 | **/green index lifecycle sections (Sub Pages 6.2)** | Second sub-sprint of the Sub Pages 6 roasting-side rebuild. Replaces the flat green-bean card grid with 3 lifecycle-state-grouped sections (Waiting for next roast / Waiting for next cupping / Resolved). **State derivation lives in `lib/lifecycle-state.ts`** — `computeLifecycleState(bean)` returns one of 4 states (`in_inventory` / `waiting_for_next_roast` / `waiting_for_next_cupping` / `resolved`) computed on read from the joined experiment + roast + cupping + roast_learnings shape. Resolution order codifies scope-doc § 8 verbatim with two edge cases the doc doesn't name explicitly: (1) pre-framework legacy lots with roasts but no experiments route to `waiting_for_next_roast` (Rancho Tio today — 1 roast, 0 experiments); (2) synthesis-done-but-V_(n+1)-not-designed also routes to `waiting_for_next_roast` per the loop framing in § 3 ("the user moves from waiting-for-next-roast → waiting-for-next-cupping → back to waiting-for-next-roast for V_(n+1)"). **Cupping gate is the scope-doc disjunction** — either `cupping_count == 0` OR `winner == null` routes to `waiting_for_next_cupping` (so Red Plum / Fazenda / Bukure surface there even though their latest experiments have `winner` populated — the page tells the user "go log the cuppings" not "design V2"). Two display helpers ship alongside: `lifecycleSectionTitle(state)` (sentence case per § 5.1) + `lifecycleStageLabel(state, referenceBatchLabel?)` (right-column status indicator: "Next roast" / "Next cupping" / "Batch #N" or "Reference"). **Index rewrite**: flat sectioned list (no `<SectionCard>` chrome — index is intentionally airier than the prior card grid). Each section: title + right-aligned column header ("Stage" for active / "Reference" for resolved) + `border-b`-separated rows. Each row: 12x12 color tile + lot name + metadata line (`origin · variety · process` with empties filtered) + right-aligned stage label. **Tile color signals lifecycle state** — `bg-latent-accent-light` (sage) for active lots, `bg-latent-fg` (near-black) for resolved (hue shift = green coffee → roasted coffee, fits design-conventions "hue-not-lightness" rule; both are existing chrome tokens, no new tokens added). Resolved rows surface `Batch #${best_batch_id}` with `^Batch\s*#?` regex strip so legacy values like `"Batch 139"` render as `Batch #139` not double-prefixed. In-inventory lots are computed but intentionally not rendered on the index (waiting for the eventual inventory page per § 5.1 punt). **All 13 live lots route correctly**: 4 waiting-for-next-roast (Rancho Tio / Higuito / Gesha Clouds / Sudan Rume Natural) + 3 waiting-for-next-cupping (Red Plum / Bukure / Fazenda Um) + 6 resolved (Sudan Rume Hybrid Washed #133 / Mandela XO #139 / Surma #25 / Oma #52 / Libertad #94 / El Socorro #88). Tabler `ti-leaf` icon swap **deferred to 6.3** — not load-bearing for the index (no leaf icon in the index mockup), lands with the detail-page hero rewrite where the emoji is currently most visible. Empty state preserved through 6.6 — still links to `/add?type=self-roasted` (deprecation lands in 6.6). Empty state CTA inline-commented as deprecation-pending so the next session reading the file doesn't fix-and-ship the link removal. Verified end-to-end via preview server: desktop (all 13 lots in correct sections, tile colors correct, Batch # labels correct) + mobile 375px (truncation clean, no horizontal scroll). `npx tsc --noEmit` + `npm run build` both pass; build emits all expected static + dynamic routes including `/green`. One TS hiccup mid-implementation: `[...Set<string>]` spread requires higher TS target than the repo configures; switched to `Array.from(out)` (same pattern as existing helpers in the repo). |
| 2026-05-13 | **Roasting redesign schema + MCP Tool surface (Sub Pages 6.1)** | First sub-sprint of the 6-part Sub Pages 6 roasting-side rebuild (scope doc: [docs/roasting/redesign.md](../roasting/redesign.md), 530 lines, Chris-authored hand-off). Phase 1 of the doc's 3-phase migration plan — purely additive schema + MCP Tool surface, no page changes, no /add deprecation. **Migration 052** created `roast_recipes` table (~30 fields with curves / drop rules / design-time predictions / Roest linkage / `rationale` for per-batch Hypothesis prose + `notes` free catch-all; RLS + 3 indexes), added `roasts.recipe_id` FK + index, added 16 cross-batch fields to `experiments` (`updated_cup_prediction_a/b/c/d` + `taste_for_a/b/c/d` + `delta_from_roast_a/b/c/d` + `delta_from_cup_a/b/c/d` per scope doc § 4.2 four-write-moments rationale), added `roast_learnings.best_roast_id` typed UUID FK + index. **Backfill** (all clean): 129 roast_recipes rows 1:1 from existing roasts (one per execution, no replication relationships expressed yet per scope doc § 7 "no aggressive dedupe"), 129 `roasts.recipe_id` linked via copied `created_at`, 6 `roast_learnings.best_roast_id` resolved via `regexp_replace + JOIN ON batch_id` from legacy text values ("133" / "Batch 139" / "25" / "52" / "94" / "88"), 2 `is_reference=true` drift flips on Mandela XO #139 + Surma #25 (Chris's Sub Pages 6 round-1 bundle opt-in). All smoke counts match expectations. **MCP Tool surface 32→34**: 2 new (`push_roast_recipe` + `patch_roast_recipe`) with full ~30-field Zod schemas, V-set UPSERT key on (experiment_id, batch_slot) when both supplied otherwise (green_bean_id, recipe_name) for one-off recipes, 4-part description convention compliant (verb cluster / WHEN / PAYLOAD / RETURNS). 6 existing Tools extended: push/patch_experiment got 16 new optional fields with per-temporal-moment describe() text; push/patch_roast got `recipe_id` FK; push/patch_roast_learnings got `best_roast_id` FK (`best_batch_id` text stays through Phase 3 for back-compat). **Resource surface**: new `roast_recipes://by-experiment/{experiment_id}` template (returns `{ experiment_id, recipes[] }` ordered by `batch_slot`); existing `roasts://by-bean/{green_bean_id}` Resource now joins `roast_recipes[]` so claude.ai reads design intent + as-recorded execution in one fetch. **Types** (hand-maintained in `lib/types.ts`, no CLI regen): new `RoastRecipe` interface, extended `Roast`/`Experiment`/`RoastLearning` with new fields, new `InsertRoastRecipe` type. **Doc updates**: copied 530-line scope doc into `docs/roasting/redesign.md` + registered in `lib/mcp/docs.ts`; CLAUDE.md § Data Model added `roast_recipes` entity bullet + 3 relationship-pattern lines + Green section lifecycle-state derivation rules + 4→13 lot count refresh; PRODUCT.md § Side Quests Sub Pages 6 converted from single-entry placeholder to 6.1-6.6 sequenced series with per-sub-sprint scope; ROASTING.md gained "Schema model" pointer to the redesign doc. **Pre-flight DB findings confirmed** during planning: 13 lots (not 4 — CLAUDE.md was stale), 6 closed / 7 active per derived state, all 6 `best_batch_id` text values resolve cleanly via regex (no orphans). **NOT in 6.1**: page changes (6.2-6.5), /add deprecation (6.6), prompt updates in `docs/prompts/*.md` (6.6 cleanup), Phase 3 legacy `expected_outcomes` / `observed_outcome_*` cleanup. |
| 2026-05-12 | **Sprint 3.1 — Architectural-queue brainstorm** | Plan-mode session producing the next-iteration sprint queue. Same shape as Sprint 2.2's V2-architecture brainstorm: 3 AskUserQuestion rounds, 11 locked decisions, ~30 architectural-queue items categorized across 4 input sources (`feedback_v2_mcp_feedback_log.md` Batches 1-20 + Sprint 2.7.5 retro + Phase 3 PR #105 deviations + Sprint 3.0 OAuth retro). **Output:** 6-sprint build queue 3.2-3.7 — Cleanup-A bundle (code + migration, ~12 items, ships next) → Auto-supersede paired sprint (#R88 + #R89 + tool-list cache visibility, ~2-3h) → Per-batch failure_boundary breach record (plan-mode + impl, #R67 + #R68) → Roest API parity Phase 3 (7 pull-side schema gaps including R65 HIGH-IMPACT timezone-drift bug) → BREWING/ROASTING doc reconciliation (subprocess spec / STAGE 8 routing / Active Lots sub-anchoring / one-shot framing / schema-describe micro-fixes) → Prompt v5 rewrites (9 incremental-sync-v4 + new-bean-intake updates). **2 scoping docs land** in `docs/features/`: `roest-api-parity-phase-3.md` (Sprint 3.5) + `experiments-breach-record.md` (Sprint 3.4 placeholder, awaits plan-mode session). **3 promotions out of debt**: defensive try/catch + console.error sweep → Cleanup-A; MCP tool-list cache visibility → Auto-supersede; Active Lots sub-anchoring → BREWING/ROASTING fixes. **R65 timezone-drift bug rides along in Roest parity** — not carved out as hotfix; symmetric with the other 6 pull-schema items. **`/green/[id]` design refresh stays Chris-owned** (Side Quests, not queued, mockup-then-scope pattern). **Parked / triggered**: recipe_variant canonical promotion (5+ values across 3+ beans), parse_cupping_transcript voice-memo pipeline, push_cupping body-length error instrumentation, #R38 dual-namespace axis confusion, #R46 tool_search ranking. **PRODUCT.md § Active Sprints** renumbered: 3.1 entry replaced by 3.2-3.7 (items 1-6); General cleanup sprint (was #2) → #7; Per-entity directed synthesis (was #4) → #8. No code, no migrations, no schema changes — all implementation lives in the future sub-sprints. Retro: `memory/project_v3_1_brainstorm.md`. |
| 2026-05-12 | **Sprint 2.7.5 — Workflow walkthrough + Roest API write dogfood** | 7-round dogfood loop across 12 PRs (#107 / #109 / #110 / #119 / #130 / #132 / #134 / #137 / #138 / #139 / #143 / #144). Bundled the Roest API write integration build (Phase 1 + 2: `push_roast_profile` / `push_inventory` / `patch_inventory`; Tool count 29 → 32) with iterative friction polish across the full roasting workflow — green bean intake → profile design → roast → cup → sync → forward design → close-out — exercised end-to-end on Higuito + REDPLUM and other lots. **Schema additions:** migration 044 on `roasts` (roest_notes / end_condition_type / end_condition_target / fc_total_cracks + worth_repeating type promotion boolean → text tristate) + migration 050 on `experiments` (additional_notes / open_questions / key_insight_confidence). **MCP-surface improvements:** `updated_fields` echo symmetry sweep across all 5 patch_* Tools (Round-5) + `canonical_values` echo on patch_experiment / patch_roast / patch_inventory for enum-validated fields (Round-7) + composite_key echo on push_cupping (Round-4) + `canonicals_updated_at` actually-writes fix in patchGreenBean (Round-7) + `hopper_load_temp: null` correction in pull_roest_log (different signal from Roest's preheat_temperature, Round-5) + corrected `mapEndConditionEnum` decoder (BEAN_TEMP=4 not 1; latent Sprint-2.5 bug surfaced via Roest UI screenshot in Round-2). **Prompt evolution:** new-bean-intake.md (tools list + routing + V1-spread default), in-process-bean-incremental-sync.md (FIRST-OF-LIKELY-TWO recipe_variant rule + operator-override check + FC Floor & Ceiling per-lot append routing + additional_notes framing + STAGE 8 forward design + cross-references), closed-bean-full-fill.md (symmetric updates), new `design-next-experiment-set.md` for V_N → V_{N+1} forward design with V1-brackets / V2-narrows / V3-confirms discipline codified. **Two standing rules extracted:** `feedback_external_drift_ok_latent_canonical_required.md` (Roest can drift; Latent DB must resolve canonical) + `feedback_migration_drift_pattern.md` (recurring class: migrations land in `supabase/migrations/` via PR but don't auto-run in Supabase; diagnostic SQL pattern + idempotent fix recipe; confirmed twice in this sprint). **Continuous-feedback channel** opens at `memory/feedback_mcp_continuous_log.md` for post-sprint friction polish + drift control. **Outstanding follow-ups (11 items)** categorized in retro: cleanup-sprint bundle (5), plan-mode sprint (1, breach record), triggered-by-evidence (3), fold-into-doc-touch (1), separate sprint (1, /green/[id] design refresh). See `memory/project_v2_7_5_workflow_walkthrough.md` for full retro + lessons. |
| 2026-05-11 | **/roasters detail rethink (Sub Pages 5)** | Re-ordered around three jobs in priority order: (1) brew-guide / typical brew style for a random coffee from this roaster, (2) general roasting + resting info, (3) recall a specific coffee. Surfaced **24 previously-invisible `RoasterEntry` fields** that flowed only into the synthesis prompt before — roastStyle / developmentBias / restCurve / primaryBrewer / filterType / extractionIntent / brewAdjustmentMethod / overExtractionTolerance / processSensitivity / failureMode / Baseline Recipe (composed inline from tempC + ratio + typicalBrewTime + agitationLevel) now render on the page UI in parallel with the prompt anchor. 9-section layout top-to-bottom: Hero (family swatch + name + family/coffee-count/location; strategy-tag pill + URL link dropped) → Brewing Philosophy (Brew Guide as anchor or "No official brew guide" + House Style + Brewing Intent) → Roasting Philosophy → Generalized Brewing Recipe (2-col grid w/ 6 fields + 3 full-width below) → Resting Info → Coffees I Have Brewed From This Roaster → **What I've Learned (synthesis card deliberately demoted** below the coffees list per Chris's three-jobs framing — synthesis is nice-to-have for roasters vs front-and-center on terroir/cultivar/processes) → Additional Information (`<CollapsibleBlock>` w/ 5 sub-blocks: Roaster Metadata label-prose rows for leftover fields including strategyTag / primaryDriver / brewGuideSource+Type / doseG / waterG / calibrationRole / confidenceLevel / bmrHouseStyle when distinct from houseStyle / bmrNotes; Common Flavor Notes via `<FlavorNotesByFamily bare>`; Cultivars / Terroirs / Processes Explored via `<TagLinkList bare>`) → Confidence (HIGH ≥5 / MEDIUM ≥2 / LOW = 1). New local `<LabelledField label value>` helper for inline `**label:** value` rows (mirror of cultivar's `<GridField>` pattern, not extracted — rule-of-one). **No adapter change, no schema change, no migration** — the rich registry was already populated; only the page UI was missing. Sparse-registry roasters degrade gracefully via auto-hiding empty sections — Latent (3-field entry) renders Brewing Philosophy w/ houseStyle row only, Roasting Philosophy + Generalized Recipe + Resting Info sections all hide. Pre-flight DB query: 24 roasters / 79 brews; top 5 = 66% of brews; 11 of 24 single-brew tail; 6 of 24 have cached synthesis (all current, no regen wave). Per-roaster archive page directed-prompt enhancement stays as a separate Longer Term Items entry — Sub Pages 5 surfaced the rich-field substrate the synthesis prompt already used; the adapter retune is a future sprint. Verified end-to-end on Shoebox (mockup match) + Hydrangea (HIGH, fully populated) + Latent (sparse, 3 sections auto-hide) + Bean & Bean (1 brew, LOW). Mobile 375px: 2-col grid stacks, CollapsibleBlock renders `<details>` collapsed, no horizontal scroll. `npx tsc --noEmit` + `npm run build` both pass; console clean. |
| 2026-05-11 | **/processes first-principles regrouping (Sub Pages 4)** | Replaced the flat text-keyed `/processes/{slug}` aggregation (28 distinct values, 19 of them 1-brew thin pages out of 79 brews total — 68% of the surface) with a 3-tier composable architecture grounded in the existing `base_process` + modifier-axis registry. Subsumed the deferred 1e.4 facet redesign in the same sprint. **Index restructured** into 3 navigation surfaces: Core Process Portals (4 base hubs sorted by brew count desc — Washed 39 / Natural 32 / Honey 8; Wet-hulled hidden until first brew lands), Modifier Index (8 entries: Anaerobic 16 / Yeast Inoculated 9 / Dark Room Dried 6 / Cold Fermentation 4 / Raised Bed 4 / Slow Dry 4 / Double Anaerobic 3 / Thermal Shock 3 — driven by `eligibleModifierIndexEntries(brews)` so the list grows as new modifiers cross the threshold), Signature Methods (Moonshadow / Hybrid Washed / TyOxidator). **Sub-page eligibility** codified per Chris's brainstorm Rules 1-3: Rule 1 (≥3 brews) earns `/processes/{base}/modifiers/{combo}`; Rule 2 (signature exemption) always earns `/processes/signatures/{name}` even at 1 brew; Rule 3 (Honey color tier structural exemption) earns `/processes/honey/{subprocess}`. ~21 page surfaces total. **5 new page kinds**: index · base hub (Hero / Process Summary / Observed Variants & Modifiers chip clusters / Brew Archetypes 5-field grid / synthesis / coffees list / Additional Information CollapsibleBlock / Confidence) · Honey subprocess sub-page (Process Breakdown chip rows / synthesis / coffees / cross-link sections) · per-base modifier-combo mini-page (mockup 2 shape) · cross-base Modifier Index page (Modifier Overview prose / By Base 4-col grid / Common Stacks list / synthesis / coffees / Additional Information) · signature method page (Process Overview prose / Process Breakdown / Observed Cup Profile bullets / synthesis ≥2 brews / coffees). **New lib infrastructure**: `lib/process-routing.ts` (deterministic kebab-case slug helpers + reverse parsers + `SUB_PAGE_THRESHOLD = 3` constant + URL builders for cross-link convenience) · `lib/process-aggregation.ts` (single source of truth for grouping brews by aggregation kind — `aggregateBaseHub` returns pure/modified split + modifierCombos[] + honeySubprocesses[] + signatures[]; `aggregateModifierIndex` returns all + byBase + commonStacks[]) · `lib/process-confidence.ts` (Rule 5 confidence labels). **Display dedupe** in new `composeProcessDisplay` helper: strips leading `"Anaerobic "` from drying modifiers when fermentation already contains Anaerobic — fixes "Anaerobic Anaerobic Slow Dry Raised Bed Natural" render bug. Modifiers joined with " + " separator; comma before base. Same dedupe applied in `structuredLabel` (aggregation chip labels). **Schema**: migration 051 created `process_aggregation_syntheses` table keyed on `(user_id, aggregation_kind, aggregation_key)` with CHECK constraint on `aggregation_kind IN ('base', 'honey_subprocess', 'modifier_combo', 'modifier_index', 'signature')`; legacy `process_syntheses` dropped (3 rows lost, regen on visit). **Adapter dispatch**: `lib/synthesis/adapters/process.ts` rewritten as `getProcessAdapter(kind)` factory exporting 5 per-kind adapters; each shapes its anchor differently (base reads `BaseProcessEntry.summary + brewArchetype`; modifier_index reads `ModifierEntry.overview + byBase breakdown`; signature reads `SignatureEntry.overview + cupProfile + producer/country/protocol facts`); 9-tier weighting + 5-step outputFormat + formatLearningRow preserved verbatim across all kinds. API route accepts `{kind, key}` body. **Registry extension**: `lib/process-registry.ts` adds `BaseProcessEntry` (4 entries with `summary` + 5-field `brewArchetype`) + `ModifierEntry` (8 entries with `overview` for the cross-base index pages) + `SignatureEntry.overview` + `SignatureEntry.observedCupProfile`; all fields optional with empty-state messaging until Phase A content authoring lands. **Cross-link migration**: 3 sites (cultivars/[id] / terroirs/[id] / roasters/[slug] "PROCESSES EXPLORED" TagLinkList) updated to link to `/processes/{baseSlug}` and dedupe by `base_process` instead of composed `process` text. **Tier B content scope** (~37 authored cells) shipped with empty-state placeholders pending Chris's Phase A authoring. **/simplify pass** extracted 3 shared component primitives: `<ProcessBreakdownRow>` (3 inline copies → import) · `<ProcessConfidenceCard>` (6 inline copies → import) · `<ProcessCoffeesList>` (6 inline copies → import). Verified end-to-end via preview server: /processes index 3 sections + live counts · /processes/washed hub all 8 sections · /processes/washed/modifiers/double-anaerobic-thermal-shock-yeast-inoculated mockup 2 verbatim · /processes/modifiers/anaerobic cross-base aggregation 16 brews/3 bases · /processes/honey/white 3 brews · /processes/signatures/moonshadow 1 brew · /processes/wet-hulled returns 404 · Anaerobic+Slow Dry+Raised Bed Natural display dedupe ✓ · mobile 375px responsive ✓ · npx tsc --noEmit + npm run build both pass. |
| 2026-05-11 | **/cultivars first-principles regrouping (Sub Pages 3)** | Scope expanded mid-kickoff from a Light terroir-shape port to a full data-model inversion when Chris's first-principles question surfaced that `mergeLineageCharacteristics`'s first-non-null + array-union across sibling cultivars was producing semantically garbled aggregate pages: Bourbon Mutation Lineage's "Acidity Style" was whichever of Bourbon Aji / Caturra / Laurina / Pacas / Purple Caturra / Villa Sarchi came first alphabetically with the field populated, even though those mutations have totally different cup behavior. Lineage deleted as a destination; cultivar is the only leaf. **Index restructured** Species (header) → Family (header w/ swatch + visible-lineage count) → Lineage (sub-header, non-link) → Cultivar (linked row w/ brew count). 0-brew cultivars hidden — 30 → 28 visible (Bourbon + Wush Wush filtered). Family cascades hide when all its lineages hide; lineage cascades hide when all its leaves hide. **Detail page rewritten** around Chris's three-act mental model (General Cultivar Info → What I've Learned → My Experiences) — 9 sections: Hero (cultivar_name h1 + species→family→lineage breadcrumb subtitle, sibling Tags row dropped) → Genetic Background (prose) → Cultivar Context (2-col grid, 4 fields: Typical Origins / Altitude Sensitivity / Limiting Factors / Market Context) → Brewing & Cup Profile (2-col grid, 8 fields: Common Processing Methods / Typical Flavor Notes / Acidity Style / Body Style / Aromatics / Terroir Transparency / Extraction Sensitivity / Brewing Tendencies) → Roasting Characteristics (2-col grid, 4 fields: Roast Tolerance / Roast Behavior / Resting Behavior / Common Pitfalls) → What I've Learned About This Cultivar (synthesis, **conditional on brewList.length >= 2** — 18/30 cultivars hide it) → Coffees I Have Brewed In This Cultivar (filtered by `cultivar_id` not `.in(siblings)`) → Additional Information (CollapsibleBlock w/ bare-prop FlavorNotesByFamily + 3 TagLinkLists for terroirs / processes / roasters) → Confidence. Local `<GridField label value>` helper renders each grid cell (label above value). **Mokka + Mandela rich content backfilled** from `docs/taxonomies/varieties.md` so no thin pages ship — only 2/30 cultivars were skeletons and both had ≥1 brew. **29 stale lineage-aggregated synthesis rows invalidated** to NULL via `UPDATE cultivars SET synthesis = NULL, synthesis_brew_count = NULL WHERE synthesis IS NOT NULL` — re-generation per-cultivar on first ≥2-brew visit via SynthesisCard's brewCount-changed trigger. Synthesis route caller now sends `[singleId]` instead of lineage siblings; `entityName` = `primary.cultivar_name`. Cultivar adapter dropped the dead `cultivarNamesInLineage` field. Cross-link audit clean — 4 sites (brews / terroirs / processes / roasters detail) already pass `cultivar.id` correctly; click "Laurina" on a brew page now lands on Laurina (semantically correct), not Bourbon Mutation Lineage. No new design tokens; reused canonical primitives (SectionCard / CollapsibleBlock / FlavorNotesByFamily bare / TagLinkList bare). Verified end-to-end on Laurina (1 brew, synthesis hidden) + Sidra (5 brews, synthesis renders) + Mokka (1 brew with rich backfilled content rendering across all 4 structured grids) + /cultivars index (28 visible, 0-brew filter working) + mobile 375px (grids collapse to 1-col, CollapsibleBlock collapsed-by-default). |
| 2026-05-10 | **/terroirs/[id] detail page rethink (Sub Pages 2)** | Reordered the terroir detail surface around Chris's three jobs: (1) quick reference about the region, (2) aggregation of what I've learned, (3) coffees list. New section order: Hero (unchanged) → `HIGH LEVEL SUMMARY` (NEW SectionCard, promoted `context` field) → MESO TERROIRS EXPLORED → TERROIR CONTEXT (now 3 fields: soil / cup_profile / why_it_stands_out) → TERROIR CHARACTER → TYPICAL PRODUCTION → WHAT I'VE LEARNED ABOUT THIS TERROIR (synthesis, the load-bearing aggregation surface) → COFFEES BREWED FROM THIS REGION (promoted up from position-bottom; label renamed from "COFFEES FROM {MACRO}") → ADDITIONAL INFORMATION (`<CollapsibleBlock>` mobile-collapsed wrapper containing Flavor Notes by Family + Cultivars + Processes + Roasters) → CONFIDENCE. Two adjacent fixes bundled: cultivar tag labels simplified from `${lineage} / ${name}` → just `${name}` ("Rosado" / "Gesha" / "Sudan Rume" / "Sidra" / "Pink Bourbon" / "Purple Caturra" / "Aruzi"); /terroirs index now hides macro_terroir groups with 0 brews (Mantiqueira Highlands + Central Plateau Highlands gone, 22 → 20 visible macros). New shared primitive: `bare`-prop on `<FlavorNotesByFamily>` + `<TagLinkList>` strips the SectionCard wrapper for nesting inside CollapsibleBlock — forward investment for Sub Pages 3-6. No directed-prompt adapter changes (existing terroir synthesis already produces the 4-6 paragraph + 4-6 practical-takeaway shape). No new design tokens. Pre-flight DB query confirmed: 36 terroirs, 22 fully described (all 9 description fields populate together), Huila Highlands `context` DB value matches mockup byte-for-byte, 2 zero-brew macro groups confirmed. Verified end-to-end on Huila Highlands (mockup match) + Northern Andean Highlands (skeleton row surfacing fully-described sibling content via macro-group aggregation) + /terroirs index + mobile 375px disclosure expand. |
| 2026-05-09 | **/brews/[id] detail page rethink** | Re-ordered the primary brew surface around the user job ("about to brew/serve, what do I do?") per Chris's design doc + mockup. New section order: Header → Reference Brew Recipe (page anchor with table + Bloom + executable pour bullets + Drawdown + Strategy/Modifier pills + Modifier Detail) → Presentation Overview → Peak Expression (dark) → Coffee Overview (4 leaf-level tags with breadcrumbs) → What I Learned (dark, promoted up the page) → Full Brew Notes (renamed from "Additional Information"; mobile-collapsed via pure-CSS `<details>` dual-tree). New `lib/pour-structure.ts` parser with composable strategy cascade (newline / semicolon / middle-dot / period+marker / arrow-chain) — handles Pepe Jijón "Pour 1: ... → ...g. Pour 2: ..." + Apricoast semicolon + Ngoma newline + Picolot/Loud Giants arrow-chain shapes. ParsedPourStep carries optional structured fields (label / time / amount_g / method) as forward investment for a future `brews.pours` jsonb migration — render layer stays the same. New components: `<RecipeTable>` (6-row 2-col anchor), `<PourStructureList>` (parsed bullets + drawdown filtered out so it surfaces in its own footer), `<CollapsibleBlock>` (pure-CSS responsive disclosure, no client component). New `splitModifierLabel(m)` helper in `lib/extraction-modifiers.ts` splits the em-dash so type/form renders as a pill while detail renders separately. Orphan field policy: render conditionally in Full Brew Notes if non-empty (strategy_notes + cooling_curve_target + terroir_connection + cultivar_connection + classification); `roast_connection` dropped from the read view (0/79 brews populate it). View + edit stay split. Brew count corrected 55 → 79 in CLAUDE.md drift. Verified across 4 brews (Pepe Jijón mockup match, Loud Giants Picolot arrow-chain → 7 bullets, CGLE Mandela XO single-pour, Gesha Village Surma self-roasted with all orphan fields populated). |
| 2026-05-09 | **WBC roasting + sourcing reference docs** | PR #128. One-off doc sprint, not from PRODUCT.md Active Sprint queue. Two new open-ideas docs in `docs/roasting/`: `wbc-roasting.md` (6 generalizable lessons from the ~100-row WBC competitor archive 2022-2025 + 4 Roest L200 hypotheses + top-level Blending experiments section with 3-batch dev ladder protocol gated by Roest `quantity_g` inventory check + top-level Rest as a structured variable section with 4-portion rest-curve protocol that fits onto existing cupping schema with no new entity + Tier 1/2/3 experiment queue) and `wbc-sourcing.md` (sourcing strategy by producer / origin / variety / process / elevation + 4-lane portfolio model + Tier 1/2/3 priority targets + Latent inventory mapped to tiers scoreboard snapshot 2026-05-09 + Latent's actual current palate vs the WBC sweet spot section reframing Tier 1 as roasting-skill upkeep rather than urgent buy given Chris's VERY-expressive + Suppression preference). Parallel to existing brewing-side WBC reference layer (`docs/brewing/wbc-reference.md` + `docs/brewing/wbc-recipes.md`) but content shape differs - sparse roasting CSV means hypothesis / experiment doc, not recipe lookup; useful competitor citations folded inline rather than maintained as a separate competitor archive. Both docs registered in `lib/mcp/docs.ts` for `read_doc` access. ROASTING.md gained 3 cross-refs (top brewing-side cross-link, New Coffee Onboarding Step 1 - Intake, Lot Knowledge intro). CLAUDE.md Documentation Index `docs/roasting/` entry expanded to list all three roasting docs. Persistent palate refinement saved to `user_taste_evolution.md` § Palate refinement (2026-05-09). |
| 2026-05-08 | **Per-entity directed synthesis** | New `lib/synthesis/` scaffolding (`types.ts` / `buildPrompt.ts` / `runSynthesis.ts` / `humanizer.ts` + vendored `humanizer-skill.md` from blader/humanizer MIT) + 4 per-entity adapters under `lib/synthesis/adapters/` (terroir / cultivar / process / roaster). Each adapter ports Chris's authored prompt verbatim (4-6 paragraphs + 4-6 practical-takeaway bullets, weighted-input table from his sample-output files, registry anchor) so a 24-brew Volcán Barú synthesis pulls on terroir-specific climate / processing / variety context instead of repeating "taste the coffee cooler." 4 synthesize routes refactored from ~120 → ~50 lines each, calling shared `runSynthesis(adapter, entity, brews)` orchestrator. Two-call pipeline: Sonnet generates raw → Sonnet humanizer pass strips AI-tells (em-dash overuse / inflated significance / rule-of-three / promotional language / etc.) before save to cache. Domain vocabulary preserved verbatim via system-prompt override (Suppression / Clarity-First / SWORKS / Picolot / etc.). Cache write surface unchanged: `terroirs.synthesis` + `cultivars.synthesis` + `process_syntheses` + `roaster_syntheses` (all already TEXT, no migration). New `<SynthesisRenderer>` parses `\n\n` paragraphs + `* ` bullet lists without a markdown lib. /simplify pass extracted shared `<SynthesisCard>` (rule-of-4 across the 4 entity wrappers) and deleted `TerroirSynthesis.tsx` + `CultivarSynthesis.tsx` + `ProcessSynthesis.tsx` + `RoasterSynthesis.tsx`. `next.config.js` `outputFileTracingIncludes` extended to bundle `humanizer-skill.md` for the 4 synthesize routes. |
| 2026-05-08 | **Track 2 — Data sanity audit** | New `scripts/data-sanity-audit.ts` (12 audit dimensions: orphan rows / childless pairs / process+grind drift between legacy text and structured columns / canonical drift on text-only columns cross-referenced against `taxonomy_overrides_queue` / Phase 3 queue health + provenance markers / roast→cupping pair completeness / modifier shape via `cleanModifiers` / flavor + structure_tags via `cleanFlavors` + `cleanStructureTags` / SR-brews-need-green_bean_id / doc_proposals staleness / synthesis cache staleness across `terroirs.synthesis_brew_count` + `cultivars.synthesis_brew_count` + `process_syntheses` + `roaster_syntheses`). Run via `npm run audit:data` (one-shot, requires `SUPABASE_SERVICE_ROLE_KEY` in `.env.local`). Dry-run report committed at `docs/audits/track-2-2026-05-08.md` — 159 findings: 11 Bucket A (process recompose ×9, provenance flag flip ×1, Rwanda admin_region orphan ×1), 64 Bucket B (orphan terroirs ×2, producer drift ×5, stale-cupping roasts ×57 most likely C after row-by-row triage), 50 info (mid-iteration roasts <7d ×18, cache staleness ×32). Substrate gap surfaced: `SUPABASE_SERVICE_ROLE_KEY` not in either local `.env.local` so v1 dry-run ran via Supabase MCP `execute_sql` — script is canonical for repeat runs once Chris adds the key. Bucket A fixes split to follow-up PR per round-1 decision. Track 2 of the 3-track General cleanup sprint (Track 1 shipped 2026-05-07 PR #119; Track 3 bundled with Claude-Design redesign). |
| 2026-05-08 | **WBC corpus active consultation (Brewing v8.5)** | PR #117. Section 4 of BREWING.md reframed from "reference-only, not consulted during a normal Coffee Brief" to actively-pulled during Step 1d (new "WBC corpus + cross-cutting control patterns check" Named Consideration alongside Cooling-Curve Design). MODIFIER_TYPES grows 3 → 4 — `role_based_pulse` promoted from the wbc-reference.md "consciously not pursuing" list to a 4th canonical Axis 2 modifier (free-text `roles` sub-field) for percolation-only per-pour role discipline. OUTPUT_SELECTION_FORMS grows 3 → 4 — `dilution` form added with optional `dilution_g` sub-field on `OutputSelectionModifier` for post-brew water addition. Brewer rotation discipline gains 3 WBC anchor cross-refs (Switch ↔ Ferket Selective Bloom; Orea v4 + Sibarist FAST + Melodrip ↔ Wölfl Extraction Push; SWORKS ↔ Carlos Medina Suppression analog). docs/brewing/wbc-reference.md gains 7 new sections per Chris's critical review: Counting Note (family vs axis), Cross-Cutting Control Patterns (Time Distribution playbook / Water Strength / Agitation Taper / Filter Behavior / Pre-Brew Conditioning / Output Selection extension), 3-tier Practical Experiment Queue, Competition-to-Home Translation Rules. Cross-cutting calibration axes deliberately stay in the doc layer rather than promoting to canonical modifiers — "keep compact strategy set, add separate doc layer" framing. Read-side dog-food validated same-day on a Newbery Khun Lao brief (claude.ai pulled wbc-recipes.md + wbc-reference.md by name, Andreas Harestad 2025 fingerprint match identified, Water Strength surfaced as cross-cutting load-bearing axis, RBP fired with agitation-taper framing). |
| 2026-05-08 | **ROASTING.md Lot Knowledge reorg + Active Lots refresh** | PR #116. 5-tier restructure of ROASTING.md's lot knowledge surface (Active Lots / Reference Roasts+Brews / Cross-Coffee Insights / Open Questions / Archive) replacing the prior scattered "How This Project Is Structured" + "Active Lots" + "One-Shot Calibrations" + "Recently Closed Lots" + "Cross-Coffee Insight Layer" + "Reference Brew Recipes by Lot" + "Per-lot generalized learnings" sections. Active Lots refresh bundled in same PR — currency check on the 5 listed lots, mid-iteration lots added. |
| 2026-05-08 | **MCP Tool descriptions audit** | PR #114 + spot-check follow-up PR #115. Description-only audit of all 32 MCP Tools against the post-#111 doc-description pattern (when / payload / returns). 22 Tools at 3/3 kept as-is; 10 Tools at 2/3 (missing explicit Returns shape) gained a `Returns { ... }` sentence — full patch_* family (6 Tools) plus push_roast_learnings, resolve_queue_entry, list_doc_sections, read_doc, read_doc_section, read_canonical, propose_doc_changes, propose_canonical_addition. Stretch goal: DESCRIPTION CONVENTION block added to top of `lib/mcp/discoverability-check.ts` (next to the existing ACTION_SYNONYMS lint guard) so future Tool authors hit the 4-part pattern. PR #115 follow-up added `fix` / `mark` / `correct` to the patch_brew + patch_roast verb clusters in ACTION_SYNONYMS after a spot-check turned up missing entries. Zero behavior or schema changes. `npm run check:mcp` 32/32 + `npx tsc --noEmit` clean. |
| 2026-05-08 | **Registry-md sync script — drift guard for 10 canonical taxonomies** | PR #112. New `scripts/check-registry-sync.ts` — for each of the 10 canonical taxonomy axes (cultivars / terroirs / processes / roasters / producers / brewers / filters / flavors / roast-levels / grinders), parse the authored `docs/taxonomies/*.md` headers + the validation-mirror `lib/*-registry.ts` canonical array and fail when they diverge. Permanent low-maintenance guard for the "2-step deliberate edit" pattern that was previously only enforced socially. Wired into `npm run check:registry-sync` so the existing CI surface picks it up without a workflow edit. |
| 2026-05-07 | **May 2026 doc cleanup PR** | Track 1 of the 3-track General cleanup sprint. Walks the full root-doc surface: PRODUCT.md restructure (7-section roadmap collapses 4 prior sections + relocates "What's Missing"), CLAUDE.md (Documentation Index + Sprint cadence 4→6 items), README.md refresh, SYNC_V2.md restructure (~555→~250 lines), ARBITER.md drift fixes, BREWING.md (April Brewer registry fix in 3 files), ROASTING.md (13 edits incl. new Roast Philosophy paragraph), 3 prompts updated, lib/mcp/docs.ts adds descriptions + registers docs/roasting/archive.md, migration 046 (cuppings.sweetness + cuppings.temperature_behavior), SYNC.md + design-brief.md deleted. |
| 2026-05-06 | **Roest API write integration Phase 1+2 (bundled)** | PR #110. 3 new MCP Tools: `push_roast_profile` + `push_inventory` + `patch_inventory`. Tool count 29 → 32. Phase 2 promoted into the same sprint as Phase 1 — bidirectional inventory sync matters for the new-bean onboarding loop now that writes flow both directions. Substrate: scope=read+write + authedWrite + getRoestCustomerInfo + 3-read retrofit. Empirical end_condition wins over OpenAPI (BEAN_TEMP=1 not 4). |
| 2026-05-06 | **Brewing v8.4 — Hybrid promotion + WBC docs refresh** | PR #108. 6th strategy promoted (Hybrid as extraction structure vs the 5 intensity strategies). 102-recipe WBC archive (`docs/brewing/wbc-recipes.md`) ships alongside refreshed `docs/brewing/wbc-reference.md`. 3 brew reclassifications from Balanced+Immersion → Hybrid (Sequential). Cooling-Curve Design as new Step 1d named consideration with `cooling_curve_target` free-text column. |
| 2026-05-05 | **Phase 3 — taxonomy_overrides_queue + provenance + 3 MCP Tools** | PRs #103 (design) + #104 (doc proposals batch) + #105 (impl). Migration 045 adds `taxonomy_overrides_queue` (single shape across 7 axes; EXCLUDE constraint on `(user_id, axis, lower(raw_value)) WHERE status='pending'` auto-collapses duplicates) + `terroir_provenance` / `cultivar_provenance` enum on `green_beans` + `brews` + `green_beans.canonicals_updated_at`. 3 new Tools (`list_taxonomy_queue` + `propose_canonical_addition` + `resolve_queue_entry`) lift count 26 → 29. ARBITER.md gained "Taxonomy queue arbitration" section + T1-T6 procedure. |
| 2026-05-05 | **Phase 2 — substrate gaps from 16 roasting + 5 brewing dog-food sessions** | PR #101 + follow-up #102 (cross-system audit findings post-#101 merge: `lib/types.ts` + `/add` page drift caught only because Chris asked — origin of `feedback_cross_system_audit.md` standing rule). |
| 2026-05-05 | **Phase 1 — prompts foundation cleanup** | PR #100. Port 7 prompts to `docs/prompts/`, MCP-served via `lib/mcp/docs.ts` `DOC_FILES` + `listDocs()`. Single source of truth that auto-updates on every Vercel deploy, replacing manual paste-into-claude.ai-project-files cycle. Cluster F prompt-v5 rewrites + Cluster A namespace updates included. |
| 2026-05-04 | **Iteration round-2 fixes + doc proposals batches** | PRs #95 / #96 / #97 / #98 / #99. Rwanda Northern Province / Central Plateau Highlands terroir registry add (#99); BREWING + ROASTING canonicals reframe (#96 + #97); doc proposals batch from May 4 brewing dog-food (#98); BREWING.md `canonicals://` Tool fallback note for clients that don't enumerate URI templates (#95). |
| 2026-05-03 | **V2 Sprint 3.0.5 — promote brews Resources to Tools** | PR #94. `brews://recent` + `brews://by-id` Resources promoted to `list_recent_brews` + `get_brew` Tools (architectural rule: claude.ai surfaces Tools to the model, Resources only as catalog). Tool count 24 → 26. |
| 2026-05-03 | **V2 Sprint 3.0 — OAuth 2.1 + PKCE for claude.ai web MCP** | PR #93. Discovery endpoints (`/.well-known/oauth-protected-resource` + `/.well-known/oauth-authorization-server`), `/api/mcp/authorize`, `/api/mcp/token`, WWW-Authenticate header on `/api/mcp` 401. Migration 043 for auth-code storage. Static client credentials via Vercel env. claude.ai web becomes the main brewing surface post-3.0 + post-2.7. |
| 2026-05-03 | **V2 Sprint 2.7 — Brewing prompt v9 + canonicalization + MCP-as-source-of-truth flip** | PR #92 — last build sprint in V2 queue. Bidirectional canonicalization: BREWING.md +173 lines, ROASTING.md -3 lines. Two new subdocs: `docs/brewing/wbc-reference.md` + `docs/roasting/archive.md`. After deploy + MCP registration, claude.ai retires 12 uploaded files (-14,614 lines from project context). Em-dash sweep (103 → 0 in BREWING) shipped same PR. |
| 2026-05-02 | **Post-2.6 cleanup mini-session** | PRs #87 (cherry-pick of #86) + #88 (registry edits) + #89 (recompose_process flag) + #90 (migration 042: terroirs UNIQUE includes meso_terroir). 17 patches + 8 orphan deletions via Sprint 2.6 patch_* Tools. |
| 2026-05-02 | **V2 Sprint 2.6 PR2 — 6 patch_* Tools** | PR #87 (cherry-pick of #86). 6 new Tools: `patch_brew` / `patch_green_bean` / `patch_roast` / `patch_cupping` / `patch_experiment` / `patch_roast_learnings`. Tool count 18 → 24. `patchBrew` helper extracted from `app/api/brews/[id]/route.ts` as single source of truth. |
| 2026-05-02 | **V2 Sprint 2.6 PR1 — strict find-or-create on FK tables** | PR #85. Closes 4 silent-failure modes on terroir / cultivar find-or-create (R7 / R8 / R12 / R23) via strict-canonical fail-fast + meso in match key + (country, macro) pair validation. |
| 2026-05-01 | **Roasting MCP feedback batches 1-9** | PRs #75 - #84. Tool discoverability sweep + workflow context + push_green_bean UPSERT + push_roast UPSERT + brews[] in get_bean_pipeline + push_cupping UPSERT + Tolima registry gap + push_cupping recipe_variant + get_bean_pipeline desc surfaces patch_cupping. PR #84 doc proposals batch from May 1-2 roasting dog-food. |
| 2026-04-30 | **V2 Sprint 2.5 — Roasting MCP tools + ROASTING.md** | PR #73 + follow-up #74. 7 new Tools (5 push + 2 Roest pull). ROASTING.md ~770-line verbatim port. Migration 039 (14 cols on roasts + cuppings + experiments + green_beans). Roest API integration via `/o/token/`. CGLE Mandela XO end-to-end dog-food. 2 substrate gaps fixed in #74 (numeric weights + preflight). |
| 2026-04-29 | **V2 Sprint 2.4 — propose_doc_changes + arbiter playbook** | PRs #66 + #67 + #68 + feedback batches #69 / #70 / #71. Polymorphic `propose_doc_changes` Tool + `doc_proposals` queue (migration 037) + ARBITER.md + per-citation `target_doc` + section-anchor reads + 4 doc-introspection + 2 canonical-introspection Tools. First prose round-trip = Dongzhe 5th brew. |
| 2026-04-28 | **V2 Sprint 2.3 — MCP scaffold + push_brew + read Resources** | First V2 build sprint — sync infrastructure live. `@modelcontextprotocol/sdk` 1.29.0 + `WebStandardStreamableHTTPServerTransport` on Vercel Node serverless. Bearer auth via `api_keys` table + migration 036. 12 canonical Resources at `canonicals://{axis}` + `brews://recent` + `brews://by-id/{uuid}` + `docs://brewing.md`. `push_brew` Tool wrapping `persistBrew`. `SUPABASE_SERVICE_ROLE_KEY` substrate gap closed mid-sprint. |
| 2026-04-28 | **V2 architecture brainstorm (Sprint 2.2)** | Architecture doc shipped at SYNC_V2.md. Five locked decisions: MCP transport / API key auth / bidirectional with Resources / repo-file source-of-truth + DB proposal queue / asymmetric write trust. Build queue locked. |
| 2026-04-27 | **V1-brews dog-food (Sprint 2.1)** | First end-to-end SYNC pipeline run. Picolot Emerald PL#015 (Mokka Natural) + Janson Green-Tip Gesha Natural Anaerobic 1010 landed. Mokka cultivar instantiated. `immersion` shipped as 4th canonical Modifier type (later absorbed into Hybrid in v8.4). |
| 2026-04-27 | **Extraction Strategy v2** | Migration 034. `EXTRACTION_STRATEGIES` extended 3 → 5 (Suppression + Extraction Push promoted) + new `brews.modifiers jsonb` array (3 canonical types: output_selection / inverted_temperature_staging / aroma_capture). Hue-separated 5-pill palette. ModifierComposer + ModifierBadges. BREWING.md gains SECTION 4 = WBC Reference. **8-registry taxonomy batch complete** post this sprint. |
| 2026-04-26 | **Brewer + Filter taxonomy adoption (sprint 1f)** | Migration 032. Two new registries from one CSV pair: 46 brewers (12 owned, 24 aliases) + 64 filters (22 owned post-dedupe, 34 aliases). Schema Option A. Material axis dropped. Orea v3/v4 ambiguity defaults to v4. xBloom net-new dripper. "Espro Bloom" relabel → xBloom Premium Paper Filters. |
| 2026-04-26 | **Producer taxonomy adoption (sprint 1l)** | Migration 031. 49-name flat → 120-entry rich registry. Schema Option A. allowOverride pattern. 6 collapses. /simplify rule-of-three hit on `canonicalizeOverridable` PATCH helper. |
| 2026-04-25 | **Grinder taxonomy adoption + V2 rework (sprint 1k)** | Migrations 029 + 030. V2 same-session rework: single EG-1 with burrs/burrShape/burrSize fields; setting axis flipped to enumerated strict (51 valid values 3.0-8.0 in 0.1 steps; 16 with rich content). New `components/GrindSettingInput.tsx`. |
| 2026-04-24 | **Roast level taxonomy + roaster /add enforcement (1m + 1h.2)** | Migration 028. 8 Agtron-anchored canonical buckets + 22 aliases. New `canonicalize()` method on CanonicalLookup fixes case-drift. CanonicalTextInput gains `allowOverride` + "Use anyway" link. |
| 2026-04-24 | **Roaster taxonomy structural port (1h.1)** | PR #52. Migration 027. 70 canonical roasters across 6 families (added SYSTEM as 6th). 4 strategy reclassifications (Rose/Noma/Picky Chemist → SYSTEM, TM Coffee → CLARITY-FIRST). |
| 2026-04-24 | **/add SR canonical rebuild (1i)** | SR steps 7+8 rebuilt to canonical pickers; `handleSaveSelfRoasted` routes through findOrCreateTerroir + findOrCreateCultivar via Promise.all. |
| 2026-04-23 | **Process enforcement (1e.3)** | PR #46. ProcessPicker (shared builder) + `/add` purchased + `/add` SR + `/edit` + persistBrew + PATCH route + save-gate. |
| 2026-04-23 | **Process schema migration + decomposition (1e.2)** | Migrations 025 + 026 (post-ship correction). 8 structured columns added to brews + 55-row backfill. |
| 2026-04-23 | **Process structural port (1e.1)** | First Phylum A2 port. Composable taxonomy. `lib/process-families.ts` → `lib/process-registry.ts`. composeProcess / decomposeProcess + LEGACY_DECOMPOSITIONS for 20 DB values. Zero DB/UI change. |
| 2026-04-22 | **Region enforcement (1d.3)** | CanonicalTextInput on `/add` + `/brews/[id]/edit` bound to `TERROIR_MACRO_LOOKUP`. `findOrCreateCultivar` + `findOrCreateTerroir` extracted to `lib/brew-import.ts`. Admin_region auto-populates from registry. |
| 2026-04-22 | **Region content backfill (1d.2)** | Migration 024. 22 UPDATEs populating 11 content columns from regions.md. climate_stress → free-text; typical_processing Title Case. Overwrites migration 009. |
| 2026-04-22 | **Region taxonomy adoption (1d.1)** | Migration 023. 121 canonical macros across 38 countries + rich `lib/terroir-registry.ts`. Meso/micro demoted to free-text. Dual-registry drift killed. 11 DB-row renames preserving 55 brew FKs. |
| 2026-04-22 | **Variety enforcement (1b)** | CanonicalTextInput on `/add` purchased review + `/brews/[id]/edit`. Edit picker flips from 26-DB-rows to 63-canonical registry with lazy find-or-create; aliases resolve on save; `isResolvable` added to `makeCanonicalLookup`. |
| 2026-04-22 | **Variety content backfill (1a.2)** | PR #36 + migration 022. 26 existing DB cultivar rows populated with 18 attribute fields from varieties.md. |
| 2026-04-22 | **Variety taxonomy adoption (1a.1)** | PR #34 + migration 021. `docs/taxonomies/varieties.md` + rich `lib/cultivar-registry.ts` (63 canonicals / 20 lineages / 48 aliases). Dual-registry drift killed. Mokka pre-req for SYNC V1 resolved. |
| 2026-04-22 | **Producer + roaster canonicalization** | PR #23 + migration 018. Mirrors migration 013's flavor-notes pattern. Audit found 2 drifted producer clusters + 5 verbose strings. New `lib/producer-registry.ts` + `lib/canonical-registry.ts` factory. New `components/CanonicalTextInput.tsx`. |
| 2026-04-21 | **SYNC.md playbook (V1-brews step c)** | `SYNC.md` at repo root — validator-table-driven. Deprecated 2026-05-07 in May doc cleanup PR (V2 MCP path is canonical). |
| 2026-04-21 | **Terroir + cultivar canonical registries (V1-brews step b)** | PR #30 — `lib/terroir-registry.ts` + `lib/cultivar-registry.ts` + factory alias tier. |
| 2026-04-21 | **BMR v7.1 → BREWING.md (V1-brews step a)** | PR #27 — `BREWING.md` at repo root. |
| 2026-04-21 | **Reference Roasts + Roasting Guide — brainstorm + feature doc** | `docs/features/reference-roast-and-guide.md`. |
| 2026-04-21 | **Mobile polish on /brews, /brews/[id], filter bar** | — |
| 2026-04-21 | **Cross-dim filters on /brews** | — |
| 2026-04-20 | **Self-roasted flow audit + /add hardening** | PR #25 + migration 020. |
| 2026-04-20 | **Green detail render polish** | — |
| 2026-04-20 | **Data-depth backfill (terroirs + cultivars)** | Migrations 015 / 016 / 017. |
| 2026-04-19 | **Experiments import + render polish** | Migration 019. |
| 2026-04-19 | **Design-system standardization** | — |
| 2026-04-19 | **Cross-link backfill across aggregation detail pages** | — |
| 2026-04-18 | **Roasters aggregation** | Migration 014. |
| 2026-04-17 | **Design polish + mobile pass** | PR #14. |
| 2026-04-17 | **Flavor canonicalization + edit UI** | Migration 013. |
| 2026-04-16 | **Processes aggregation** | Migration 012. |
| 2026-04-16 | **Producer column + backfill** | PR #12 + migration 011. |
| 2026-04-16 | **Extraction strategy sprint** | PRs #10 / #11. |
| 2026-04-15 | **Terroir macro redesign** | PR #5. |
| 2026-04-14 | **Cultivar lineage redesign** | PR #4. |
| (earlier) | **Purchased-coffee import flow, build hygiene, initial schema + backfills** | PR #9 + migrations 001-010. |

---

## See also

- **Per-sprint retros:** `memory/project_*.md` — full narrative + lessons per sprint, indexed in `memory/MEMORY.md`.
- **Migrations:** `supabase/migrations/*.sql` — each filename + header comment is the authoritative record of what changed.
- **Active and queued sprints:** [PRODUCT.md § Roadmap](../../PRODUCT.md#roadmap).
