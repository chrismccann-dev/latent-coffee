# Completion report — Roasting-prompt hygiene batch (10 items, one PR)

**PR:** https://github.com/chrismccann-dev/latent-coffee/pull/579
**Merge SHA (main):** `21a40b9`
**Kickoff brief:** [roasting-prompt-hygiene-batch-kickoff.md](docs/sprints/roasting-prompt-hygiene-batch-kickoff.md) (2026-07-15 plan-feedback pass)
**Shipped ledger row:** [docs/sprints/shipped.md](docs/sprints/shipped.md) 2026-07-15.

## 1. Plan restated

Ten small prompt gaps from the backlog's `## Open — prompt / doc-touch` section, each individually too small for a sprint, landed as one PR of twelve edits across `docs/prompts/{log-roast,log-cupping,close-lot,one-shot-closeout,start-lot,bundled-brewing-completion}.md` plus the cross-reference surfaces the #42 reorder touched. Scope out: #58 / Path C-2 (owned by the lifecycle/SPG grilling session), MCP/schema code, anything needing a fresh operator decision beyond the flagged recommended defaults (#42 reorder-vs-4.5, #47 rule line, #45a coordination).

## 2. Per-item recap (item → file → anchor), including divergences

| Item | Landed at | Divergence / note |
|---|---|---|
| **#40** drop rules REQUIRED | `start-lot.md` STAGE 3 (b) drop-rules bullet + `log-cupping.md` STAGE 6 write (b) | **Retargeted.** Filed as "log-roast/start-lot STAGE 6"; log-roast.md has no recipe-design stage — recipe design lives at start-lot STAGE 3(b) (V1) and log-cupping STAGE 6(b) (V_(n+1)). Rule as specified: REQUIRED per slot when the experiment varies `end_condition_target`, varies a lever with per-slot pre-FC/stall divergence, or leaves slots at different fast/slow risk; optional only when every slot shares one end condition and one risk profile. |
| **#42** STAGE ordering | `close-lot.md`: STAGE 3 = "Link (or push) the optimized brew", STAGE 4 = "Write the per-lot roast learnings row" + ordering-rationale block under the STAGE 3 header | Plain reorder (the recommended default). No downstream dependency broke: the learnings write needs only STAGES 1-2 outputs; the brew link needs only STAGES 1-2. No STAGE 4.5 fallback needed. Cross-refs renumbered: `bundled-brewing-completion.md` (4 spots), `coordinator/operator-guide.md`, `coordinator/handoff-rules.md`, `cupping-specialist/cluster/pod-1-routing.md` (2), `CONTEXT-roasting.md` (Link entry + Scope tags entry), `docs/adr/0009` / `0012` / `0019`. Left alone deliberately: `wbc-roasting-archivist/cluster/wbc-roasting.md:297`'s "close-lot.md STAGE 4" pointer (it references the rest-curve/learnings home, which IS STAGE 4 post-reorder). |
| **#53** resume preamble | `close-lot.md` + `one-shot-closeout.md`, top of STAGE 5 | "Resuming a partially-completed close-out": STAGE 1's pipeline read shows roast_learnings existence (patch, don't re-push) + `is_reference` / `optimized_brew_id` state; check `list_doc_proposals(status: "pending", target_doc)` before re-issuing, skip citations whose `(target_doc, section_anchor)` is already pending. |
| **#43a** auto-split granularity | `close-lot.md` STAGE 5 auto-split paragraph | **Code-verified answer** (read `lib/mcp/propose-doc-changes.ts`): the server neither splits nor bundles — the "auto-split" is entirely a client-side prompt rule; every call queues independently, and auto-supersede matches per citation `(target_doc, section_anchor)` via the `@>` contains-filter. Documented rule: when the split fires, go fully granular — one call per `(target_doc, section_anchor)` pair globally, no within-target_doc re-bundling. |
| **#43b** soft-retire region | `close-lot.md` STAGE 5 Active Lots bullet | Operator call recorded: empty-replace = the h1 status-block region ONLY (prose between the `#` h1 and the first `##`); H2 sections preserved as historical/cross-ref anchors. |
| **#43c** drop attribution | `close-lot.md` STAGE 2 new bullet | Confirm attribution when `end_condition_type: "manual"` pairs with a drop temp meaningfully under the designed bean_temp trigger (Batch 194 provisional case); correct via `patch_roast` at close-out. |
| **#44** clarity bundle (5) | `log-cupping.md`: STAGE 5 Path A ✓ check-line; STAGE 4.4 Medium-High worked example (N≥3 within-lot OR N≥2 cross-lot + falsifier); STAGE 3 cooling_arc_pattern per-value examples + flat-vs-hold discriminator; STAGE 4.5 gate re-entry candidate-flag re-assess; STAGE 5 Path B control-experiment-as-single-variable-sweep | Ride-along: `log-roast.md` STAGE 5's stale ladder pointer fixed (`log-cupping.md STAGE 3` → `STAGE 4.4`, with the CONTEXT-roasting canonical home named). |
| **#41** seed-then-bump | `log-cupping.md` STAGE 7 cross-coffee-pattern bullet | The backlog filed it against "STAGE 6"; the doc-proposal stage is STAGE 7 in the current numbering (the label predates the prompt rewrite). Rule: prefer REPLACE on the seeded Low-confidence entry (bump marker, drop hedges, add resolution evidence) — one pattern, one entry. |
| **#45a** SPG note home | `log-cupping.md` STAGE 5 Path C-2 re-entry paragraph | SPG free-text note goes in the cupping's `overall`, prefixed `SPG: brewed alongside <finalist sibling roast_id> at <SPG recipe>`. Verified against `lib/mcp/push-cupping.ts`: cuppings has NO `additional_notes` column (the old prose pointed at a phantom field); also dropped the stale "until the `eval_method` migration lands" clause — it landed. **Coordination flag honored:** the lifecycle/SPG grill had not ruled at land time; if it re-homes the note, its call wins over this edit. |
| **#45b** append-new-H2 | `close-lot.md` STAGE 5 submit paragraph + `bundled-brewing-completion.md` STEP 2 | Append ops accept new `##`/`###` headers in `proposed_text`; they land at the end of the anchored section and read as new sections positioned relative to subsequent section boundaries. |
| **#47** CCIL N=2 rule | `one-shot-closeout.md` STAGE 5 CCIL bullet | Codified the prior operator lock: a hypothesis replicated across two one-shots (N=2 cross-lot) stays BELOW the CCIL bar — hold in `general_takeaway` with scope_tags; promote at N=3, or earlier only when the ladder genuinely reads Medium-High (which N=2 cross-lot reaches only WITH a tested falsifier). Flagged in the PR body for veto. No CONTEXT-roasting glossary entry added — judged a threshold clarification of the existing Key-insight confidence ladder entry, not new vocabulary. |
| **#50** net-new roaster card | `bundled-brewing-completion.md` STEP 2, roaster/ target bullet | Append after the LAST existing card section (or the file-level h1 anchor) with the full new `## <Roaster>` header + card body in `proposed_text`; explicitly warns that targeting the roaster's own non-existent anchor yields unresolved-anchor + a wrong-roaster closest_match. |
| **#10** anchor confidence | `start-lot.md` STAGE 2: frame table `context` row + V1 width rule | Retargeted from the retired `new-bean-intake.md` per the backlog note. The frame's `context` now states the anchor + confidence explicitly; the width rule gains the overlap-scaled ladder (same cultivar+process+altitude = High / one axis = Medium / none = Low, driving spread width). |

## 3. Verification results (actual outputs)

- `npm run check:doc-sizes` — **all Tier-1 surfaces within cap.** Prompt tier after the batch: `log-cupping.md` 29.6 KB / 40 (was ~24 pre-batch — the #44 examples are the bulk); `close-lot.md` **35.5 KB / 40 ⚠️ warning band** (was 33.5 — the reorder rationale + #53/#43a/b/c additions); `one-shot-closeout.md` 28.3; `log-roast.md` 29.3; `start-lot.md` 17.1; `bundled-brewing-completion.md` 16.6.
- `npm run check:doc-links` — **0 live misses** (437 files scanned; 104 archive warnings, non-gating by design). Note: the gate was RED on main before this PR — 5 pre-existing file-relative `../sprints/` links in `docs/product/feedback-backlog.md` (from the 2026-07-15 plan-feedback pass, PR #577) were failing; fixed to root-relative in the same PR.
- **Six-actor trace:** Actor 2 (prompts) = the whole sprint. Actor 5 (Claude Code docs) = CONTEXT-roasting.md stage-number pointer updates only (2 entries); no new vocabulary introduced, no glossary addition (see #47 note above). Actors 3/4/6 **explicitly skipped**: no Tool schema or description change, no new `docs://` Resource (no `lib/mcp/docs.ts` / `check:mcp-bundle` run needed — no new registered file), no schema/migration/UI change. Actor 1: the prompts render to the operator via repo reads / `read_doc`; no app surface changed.
- Roadmap currency: this sprint shipped from the feedback backlog (not the roadmap Active queue); backlog flips + shipped.md row landed in PR #579 per the lifecycle rule.

## 4. Deferred / surprising / new

- **close-lot.md is now the closest 40KB-cap watcher (35.5/40).** The next close-lot-touching sprint should budget a Pattern J prune (candidates: the duplicated push_brew fallback field spec shared with `bundled-brewing-completion.md`, which the fallback path already points at).
- **New drift observed, needs routing (route-feedback candidate):** `log-cupping.md` STAGE 3's rest-days-drift bullets instruct prefixing `additional_notes` on the cupping row (`REST_DAYS_DRIFT:` / `REST_DAYS_DRIFT_COMPARISON:`) — but the cuppings table has no `additional_notes` column (same phantom-field class as #58 facet 2 / #45a). Left untouched here because Path C-2 / cupping-schema reconciliation belongs to the lifecycle/SPG grilling session; flagging so it gets folded into that pass.
- **Convention divergence noted, not changed:** `one-shot-closeout.md` STAGE 5 still instructs a hard empty-replace of the closed lot's active-lot block, while `close-lot.md` carries the soft-retire convention (#43b clarified the latter). Close-lot's own text says hard-blank is acceptable when no cross-refs exist — usually true for one-shots — so this may be intentional; worth one line of operator confirmation at the next grill (queued observation, not shipped).
- **#45a stands unless the grill re-homes the SPG note** — revert is a single-paragraph edit in `log-cupping.md` Path C-2 if the lifecycle/SPG session picks a different home (e.g. adds `cuppings.additional_notes`).
- Nothing else deferred; all twelve edits landed.
