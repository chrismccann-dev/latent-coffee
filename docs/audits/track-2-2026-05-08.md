# Data Sanity Audit — 2026-05-08

Track 2 of the General cleanup sprint. 12 dimensions audited. **159 findings across 7 dirty dimensions.**

Generated by `scripts/data-sanity-audit.ts`. Run via `npm run audit:data` (one-shot, requires `.env.local` with `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`).

> **Note:** This v1 dry-run was generated by running each audit dimension as parallel SQL via the Supabase MCP because the local `.env.local` lacks `SUPABASE_SERVICE_ROLE_KEY`. The script at `scripts/data-sanity-audit.ts` is the canonical artifact; runs reproducibly once Chris adds the service-role key locally (or Vercel env-pulls it). All findings below are real production state at audit time.

## Triage legend

- **Severity A** — inline-fixable. Single-row UPDATE or short series; bundle into a follow-up PR with each fix on its own commit.
- **Severity B** — needs its own mini-sprint. Multi-row data correction, schema drift requiring migration, or interpretive judgment.
- **Severity C** — accept-as-known-state. Should round-trip into `KNOWN_STATE` in `scripts/data-sanity-audit.ts`.
- **Severity info** — observational only, not drift (e.g. cache staleness, queued legitimate overrides).

## Bucket totals

| Severity | Count | Where |
| --- | --- | --- |
| A | 11 | dim 3 process recompose (9) · dim 6 provenance (1) · Rwanda admin_region drift (1) |
| B | 64 | dim 1 orphan terroirs (2) · dim 5 producer drift (5) · dim 7 stale roasts >30d (57) — most likely C after round 2 |
| C | 0 | (round 2 will reclassify dim 7 stale-but-known) |
| info | 50 | dim 7 mid-iteration <7d (18) · dim 12 cache staleness (32) |

---

## 1. Orphan rows

> Scanned 60 terroirs, 23 cultivars, 4 green_beans, 18 experiments. terroirs/cultivars orphan = 0 brews + 0 green_beans referencing.

**Status:** 2 findings — A=0 B=2 C=0 info=0.

| Severity | Row | Detail |
| --- | --- | --- |
| B | terroirs/389e497b | Rwanda · Lake Kivu Highlands · meso="Nova Washing Station catchment" — 0 brews + 0 green_beans reference. Candidate for delete. |
| B | terroirs/25ff55cc | Rwanda · Central Plateau Highlands · meso="Gicumbi / Bukure" admin_region="Southern Province" — 0 brews + 0 green_beans reference. AND collides with terroirs/cee51c04 which has the same meso under "Northern Province" (correct location of Bukure). Candidate for delete; the cee51c04 row is the canonical one. |

## 2. Childless pairs

> Scanned cuppings against roasts. SR-brew-without-green_bean_id is checked separately in dimension 10.

**Status:** 0 findings — clean.

## 3. Process drift (legacy text vs structured)

> Scanned 79 brews. Compares `brews.process` (legacy denormalized text, kept through 1e.4) against `composeProcess(structured-cols)`.

**Status:** 9 findings — A=9 B=0 C=0 info=0.

| Severity | Row | Detail |
| --- | --- | --- |
| A | brews/5b6530c0 · Hydrangea Coffee — Letty Bermudez | Legacy="Double Fermentation Thermal Shock" → composed="Double Anaerobic Thermal Shock Yeast Inoculated Washed". |
| A | brews/950728e4 · Oma Coffee Roaster — Sidra Cold Fermented Washed - Finca Soledad Sidra - Pepe Jijon | Legacy="Cold Fermented Washed" → composed="Cold Fermentation Washed". |
| A | brews/a881c09f · Hydrangea Coffee — El Paraiso Lychee | Legacy="Double Anaerobic Thermal Shock" → composed="Double Anaerobic Thermal Shock Yeast Inoculated Washed". |
| A | brews/ba2caa10 · Leaves Coffee — Panama Elida - Dark Room Dry Natural Gesha | Legacy="Dark Room Dry Natural" → composed="Dark Room Dried Natural". |
| A | brews/d14fc3e9 · Luminous Coffee — Colombia Sidra Bourbon - Washed Sakura Co-Ferment | Legacy="Washed Sakura Co-ferment" → composed="Floral Co-ferment Washed". |
| A | brews/fd346045 · Scenery Coffee — Anoxic Natural, Rosado | Legacy="Anoxic Natural" → composed="Anaerobic Natural". |
| A | brews/7b54b706 · Moonwake Coffee Roasters — Lamastus Family El Burro Lot #16 ASD Natural Gesha | Legacy="ASD Natural" → composed="Anaerobic Slow Dry Natural". |
| A | brews/7aef16d6 · Moonwake Coffee Roasters — Catimor Yeast Anerobic Natural - Project One | Legacy="Yeast Anaerobic Natural" → composed="Anaerobic Yeast Inoculated Natural". |
| A | brews/1e84af99 · Strait Coffee — Panama Finca Deborah Echo Gesha (Washed + Cascara Infused) | Legacy="Washed Cascara Infused" → composed="Cascara Infusion Washed". |

**Fix path:** all 9 are `UPDATE brews SET process = composeProcess(structured-cols) WHERE id = …`. The structured columns are authoritative post-1e.2; the legacy text just needs recompose. Sprint 1e.4 was deferred and will eventually drop the column entirely; in the meantime this UPDATE pass keeps the denormalized display in sync.

## 4. Grind drift (legacy text vs structured)

> Scanned 79 brews. Compares `brews.grind` against `composeGrind(grinder + grind_setting)`.

**Status:** 0 findings — clean.

## 5. Canonical drift on text-only columns

> Scanned 79 brews × 6 canonical axes (roaster / producer / brewer / filter / grinder / roast_level). Cross-references `taxonomy_overrides_queue` by (axis, raw_value, source_id) to label queued-override (info) vs silent-drift (B).

**Status:** 5 findings — A=0 B=5 C=0 info=0.

`taxonomy_overrides_queue` was empty at audit time (0 rows total), so every non-resolvable value is silent drift — Bucket B regardless of axis.

| Severity | Row | Detail |
| --- | --- | --- |
| B | producer="Jannette & Kai Janson" (×1 brew) | Non-resolvable in PRODUCER_LOOKUP. Likely needs a new canonical entry OR alias to "Jannette & Kai Janson (Janson Farms)" (which is itself non-resolvable — see next). |
| B | producer="Jannette & Kai Janson (Janson Farms)" (×1 brew) | Non-resolvable. **Note:** `check:registry-sync` reports this name IS in `docs/taxonomies/producers.md` but missing from `lib/producer-registry.ts` (pre-existing registry-md drift). The .ts mirror is the lookup's source of truth, hence the audit's "non-resolvable" verdict. Resolution requires a single edit to add the entry to `PRODUCERS` in `lib/producer-registry.ts` matching the .md content. |
| B | producer="Justin Boudeman / Longboard Coffee" (×1 brew) | Non-resolvable. Add as new canonical (per CLAUDE.md "net-new producers more common than net-new cultivars" framing). |
| B | producer="Pocho Gallardo" (×1 brew) | Non-resolvable. Add as new canonical. |
| B | producer="Rumu Damo Washing Station" (×1 brew) | Non-resolvable. Add as new canonical (washing station entry). |

All 6 brewer / 12 filter / 1 grinder / 4 roast_level / 24 roaster distinct values resolve cleanly through their respective `*_LOOKUP`. All 27 distinct structure_tag values resolve cleanly through STRUCTURE_LOOKUP.

**Fix path:** mini-sprint to add the 5 producer entries to `lib/producer-registry.ts` + `docs/taxonomies/producers.md` (2-step deliberate edit per registry adoption history) OR alias them to existing canonicals if they're drift renames. Then run `audit:data` again to confirm clean.

## 6. Phase 3 queue health

> Scanned 0 taxonomy_overrides_queue rows + 4 green_beans for provenance markers. Stale-pending threshold = 7 days.

**Status:** 1 finding — A=1 B=0 C=0 info=0.

| Severity | Row | Detail |
| --- | --- | --- |
| A | green_beans/be477009 | El Paraiso Red Plum Castillo (REDPLUM-CAS-2026): `terroir_provenance='auto_created'`, `canonicals_updated_at` IS NULL. Confirm whether `terroir_id` resolves to a canonical row — if so, flip flag to `canonical` + bump `canonicals_updated_at`. Per memory rule (`feedback_external_drift_ok_latent_canonical_required.md`): Latent-side `auto_created` flag on a row that resolves to canonical = bug. |

## 7. Roast → cupping pair completeness

> Scanned 75 roasts × 0 cuppings (no cupping rows on bb33-user). Severity escalates by age: <7d info → 7-30d C → >30d B; is_reference=true always B.

**Status:** 75 findings — A=0 B=57 C=0 info=18.

**Round-2 candidate for known-state.** This dimension is the most volume-heavy and the most likely to be largely Bucket C. Chris's cupping cadence is "iterative until the lot resolves" — many of these batches were dialled in but not formally cupped. Roasts older than 30d with no cuppings on the user_id at all is suspicious — was the cupping data ever ported from the spreadsheet?

Bucket B candidates worth particular attention:
- 12 `is_reference=true` roasts with 0 cuppings (62 to 111 days old): these are the lots Chris designated as the canonical answer. Reference roasts without cupping data is unusual.
- 4 Sudan Rume Hybrid Washed batches at 47-50d (recent enough to remember).

Detail rows (75 total — see SQL dump in audit transcript). Summary by age band:

| Age band | Count | Severity | Notes |
| --- | --- | --- | --- |
| 1d | 3 | info | El Paraiso Red Plum Castillo — current iteration |
| 5d | 15 | info | 5 distinct lots, mid-iteration |
| 47-50d | 4 | B | All Sudan Rume Hybrid Washed |
| 55-112d | 53 | B | Spread across Gesha Village Oma, Guatemala Libertad/El Socorro, others. 12 are is_reference=true. |

**Round-2 Q for Chris:** are the 53 stale-cupping roasts a known-state (cupping cadence drift) → Bucket C? Or were cuppings entered but lost during a migration → Bucket B (data recovery)?

## 8. Modifier shape integrity (brews.modifiers jsonb)

> Scanned 79 brews. Validates via `cleanModifiers()` + post-v8.5 sub-field checks (dilution_g, roles).

**Status:** 0 findings — clean.

## 9. Flavor / structure_tags integrity

> Scanned 79 brews. Validates `brews.flavors` via `cleanFlavors()` and `brews.structure_tags` via `cleanStructureTags()` (canonical-resolvable).

**Status:** 0 findings — clean. All 47 brews with structure_tags use only the 27 canonical descriptors observed (each resolves through `STRUCTURE_LOOKUP`).

## 10. SR brews must have green_bean_id

> Scanned 79 brews for `roaster='Latent' AND green_bean_id IS NULL`. Any finding is broken state.

**Status:** 0 findings — clean.

## 11. doc_proposals queue health

> Scanned 0 doc_proposals rows; threshold is 14d for pending.

**Status:** 0 findings — clean.

## 12. Synthesis cache staleness

> Cache vs source comparison across terroirs / cultivars / process_syntheses / roaster_syntheses. Severity is info — cache staleness ≠ data drift; use to schedule a re-synthesize pass when convenient.

**Status:** 32 findings — A=0 B=0 C=0 info=32 (treated as clean for exit code).

### 12a — terroirs.synthesis_brew_count (6 mismatches)

| Row | Cached | Actual | Notes |
| --- | --- | --- | --- |
| terroirs/389f8840 — Colombia · Western Andean Cordillera · Popayán plateau | 2 | 4 | undercount — 2 new brews since last synth |
| terroirs/25baa521 — Ethiopia · Sidama Highlands · Bensa | 5 | 6 | undercount — 1 new |
| terroirs/d606b3ad — Colombia · Central Andean Cordillera · Ciudad Bolívar | 4 | 1 | overcount — likely re-FK'd brews |
| terroirs/1771a1e5 — Panama · Volcán Barú Highlands · Boquete Valley | 24 | 23 | overcount by 1 |
| terroirs/80c8ee17 — Colombia · Western Andean Cordillera · Trujillo | 4 | 2 | overcount |
| terroirs/9e3e000f — Panama · Volcán Barú Highlands · Jurutungo | 24 | 1 | **major overcount** — 24 cached but only 1 brew references this row. Synthesis likely cached at the macro level but stored on the wrong meso row. |

**Architectural note (Bucket B candidate, separate sprint):** the per-terroir-id synthesis cache vs the per-(country, macro) detail page aggregation is a substrate mismatch. Two Volcán Barú Highlands rows both report cached=24 — same synthesis ran twice, stored on different meso rows. Fix at substrate level by keying synthesis cache on (country, macro) instead of terroir_id, OR by making the detail page synthesize fresh-with-cache at the (country, macro) key.

### 12b — cultivars.synthesis_brew_count (23 mismatches)

Most are mild over/undercounts where brews shifted between cultivar rows over time (likely from sprint 1a Variety taxonomy adoption). Sample:

| Row | Cached | Actual |
| --- | --- | --- |
| cultivars/cb1e1af2 — Gesha · Gesha lineage | 22 | 34 (undercount — 12 new brews) |
| cultivars/ddfce7fc — Bourbon · Bourbon (classic) | 4 | 0 (orphan synthesis — brews re-FK'd to other Bourbon variants) |
| cultivars/87d2f24a — Sudan Rume | 6 | 3 |
| cultivars/dc73c6cd — Ethiopian landrace population | 6 | 3 |
| cultivars/e5752a92 — Mejorado | 5 | 2 |
| cultivars/8cf189aa — Pacamara | 2 | 3 |

(17 more mild mismatches — full list available in audit transcript.)

### 12c — process_syntheses (1 stale)

| Process | Cache updated | Latest brew |
| --- | --- | --- |
| Washed | 2026-04-17 | 2026-05-05 |

### 12d — roaster_syntheses (2 stale)

| Roaster | Cache updated | Latest brew |
| --- | --- | --- |
| Moonwake Coffee Roasters | 2026-04-30 | 2026-05-07 |
| Picolot (Brian Quan) | 2026-04-18 | 2026-05-07 |

**Fix path:** none required for v1 — caches refresh on next synthesis trigger. If routine drift is unacceptable, queue a "re-synthesize stale dimensions on cron" mini-sprint.

---

## Additional findings (not part of the 12 dimensions, surfaced incidentally)

| Severity | Where | Detail |
| --- | --- | --- |
| A | terroirs/25ff55cc vs cee51c04 | Two Rwanda · Central Plateau Highlands rows share the meso "Gicumbi / Bukure" but disagree on admin_region (Southern vs Northern). Bukure is in Northern Province (Rwanda); the Southern row is wrong. Already flagged in dim 1 as orphan — delete the orphan to resolve. |
| B | substrate | Synthesis cache keyed on `terroir_id` while detail page aggregates per `(country, macro)` — multiple meso-variant rows produce N copies of the same synthesis (visible in dim 12a Volcán Barú entries). Architectural sprint: re-key synthesis cache on `(country, macro)` OR have the detail page synthesize-with-cache at that key. |

---

## Triage decision matrix

**Bucket A (next PR):**
- Dim 3: 9× `UPDATE brews SET process = '<canonical>' WHERE id = …` — straightforward recompose pass.
- Dim 6: 1× green_beans provenance flip on `be477009` — confirm canonical first.
- Additional: delete the orphan Rwanda row (`25ff55cc`).

Total: 11 single-row writes. Mirrors the brief's threshold; ship as a focused PR.

**Bucket B (each its own mini-sprint):**
- Dim 1 (delete orphan terroirs): 2 rows. Could fold into the Bucket A PR since it's also single-row deletes; or queue as a separate "duplicate terroir cleanup" sprint that also addresses Volcán Barú duplication.
- Dim 5 (5 producer entries): 2-step deliberate edit per producer per CLAUDE.md. Fold into next producer-research sprint or stand-alone "producer-canonical-drift cleanup" sprint.
- Dim 7 (stale-cupping roasts): pending round-2 reclassification. Most likely Bucket C.
- Dim 12 architectural (synthesis cache key): substrate sprint. Cross-cuts the planned Track 3 page-design refresh — could bundle there.

**Bucket C (round-2 confirmation pending):**
- Dim 7 stale roasts older than 30d: ask Chris if the cupping data was never ported from the spreadsheet (Bucket B data recovery) vs. if these are intentional no-formal-cupping batches (Bucket C).
- Dim 12 cultivar Bourbon orphan synthesis (cached=4, actual=0): if this is expected aftermath of cultivar re-FKing during sprint 1a, encode as known-state.

**Bucket info (no action):**
- Dim 7 mid-iteration roasts <7d: 18 rows.
- Dim 12 cache staleness: 32 rows. Refresh on next synthesis trigger.

---

## Round-2 answers from Chris (2026-05-08)

1. **Stale-cupping roasts (53 rows >30d):** *mixed — some known-state, some missing data.* Leave as B-pending; Chris will triage row-by-row in a future cleanup window. No `KNOWN_STATE` entry encoded this round.
2. **Bourbon cultivar synthesis (cached=4, actual=0):** *investigate — may indicate a bug.* Promoted to Bucket B finding; queue a focused investigation as a side quest. The 4 brews previously linked were re-FK'd to other Bourbon variants during sprint 1a; question is whether the synthesis row should auto-clear on count→0 (substrate fix) or be deleted manually (one-shot fix).
3. **5 non-resolvable producers:** *defer triage row-by-row.* Stays as B-pending. No registry edits this PR.
4. **Synthesis cache architectural finding:** *wait for Track 3 page-design refresh* — bundle the cache-key re-architecting there.

---

## Known-state constants in effect

- **experiments_orphan_from_green_beans** — Spreadsheet-only experiments blocked on green_beans backfill (5 missing beans). Per workflow rule: event-driven uploads, no backlog sprint. (Pre-seeded in `KNOWN_STATE`; this run found 0 such orphans because the affected rows aren't in the experiments table at all — they couldn't be inserted under the NOT NULL FK.)

(Round-2 produced no new Bucket C entries — Chris kept the stale-cupping and producer findings as B-pending for row-by-row triage rather than blanket known-state acceptance.)

---

## Substrate gap surfaced during the dry-run

`SUPABASE_SERVICE_ROLE_KEY` is not present in either the worktree's `.env.local` or the main checkout's `.env.local` (only `NEXT_PUBLIC_SUPABASE_*` + `ROEST_*` + `OAUTH_*` keys). The script crashes on `createServiceClient()` until Chris adds the key locally — most likely via `vercel env pull` since the Vercel deploys clearly have it (the MCP server uses it in production per Sprint 2.3 retro). This v1 dry-run therefore ran via the Supabase MCP `execute_sql` tool instead of the script itself. Adding the key to `.env.local` is a one-line fix Chris can do whenever he wants to re-run locally; not a script defect.

## Pre-existing drift surfaced by parallel checks

`npm run check:registry-sync` (the registry-md drift guard from PR #112) reports 4 pre-existing drift entries:

- producer registry: `Nelsyn Hernández` in `.ts` but missing from `.md`; `Jannette & Kai Janson (Janson Farms)` in `.md` but missing from `.ts`.
- flavor base: `Rosemary` in `.ts` but missing from `.md`.
- flavor modifier: `Herbal` in `.md` but missing from `.ts`.

These are NOT introduced by Track 2 — they predate this PR and exist on `main`. The producer registry drift is causally linked to dim 5 finding #2 (Janson Farms variant). Resolution is a separate cleanup PR; out of scope here. (Track 2 did not run `check:registry-sync` against `main` to confirm pre-existence — Chris can verify via `git stash && npm run check:registry-sync && git stash pop` if needed.)
