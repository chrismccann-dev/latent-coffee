# Grilling queue

Standing list of concepts, terminology, and framing questions for the next `/grill-with-docs` session. Append items as they surface during dog-food sessions, sprint reviews, audit work, or anywhere claude.ai shows substrate ambiguity. Drained at each grill pass — landed terminology goes to [CONTEXT.md](CONTEXT.md), decisions go to [docs/adr/](docs/adr/), sub-skill cluster docs absorb operational detail.

Per [feedback_grilling_refresh_at_feature_ship](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_grilling_refresh_at_feature_ship.md): grilling fires at the start/end of every feature ship. Between ships, this queue accrues — keeps candidates from being lost to chat scrollback.

## How to use

- **Append** anytime a vocabulary question / framing ambiguity / unlock-decision surfaces. Include the source (which sprint, which lot, which session) so the grill has context.
- **Grade** items by readiness:
  - **READY** — enough data / decisions to lock at the next grill
  - **OBSERVING** — needs more lived data first (count required observations if known)
  - **BRAINSTORM** — concept still fluid; grill explores the shape before naming
- **Drain at grill** — each item lands as a CONTEXT.md entry, ADR, sub-skill cluster doc patch, or "no action — common understanding confirmed." Move to § Resolved with date + landing target.

This is distinct from [feedback_mcp_continuous_log.md](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_mcp_continuous_log.md): friction log is bug/fix-shaped per-session journal; grilling queue is concept-clarification-shaped standing list. Different intent, different drain cadence.

---

## Outstanding queue

> **Resync grill 2026-05-31 drained items 35 / 36 / 39-residual / 40 / 42 / 43 + the doc-size tripwire review + XO-as-signature — see § Resolved for outcomes** (full session record: [docs/sprints/grilling-2026-05-31-resync.md](docs/sprints/grilling-2026-05-31-resync.md)). Still OUTSTANDING by design: **Item 38** (OBSERVING, trigger folded into the claude.ai grilling review's context-window check) + **Item 41** (OBSERVING, no action until N≥3). **Items 37 + 38 + 5b consolidated** into the new recurring **claude.ai grilling review** (`feedback_claude_ai_grilling_review`). Items 2a / 5b / 18 / 20 / 24 / 25 untouched this grill (deferred-pending-trigger).

### From Sprint R Phase 4 kickoff brief (2026-05-21 — Step 4 topics)

2a. **Workflow-sub-skill file rename to "process" framing** — `brewing-assistant/cluster/operational-guide.md` covers the FULL brewing process (intake + iteration + archive); `roasting-assistant/cluster/onboarding-protocol.md` covers only STAGE 1 of the roasting process (green-bean intake → first 3 roasts). The asymmetry reflects real structural differences (brewing short-cycle / roasting state-heavy multi-stage), but the naming would benefit from Chris's umbrella mental model: "the roasting process" / "the brewing process," with onboarding-protocol becoming a stage section under a `roasting-process.md` umbrella. Defer until the roasting-assistant cluster grows additional stage docs (cupping-protocol.md / iteration-protocol.md / close-protocol.md) — at that point the rename becomes more obvious. **Today's path forward:** keep current names, lock the vocabulary distinction in CONTEXT.md (done 2026-05-23 grill, Item 2), revisit rename when the multi-stage roasting cluster fills in.
   - **Grade:** OBSERVING (deferred pending additional roasting-stage cluster docs)
   - **Source:** Item 2, Sprint R Phase 4 Step 4 grill (2026-05-23); trigger tightened Item 2a, Group 5 grill (2026-05-24, audio-ratified by Chris)
   - **Promotion trigger** (tightened 2026-05-24): ≥2 additional roasting-stage cluster docs in `roasting-assistant/cluster/` (candidates: cupping-protocol.md / iteration-protocol.md / close-protocol.md). Count-based mechanical trigger; lived-speech-based alternative rejected at grill as harder to verify across sessions.
   - **Suggested landing:** rename sprint when triggered; updates Operator-guide / operational-guide / onboarding-protocol / operational-reference CONTEXT.md entry to retire the "open rename question" flag

5b. **Empirical claude.ai context-window measurement (cross-party grill)** — Chris-offered at Item 5 grill (2026-05-23): "if we need to do another claude.ai grilling session between you, Claude, and claude.ai, to figure out how much it could accurately retain and what the context scope creep is on it, I'm okay to do that." The 50% context-window heuristic that anchors ADR-0014's pattern-aware tier thresholds is a best-guess; lived measurement (instrument a typical brewing + roasting session, capture substrate-pull totals via Tool-call logs, observe quality degradation patterns at specific token thresholds) would replace the heuristic with calibrated numbers. Could shift any of the 7-tier thresholds up or down materially.
    - **Grade:** READY (cross-party session needed; Chris-offered, not yet scheduled)
    - **Source:** Item 5, Sprint R Phase 4 Step 4 grill (2026-05-23) — Chris-offered cross-party session
    - **Suggested landing:** dedicated cross-party grilling session — Chris + Claude Code + claude.ai instrumented; outputs ADR-0014 amendment with empirical thresholds
    - **First data (2026-05-31, claude.ai grilling review Component 3):** roasting peak ~55-65% of the 200K window (mid-to-full arc; REDPLUM mid-V-set is a lower bound - close-out pushes higher), and the dominant category is **MCP payloads**, not docs or conversation. Session-start docs are cheap (~5-8%). The 50% heuristic isn't breached yet, but the lever is MCP-payload accumulation, not doc load - the ADR-0014 amendment should threshold against that. **Baseline is forward-only:** all pre-2026-05-21 lots (COS-HIG-BOR, RANCHOTIO) straddle the architecture change and can't be retroactively measured; REDPLUM + RANCHOTIO are the first valid data points, with a clean full-lifecycle number arriving when REDPLUM closes. Still READY for the full instrumented amendment.
    - **Second data (2026-06-05, claude.ai grilling review Component 3 — REDPLUM, now 3 sessions deep):** peak **~80-90%**, up from ~55-65%, and the **dominant bucket FLIPPED**: conversation prose ~50-55% (now largest, mostly claude.ai's own long structured responses across V2 log-roast → V2 log-cupping → SPG + V3 design stacked in one thread), MCP payloads ~30-35% (no longer dominant), docs ~3-5%. The amplifier is **prose duplication** (same V_n narrative restated across push_roast + patch_experiment + propose_doc_changes + summary tables, ~3x needed density). Heaviest single MCP call: get_bean_pipeline ~25-35KB mid-V-set (claude.ai skipped later pulls to cope, accepting stale-state risk). **Refines the amendment target:** at multi-session scale the binding constraint is **conversation-history accumulation across stacked sessions**, not MCP payloads - which is the direct empirical case for the **Lot Coordinator / V-Set Assistant split** (ephemeral per-V-set sessions that emit thin results packets and stop). Full record: [docs/sprints/grilling-2026-06-05-claude-ai-review.md](docs/sprints/grilling-2026-06-05-claude-ai-review.md). Clean full-lifecycle number still awaits REDPLUM close (will exceed 80-90%).


### From CGLE Sudan Rume Natural V5 cupping (2026-05-21 — feedback log Round 9 / PR #216)

---

### From Round 10 dog-food haul (2026-05-22 — Sudan Rume Path A close + Higuito first full close-lot.md + Wush Wush first start-brew)




16. **CONTEXT.md zone split (promoted to next-sprint candidate at Item 3 grill, 2026-05-23)** — 437KB exceeds context window; claude.ai falls back to grep. **Chris re-flagged at Item 3 grill:** "we probably need to plan to break these things up... I'm about to do a brewing side, here's the context around the brewing side." Locks option (a) — split into `context-roasting.md` + `context-brewing.md` + `context-shared.md` (aligned to the **Zone** vocabulary locked in Item 3). Each session-start prompt loads only the relevant zone(s) + shared, dropping ~half the context-window cost per session. Sprint scope: re-organize CONTEXT.md by zone, update all references (CLAUDE.md / MCP catalog / docs/prompts/*.md / sub-skill SKILL.md), shipped as a single docs sprint. Probably the highest-priority post-grilling sprint candidate.
    - **Grade:** READY → **next-sprint candidate** (promoted from option (b) deferred fix to option (a) full split at Item 3 grill, 2026-05-23)
    - **Source:** Wush Wush start-brew / Round 10 / item 9 + Item 3 Sprint R Phase 4 Step 4 grill / 2026-05-23
    - **Suggested landing:** dedicated docs sprint — re-organize CONTEXT.md by zone + propagate refs across MCP catalog + prompts + SKILL.md files

### From Round 11 Wush Wush full brewing iteration + bundled-brewing-completion (2026-05-22)

18. **Switch open-drawdown coarsen-for-faster-rinse — generalizes beyond Intensity-Clarity Split?** — Wush Wush session surfaced the counterintuitive lever "coarsen the bed so the open phase drains as a true fast rinse — on Hybrid the open phase is a rinse, not an extraction." Documented in hybrid.md for Intensity-Clarity Split. Open question: does this lever apply to other Hybrid sub-forms (Sequential, Phase-Mapped, Selective Bloom, Temperature-Staged) or is it specific to Intensity-Clarity Split? Two structurally distinct parts to the lever: (1) structural insight — open phase on Switch Hybrid = rinse, not extraction (plausibly general by valve geometry); (2) tactical lever — coarsen the bed to drain fast (may depend on sub-form's specific use of open phase). N=1 isn't enough to lock either part's altitude. If general, lever belongs in `brewing-equipment-expert/cluster/brewers.md § Hario Switch` as a Switch operating note; if specific, stays in hybrid.md per-sub-form callout.
    - **Grade:** OBSERVING (1 lived data point on 1 sub-form; trigger tightened to cross-sub-form coverage)
    - **Source:** Round 11 / Wush Wush Brew 4; trigger tightened Item 18, Group 5 grill (2026-05-24, audio-ratified by Chris)
    - **Promotion trigger** (tightened 2026-05-24): **N≥1 confirming brew on at least 2 of {Sequential, Phase-Mapped, Selective Bloom, Temperature-Staged}**. Cross-sub-form coverage requirement prevents promoting on multiple confirms within the same sub-form. At promotion time, decide whether to split-promote (structural insight → Switch-level; tactical lever → may stay sub-form-specific) or full-promote (both parts → Switch-level with sub-form-specific qualifiers).
    - **Suggested landing:** brewing-equipment-expert/cluster/brewers.md § Hario Switch operating note (if general) OR hybrid.md per-sub-form callout (if specific) — split-promote vs full-promote decision deferred to promotion-time grill once cross-sub-form data lands


20. **Medium-roast handling — load-bearing OR "named consideration"?** — Round 10 shipped a medium-roast hook in operational-guide Step 1 as a "named consideration." Round 11's Wush Wush iteration showed it should have LED strategy selection, not been a footnote. Question for grilling: is the hook prominent enough in its current form, OR should "roast level" be promoted to a Process / Variety / Roast-Level signal flag at Step 1b (parallel to the existing Process and Variety signals) so claude.ai weights it as a first-class driver rather than discretionary?
    - **Grade:** OBSERVING (1 lot + 1 strong iteration-time validation; trigger tightened to require intake-time leads)
    - **Source:** Round 11 / Wush Wush iteration / item 9; trigger tightened Item 20, Group 5 grill (2026-05-24, audio-ratified by Chris)
    - **Promotion trigger** (tightened 2026-05-24): **N≥2 more medium-roast specialty naturals where over-extraction-risk-as-roast-character framing LEADS strategy selection at intake** (not just retroactively confirmed at iteration time). The bar is intake-time leads, not iteration-time validation — Wush Wush validated retroactively after Brew 1 over-extracted on default Clarity-First; the meaningful test is whether the hook drives intake-time strategy selection before Brew 1 fires. Per Chris-flag at Item 15 grill (Group 2, 2026-05-24): inventory has darker-than-normal lots expected to land in brewing rotation, so trigger likely to fire soon.
    - **Suggested landing:** operational-guide Step 1b signal-table promotion + CCIL pattern promotion in same sprint when trigger fires; substrate destinations are aligned across both surfaces

24. **Pre-shipped substrate during active dog-food session — coordination process** — Round 11 shipped the hybrid.md Intensity-Clarity Split entry as part of the BLOCKING-fix PR, derived from the in-progress Wush Wush session. When claude.ai later reached propose_doc_changes STAGE 2, the content was already there — claude.ai correctly paused rather than file a duplicate. Process lesson: when Claude Code pre-ships substrate updates derived from an active dog-food session (rather than waiting for claude.ai's natural propose_doc_changes path), flag the pre-populated cluster doc paths explicitly to Chris so claude.ai's session doesn't get confused at proposal time. Consider: does PR commit-message convention need a "Pre-populated substrate paths (will collide with active claude.ai proposals)" callout?
    - **Grade:** OBSERVING (was BRAINSTORM; demoted to OBSERVING at Group 5 grill 2026-05-24 — mechanism design not load-bearing today; defer to recurrence)
    - **Source:** Round 11 continuation / 2026-05-22 hybrid.md proposal pause; trigger tightened Item 24, Group 5 grill (2026-05-24, audio-ratified by Chris)
    - **Promotion trigger** (tightened 2026-05-24): **≥1 lived instance of claude.ai pause-behavior failure** — duplicate proposal filed by claude.ai, OR operator confusion at arbitration time due to substrate-state vs proposal-state misalignment, OR collision-caused workflow drift. Trigger is the FAILURE case, not the SUCCESS case (Round 11 was success — proves existing pattern works at N=1). Per Chris-audio 2026-05-24: this is primarily a machine-layer concern (claude.ai + MCP server + Claude Code coordination), so the trigger watches for machine-layer failure modes rather than Chris-side ergonomics. **Companion watch-item**: WBC-vocabulary growth (Vannelli Aroma Capture / Wölfl Extraction Push / Ferket Selective Bloom Hybrid / etc.) may eventually create taxonomy collision risk between proper-name WBC vocabulary and existing canonical substrate names; if WBC vocab keeps multiplying past current ~5 named-pattern threshold and starts colliding, a formal naming taxonomy may be needed. Chris-flagged as future trigger, not actionable today.
    - **Suggested landing:** when trigger fires — commit-message / PR-description convention update OR an operational-guide note in the bundled-brewing-completion / close-lot prompts about checking propose_doc_changes targets for recent commits; OR formal naming taxonomy if WBC-vocabulary collision is the failure shape

### From Round 12 — first lived one-shot lot run (2026-05-23, MH Elgon Ladies' Lot)

25. **Inventory doc roast-strategy bullets vs CCIL — substrate cleanup sprint** — V5 inventory doc has roast-strategy guidance bullets for the 10 MH Paradigm Shift lots + the 3 Taza Dorada lots that prescribe stale numeric guidance (notably "120°C hopper, trim peak 2°C if lower-density" on multiple lots — directly contradicting the Rancho Tio Emilio one-shot canonical correction of "full anchor energy, no altitude downhedge, 125°C hopper"). The "CCIL wins on numeric conflict" rule landed in one-shot.md STAGE 2 (this round) is the runtime safeguard, but the inventory doc itself still carries the stale bullets. Cleanup: propose_doc_changes pass updating each lot's hopper + peak-trim guidance to match the canonical correction. **HOLD until Day 7 Mount Elgon cupping confirms** that #133-full-energy actually lands on Ugandan terroir — if the cup confirms, the propose_doc_changes pass is backed by lived evidence; if the cup misses, the doc may need terroir-scoped guidance rather than a verbatim CCIL match.
    - **Grade:** OBSERVING (HOLD re-confirmed at Group 5 grill; waiting on Day 7 Elgon cup, expected ~2026-05-30)
    - **Source:** Round 12 / Mount Elgon Ladies' Lot intake (MH Paradigm Shift 2026 Uganda sample pack); branch logic + operator-fixed-constants framing locked at Item 25, Group 5 grill (2026-05-24, audio-ratified)
    - **Re-open trigger** (locked 2026-05-24): Day 7 Mount Elgon cupping fires on batch #199 (~2026-05-30). Two branches:
      - **Branch A** — #133-full-energy lands on Uganda terroir → ship the propose_doc_changes pass on V5 inventory doc lots (MH 10 + Taza Dorada 3), correcting "120°C hopper" guidance to **125°C hopper-load + 117°C charge** (per the operator-fixed constants locked in this session — see CONTEXT.md § Operator-fixed roast constants), and reframing "trim peak 2°C if lower-density" guidance per the full-anchor-energy canonical.
      - **Branch B** — #133-full-energy misses on Uganda terroir → re-grill to decide whether terroir-scoped guidance is needed (e.g., Uganda-specific hopper/peak adjustments) instead of verbatim CCIL match. The operator-fixed constants stay locked (125°C / 117°C); the variation surface is recipe-level levers (peak inlet / curve / fan).
    - **Suggested landing:** propose_doc_changes pass on V5 inventory doc lots when Branch A fires; new grilling-queue item if Branch B fires


### From Round 13 — first full V-set lifecycle close (2026-05-23, CGLE Sudan Rume Natural)

### From Round 14 — four log-roast sessions / Step 3 dog-food close (2026-05-23)

35. **Drop rules UI persistence in Latent app — view changes after roast logged** — Mt Elgon: Chris couldn't re-find the drop rules mid-session because the lot's page flipped from "waiting for next roast" view (which surfaces the design-intent panel) to "waiting for next cupping" view (which hides it). The operator needs drop rules AT the machine DURING the roast; the view that surfaces them disappears the moment the prior roast was logged. App UX issue, not prompt: `/green/[id]` page should keep drop rules accessible during active roasting OR persist them in a way the operator can grab mid-roast (Roest profile notes field already serves as durable backup, but the app's own surface should match).
    - **Grade:** READY (Latent app feature — render drop rules persistently across lifecycle states OR add a "show recipe design intent" toggle on the resolved-pending / waiting-for-cupping views)
    - **Source:** Round 14 / Mt Elgon batch 199 session feedback
    - **Suggested landing:** Latent app sprint — `/green/[id]` page UX work + possibly a dedicated "active roast" view that pulls the recipe design intent + drop rules into a roast-time HUD

### From Load orchestration verification (2026-05-25, brewing + roasting paper-walks)

36. **"Strategy zone / Wrong-zone trap" terminology check** — claude.ai recognized the concepts during the brewing paper-walk but flagged that the exact labels aren't section anchors in operational-guide. The concept lives in the mechanics-vs-intent symmetry rule (Suppression/Clarity-First mechanics overlap; Full Expression/Extraction Push mechanics overlap; intent differs). Worth a grep across substrate to confirm whether these terms are anchored anywhere or just operational shorthand. If unanchored, either anchor them in operational-guide / wbc-reference / context-brewing or drop the labels from prompts that reference them as canonical.
    - **Grade:** READY (substrate vocabulary audit + decide anchor-or-drop)
    - **Source:** [docs/sprints/load-orchestration-verification-findings-2026-05-25.md](docs/sprints/load-orchestration-verification-findings-2026-05-25.md) § Brewing session
    - **Suggested landing:** vocabulary audit followed by either anchor placement or prompt-language softening

37. **claude.ai project memory currency lags substrate** (second confirmation) — both brewing + roasting project memories still describe the docs://context-roasting.md / context-shared.md split as if those are load-bearing fetches. Post Pattern J pruning (2026-05-25, PR #247), CONTEXT-shared.md is a 49 KB index pointing at docs/reference/*.md reference docs. Memory descriptions of where things live are out of date. Not a load issue (memory is just descriptive, no fetch instruction left to fire after the load-orchestration sprint) but a substrate-currency drift that compounds. Banked here for the next claude.ai memory-chatbox refresh pass.
    - **Grade:** READY (memory-chatbox edits in both projects)
    - **Source:** [docs/sprints/load-orchestration-verification-findings-2026-05-25.md](docs/sprints/load-orchestration-verification-findings-2026-05-25.md) § Roasting session
    - **Suggested landing:** small memory-chatbox refresh pass touching both projects' "Purpose & context" / "Architecture" descriptions
    - **Disposition (2026-05-31):** ✅ DRAINED in the first claude.ai grilling review. Real drift was the *opposite* of the bank note - roasting memory mislabeled context-roasting.md / context-shared.md as "stubs" (under-fetch framing), not "load-bearing fetches." 5 memory edits across both projects (roasting: RoR-shipped / active-lots slim / tool count 8 / instruments / CONTEXT-stub tidy; brewing: instruments) landed clean in a single chatbox pass. Recurring protocol (`feedback_claude_ai_grilling_review`) supersedes this one-time item going forward.

38. **Operator guide cross-reference density — architectural watch-item** — both calibration responses noted the operator guide is "studded with cross-reference links" pulling toward link-following habit. The doc is correctly doing its job as a directory but the directory shape is itself a drift vector for over-fetching. Worth tracking if session-start loads creep upward over time. Pairs with the load-orchestration spec's "operator-guide exemption is the most-likely-to-drift line" canary — if the same drift surface fires twice, that's the signal to audit the exemption itself (potentially: split operator-guide's Canonical taxonomy lookups + Schema model + Session debrief paste template into on-demand pulls, per the pre-flight audit's deferred follow-up).
    - **Grade:** OBSERVING (watch-item; trigger is session-start load creep OR a second drift surface fire)
    - **Source:** [docs/sprints/load-orchestration-verification-findings-2026-05-25.md](docs/sprints/load-orchestration-verification-findings-2026-05-25.md) § Overall feel + [docs/sprints/load-orchestration-preflight-audit-2026-05-25.md](docs/sprints/load-orchestration-preflight-audit-2026-05-25.md) § Operator-guide split-check
    - **Suggested landing:** if triggered, operator-guide split sprint extracting 3 candidate sections (Schema model / Canonical taxonomy lookups / Session debrief paste template) per the pre-flight audit's deferred-follow-up table
    - **Disposition (2026-05-31):** DISPROVEN by the first claude.ai grilling review context-window check. On current architecture, session-start docs are the *cheap* category (~5-8% of window on the REDPLUM thread; operator-guide wasn't even fetched upfront). Operator-guide load creep NOT confirmed - **do not pursue the operator-guide split.** The real context cost is MCP payloads: full-registry `read_canonical` (flavors ~20K tokens, ~5% used) + non-incremental `get_bean_pipeline` accumulation. Optimization redirected there (see PRODUCT.md roadmap: read_canonical name filter + get_bean_pipeline `since:`). Downgrade/close.

### From Sub-sprint 4c brews polish (2026-05-28 — deferred Item 4)

39. **✅ SHIPPED — Structured `pour_structure` schema (step-objects)** — the `data-model` session (2026-05-30, migration 074) drained this. Chris kicked off with a full audio readout + 7 real recipe cards spanning complex (SWORKS dial transitions, manual-lever immersion) + simplistic (clean Melodrip) pour structures; the storage-shape call was made against that real range via AskUserQuestion (capture-first, ask-don't-ship). Resolutions to the (a)-(e) open calls: **(a)** valve = **free-text with recommended vocab** (SWORKS registry supplies the words; single owned valve brewer, don't-overcomplicate) — NOT a strict enum; **(b)** `to_g` cumulative confirmed; **(c)** kettle stance lives as **prose in `detail`**, NOT a typed per-step/brew-level field; **(d)** drain-and-reclose also **prose in `detail`** — no typed `drain` object; **(e)** migration = **Hybrid** — structured `brews.pours` jsonb forward, parser kept as read-fallback for legacy NULL-`pours` rows, hand-backfill only the 6 BS-1 brews + lock-reference recipes (no lossy mass-parse). Net shape is deliberately **lighter than the brainstorm md** (six flat keys `{type,at,to_g?,pour_s?,hold_s?,valve?,detail?}`, no nesting). `equipment` + `water_recipe` brew-level fields already shipped 4c. Full six-actor trace landed in the same PR. **Residual for the next grill:** add a `Pour step` glossary term to [CONTEXT-brewing.md](CONTEXT-brewing.md) (CONTEXT grows grilling-first, not bulk-authored — so this term waits for a grill rather than landing in the build PR).
    - **Grade:** ✅ SHIPPED 2026-05-30 (data-model session, migration 074). Residual: CONTEXT-brewing `Pour step` term at next grill.
    - **Source:** [docs/sprints/sub-sprint-4c-brews-polish-complementary-pass-2026-05-28.md](docs/sprints/sub-sprint-4c-brews-polish-complementary-pass-2026-05-28.md) § Item 4 + Chris's "Brew Recipe Data Structure - Claude Brainstorm" handoff + the data-model session audio readout (2026-05-30)

### From Sub-sprint 4f processes polish (2026-05-28 — deferred F4 + Anoxic note)

40. **Process-registry authored content has no arbiter-queue reminder + honey-subprocess description is a missing field** — Two-part gap surfaced in Chris's 4f Phase 1 audit (Moonshadow / black honey "pending authoring" empty states). **(a)** `list_skeleton_entries` (the third arbiter queue type) covers only the **producer** + **roaster** registries via an explicit `skeleton: true` flag; process-registry content fields (`SignatureEntry.overview` / `BaseProcessEntry.summary` / `brewArchetype` / `ModifierEntry.overview`) have no flag and feed no queue — they only render empty-state prose on the page, so nothing reminds Chris to author them. Fix options: extend `list_skeleton_entries` to a third (process) axis, OR derive "unauthored = overview missing" rather than an explicit flag. **(b)** Honey subprocesses (Black / White / Generic Honey) have **no description field at all** — there is `BaseProcessEntry.summary` for Honey-the-base but no per-subprocess slot; "black honey description" is a *missing field*, not an unauthored one. Adding it = net-new registry shape (`HoneySubprocessEntry` or a subprocess-summary map) + render path on `[base]/[subprocess]/page.tsx`. Both parts cross into MCP-surface / content-architecture territory (Actor 4); deferred from the 4f light series-closer per Chris's "defer both."
    - **Grade:** READY (small; (a) is an MCP-Tool extension, (b) is a registry-shape + render add — can ship together or separately)
    - **Source:** [docs/sprints/sub-sprint-4f-processes-polish-complementary-pass-2026-05-28.md](docs/sprints/sub-sprint-4f-processes-polish-complementary-pass-2026-05-28.md) § F4
    - **Suggested landing:** a thin arbiter-ergonomics sprint, or fold into the next time `process pending arbitration` is run with bandwidth to author the overviews together

41. **Anoxic-qualifier aggregation promotion — revisit at N≥3** — 4f Q4 locked **Path 1: leave it.** `Anoxic` stays a recorded `fermentation_qualifiers` annotation; processes aggregation correctly stays at the Anaerobic modifier per the locked T3/CR-5 convention. Only 1 brew (the Rosado row, migration 059) carries it today — below the ≥3 aggregation floor the whole processes IA already runs on (modifier-index Rule 1). **Trigger:** when 3+ brews carry the Anoxic qualifier, revisit whether to promote it from a qualifier annotation to a first-class `fermentation_modifier` (which WOULD aggregate / get a modifier-index page / appear in the display string) — that promotion reverses T3/CR-5 and needs a data migration to move values between columns, so it's a deliberate grill, not an autonomous flip.
    - **Grade:** OBSERVING (watch the Anoxic brew count; no action until N≥3)
    - **Source:** [docs/sprints/sub-sprint-4f-processes-polish-complementary-pass-2026-05-28.md](docs/sprints/sub-sprint-4f-processes-polish-complementary-pass-2026-05-28.md) § Anoxic

### From the redesign arc (Sprints 1-6 + polish buckets, 2026-05-29 → 05-30)

42. **Per-surface mobile pattern → ADR candidate** — Redesign Sprints 1-4 locked a real responsive convention but it lives only as CLAUDE.md § Design-conventions prose: use `order-*` reordering (single tree) where mobile is a *resequence* of the same blocks; use a CSS container-query dual-subtree (`.s2-desktop`/`.s2-mobile`, both in the DOM, revealed at the **520px crossover** on `.ssp-page`'s content-box) where mobile needs a genuinely different *composition* (e.g. the cupping transposed-table → slot-cards recomposition in Sprint 2). Both are no-`@media`. The pattern was applied consistently across the green views but never formalized as a decision record, so the next surface re-derives it. Candidate: an ADR capturing the two-pattern rule + the 390/1024 two-point model + the "container queries not @media" infra choice, so future per-surface work picks the right pattern by lookup.
    - **Grade:** READY (the convention is lived + stable across 4 sprints; just needs an ADR + a CONTEXT/CLAUDE pointer)
    - **Source:** Redesign Sprints 1-4 (2026-05-29); CLAUDE.md § Design conventions "per-surface mobile pattern rule" + ratification #3
    - **Suggested landing:** new ADR (per-surface mobile pattern) + a one-line CONTEXT pointer; CLAUDE.md § Design-conventions already carries the prose, so this is formalize-don't-author

43. **Detail-view hero-tile reconciliation** — CLAUDE.md flags an explicit unreconciled loose end: the `--tile-*` lifecycle gradient (used by the `/green` index + the MB-6 green cards: sage `next-roast` / olive-bronze `next-cupping` / roasted-brown `resolved` / grey `unresolved`) vs the per-surface emphasis **hero tiles** on the green detail views (cupping = lavender `#7A6E9E` cup-emphasis, waiting-roast = amber `#A88037` roast-emphasis). The detail-view hero tiles "still use the old bindings — they reconcile in the green per-surface sprint" (CLAUDE.md § Design conventions, lifecycle-tile gradient note). Grill decides: reconcile the detail hero tiles onto the lifecycle gradient (consistency), OR bless the divergence (the per-surface emphasis colors are doing a different semantic job than the index state-gradient — arguably correct to differ). Small either way.
    - **Grade:** READY (decision + possibly a small green touch-up; no new infra)
    - **Source:** Redesign Sprints 2-3 (per-surface tile reconciliation deferred) + CLAUDE.md § Design conventions lifecycle-tile-gradient note
    - **Suggested landing:** grill decision → either a small green-detail tile sprint (reconcile) or a CONTEXT/CLAUDE note blessing the divergence (close the open flag)

### From the claude.ai grilling review (2026-05-31 - first run of the recurring protocol)

44. **Deprecated prompts still fetchable + silently serve old content** - the 2026-05-31 context-window check found `in-process-bean-incremental-sync.md` fetched cleanly and worked, masking that it's deprecated (current lifecycle is start-lot -> log-roast -> log-cupping -> close-lot); the RANCHOTIO thread similarly loaded the old `roasting.md` monolith. A deprecated prompt should return a deprecation/redirect header (mirror the BREWING.md / ROASTING.md redirect-stub pattern) naming the supported replacement, rather than serving stale content that succeeds and hides the drift.
    - **Grade:** READY (concrete doc/MCP fix, low risk)
    - **Source:** claude.ai grilling review 2026-05-31, Component 3 (REDPLUM + RANCHOTIO context-window self-audit)
    - **Suggested landing:** convert the 3 deprecated roasting prompts (new-bean-intake / in-process-bean-incremental-sync / closed-bean-full-fill) to redirect stubs in `docs/prompts/` pointing at the lifecycle replacements; verify docs.ts serves the stub

45. **No write-side schema cheat-sheet doc -> trial-and-error schema discovery** - the 2026-05-31 context-window check (REDPLUM self-audit) found schema learned by trial-and-error (worth_repeating enum reject, push_experiment field-name mismatch, end_condition_target null-on-manual rule), each surprise costing 1-2 retry round-trips. claude.ai's framing: "the cumulative cost of NOT having a schema doc probably exceeds any single doc I did load." A compact `docs://schemas/<table>.md` per write-side table (roasts / roast_recipes / experiments / cuppings / brews) loaded once per session, est. 5-8 round-trips saved. Scoping question: author fresh vs. surface the existing push_* Zod schemas (which already encode this but aren't visible until the tool is called).
    - **Grade:** READY (net-new doc set; small scoping decision on source-of-truth: author fresh vs generate from Zod)
    - **Source:** claude.ai grilling review 2026-05-31, Component 3 (REDPLUM)
    - **Suggested landing:** docs sprint - author or generate `docs/schemas/<table>.md` per write table + register in lib/mcp/docs.ts

### From Glitch Coffee roaster enrichment + roast-brew philosophy reflection (2026-06-06)

50. **"Intensity-Clarity Split" term has forked scope — brewing-phase strategy vs whole roast-brew house philosophy** — surfaced when Chris talked through how the Glitch Coffee (Tokyo) house style maps onto his own emerging direction. Chris reached for "intensity-clarity split" to name a *coupled roast→brew philosophy*: select fruit-heavy expressive greens → roast *slightly more developed* than a clean washed (pull max flavor on the roast side, Glitch's "developed-light" direction but less far) → counterbalance with low-temp brewing to suppress the bitterness/roast externalities the extra development risks ("pull everything, suppress the downside" across BOTH axes). But **"Intensity-Clarity Split" is already a defined term in [CONTEXT-brewing.md](CONTEXT-brewing.md)** meaning something narrower: one of the Hybrid brewing sub-forms (a phase structure where bloom + first pour = the "intensity phase"). Two real questions for the grill: **(a)** does the broad roast-brew philosophy need its own distinct name (the roasting-side mirror of the already-documented brew-side **Suppression** lean), or does "intensity-clarity split" legitimately span scopes? **(b)** if it earns a name, where does it anchor — CONTEXT-roasting (it is roasting-led), a roasting-assistant cluster doc, or stays in memory until tested? Related: the roasting-side articulation (develop-more-then-suppress coupling) is net-new substrate — currently captured only in [user_taste_evolution.md](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/user_taste_evolution.md) (2026-06-06 update), not in any CONTEXT/cluster doc. Concrete test hypothesis banked alongside: the most over-expressive lots (e.g. **Mandela**) were likely pushed too far — next pass should try lower brew temp AND less roast development.
    - **Grade:** BRAINSTORM (concept still firming up; N=1 articulation, untested on a lot)
    - **Source:** Glitch Coffee roaster-enrichment session (2026-06-06), Chris audio reflection after the [Glitch transcript](docs/taxonomies/roasters.md) enrichment (PR #415)
    - **Promotion trigger:** the develop-more-then-suppress coupling tested on 1-2 lots (does the lower-temp-brew + restrained-development pairing actually rein in an over-expressive lot like Mandela?). Until then, memory is the right altitude — don't formalize into CONTEXT/cluster.

### From the Lot Coordinator 2026-06-09 resolution session

51. **Drop-rules mechanism edits/improvements (operator has specific changes)** — during the prediction-structure fork of the 2026-06-09 Lot Coordinator grilling, Chris flagged that he has specific edits + improvements to *how drop rules are designed/executed* (surfaced from his most recent live roasting session), and explicitly parked them ("let's hold that for another time"). This is **distinct from item 35** (drop-rules *UI persistence* — getting the rule to render at the machine; folded into the predicted-vs-actual delta surface) — item 51 is about the *content/logic of the rules themselves* (what to do when the roast runs long / never hits FC / runs fast). Load-bearing because drop rules are exactly the mechanism that opens the design-prediction-vs-roast-actual gap in the three-point delta chain ([ADR-0024](docs/adr/0024-lot-coordinator-claude-code-native.md) § 4), so getting them right feeds the Coordinator's V-set design. **CAPTURED 2026-06-09** — Chris gave 9 lived slots (Gesha Clouds V4 / Wush Wush V2 / Red Plum V3); synthesized into the **drop-rule authoring standard** at [roasting-coordinator/cluster/drop-rules.md](docs/skills/roasting-coordinator/cluster/drop-rules.md): two branches, one imperative line each, anchored on clock-time + drop-temp only, **no explanation** and **no FC-timing conditionals** (the operator can't track FC mid-roast), one action per branch. The recurring failure was prose mixing explanation with instruction + multi-case branching + FC-timing conditions — unparseable at the machine.
    - **Grade:** CLOSED 2026-06-11 (standard authored Claude-Code-side 2026-06-09; claude.ai back-prop landed at dogfood — see below)
    - **Source:** Lot Coordinator 2026-06-09 resolution session (parked mid-fork) → captured same day from 9 lived slots
    - **Follow-up DRAINED 2026-06-11 (Lot Coordinator dogfood Phase A):** the standard is back-propagated to the claude.ai-facing roasting-assistant cluster at [docs/skills/roasting-assistant/cluster/drop-rules.md](docs/skills/roasting-assistant/cluster/drop-rules.md) (full-text mirror — the canonical roasting-coordinator copy is not MCP-registered, so the mirror carries the content; canonical wins on disagreement), referenced from the roasting-assistant SKILL.md Construct line, registered in `lib/mcp/docs.ts`, `check:mcp-bundle` green. Reaches in-flight claude.ai lots on their next fresh-session catalog refresh (rode the one dogfood-batched grilling review as planned).
    - **Suggested landing:** at grill — decide name + scope; if it graduates, CONTEXT-roasting headword (roasting-led) + cross-link to the brewing **Suppression** + **Intensity-Clarity Split** entries to disambiguate the forked term

### From the doc-pruning arc (cases 001-007, 2026-06-03 → 06-04)

47. ~~**Doc-pruning op vocabulary lock (CONTEXT-shared grill)**~~ — **RESOLVED 2026-06-05** (post-Cluster-B grilling session). Grilled into [docs/reference/mcp-architecture.md](docs/reference/mcp-architecture.md) (the self-improvement-vocabulary home) + a new standalone [ADR-0022](docs/adr/0022-formalization-tax-and-self-improvement-counterbalance.md). Landed headwords: the **six shapes** (extract / split / consolidate / archive / delete / re-home), **Formalization tax**, **Graduation threshold (N=3)**, **Arbiter-shaped mechanism**, **Prototype-as-input-not-canon**. Applied the formalization-tax discipline *to the vocabulary itself*: the mechanism-internal terms ("tripwire registry," "post-tripwire pruning exercise," "loading-profile-aware tiering," "destinations-first/index-last," "preserve-the-append-target") did NOT earn standalone headwords — they stay in their operational docs (doc-tripwires.md / ADR-0013 / the pruning-cases). "uniform n-threshold graduation" generalized into the universal **Graduation threshold (N=3)**. See § Resolved.

48. **MCP SKILL.md description drift: cupping-specialist + roasting-assistant vs the live prompt** — case 006 (log-cupping prune) flagged a *pre-existing* drift (not introduced by the prune): the `cupping-specialist` + `roasting-assistant` SKILL.md descriptions in `lib/mcp/docs.ts` still say "Path C-2 / real-pourover discriminator" and "STAGE 3 V_(n+1) design intent" — both already loose vs the current prompt. Reconcile against the live log-cupping.md flow.
    - **Grade:** READY (concrete MCP-description fix, low risk) — but **best folded into the roasting-coordinator grill** (the STAGE 5/6 design-ownership boundary that case 006 deferred there is the same surface; reconcile descriptions when that boundary firms rather than twice)
    - **Source:** [pruning case 006](docs/sprints/pruning-cases/006-log-cupping.md) § Six-actor (Actor 3 pre-existing drift)
    - **Suggested landing:** `lib/mcp/docs.ts` description reconcile, at the roasting-coordinator / Lot Coordinator plan sprint

49. ~~**Concrete-example-first reasoning (Chris's design heuristic — needs a name)**~~ — **RESOLVED 2026-06-05** (deliberate grill, brief + outcome: [docs/sprints/grill-concrete-example-first-kickoff-2026-06-05.md](docs/sprints/grill-concrete-example-first-kickoff-2026-06-05.md)). Named **`concrete-example-first`** (rejected "example-first design" / "walk-the-example" / "lived-example-first" / "show-don't-tell"). Landed as a method headword in [docs/reference/mcp-architecture.md](docs/reference/mcp-architecture.md) + a one-line pointer in [ADR-0022 § Mechanism 1](docs/adr/0022-formalization-tax-and-self-improvement-counterbalance.md); **no standalone ADR** (it is vocabulary, not a hard-to-reverse decision). Locked relationships: human-facing generative **twin of the Graduation threshold (N=3)** (feeder → gate, shared N=3 dial); interrelated **sibling** (not parent/child) of **prototype-as-input-not-canon** (construction axis vs consumption axis). The shared-root umbrella stance was **considered and parked at N<3** — mint only if it recurs 3+ times. Enriched the definition past the original flag with two clauses: *do-it-for-real (forcing function)* + *generalize-from-lived-not-imagined* (the plan/execute deviation rationale). See § Resolved.

### From the first live dogfood brew (2026-06-16 — XLIII Daterra "Ziriguidum #2" Laurina)

52. **Unresolved-state analog for a purchased roasted bean** — first lived case (2026-06-16, XLIII Coffee · Daterra "Ziriguidum #2" Laurina, Natural / Cryo-Anaerobic 140H, WB Agtron 82.5 very light). A three-brew extraction climb (Suppression → extraction-max two-cycle immersion; finer 6.5→6.0→5.6, hotter 92→98→100°C, longer steep, aggressive agitation) moved the cup grassy/sour → stone-fruit/jasmine/faint-sparkle but never reached *development*; the extreme push bought muddiness with **no flavor return**. Diagnosis: roast-side ceiling (under-developed ultra-light roast), not a brewing-resolvable problem — and **not attributable to cultivar / process / terroir / roaster** (no roaster card, no generalizable axis learning). Chris's framing: this is effectively an **unresolved state for a purchased roasted bean**, a category the brewing model has no slot for. Roasting has a first-class unresolved lot state (`why_this_roast_won` discriminator) because Chris controls the roast; brewing implicitly assumes a purchased roast is a fixed-but-*resolvable* input. Decision this session: **did not `push_brew`** — synthesis-pollution argument: a degenerate under-developed-roast row would inject misleading notes into the Laurina / Cerrado / anaerobic-natural synthesis corpora it joins by FK, misrepresenting axes the cup says nothing true about; the recipe isn't repeatable either. Grill questions: **(a)** does brewing need an explicit unresolved / non-representative concept for purchased beans that can't be brought to resolution — a brew-row flag that excludes the row from axis-synthesis aggregation, OR a documented no-push convention? **(b)** generalizable brewing learning worth siting regardless: on an under-developed ultra-light roast, extraction tops out *early* — push to clean-max and STOP; extreme agitation past that muddies without developing, and the *absence* of any over-extraction harshness even at extreme push is itself the tell that the limiter is **development, not extraction** (the best cup here was the mid-climb Brew 2, not the maxed-out Brew 3).
    - **Grade:** BRAINSTORM (N=1; Chris notes it has never happened before — concept still firming up)
    - **Source:** first live `/brew` dogfood (2026-06-16), XLIII Daterra Ziriguidum #2 Laurina; brew-skill close retro
    - **Promotion trigger:** N≥1 second purchased lot that resists resolution by extraction technique due to a roast-side ceiling (confirms the category isn't a one-off), OR Chris decides the no-push convention should be explicit in the brew skill / completion prompt regardless of recurrence
    - **Suggested landing:** grill decides — either a brews-schema flag (non-representative / exclude-from-synthesis) + a completion-prompt branch, OR a documented no-push convention in [`.claude/skills/brew/SKILL.md`](.claude/skills/brew/SKILL.md) + [bundled-brewing-completion.md](docs/prompts/bundled-brewing-completion.md); the (b) brewing learning may land separately in the brewing-historian by-coffee-family / operational-guide regardless

## Substrate pruning candidates

Lived examples of substrate sections that may warrant pruning (section-level removal / extraction to separate file / consolidation / archival). Pattern J — Substrate pruning ([ADR-0013](docs/adr/0013-self-improvement-primitives.md)) was light-touch-shipped 2026-05-24 with the mechanism deliberately deferred until lived candidates covering all five shapes are worked through operator + Claude Code back-and-forth (case-study-driven generalization per Chris's framing at Item 16c grill). This section accumulates candidates; each becomes a worked example. **Systematization is deferred until all five shapes have a worked example** — full protocol at [docs/features/doc-pruning-mechanism-brainstorm-2026-06-03.md](docs/features/doc-pruning-mechanism-brainstorm-2026-06-03.md).

**Why this section exists:** ADD-shaped self-improvement patterns (A-E in ADR-0013) compound substrate monotonically; without a pruning surface, load-bearing content gets buried by stale + over-detailed sections that surface at retrieval time but don't earn their keep. Pattern J is the counterbalance.

**Protocol (locked 2026-06-03, Cluster B doc-pruning brainstorm):**
- **Trigger:** doc-size tripwires now cover all constantly-loaded core docs (root + CONTEXT-* + claude.ai prompts + clusters) — canonical registry at [docs/architecture/doc-tripwires.md](docs/architecture/doc-tripwires.md). A tripwire firing schedules a **manual post-tripwire pruning exercise** (operator + Claude Code), not an autonomous prune.
- **Shape-coverage bias:** the five-shape ops = extract / split / consolidate / archive / delete. After case 002, `extract` / `split` / `consolidate` / `delete` all have worked examples — **only `archive`-as-standalone remains thin** (redirect-stub precedent only). Each exercise must deliberately hunt the missing shape ("is the honest move here actually an archive?") rather than reflex-extract.
- **Structured capture:** each exercise produces a handoff doc in [docs/sprints/pruning-cases/](docs/sprints/pruning-cases/) (header: doc / trigger / shape(s) / judgment calls / heuristic learned) so lessons aggregate. Template: [_template.md](docs/sprints/pruning-cases/_template.md); seed: [001-claude-md-compaction.md](docs/sprints/pruning-cases/001-claude-md-compaction.md); first non-extract case: [002-context-roasting.md](docs/sprints/pruning-cases/002-context-roasting.md).
- **delete = flag-only:** Claude Code flags delete candidates for Chris; never deletes content-existing-nowhere-else autonomously. Case 002 surfaced a lower-risk sub-class (provenance shrapnel + deferred-future TODOs — git-recoverable, zero downstream consumers) as the candidate for the first *autonomous*-delete rule.
- **Next dogfood (case 003):** PRODUCT.md (live fire 127/120 KB; framed to land the missing `archive` shape — closed/shipped roadmap sections → archival surface) — kickoff at [docs/sprints/product-md-prune-kickoff-2026-06-03.md](docs/sprints/product-md-prune-kickoff-2026-06-03.md). After 003, all five shapes are covered across three doc-shapes → systematization decision.

**Procedure:** when a candidate surfaces during a sprint / grill / arbiter walk, file it under § Open candidates rather than acting inline; it gets worked at the next post-tripwire exercise.

Format per entry:

```
N. **<Candidate name>** — <what section / file / pattern is the pruning candidate>. **Why prune:** <staleness / over-detail / superseded / belongs-elsewhere reason>. **Proposed mechanism:** <delete / extract-to-separate-file / consolidate / archive>. **Surfaced:** <source — which sprint / grill / arbiter walk>. **Status:** candidate.
```

### Worked examples (reference)

These are completed pruning instances. They're not active candidates; they're substrate-precedent for the case-study methodology. Future candidates derive generalized rules in part by studying what worked here.

1. **PRODUCT.md § Recently shipped → docs/sprints/shipped.md** — sprint-by-sprint shipped log extracted from PRODUCT.md during May 2026 doc cleanup; 1-line pointer left in place. **Why pruned:** PRODUCT.md was bloating with chronologically-appended sprint entries; the log was load-bearing but didn't need to live in PRODUCT.md's working set. **Mechanism used:** extract-to-separate-file (sub-mechanism of mech-a). **Result:** successful in lived practice — PRODUCT.md stayed at structural-doc scope; shipped log preserved as audit trail in a dedicated file. **Sub-mechanism observation:** extract-to-separate-file works cleanly when the extracted content has chronological-append shape (the source doc was carrying log-style entries, not living glossary entries). **Surfaced:** Chris-recalled at Item 16c grill, 2026-05-24; verified at substrate-write time. **Status:** worked example — closed.

2. **CLAUDE.md compaction (#352)** — 152.4 KB → 38.1 KB; per-page IA + per-column histories + design token map extracted to 4 on-demand `docs/architecture/*` + `docs/design-system.md` docs behind pointer stubs. **Why pruned:** over the 120 KB root tripwire; coffee-domain reference was standing in every-session context. **Mechanism used:** extract + split (move-never-delete). **Result:** CLAUDE.md back to the lean "how we build" core; 0 missing lines verified. **Sub-mechanism observation:** confirms the extract heuristic — on-demand-reference shape extracts cleanly; the entity-roster-vs-column-history split line is stable. **Filed as full structured case:** [docs/sprints/pruning-cases/001-claude-md-compaction.md](docs/sprints/pruning-cases/001-claude-md-compaction.md). **Surfaced:** retroactively filed 2026-06-03. **Status:** worked example — closed.

3. **CONTEXT.md zone split (#244)** — 483 KB single glossary → 3 zone docs (CONTEXT-roasting / CONTEXT-brewing / CONTEXT-shared) + a redirect stub. **Why pruned:** the mega-glossary forced claude.ai to load all zones when a session only needed one. **Mechanism used:** split (so each claude.ai zone loads only its file). **Result:** zone-scoped loading; back-compat stub preserved. **Sub-mechanism observation:** split is the right move when one doc serves multiple disjoint load-surfaces (roasting vs brewing sessions); note the zone files are themselves now living glossaries that will force consolidate/delete later (→ CONTEXT-roasting is case 002). **Surfaced:** retroactively filed 2026-06-03 (Sprint R Phase 4 Step 5, shipped 2026-05-25). **Status:** worked example — closed.

### Open candidates

1. **mcp-architecture.md scope drift → future `CONTEXT-system.md`** — the file titled "Latent Glossary — MCP / Sync Architecture" now also holds **system-level** vocabulary that is not about the claude.ai↔Latent *seam*: the self-improvement method family (Formalization tax / Graduation threshold / Arbiter-shaped / Prototype-as-input-not-canon / Concrete-example-first / Pruning shapes) plus the pre-existing **System self-improvement loop** + **Championship-mode** disposition headwords. **Why prune:** the zone-aligned CONTEXT split (roasting / brewing / shared) left no home for "the Latent system *of itself*" concepts, so this reference doc (itself extracted from CONTEXT-shared 2026-05-25) became the de-facto catch-all and its title undersells its contents. **Proposed mechanism:** re-home / split into a new system-tier glossary (`CONTEXT-system.md` or `docs/reference/self-improvement.md`). **Why not now (N=1):** Chris flagged it as a curiosity during the Item-49 grill (2026-06-05) and explicitly said *don't move it yet — wait for the doc-size tripwire to fire*; re-home is an abstraction-level structural call (breaks every `mcp-architecture.md#anchor` link, shifts the doc-tripwire registry + CONTEXT-MAP), so it goes through accumulate-then-arbitrate, not a reflex. **Surfaced:** Item-49 grill, 2026-06-05. **Status:** candidate — deferred until tripwire.

## Ratification queue

Items where Claude Code (or claude.ai) made substantive substrate calls **without Chris's audio input** — typically via a kickoff brief that framed grilling work as planned-execution work, or a session that defaulted to "ship without grilling." Each entry names the specific call that needs audio-ratification, points at the shipped PR, and proposes the ratification question.

**Procedure:** the next grilling session walks this list FIRST (before draining outstanding items). For each entry: ask Chris in long-form prose, wait for audio reply, then either ratify (move to § Resolved with the audio-confirmed details captured) or revert / ship a follow-up PR with the corrected substrate.

**Why this section exists:** Sprint R Phase 4 Step 4 Group 3 (2026-05-24) surfaced the failure mode — kickoff briefs that reference the autonomy rule or pre-pick implementation details push grilling-grade interpretive work into ship-without-sign-off territory. The convention fix lives in [`feedback_grilling_vs_executing_distinction.md`](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_grilling_vs_executing_distinction.md); this section is the durable surface that surfaces the *consequences* of past violations so they don't compound silently.

Format:

```
- **<Item> — <call made>** — shipped <YYYY-MM-DD> via [#NNN](url) without grilling. The specific substantive call: <what was decided + what alternatives existed>. Ratification question: <how to ask Chris>. Acceptable defaults landed (yes/no/partial).
```

### Open ratification entries (Sprint R Phase 4 Step 4 Group 3, 2026-05-24)


## Resolved (append-only history)

When grill items resolve, move them here with date + landing target. Format:

```
- **<Item name>** — resolved YYYY-MM-DD. Landed as: <CONTEXT.md entry / ADR-NNNN / cluster doc patch / no action>. Source PR: #NNN.
```

### Claude Design polish audit system-level items — 2026-06-12 (answers from Chris's kickoff-brief reply, executed in the polish-audit sprint)

- **Item 52 — Chrome @media exception** — resolved 2026-06-12. Chris ratified option (a) + the `lg:` move: container queries govern detail surfaces; viewport chrome (Header / BrewsFilterBar / index grids) uses Tailwind @media, and the Header + filter-bar collapse moved `md:` → `lg:` so chrome honors the two-point 390/1024 model (tablets 768-1023 get the hamburger + collapsed filters). Landed as: design-system.md § Spacing "Chrome @media exception" amendment + the two collapse edits in [components/Header.tsx](components/Header.tsx) / [components/BrewsFilterBar.tsx](components/BrewsFilterBar.tsx).
- **Item 53 — which meaning keeps #4A7C59** — resolved 2026-06-12. Chris ratified the recommendation: `resolved-emphasis` keeps the hex (most call sites, strongest semantic weight); the focus ring (`accent-light`) keeps it as interaction chrome; `--tile-next-roast` re-derived to leaf green `#5F7A33` (yellower hue, same luminance band so white face text holds) in `app/globals.css` + `tailwind.config.ts`, verified at preview against the other tile stops. Landed as: token value change + design-system.md § Palette update.
- **Item 54 — barBlocks vs confidenceFor scale unification** — resolved 2026-06-12. Chris ratified the unification: `barBlocks` fills at 1 / 2 / 3-4 / 5-7 / 8+ so HIGH (5+) lights the 4th block; the Chris-locked 1 / 2-4 / 5+ LOW/MEDIUM/HIGH thresholds stay byte-identical. ConfidenceCard's 🟢🟡🔴 emoji replaced with the white-fill 5-block bar (`.conf-bar`) in the same change; `lib/confidence.ts` emoji field removed. Visible deltas: 3-brew and 5-brew index rows each gain one block. Landed as: [components/IndexList.tsx](components/IndexList.tsx) + [components/ConfidenceCard.tsx](components/ConfidenceCard.tsx) + design-system.md / page-ia.md count-bar wording.

### Lot Coordinator dogfood Phase A — 2026-06-11

- **Item 46 — Lot Coordinator / V-Set Assistant vocabulary lock** — resolved 2026-06-11. The full term set (Roasting Coordinator / V-Set Assistant / Roasting Brief / V-set Handoff Packet / V-set Results Packet / predicted-actual boundary / three-point delta chain) landed in the Claude-Code substrate 2026-06-09: the `Vocabulary discipline` sections of [roasting-coordinator/SKILL.md](docs/skills/roasting-coordinator/SKILL.md) + [v-set-assistant/SKILL.md](docs/skills/v-set-assistant/SKILL.md) + [ADR-0024](docs/adr/0024-lot-coordinator-claude-code-native.md). The narrowed cross-ref-stub question (do the claude.ai glossaries need stubs for cross-domain packet legibility?) was grilled at dogfood kickoff: **Chris ruled NO STUBS** — the SPG / Optimized-Brew packets are self-explanatory by design (thin, explicit intent line), "reference roast" is already a CONTEXT-roasting headword, and a brewing session doesn't need the Coordinator architecture to execute its task. Zero claude.ai context tax. (Also noted during the grill: the packets land in *brewing* sessions, so any future stub would belong in CONTEXT-brewing, not CONTEXT-roasting as originally queued.) Landed as: no action on CONTEXT-*; vocab home = Claude-Code substrate.
- **Item 51 follow-up — drop-rules standard claude.ai back-prop** — drained 2026-06-11 (see item 51 in the active list above, now CLOSED): full-text mirror at [docs/skills/roasting-assistant/cluster/drop-rules.md](docs/skills/roasting-assistant/cluster/drop-rules.md), registered in lib/mcp/docs.ts, check:mcp-bundle green.

### Per-lot file registration (feedback-backlog #33) - 2026-06-06

- **Per-lot learnings / calibrations file registration ("future Tool sprint" tracker)** - resolved 2026-06-06. The close-lot.md / one-shot-closeout.md STAGE 5 inline note that flagged net-new per-lot file creation as pending "create-in-registered-directory support (future Tool sprint; tracked in docs/grilling-queue.md)" is **drained**. Landed via the **per-lot-file-registration arbiter ticket** flow (feedback-backlog #33, option (b) - human-gated, NOT option (a) auto-register): close-out STAGE 5 emits a `ticket_type: per-lot-file-registration` fenced block (lot_slug / target_path / seed_content / source); a new [ARBITER.md § Per-lot file registration tickets](ARBITER.md) section consumes it (P1-P6: read -> validate whitelisted prefix + slug match -> register SKILL_FILES + DOC_DESCRIPTIONS -> `npm run check:mcp-bundle` glob-verify -> seed file -> commit/PR/merge). Whitelisted prefixes: `skills/roasting-historian/cluster/learnings/<lot-slug>.md` + `skills/roasting-historian/cluster/one-shot-calibrations/<lot-slug>.md` (any other net-new skills/ path stays hard-rejected). Also landed: a catch-time->hint-time rejection-message improvement in `lib/mcp/propose-doc-changes.ts` (a well-formed per-lot path now points the operator at the ticket flow instead of a bare "register first"). Source PR: see [docs/sprints/per-lot-file-registration-completion.md](docs/sprints/per-lot-file-registration-completion.md).

### Resync grill — 2026-05-31 (record: [docs/sprints/grilling-2026-05-31-resync.md](docs/sprints/grilling-2026-05-31-resync.md))

- **Item 42 — Per-surface mobile pattern → ADR** — resolved 2026-05-31. Landed as: [ADR-0018](docs/adr/0018-per-surface-mobile-pattern.md) (IA-first framing — one shared IA, mobile as forcing function; two techniques `order-*` single-tree vs container-query dual-subtree; infra = container queries + 390/1024) + new design-convention term **"Mobile as forcing function (single shared IA)"** in CLAUDE.md § Design conventions + PRODUCT.md § Design System.
- **Item 43 — Detail-view hero-tile reconciliation** — resolved 2026-05-31. Decided: the detail hero cover **reconciles ONTO the lifecycle `--tile-*` gradient** (per state — sage / olive-bronze / roasted-brown / grey, matching the index card); the **in-content emphasis accents STAY** (amber drop-temp + drop-rules, lavender predicted-cup + producer-notes, green resolved verdict + reference-cup). Two separated color axes: hero = lifecycle state, accent = task salience. Recorded in CLAUDE.md § Design conventions; small `SspNamePlate` cover-rebind touch-up on the 3 green detail views QUEUED (post-grill).
- **Item 39 residual — "Pour step" glossary term** — resolved 2026-05-31. Landed as: new **"Pour"** headword in CONTEXT-brewing.md (mechanical liquid-addition event; stored `brews.pours[]`; bloom = index 0; mid-pour valve changes stay inside one pour via `detail`/`valve`) + a reconciliation appended to the **Phase** _Avoid_ note. Named "Pour" not "Pour step" — Chris does not use "step"; it is a code-only artifact of the `PourStep` TS type. Phase clarified as the expansive design-time grouping (can span multiple pours: intensity phase = bloom + first pour).
- **Item 36 — "Strategy zone" / "Wrong-zone trap" anchoring** — resolved 2026-05-31. **No action.** Grep confirmed both are first-class CONTEXT-brewing headwords; their absence from operational-guide / prompts is the intended "glossary names the meta-concept, operational docs run it procedurally" layering, not drift. No anchor needed, nothing to drop.
- **Item 40 — Process-registry arbiter-queue reminder + honey-subprocess description** — decided 2026-05-31 (execution queued, not run in grill). Approach locked: blank process content (`SignatureEntry.overview` / `BaseProcessEntry.summary` / `brewArchetype` / `ModifierEntry.overview`) joins the **skeleton/arbiter queue via derive-from-missing-overview** (no explicit flag), surfaced in `list_skeleton_entries` as a low-priority reminder; honey subprocess = a **lightweight description map** (persist only when the roaster states the honey process). Both fold into the expanded **taxonomy-fill side-quest** (PRODUCT.md § Roadmap, side quests). The blank `/processes` XO overview is the live example.
- **Item 35 — Drop-rules persistence / roast-time HUD** — triaged + re-graded 2026-05-31 (READY → folded, no standalone slot). Sub Pages 6.8 (2026-05-24) already mitigated the acute "hidden on cupping view" pain (drop rules now render collapsed on cupping + resolved views). The residual roast-time HUD **+ the drop-rule-as-deviation-attribution-key need** (claude.ai couldn't explain manual-hold-vs-auto-drop because the rule didn't persist) fold into the **predicted-vs-actual delta tier**. Not Cluster A (that's cross-domain modeling).
- **XO-as-signature substrate fix** — resolved 2026-05-31. **No substrate work needed** — XO already a signature canonical (process-registry.ts:352 + processes.md:115 + roadmap), and brew `a116bb9c` (CGLE Mandela XO Batch 139 Reference) is correctly tagged `signature_method:'XO'`. Stale "make XO a signature method" roadmap side-quest **dropped**. Residual = the blank `/processes` XO overview (skeleton) → folds into Item 40.
- **Doc-size tripwire review** — done 2026-05-31. Measured: **CLAUDE.md 149KB (OVER by 29KB)** — prior "~114KB / under tripwire" was badly stale; **PRODUCT.md 122KB (marginally over)** — prior "~140KB" also stale (it's smaller); CONTEXT-roasting.md 105KB (watch). Stale figures corrected in CLAUDE.md § Standing tripwires. **Root-doc compaction/split sprint QUEUED** (Cluster B, interleaved alongside Cluster A): CLAUDE.md primary (extract the giant per-page IA blocks for `/brews/[id]` + `/green/[id]` → `docs/architecture/page-ia.md` or per-surface docs, pulled on demand) + PRODUCT.md + a "what else moves out" review pass. **Compaction sprint SHIPPED 2026-06-02** (Cluster B item #2, [shipped.md](docs/sprints/shipped.md)): CLAUDE.md **152.4KB → 38.1KB**, PRODUCT.md **132KB → 114KB**. Extracted 4 on-demand docs — `docs/architecture/page-ia.md` (per-page IA), `docs/architecture/data-model.md` (column histories + relationships), `docs/architecture/registries.md` (canonical-registry impl + flavor), `docs/design-system.md` (full token map, shared by both root docs). Move-never-delete; pointer stubs left for every extracted section. Tripwire figures in CLAUDE.md § Standing tripwires updated to the post-compaction reality.
- **Items 37 + 38 + 5b → consolidated into the "claude.ai grilling review"** — 2026-05-31. New standing rule [`feedback_claude_ai_grilling_review`](~/.claude/projects/-Users-chrismccann-latent-coffee/memory/feedback_claude_ai_grilling_review.md): a 3-part recurring check at every grilling-session close — (1) **memory + instruction currency** [Item 37], (2) **cross-party claude.ai grill**, (3) **context-window usage check**, roasting-emphasized [absorbs Item 38's load-creep trigger detection + Item 5b's empirical context-window measurement]. Item 38 + 41 remain OBSERVING in the outstanding queue. Lands as a Cluster B member (PRODUCT.md § Roadmap). The Item 37 paste-ready memory block gets finalized in the first run of this protocol.

- **Item 1 — CCIL framing vocabulary** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). Landed as: (a) new CONTEXT.md entry **Variety throughline** (per-layer mechanics folded in); (b) source-diversity refinement appended to existing **Cross-coffee insight layer** entry (self-roasted lots count as N=1 source per bean); (c) new file `docs/skills/ccil/cluster/observing.md` holding 3 candidate structural concepts (process throughline, process-scoped-vs-variety-scoped hypothesis class, cross-domain tension as CCIL section convention); (d) ARBITER.md § CCIL observing list review extending standard `process pending arbitration` to walk the observing list. Source PR: [#232](https://github.com/chrismccann-dev/latent-coffee/pull/232).
- **Item 2 — Master-doc redirect stub vocabulary** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). Landed as 3 new CONTEXT.md entries in § MCP / Sync Architecture: (a) **Redirect stub** (what BREWING.md / ROASTING.md / docs/taxonomies/sworks.md are post-Wave-4-PR-4b — h1 + pointer table, permanent state, anchor back-compat preserving); (b) **Cluster path** (`docs/skills/<sub-skill>/cluster/<file>.md` URI shape used in `docs://` Resources + `propose_doc_changes` target_doc); (c) **Operator-guide / operational-guide / onboarding-protocol / operational-reference** (four cluster-leaf file names + their tier+domain scoping, with the operational-guide vs onboarding-protocol asymmetry explained as a real structural difference reflecting Chris's roasting-state-heavy vs brewing-sequential framing). Open rename question for workflow-sub-skill files split out to grilling-queue item 2a (OBSERVING). Source PR: [#232](https://github.com/chrismccann-dev/latent-coffee/pull/232).
- **Item 3 — 3-tier sub-skills vocabulary** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). Landed as: (a) one consolidated CONTEXT.md entry **Sub-skill tiers + Master Coordinator + Chris's vocabulary aliases** locking the 3 tiers + plan/execute Workflow split + Chris's "Expert" / "Zone" lived-speech aliases + Master Coordinator role + 4 forbidden conflations; (b) temporal constraint appended to existing **Cross-coffee insight layer** entry — CCIL fires only AFTER complete cycles (lot close / brew archive), never during in-flight workflows; (c) promoted grilling-queue item 16 (CONTEXT.md zone split) from OBSERVING to next-sprint candidate, locking option (a) — split into `context-roasting.md` + `context-brewing.md` + `context-shared.md` aligned to Zone vocabulary; (d) new outstanding queue item 16b (sub-skill rename to `<X> Expert` framing, OBSERVING, defer pending sustained lived-speech evidence). Source PR: [#232](https://github.com/chrismccann-dev/latent-coffee/pull/232).
- **Item 4 — Per-lot directory structure** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). Landed as: (a) new CONTEXT.md entry **Per-lot directory taxonomy** (active-lots / one-shot-calibrations / learnings 3-dir model + redirect-stub close-out convention; brewing zone scoped out — no per-lot dirs there); (b) new CONTEXT.md entry **Three big levers** — major load-bearing concept Chris articulated this turn: production lever (biggest, Chris can't touch) / roasting lever (medium, V-set iterative reach) / brewing lever (smaller, final polish); explains why one-shot lots pull the brewing lever harder; (c) updated `docs/skills/roasting-historian/cluster/active-lots/README.md` to flip the "delete on close" convention to "redirect stub on close" per lived practice; (d) new outstanding grilling-queue item 16c — **Substrate pruning discipline** (Chris-flagged meta-pattern; section-level pruning mechanism design; BRAINSTORM, future sprint). Source PR: [#232](https://github.com/chrismccann-dev/latent-coffee/pull/232).
- **Item 5 — Pattern F decomposition tripwires** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). Landed as: (a) new ADR-0014 — Pattern F pattern-aware threshold tiers (amends ADR-0013); 7-tier table with Historian 250KB / Archivist 200KB / Equipment 150KB / Workflow 100KB / Coordinator 80KB / CCIL 150KB / root-doc 120KB caps; resolves ADR-0013 internal ambiguity (120KB vs 60KB cluster threshold); locks the claude.ai-context-window-as-binding-constraint reasoning; (b) new CONTEXT.md entry **Pattern F decomposition tripwires (pattern-aware tiers)** with the threshold table + pointer at ADR-0014; (c) 2026-05-23 lived audit: 3 previously-"exceeded" clusters now compliant under pattern-aware tiers (roasting-historian 192/250, brewing-historian 188/250, brewing-equipment-expert 144/150); PRODUCT.md (140KB / 120KB root) flagged as parallel-session compaction candidate; 4 clusters approaching tripwire surfaced as watch-items; (d) new outstanding queue items 5a (PRODUCT.md compaction parallel session) + 5b (empirical claude.ai context-window measurement cross-party grill). Source PR: [#232](https://github.com/chrismccann-dev/latent-coffee/pull/232).
- **Item 9 — Recipe-noun discipline triple verification** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). **No action — common understanding confirmed.** CONTEXT.md lines 33-43 already lock the triple (Recipe = roast_recipes row / Roest profile = JSON machine artifact / anchor profile = curve-shape transferable framework) with `_Avoid_` directives against the known drift patterns. Spot-check of CGLE SR Natural V5 closed-lot learnings shows "profile" prose hewing to the canonical curve-shape sense per CONTEXT.md line 42-43's preserved curve-shape references (CF-Light profile / washed profile / natural profile). V5 lived usage validates the lock — no drift, no edits needed.
- **Item 11 — Slug-convention drift active-lots vs learnings** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). Chris locked Option (a): **canonical lot code (lowercase-kebab) across all 3 per-lot directories** (active-lots/ + learnings/ + one-shot-calibrations/). Same slug across all dirs means trivial cross-ref + matches substrate identifiers (Roest profile names, batch IDs, experiment_ids) + year suffixes prevent collision on re-purchase. Landed as: (a) 7 file renames via `git mv` in `docs/skills/roasting-historian/cluster/learnings/` — cgle-mandela-xo.md → cgle-mandela-xo-2026.md; cgle-sudan-rume-hybrid-washed.md → cgle-srume-washed-2026.md; gua-el-socorro-java.md → gua-soc-java-2024.md; gua-libertad-aurelio-del-cerro.md → gua-lib-adc-2024.md; gv-oma-lot-25-035.md → gv-oma-25-035.md; gv-surma-lot-25-039.md → gv-surma-25-039.md; rancho-tio-emilio.md → ecu-td24-ranchotio-tm-washed.md; (b) 14 cross-file ref updates across `lib/mcp/docs.ts` (Resource catalog) + 6 cluster docs (siblings + patterns/by-process/washed.md + patterns/by-process/honey.md + patterns/by-cultivar/gesha.md + active-lots/redplum-cas-2026.md + one-shot-calibrations/README.md); (c) **Slug convention** section appended to CONTEXT.md § Per-lot directory taxonomy locking the convention. shipped.md + grilling-queue.md historical references intentionally left as-is (audit-trail integrity — descriptive prose of past sprints reflects what was named at the time). Source PR: [#232](https://github.com/chrismccann-dev/latent-coffee/pull/232).
- **Item 19 — Pivot-destination heuristics CCIL promotion** — resolved 2026-05-23 (Sprint R Phase 4 Step 4 grill). **OBSERVING — relocated to CCIL observing list, no substantive grilling decision needed.** Current state: heuristics correctly housed in brewing-assistant operational-guide Step 3; N=1 lived pivot (Wush Wush Brew 2 → Brew 3) validates the "single-axis loud" → Suppression heuristic. Promotion fires once N≥2-3 strategy pivots across different residual-problem shapes confirm the mapping holds. Landed as: new entry in [docs/skills/ccil/cluster/observing.md § Pivot-destination heuristics](docs/skills/ccil/cluster/observing.md) so the standard `process pending arbitration` arbiter walk surfaces this candidate alongside the other observing-list items. No code/doc edits to operational-guide; the candidate lives in observing.md until promotion threshold met.
- **Item 22 — Terroir resolver cross-country fuzzy-match bug** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill, item-22 standalone PR). Landed as: (a) 3 new helpers in `lib/terroir-registry.ts` — `canonicalizeMacroInCountry(country, macro)` country-scopes the resolver candidates and aliases; `macrosForAdminRegion(country, candidate)` surfaces the registered macros when a caller types an admin_region as if it were a macro; `adminRegionsForCountry(country)` returns the distinct admin_regions for a country; (b) `findOrCreateTerroir` in `lib/brew-import.ts` swapped from `TERROIR_MACRO_LOOKUP.findClosest` (country-agnostic) to `canonicalizeMacroInCountry` (country-scoped), with the admin_region hint appended to the error when the input matches an admin_region — closes the lived "Minas Gerais → Mindanao Highlands" cross-country drift; (c) new `scripts/check-terroir-resolver.ts` permanent regression guard wiring 10 cases (admin_region misuse, cross-country drift candidate "Sidama" under Panama, alias country-scope, unknown country, empty input, round-trip via `getTerroirEntry`); (d) `npm run check:terroir-resolver` wired in package.json. tsc clean. Source PR: [#233](https://github.com/chrismccann-dev/latent-coffee/pull/233).
- **Item 14 — Brewer-rotation tiebreaker logic** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill). Landed as: (a) new "Decision rule when two brewers fit the cup goal" subsection in `docs/skills/brewing-equipment-expert/cluster/operational-reference.md` directly under the Brewer System table — names the lived April vs Kalita case from Wush Wush brewing iteration with the geometry context (April wider base → integration + softness on really expressive coffees with crazy flavors needing pull-together; Kalita 155 narrower metal base → integration with preserved sharpness on coffees that aren't inherently as expressive), flags the lived-experience caveat (no controlled neutral test yet), and generalizes the principle ("drill into the secondary register"); (b) cross-ref pointer added to brewing-assistant operational-guide Step 2 so the decision is discoverable from the in-flight BREW PROMPT path. Rotation-debt demoted to follow-on tiebreaker per Chris's framing. Source PR: [#235](https://github.com/chrismccann-dev/latent-coffee/pull/235).
- **Item 7 — Path C-2 naming post-fire** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill). **Significant scope expansion from brief framing.** Chris's audio reply confirmed simulated-pourover gate is a routine end-of-V-set workflow (not edge-case ambiguity disambiguation as the original Path C-2 framing implied) and surfaced the schema preference at vocabulary lock time. Landed as: (a) full rename Path C-2 → Simulated Pourover Gate across `CONTEXT.md` (rewrote the C-2 bullet under "Pre-V_n calibration gate" with Chris's reframed shape — end-of-V-set decision-support cup on real pourover setup with non-optimized close-to-end-state recipe + the V_n winner + secondary + V_(n-1) winner cup-set + the xBloom-vs-end-cup motivation history); (b) `docs/skills/cupping-specialist/SKILL.md` § Path routing updated; (c) `docs/prompts/log-cupping.md` STAGE 4 fully rewrote the path section with proactive + reactive trigger split (proactive: nearing reference, lived end-of-V-set; reactive: cup-side variant inversion — with wide WB-to-ground delta as a roast-quality concern that may motivate the SPG read, NOT a routing trigger per Item 8 framing) — preserves the lived V5 (CGLE Sudan Rume) + Higuito V3 cases; (d) `docs/skills/cupping-specialist/cluster/pod-1-routing.md` § Today's substrate + § Lived-practice trigger fires + § Rewrite direction options + § Schema scoping + § Punted items all updated — schema preference locked at vocabulary level (`cuppings.eval_method = 'Simulated Pourover'` per Chris's "should fit cleanly in the cupping method"); migration deferred to full POD-1 rewrite sprint; trigger conditions #1 + #3 effectively advance via retroactive lived-practice recognition; trigger #2 confirmed N/A (one-shot has 1 cup read, no simulated-pourover need). Historical "Path C-2" references in sprint/archive docs preserved (audit-trail integrity per Item 11 convention). Source PR: [#235](https://github.com/chrismccann-dev/latent-coffee/pull/235).
- **Item 8 — WB-to-Ground delta as primary signal** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill). **Relocated to CCIL observing list** matching the Group 1 pattern (Items 1, 19) and the brief's OBSERVING grade. Landed as: (a) new entry `Wide WB-to-Ground Agtron delta as roast-quality negative signal` in [docs/skills/ccil/cluster/observing.md](docs/skills/ccil/cluster/observing.md) capturing Chris's framing — thin delta is positive affirmation of clean internal development; wide delta is potential negative affirmation suggesting internal underdevelopment + repeatability concern — plus the origin story (Chris's non-counterflow → counterflow mode switch surfaced fast-roasted-internally-underdeveloped failure mode; WB Agtron at roast + Ground Agtron at cupping became the surface/core split signal); promotion trigger requires 2-3 more cases plus disambiguation between a CCIL roast-quality pattern doc vs an SPG motivating-factor in pod-1-routing.md; (b) Item 7 substrate corrected — SPG reactive-trigger framing in CONTEXT.md + log-cupping.md + cupping-specialist/SKILL.md softened to clarify wide delta is **primarily a roast-quality signal, not a routing-decisive SPG trigger** (the lived V5 SPG fire was motivated by the roast-quality concern; SPG was the operator's validation response, not a deterministic delta-magnitude routing rule). Existing magnitude tier framework at counterflow-observations.md (≤3 even / 4-6 working / >7 wide) stays as the substrate-level interpretation table. Source PR: [#235](https://github.com/chrismccann-dev/latent-coffee/pull/235).
- **Item 15 — Medium-roast specialty natural pattern promotion** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill). **Relocated to CCIL observing list** matching the Group 1 pattern (Items 1, 19) and the brief's OBSERVING grade. Landed as: new entry `Medium-roast specialty natural over-extraction lever` in [docs/skills/ccil/cluster/observing.md](docs/skills/ccil/cluster/observing.md) — names the over-extraction-risk-is-roast-character lever (evaluate cooler + accept chocolatier register, NOT push harder) with Wush Wush (Untold) as the N=1 seed observation. Chris-flagged at grill: he has a couple of inventory lots with darker-than-normal readings expected to land in brewing rotation, so the candidate is likely to advance to a brewing-historian CCIL entry promotion soon. Operational-guide § Roaster roast-level hook stays as the runtime guidance until threshold met. Source PR: [#235](https://github.com/chrismccann-dev/latent-coffee/pull/235).
- **Item 29 — Ground-Agtron-not-WB rule for extraction prior** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill). Landed as: new sub-block in [brewing-assistant operational-guide.md § Step 1d](docs/skills/brewing-assistant/cluster/operational-guide.md) — **Ground-Agtron-over-WB extraction-prior rule** + **Measurement-availability asymmetry by source** captures Chris's lived asymmetry. For SELF-roasted lots, the ground Agtron at cupping time (`cuppings.ground_agtron`) is the authoritative brewing-side extraction prior over the WB read; for PURCHASED roasted lots, ground Agtron is deliberately NOT measured (Chris-confirmed overkill decision after asking claude.ai at inventory step); the brief falls back to WB Agtron + roaster-stated roast level. Lived case (CGLE Sudan Rume Natural brewing iteration, Round 13): WB 78.7 read as light → finer/warmer hedge over-extracted; ground 68.1 at dose-out was the correct prior. Cross-refs counterflow-observations.md § WB-to-Ground Agtron Delta as Development Signal for the surface-vs-core mechanism. Explicit "do NOT push Chris to start measuring ground on purchased lots" guard against future claude.ai sessions re-litigating the operator decision. Source PR: [#235](https://github.com/chrismccann-dev/latent-coffee/pull/235).
- **Item 28 — Temperature primacy extends to traditional natural aromatic landraces** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 2 grill). **Relocated to CCIL observing list** per brief's OBSERVING grade. Landed as: new entry `Temperature primacy extends to traditional natural aromatic landraces` in [docs/skills/ccil/cluster/observing.md](docs/skills/ccil/cluster/observing.md) — names the candidate generalization from the existing 4-origin anaerobic-natural pattern (Colombia / Ethiopia / Panama / Picolot Simba Cold Room) to traditional (non-anaerobic) natural aromatic landraces. CGLE Sudan Rume Natural Batch 187 (Round 13, 91°C → 89°C single-variable temperature resolve) is the N=1 seed. Promotion trigger: 2-3 more traditional-natural aromatic landraces confirm; promote by broadening the existing brewing-historian `Anaerobic-Natural Suppression + Temperature-Primacy pattern` to `Aromatic-landrace + natural process Suppression + Temperature-Primacy pattern`. Source PR: [#235](https://github.com/chrismccann-dev/latent-coffee/pull/235).
- **Item 5a — PRODUCT.md compaction sprint** — resolved 2026-05-24 (parallel session, separate from the Group 2 grill). Landed as: PRODUCT.md compacted from 122KB → 93KB (24% reduction, under 120KB tripwire per ADR-0014). Parallel-session execution per Chris's standing offer at Item 5 grill — fired by a sibling session while the Group 2 grill drained operational-rules items in this thread. Source PR: [#234](https://github.com/chrismccann-dev/latent-coffee/pull/234).
- **Item 27 — SL14 + Nyasaland canonical promotion** — resolved 2026-05-24 (arbiter pass, separate from the Group 2 grill). **No registry edits needed in Group 3 — already shipped.** Both raw_value entries promoted through the standard `process pending arbitration` arbiter walk on 2026-05-24T03:23 UTC: `SL14` promoted as canonical (Modern Hybrids / Multi-parent hybrid lineage; `SL-14` aliased to canonical); `Nyasaland` promoted as canonical (Typica Family / Typica lineage; Typica-from-Jamaica origin anchored over the earlier evidence-note Bourbon-derived suggestion). Both `lib/cultivar-registry.ts` mirror entries + `docs/taxonomies/varieties.md` rich-content entries already in place. Queue entry status `pending` → `promoted` on both. Grilling-queue drain in this Group 3 PR is documentation-only — surfaced as `READY` in the brief but the work itself had already been completed by the arbiter the day before.
- **Item 31 — `fc_audibility` enum extension** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 3 grill). Landed as: (a) migration 066 — `roasts.fc_audibility` CHECK constraint extended with 5th value `did_not_fire` for the underdeveloped-low-energy-probe case (bean did NOT reach FC temperature); (b) `lib/mcp/push-roast.ts` + `lib/mcp/patch-roast.ts` Zod `.enum([...])` arrays extended + descriptions updated with the new value's semantics + the "fc_start/fc_temp/dev_time_s MUST be null" co-rule; (c) `CONTEXT.md § FC audibility state` rewritten from 4-value to 5-value enum + `did_not_fire` distinguished from `silent` (event didn't happen vs event happened without sound) + `_Avoid_` block extended to warn against coercing `did_not_fire` into `ambiguous`; (d) `docs/prompts/log-roast.md` STAGE 2 `fc_audibility` bullet restructured as a labeled 5-bullet list + STAGE 3 "Profile trigger never fired" case updated from `ambiguous` to `did_not_fire`; (e) `docs/prompts/one-shot.md` STAGE 2-3 same pattern. Pre-Round-14 underdeveloped probes that were forced into `ambiguous` may be patched to `did_not_fire` via `patch_roast` when re-reviewed; no automated rewrite.
- **Item 32 — Maillard% / dev% rule when fc_start is null** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 3 grill). **Reframed as prompt-level convention, NOT schema rule.** Pre-flight inspection: `roasts.maillard_pct` and `roasts.dev_pct` are NOT stored columns on the `roasts` table — they're values computed in claude.ai prose (`observed_outcome_*` / `delta_from_roast_*` / `roast_learnings.what_didnt`) from the curve via `(fc_start - yellow_time) / drop_time × 100` (and the dev variant). The brief's "computed column / check constraint" options assumed columns that don't exist; the right fix is a prose convention that the calculator must respect. Landed as: (a) `CONTEXT.md § Maillard %` extended with a "Computation rule on `did_not_fire` batches" sub-section locking the "record 'Maillard % N/A (FC did not fire)' instead of the computed artifact" convention + `_Avoid_` block extended; (b) `docs/prompts/log-roast.md` STAGE 3 "Profile trigger never fired" case includes the Maillard % N/A directive alongside the `did_not_fire` enum-value mention (paired since both fire on the same case); (c) `docs/prompts/one-shot.md` STAGE 2 same. Schema-level constraint on `dev_time_s IS NULL OR fc_start IS NOT NULL` could be added as a follow-on optimization but the prose convention is the load-bearing fix.
- **Item 33 — `subtle` FC dev_time handling** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 3 grill). **Trust the timestamp.** Bukure v2b had a real `fc_start` at 5:27 with 1 crack — a snap fired even though the canonical multi-snap signature didn't, and 18s dev computed off that fc_start is the right value. Landed as: (a) `CONTEXT.md § FC audibility state § 2. Subtle` rewritten to explicitly state "Treated operationally as not-audible for protocol-stack purposes BUT the FC timestamp itself is real and trustable when a specific fc_start fires — record `fc_start` / `fc_temp` / `dev_time_s` from the timestamp as captured" + Round 14 Bukure v2b case-study reference; (b) `docs/prompts/log-roast.md` STAGE 2 `fc_audibility` `subtle` bullet expanded with the same clarification. Subtle is structurally distinct from `did_not_fire` (event happened weakly vs event didn't happen) — the not-audible operational-protocol coercion was over-extending into the timestamp-trustability question.
- **Item 34 — `is_reference_candidate` timing convention** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 3 grill). **`log-cupping.md` STAGE 3 owns the flag; `log-roast.md` STAGE 3 must leave it unset.** Cup-grounds only, never roast-grounds. The flag's definition ("reads as plausibly the lot reference at close-out") is by construction a cup-quality judgment; roast structure alone can mislead — late-blooming aromatic lots can roast cleanly without being cup-reference quality. Gesha Clouds v3a was the lived case (flagged true at roast-time on closest-to-#172 reasoning; "that was a guess"). Landed as: (a) `CONTEXT.md § Reference candidate` extended with a new "**Timing convention**" paragraph locking the rule + Gesha Clouds case study; (b) `docs/prompts/log-roast.md` STAGE 3 push_roast bullet block extended with an explicit `is_reference_candidate: leave unset (NULL)` directive with rationale; (c) `docs/prompts/log-cupping.md` STAGE 3 § Mark the leading slot as a reference candidate strengthened with a "**This prompt owns the flag**" lead paragraph + the convention pointer; (d) `docs/prompts/one-shot.md` STAGE 2-3 `push_roast` bullet includes the leave-unset convention with the one-shot-specific note (the candidate flag is rarely meaningful pre-closeout on one-shots since they collapse the V-set lineage, but the never-at-roast-time rule still applies).
- **Items 21 + 23 — Tool description hygiene (collision pattern + runtime-behavior accuracy)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 3A grill, paired drain). **Substrate output: convention lock at [ARBITER.md § Tool description writing convention](ARBITER.md).** The convention has 4 parts — (1) vocabulary deferrals point to substrate (CONTEXT.md / ADR / schema column comment), not sibling Tools; (2) sibling create-vs-update refs use descriptive language ("companion INSERT path of the same domain") not Tool naming; (3) workflow-chain refs MAY be retained when load-bearing; (4) read-only Tools must NEVER reference write Tools (the Round 11 BLOCKING case). Item 23 accuracy convention bundled in same section — descriptions reflect the **strictest validation path**, not the most permissive (lossy summary causes session-time expectation drift). **Audit + sweep landed**: rewrote 12 pure vocabulary deferrals across `patch-cupping.ts` (3 fields: sweetness / aromatic_behavior / structural_behavior), `patch-roast.ts` (6 fields: roest_notes / end_condition_type / fc_audibility / worth_repeating / is_reference / is_reference_candidate), `patch-green-bean.ts` (1 field: is_one_shot), `patch-roast-recipe.ts` (2 fields: was_backfilled / backfill_notes); rewrote 7 sibling-Tool description references in `patch-cupping.ts`, `patch-experiment.ts`, `patch-roast.ts`, `patch-roast-learnings.ts`, `patch-roast-recipe.ts`, `patch-inventory.ts`, plus 3 push-side "use patch_X" boilerplate refs in `push-brew.ts`, `push-roast.ts`, `push-green-bean.ts`, `push-cupping.ts`; fixed 1 read-only-→-write-Tool reference in `canonical-tools.ts` (read_canonical "before push_brew" → "BEFORE composing any write payload"). tsc clean. The agent-driven first audit pass identified 1 file with explicit collisions + 10 with possible accuracy drift; pre-flight grep surfaced the deeper deferral pattern across all `patch-*.ts` files (the agent had under-counted by missing the field-level deferrals). Net: ~25 description edits + 1 convention lock + queue drain.
- **Item 31 — `fc_audibility` 5th value name `did_not_fire` + substrate enrichment** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 1 Item 31). **Audio-ratified by Chris.** Name `did_not_fire` ratified as canonical 5th enum value over alternatives `no_fc` / `sub_threshold`; cosmetic inconsistency with the bare-word 4-value style is an accepted cost for the descriptive verb-structure that preserves the event-vs-non-event distinction unambiguously. **Follow-up substrate enrichment landed** (Gaps 1 + 2 question): (a) [CONTEXT.md § FC audibility state](CONTEXT.md) `did_not_fire` entry rewritten to layer both grounds (detection-failure surface signal + bean-temp endpoint disambiguator vs `silent`), with a new **Disambiguation rule** sub-block routing the three no-detection states (`did_not_fire` / `silent` / `ambiguous`) by bean-temp endpoint + operator hedging tone; (b) [docs/skills/roest-knowledge/cluster/protocols/fc-marking.md](docs/skills/roest-knowledge/cluster/protocols/fc-marking.md) extended with new **Roest auto-detect mechanism** section covering the 195°C activation threshold + 75% sensitivity setting + two-or-more snaps confirmation rule + visual band marking + operator override pattern + recipe-construction implication (dev-time-anchored vulnerable to FC-detection failures; drop-at-bean-temp robust); the stale "4-value enum" footer reference updated to "5-value enum (audible / subtle / silent / ambiguous / did_not_fire)" with cross-ref to the disambiguation rule. The enrichment closes Chris's elaboration that `did_not_fire` operationally means "neither machine auto-detect nor operator manual mark fired, AND bean-temp endpoint supports the read" — the surface detection signal aligns with the structural read.
- **Item 32 — Maillard% prose convention + DB CHECK constraint follow-on** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 1 Item 32). **Audio-ratified by Chris.** Prose convention shape (`record "Maillard % N/A (FC did not fire)" instead of emitting the bogus computed number`) ratified as already-shipped from PR #236. **Alternative 2 ratified as follow-on**: DB CHECK constraint added via [migration 068](supabase/migrations/068_did_not_fire_null_co_rule.sql) (`roasts_did_not_fire_nulls_check`) enforcing the triple-null co-rule (`fc_audibility = 'did_not_fire' ⇒ fc_start IS NULL AND fc_temp IS NULL AND dev_time_s IS NULL`) at the DB layer — closes the gap that the Tool-description prose said "MUST be null" but no programmatic enforcement existed (Zod schemas were `.optional().nullable()` with no cross-field refinement; raw SQL writes had no guard). Migration is idempotent (DROP/ADD pattern) + pre-flight DO $$ block asserts zero existing violations before applying (today: zero `did_not_fire` rows exist, the lived underdeveloped probes Bukure v2a / Mt Elgon 199 were pushed as `ambiguous` pre-066 and not yet patched). **Substrate cross-refs landed**: (a) [CONTEXT.md § Maillard %](CONTEXT.md) `Computation rule on did_not_fire batches` sub-block extended to name the DB constraint as the data-integrity-time guard (calculator-output-time fix + data-integrity-time guard as belt-and-suspenders on the same invariant); (b) [docs/skills/roest-knowledge/cluster/protocols/fc-marking.md](docs/skills/roest-knowledge/cluster/protocols/fc-marking.md) extended with **Roest UI behavior on no-FC batches** sub-block capturing Chris's audio screenshot evidence (Red Plum v2a batch #196: FC time N/A / N/A, Dev time N/A, phase breakdown shows only drying 41.7% + Maillard 58.3% with no dev segment) — the Roest machine's own UI is the model the Latent data layer mirrors. Bundled into this PR (not separate follow-up) per minimal-cost framing of the locked decision.
- **Item 33 — `subtle` FC: trust the timestamp + dual-threshold operator-mark discipline** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 1 Item 33). **Audio-ratified by Chris.** Trust-the-timestamp rule ratified as-shipped from PR #236 — when a specific fc_start fires on a subtle batch, record `fc_start` / `fc_temp` / `dev_time_s` from the timestamp; do NOT null them for operational symmetry with silent / ambiguous. Subtle is bean-property-grounded (cellulose modification reduces but doesn't eliminate snap expression); a snap that mechanically fires is a real cracking event by definition. Operational symmetry argument (null timestamps on subtle for uniform shape across the 4 not-audible states) rejected — conflates the data layer (what to record about the batch) with the protocol layer (how to react to the batch in recipe design). **Follow-up substrate enrichment landed** (Chris's audio elaboration on operator-mark discipline): (a) [docs/skills/roest-knowledge/cluster/protocols/fc-marking.md § Operator override pattern](docs/skills/roest-knowledge/cluster/protocols/fc-marking.md) extended with new **Manual-mark discipline (dual-threshold rule)** sub-block — the operator-mark is legitimate when both temperature window (above 200-202°C) AND timing window (near expected FC range, e.g. ~4:30) cross threshold simultaneously; in that joint window the operator does NOT have to wait for the machine's two-crack confirmation; a single snap in the wrong temperature zone (below 200°C) or wrong timing window is suspect and must NOT be marked; (b) [CONTEXT.md § FC audibility state § 2. Subtle](CONTEXT.md) extended to cite the dual-threshold discipline as what makes subtle-batch timestamps trustable + cross-ref to fc-marking.md. The Bukure v2b 5:27 manual mark with 1 snap + 18s dev is the canonical lived case — past 200°C, near expected FC window, so the snap WAS the event even though the canonical multi-snap signature didn't fire.
- **Item 34 — `is_reference_candidate` cup-grounds-only ownership + Pre-declaration discipline for `is_reference` declaration** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 1 Item 34). **Audio-ratified by Chris.** Cup-grounds-only ownership of `is_reference_candidate` ratified as-shipped from PR #236 — log-cupping.md STAGE 3 is the exclusive owner, log-roast.md STAGE 3 leaves the flag NULL. The Gesha Clouds v3a "that was a guess" precedent supports never-at-roast-time; single-owner discipline prevents drift; the flag's definition (cup-quality judgment) is by construction cup-grounded. **Follow-up substrate enrichment landed** (Chris's audio elaboration on `is_reference` declaration discipline + Cupping Specialist push-back duty): (a) [CONTEXT.md § Reference roast](CONTEXT.md) extended with new **Cross-domain workflow-transition gate** paragraph — declaring `is_reference = true` is the load-bearing gate that transitions a lot from roasting-side to brewing-side workflow; declaration is intentionally definite, not ambiguous (`is_reference_candidate=true` is hedged mid-flight, `is_reference=true` is the commit); cross-ref to the Cupping Specialist's push-back duty + one-shot exemption note; (b) [docs/skills/cupping-specialist/SKILL.md](docs/skills/cupping-specialist/SKILL.md) extended with new **Pre-declaration discipline (Path A push-back duty)** top-level section — Cupping Specialist MUST push back on V-set Path A when any of 3 pre-conditions is missing (multiple V-set iterations / full Day 7 xBloom cupping / Simulated Pourover Gate cup-set on V_n winner + secondary + V_(n-1) winner); standard push-back language locked verbatim: *"If you really want to declare this a reference roast, I suggest we do a simulated pourover recipe first."*; one-shot exemption (structural — single roast / single cup read means no SPG cup-set possible); Pattern H audit signal logging on Path A overrides; (c) Path A description at SKILL.md line 26 extended with inline pointer to the push-back duty section; (d) [docs/prompts/log-cupping.md § Path A](docs/prompts/log-cupping.md) STAGE 4 extended with Pre-declaration discipline check + the 3 pre-conditions + the standard push-back language verbatim + one-shot exemption note + cross-ref to the cupping-specialist sub-skill spec; (e) [docs/prompts/one-shot-closeout.md § STAGE 2](docs/prompts/one-shot-closeout.md) extended with explicit One-shot exemption header — `is_reference: true` lands directly without push-back gate; the cross-domain transition framing still applies. The push-back duty creates a structural quality gate at the boundary where the cross-domain transition begins, matching Chris's "call it correct and not have that part be ambiguous" framing.
- **Items 21 + 23 — Tool description hygiene 4-part convention + #4a crash-recovery carve-out + opening principle** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 1 Items 21+23). **Audio-ratified by Chris (all 3 sub-questions).** Sub-question (a) strict #4 rule (no read-to-write naming beyond #4a carve-out) ratified — pre-write check refs and FK-pipeline refs use descriptive phrasing, not Tool names. Sub-question (b) #4a carve-out scope (post-write recovery / orphan-detection / post-write verification — all share the property that the operator is already in a write-Tool mental model when they hit the read Tool) ratified as-is, no widening or narrowing. Sub-question (c) the ~25 descriptive-phrasing rewrites (`"BEFORE composing any write payload"` / `"for composing change proposals"` / `"the change-proposal target_doc identifier"` / `"the queue-entry resolution path"` / `"the green-bean field-level mutation companion"`) ratified — collision-avoidance benefit at session-time outweighs the asymmetric discoverability cost for a future reverse lookup. **Follow-up substrate enrichment landed** (Chris's audio surfaced the load-bearing reasoning): [ARBITER.md § Tool description writing convention](ARBITER.md) extended with new **Opening principle** paragraph at top of section locking the framing — *"Tool descriptions are claude.ai-facing, not human-readable docs"* + the explicit instruction to optimize for claude.ai's `tool_search` retrieval at session-time, not for human readability. Chris-as-human is explicitly NOT the reader (he doesn't insert data via MCP — claude.ai sessions do); substrate docs (CONTEXT.md / ADRs / cluster docs / ARBITER.md) carry the human-readable layer. This framing justifies the strict #4 rule downstream — Chris-as-human loses nothing from the descriptive-phrasing rewrites because Chris-as-human is not the reader; claude.ai gains the collision-avoidance benefit. The principle is now the foundation that makes the 4-part rule + #4a carve-out structurally coherent.
- **Item 10 — Half-migrated lot taxonomy** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 10). **Audio-ratified by Chris. No substrate action.** Reframed as **architectural-transition artifact, not observing trend.** The CGLE Sudan Rume Natural V5 lived case (`push_roast_profile` fired without `push_roast_recipe`) is a byproduct of the substantial substrate changes happening mid-lot — V5's lifecycle spanned the period when the roasting-side architecture itself was being built (post-Sub-Pages-6 `roast_recipes` entity introduction + Wave 1 through Wave 4 PR 4b sub-skills decomposition + multiple prompt rewrites). Chris's framing: "this is more because we've made so many changes artifact, not like an observing trend or whatever. Roasting is a very long cycle thing, so these things take a while... most of the stuff on the roasting side wasn't even built in the app itself when I started CGLE Sudan Rume Natural." Long-cycle lots in flight during architecture changes are EXPECTED to show drift artifacts; the V5 case doesn't predict structural recurrence. **Promotion trigger** if recurrence justifies real detection work: see this pattern on a **net-new lot** that started AFTER architecture stabilization (post-current Wave 4 PR 4b state). Until then: no observing-list entry, no STAGE 0 detection logic, no typology. Round 14's Gesha Clouds STAGE 1(b) backfill (Item 36 in this same Group 4 — drained separately) is similarly a transition artifact, not net-new pattern signal. If a net-new post-stabilization lot exhibits the pattern, re-open as a fresh grilling-queue item with N=1 lived case under the new framing.
- **Item 12 — Candidate run-off pourover concept needs a name** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 12). **Audio-ratified by Chris.** **Absorbed by Item 7's Simulated Pourover Gate rename** — "candidate run-off pourover" is pre-rename (Round 10 / 2026-05-22) vocabulary for the SAME lived workflow that Item 7 / Group 2 grill locked as Simulated Pourover Gate. Chris's audio elaboration confirmed the overlap: same lived shape (V_n leading + V_n secondary + V_(n-1) winner brewed at a non-optimized close-to-end-state recipe to break a tie before committing the reference roast), same lived motivation (the xBloom-to-end-optimized-pourover delta is large enough that picking the winning candidate at xBloom_gate is not always the same as picking the winner at end-state, and running full optimization for each finalist is too expensive). **Schema scoping audio-confirmed as already locked**: `cuppings.eval_method = 'Simulated Pourover'` is the right shape; no separate runoff wrapper, no candidate-runoff sub-workflow concept, no separate `is_run_off` / `recipe_variant: 'run_off'` / `run_off_brew_recipe` field. Chris's framing: *"This should just slide cleanly into the existing cupping structure, and it slots cleanly just through that, um, cupping evaluation method."* The migration ships as part of the future POD-1 rewrite, not now (cluster/pod-1-routing.md § Schema scoping). **Substrate enrichment landed**: [CONTEXT.md § Simulated pourover gate](CONTEXT.md) extended with (a) explicit "Operational economy of SPG over per-finalist full optimization" sub-block capturing Chris's audio "I don't wanna spend the time to do an optimized recipe for all three of those at the end" rationale + the xBloom-to-end-optimized delta as the structural reason SPG exists; (b) Phase 2 / Item 12 re-confirmation of the cuppings.eval_method = 'Simulated Pourover' scope decision + the explicit "no separate wrapper" framing; (c) **Aliases** sub-block locking "candidate run-off pourover" (pre-rename Round 10) and "runoff competition" (Chris's lived speech 2026-05-24) as the same workflow — different framings (runoff emphasizes the tournament shape; SPG emphasizes the recipe shape), same event. Future grilling queues searching for "candidate run-off" or "runoff" will find the canonical SPG vocabulary via the alias block.
- **Item 13 — Rest-days drift as structured field vs prose-only** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 13). **Audio-ratified by Chris.** Alternative 1 (stay prose-only) locked. Reasoning: drift is a relational concept (delta between rest_days of two cuppings being compared), not a per-row property. `cuppings.rest_days` already captures absolute age per cupping (since migration 001); drift is derivable at query time via SQL JOIN on `green_bean_id` + rest_days delta. A per-row `rest_drift_flag` / `rest_drift_days` field would duplicate derivable data with implicit comparison reference. The existing `REST_DAYS_DRIFT:` prose prefix in `additional_notes` catches the at-comparison-moment hedge where it matters operationally. If cross-lot drift analysis becomes a real workflow, build a query-time SQL view (deferred Alternative 5), not a per-row field. **Follow-up substrate enrichment landed** (Chris's audio elaboration on two sources of drift): (a) [CONTEXT.md § Rest-days drift](CONTEXT.md) — new glossary entry as sibling to **Rest behavior** capturing the concept + the two structural sources (within-V-set scheduling drift ~1-2 days managed vs cross-V-set comparison drift 7+ days structural) + schema scoping decision (prose-only locked) + _Avoid_ block. The Sudan Rume V5A (Day 9) vs 169 (Day 17) case is named as canonical lived instance for source 2; (b) [docs/prompts/log-cupping.md](docs/prompts/log-cupping.md) `rest_days` field block extended with the two-source distinction + new `REST_DAYS_DRIFT_COMPARISON:` prefix for the cross-V-set case (separate from the existing `REST_DAYS_DRIFT:` prefix used for within-V-set scheduling drift) — denotes structural unfairness in the comparison frame, important because no structured experiments yet exist on rest-days effects across the 7+ day magnitude. Chris's note "nothing for you to do" for source 2 specifically meant no schema change needed; the awareness context still warranted substrate capture so the prompt + glossary preserve the distinction for future cuppings.
- **Item 17 — Roasted-variant-of-same-green workflow modeling** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 17). **Audio-ratified by Chris with stronger lived-evidence count than queue baseline.** Alternative 3 (FK link `green_beans.peer_reference_brew_id`) locked over Chris's earlier "keep separate" instinct, after his audio surfaced N=5+ lived instances rather than the queue's N=1-2: (1) CGLE Sudan Rume Natural (special-guest roasted variant — same farm, same lot, same time, same everything); (2) Wush Wush (peer-roasted version); (3-N) every bean from Untold Coffee Lab (paired roasted variant for each lot); (N+1) one more upcoming green bean with peer-roasted variant planned. Chris's framing: "*if there's a low friction way to link them but not have this, uh, explode the product surface yet, but keep that connection going forward, I think that'd be a good thing to do, presuming it's a lightweight surface area to take on.*" **Lightweight FK landed**: (a) [migration 069](supabase/migrations/069_green_beans_peer_reference_brew.sql) — `green_beans.peer_reference_brew_id uuid REFERENCES brews(id) ON DELETE SET NULL`, idempotent (ADD COLUMN IF NOT EXISTS guard), no backfill (operator patches existing pairs via `patch_green_bean` after the fact); (b) [lib/types.ts](lib/types.ts) `GreenBean` interface extended; (c) [lib/mcp/push-green-bean.ts](lib/mcp/push-green-bean.ts) + [lib/mcp/patch-green-bean.ts](lib/mcp/patch-green-bean.ts) Zod schemas + Tool descriptions extended — note the patch description specifies that this field is typically set HERE (post-push) rather than at green-bean push time because the peer brew row lands separately via the brewing-side write path; (d) [lib/roast-import.ts](lib/roast-import.ts) `GreenBeanPayload` + `PatchGreenBeanPayload` + `GREEN_BEAN_PATCH_FIELDS` whitelist extended + `persistGreenBean` INSERT row extended; (e) [CONTEXT.md § Peer-roasted reference brew](CONTEXT.md) — new glossary entry as sibling to **Rest-days drift** capturing the concept + ~25-30%+ coverage stat + lived instances + schema (1:1 nullable FK, extensible to join table if many-to-many emerges) + relationship-to-other-concepts (distinct from Reference roast / Reference cup / Control experiment) + _Avoid_ block; (f) [PRODUCT.md § Longer Term Items](PRODUCT.md) — new roadmap entry "Peer-roasted reference brew UI surface" capturing the deferred UI work (resolved-view sub-card + brewing-side workflow at Path C-1 + optional aggregation view) + promotion triggers (lots with FK populated reach 3+ closed-and-paired / brewing-side substrate sprint intersection / claude.ai friction case). No prompt-level changes today (claude.ai can pass the FK in future write payloads when known; otherwise backfilled via patch_green_bean as operator walks existing pairs). Extensibility preserved — if many-to-many becomes a real pattern, migrate to a join table later.
- **Item 26 — Blend-cultivar formal schema modeling** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 26). **Audio-ratified by Chris. No short-term schema work.** Alternative 1 (defer + PRODUCT.md § Longer Term Items entry) locked. Chris-confirmed lived count: ~2 brewing-side blend instances historically (visible as canonical entries on `/cultivars` — "Red Bourbon / Mibirizi blend" + "Bourbon / Caturra blend"); 1 roasting-side blend lot (Mount Elgon Ladies' Lot SL28 + SL14 + Nyasaland from Round 12); no anticipated blend lots in the upcoming pipeline. Chris's framing: "*most of the brewing and roasting side, it all tends to be single lot, single cultivar type stuff. However, that being said, I do run into these every so often, so I do need to have some way to cleanly deal with these.*" Two existing patterns coexist with no plan to unify in short term: (a) brewing-side promotes blends to their own canonical cultivar entry in `lib/cultivar-registry.ts` (treats the blend as a distinct cultivar string, loses composition resolution); (b) roasting-side 4-step operator-driven (representative canonical + free-text variety + propose_canonical_addition for net-new + comma-string to Roest), documented in one-shot.md + start-lot.md STAGE 1. **The real future trigger is the WBC blending research project** — Chris flagged that blending is a big concept in WBC reference documents (winners blend across cultivars / farms / roasts), and he plans to experiment with structured blending as a future research direction. Schema work at that point needs to support blending as an experimental variable (likely a `lot_compositions` join table or `green_beans.blend_composition jsonb`), not just static lot composition. **Substrate enrichment landed**: [PRODUCT.md § Longer Term Items](PRODUCT.md) — new roadmap entry "Blend-cultivar schema modeling + future blending research framework" capturing both existing patterns + the WBC research project trigger + cross-ref to existing [CONTEXT.md § Same-green roast layering](CONTEXT.md) for the same-green roast-layering experimental framework already scoped. No CONTEXT.md glossary work (the existing brewing-side and roasting-side patterns are already well-documented; the WBC blending research framework belongs to a future sprint).
- **Item 30 — One-shot kickstart prompt symmetry** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 30). **Audio-ratified by Chris. No substrate action.** Alternative D-equivalent locked (status quo, no clarifying note, no README, no wrapper files). Chris's friction at Round 13 ("do I just use log-roast for one-shot?") was a first-encounter learning moment specific to his initial one-shot lot (Mount Elgon Ladies' Lot, the first lived one-shot run) — operator mental model defaulted to V-set's 4-named-prompts pattern and surfaced the asymmetry. Now that the one-shot workflow is internalized ("the loop is pretty straightforward, it's more of a sequential loop, onboard, one roast, cupping seven days, then throw it to the brewing cycle"), the operator-side ambiguity is resolved without substrate work. Chris's explicit framing: "*I just need one prompt for that one. I don't have to remember at that anywhere else. I have it saved. I can keep using it. I don't think we need a read me for operator templates or anything. I honestly think this is a concept that we don't need to integrate, and I just need to use the same one each time, which I got now.*" The structural asymmetry between V-set (multi-stage iteration → multi-prompt symmetry) and one-shot (single in-flight loop → single prompt) is correct and intentional; no formalization needed. If a future operator (or claude.ai picking up cold) re-encounters the same friction, the lived path is "open one-shot.md for all in-flight stages, one-shot-closeout.md for close-out" — re-litigating the substrate question only if recurrence patterns suggest the friction isn't a one-time learning moment.
- **Item 25 — Inventory doc roast-strategy bullets vs CCIL (Group 5) + Operator-fixed constants finding** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 25 with significant scope expansion). **Audio-ratified by Chris (α — ship Item 25 as scoped + capture the operator-fixed constants finding as a Group 5 sub-finding).** Phase 1 — Item 25 as scoped: alternative (b) locked — HOLD re-confirmed pending Day 7 Mt Elgon cupping (~2026-05-30); two-branch re-open logic locked (Branch A = ship propose_doc_changes pass on V5 inventory doc; Branch B = re-grill for terroir-scoped guidance). Phase 2 — operator-fixed constants finding: Chris's audio reply surfaced a substrate gap I didn't have visibility into. The Rancho Tio canonical "125°C hopper" correction matches Chris's lived BBP alert at 125°C (load beans into shoot/hopper), and Chris also charges at 117°C (drop beans into drum) — both constants operator-fixed across every roast, never varied. The substrate (between-batch-protocol.md) already documented the workflow but didn't name these as "operator-fixed constants" with the consistency-by-default + design-around-not-vary principle locked at vocabulary level. Substrate proposals to vary `hopper_load_temp` from 125 or `charge_temp` from 117 across batch slots were being silently ignored by Chris's muscle memory — silent drift between substrate-intent and operator-execution. **Substrate landed** (h-3 — both CONTEXT.md glossary + protocol-doc framing): (a) [CONTEXT.md § Operator-fixed roast constants](CONTEXT.md) — new glossary entry naming the term + lists the 4 constants (10-min warm-up dry run / BBP / 125°C hopper-load / 117°C charge) + lived sequence (turn on → warm-up → dry roast to 140°C → BBP → 125°C alert → load shoot → 117°C drop → roast → 140°C end → BBP → repeat) + consistency-by-default principle + recipe-schema-vs-lived-practice asymmetry note + `_Avoid_` block warning against the silent-drift drift cases; (b) [docs/skills/roest-knowledge/cluster/protocols/between-batch-protocol.md](docs/skills/roest-knowledge/cluster/protocols/between-batch-protocol.md) — added a header callout block framing the existing standard workflow as operator-fixed constants per the new CONTEXT.md entry; (c) [docs/grilling-queue.md Item 25 Outstanding entry](docs/grilling-queue.md) — Branch A action sharpened to reference the operator-fixed constants explicitly (125°C hopper-load + 117°C charge, not just "125°C hopper"); Branch B framing notes that operator-fixed constants stay locked regardless, terroir-scoping happens at recipe-level levers. **Lived precedent captured at substrate-write time**: V5 Sudan Rume Washed empirical finding (load beans at 125°C vs 120°C shifted FC timing by ~35s + FC temp by ~7°C) already documented in between-batch-protocol.md § Hopper Pre-Load Timing — this is what locks the 125°C hopper-load value as not-arbitrary. The constants gap was visibility-of-discipline, not absence-of-protocol. **Implication for future substrate work**: claude.ai recipe proposals + inventory doc roast-strategy bullets + any other roasting-side substrate must respect the operator-fixed constants (write them as defaults; do not propose varying them across V-set slots). If a V-set's structural question genuinely requires varying the constants, the variation must be front-and-center in primary_question + variables + rationale prose — not buried in a recipe field where muscle memory will override. Source PR: TBD (this Group 5 drain).
- **Item 24 — Pre-shipped substrate coordination process (Group 5)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 24). **Audio-ratified by Chris. No substrate ship; defer pending recurrence.** Alternative (d) locked — grade demoted BRAINSTORM → OBSERVING; promotion trigger tightened from fuzzy "wait for pattern to repeat" to concrete failure-case definition: **≥1 lived instance of claude.ai pause-behavior failure (duplicate proposal / operator confusion / collision-caused workflow drift)**. Trigger watches the FAILURE case, not the SUCCESS case — Round 11 Wush Wush hybrid.md ICS entry was a success (claude.ai correctly paused on collision), so proves existing pattern works at N=1. Rejected: (a)/(b)/(c) lock convention now (premature mechanism commit on N=1 success case); (e) light-touch precedent capture in ARBITER.md (queue entry itself preserves the precedent — duplicating in ARBITER.md is over-investment per Pattern J pruning principle). **Chris-audio framing**: this is primarily a machine-layer concern (claude.ai + MCP server + Claude Code coordination), echoes the [Item 16b machine-layer-vs-operator-legibility principle](CONTEXT.md) locked earlier in this session — collision detection is the machine layer's job, Chris-side ergonomics is downstream. **Companion forward-looking watch-item flagged by Chris**: WBC-vocabulary growth (Vannelli Aroma Capture / Wölfl Extraction Push / Ferket Selective Bloom Hybrid + future additions) may eventually create taxonomy collision risk if vocab keeps multiplying past current threshold; if so, a formal naming taxonomy may be needed. Not actionable today; captured in the Item 24 Outstanding-entry promotion-trigger language as a future trigger. Landed as: [docs/grilling-queue.md Item 24 Outstanding entry](docs/grilling-queue.md) — grade demoted + trigger tightened to failure-case definition + WBC-vocabulary watch-item flagged as future trigger + Chris's machine-layer framing captured. Source PR: TBD (this Group 5 drain).
- **Item 20 — Medium-roast handling + dark-roast override discipline (Group 5)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 20). **Audio-ratified by Chris in two phases: (b) trigger tightening + (ε) light-touch substrate addition + (g2) Agtron-guard softening.** Phase 1 — alternative (b) locked: tightened promotion trigger across both substrate destinations (operational-guide hook + CCIL observing list candidate) to "N≥2 more medium-roast specialty naturals where over-extraction-risk-as-roast-character framing **LEADS strategy selection at intake** (not just retroactively confirmed at iteration time)." Bar is intake-time leads, not iteration-time validation. Per Chris-flag at Item 15 grill (Group 2): inventory has darker-than-normal lots expected soon, trigger likely to fire. Phase 2 — Chris's audio expanded scope significantly: roast level can be a TIER-ZERO OVERRIDE signal at the dark end (analog to heavy co-ferment / aggressive process), and the brewing expert should be more forceful about confirming uncertain roast levels at intake. Alternative (ε) + (g2) locked. **Substrate landed:** (a) [operational-guide.md § Roaster roast-level hook](docs/skills/brewing-assistant/cluster/operational-guide.md) — existing hook extended with three-discipline forceful-confirmation sub-block: (1) ask for Agtron reading when uncertainty signals fire (not blocking; framed in practical-constraint context — Lighttells CM200 home-only, 15g insufficient, needs ~30g / two doses); (2) visual-confirmation alternative when Agtron unavailable at this brewing location (Chris's roasting experience supports broad light / light-medium / medium / past-medium distinction visually); (3) cup-side signal recognition (very roasty / bitter / smoke / harsh qualities at lower temperatures as dark-roast tells routing subsequent iterations even when intake-time confirmation was skipped). General principle locked: "more forceful but not blocking." Trigger language updated to align with grilling-queue Item 20 tightening (intake-time leads requirement). (b) [operational-guide.md § Do NOT push Chris guard at line 49](docs/skills/brewing-assistant/cluster/operational-guide.md) — softened with explicit carve-out: existing Item 29 routine-case guard preserved; uncertainty-case carve-out fires when potentially-darker-than-default signal surfaces (roaster product page, visual cues, cup-side tells). Not blocking. (c) [docs/skills/ccil/cluster/observing.md § Medium-roast specialty natural over-extraction lever](docs/skills/ccil/cluster/observing.md) — trigger language aligned with operational-guide tracking (intake-time leads requirement). **Practical-constraint awareness preserved verbatim per Chris's audio**: CM200 home-only / 15g dose insufficient / two-dose extraction / time-constraint cases. **Altitude restructure deferred** — Step 1b signal-table promotion (alternative γ / δ) stays gated by the same Item 20 trigger (≥2 more medium-roast specialty naturals at intake); ships in same future sprint as CCIL pattern promotion. Source PR: TBD (this Group 5 drain).
- **Item 18 — Switch open-drawdown coarsen lever generality (Group 5)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 18). **Audio-ratified by Chris.** Alternative (b) locked — re-confirm OBSERVING with tightened cross-sub-form coverage trigger: **N≥1 confirming brew on at least 2 of {Sequential, Phase-Mapped, Selective Bloom, Temperature-Staged}**. Rejected: (a) re-confirm-as-is (current trigger was fuzzy); (c) split-promote structural insight to Switch-level now (premature — open-phase-as-rinse claim is structurally argued but not lived-confirmed across sub-forms; if Temperature-Staged or Phase-Mapped Hybrid uses the open phase as a phase-banded extraction zone, the structural insight needs amendment); (d) full-promote to Switch-level with caveat (over-claims further); (e) close as ICS-specific (under-claims — structural argument is strong enough to warrant testing, not strong enough to assume generality). The two-part lever structure (structural "open phase = rinse" + tactical "coarsen the bed") gets revisited at promotion-time once cross-sub-form data lands — split-promote vs full-promote decision deferred. Landed as: [docs/grilling-queue.md Item 18 Outstanding entry](docs/grilling-queue.md) — tightened trigger language + Group 5 source-tag added + two-part lever framing surfaced. **Chris audio also surfaced this as a textbook example of the brewing-expert / WBC-reference-document compounding-knowledge loop working in real time** — claude.ai's brewing expert pulled a non-obvious lever (coarsen-for-faster-rinse) from substrate to solve a real cup-side problem (push expressiveness while tamping roast character on a medium-roast specialty natural). Filed as an architecture-validation candidate for the deferred Gap C consolidation (per Item 6 / Group 5 resolution) — not shipped as substrate today, but flagged for the future architecture-retro sprint's kickoff brief. Source PR: TBD (this Group 5 drain).
- **Item 16c — Substrate pruning discipline (Group 5)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 16c). **Audio-ratified by Chris (alternative d + case-study addition).** Chris's lean was toward (a) the active arbiter pass — "we keep adding, adding, adding... but there's no take-away mechanism... we should just have a counterbalance towards taking away and removing things... if you bloat too much of the context window, then performance will decrease" — but agreed (d) light-touch ship is the right floor since the mechanism shape needs lived case studies to crystallize. **Companion addition surfaced by Chris's audio:** case-study-driven generalization methodology — pick 2-3-4 lived pruning candidates, work through with operator + Claude Code back-and-forth, derive generalized rules from the worked examples; explicit analog to the research-project methodology Chris was exercising in parallel (filter arc, 2026-05-21 → 2026-05-26) that landed as the Research Coordinator + Research Assistant pair per [ADR-0017](docs/adr/0017-research-assistant-architecture.md) on 2026-05-27 — Learning Assistant + Learning Knowledge deprecated in the same ship. **Landed as:** (a) [ADR-0013 amended](docs/adr/0013-self-improvement-primitives.md) — title updated `9 patterns` → `10 patterns`; new Pattern J — Substrate pruning inserted inline after Pattern I with full mechanism candidates (mech-a/b/c/d) preserved as scoping options + case-study-driven generalization approach + lean-toward-mech-a as eventual target + cross-ref to ADR-0014 Pattern F as orthogonal complement (F = split, J = prune); Per-sub-skill pattern mapping updated to note Patterns I + J as cross-cutting; (b) [ARBITER.md § Substrate pruning review](ARBITER.md) — placeholder section added explicitly marked "mechanism deferred pending case studies"; walk-cadence locked at "when grilling-queue candidate-list reaches ≥2 entries, schedule a pairing sprint"; (c) [CONTEXT.md § Pattern F decomposition tripwires](CONTEXT.md) — Pattern J inverse-orientation-companion paragraph added; `_Avoid_` extended to warn against treating Pattern F as the only compaction mechanism; (d) [docs/grilling-queue.md § Substrate pruning candidates](docs/grilling-queue.md) — new top-level section opened with procedure + format + Worked-examples sub-section seeded with the PRODUCT.md → docs/sprints/shipped.md case study (verified 2026-05-24 — Chris's recall held: "Recently shipped" is now a 1-line pointer, full log lives in shipped.md); Open candidates sub-section empty pending future surfacing. **Existing case study verified at substrate-write time:** PRODUCT.md § Recently shipped extraction is the existing worked example of mech-a + extract-to-separate-file sub-mechanism; successful in lived practice; preserved as substrate-precedent in the new candidates section. Source PR: TBD (this Group 5 drain).
- **Item 16b — Sub-skill rename to `<X> Expert` framing (Group 5)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 16b). **Audio-ratified by Chris. Closed as no-action.** Chris's audio reframed the question: substrate naming serves the machine layer (Claude Code + claude.ai + MCP server + Latent app routing), not Chris-side legibility. He doesn't reference substrate names in lived workflow speech — he gives claude.ai the prompt and lets the system route. The CONTEXT.md alias entry already bridges his rare reference needs. The original deferred-rename framing implicitly assumed substrate naming should track Chris-side legibility; Chris's audio confirmed it shouldn't, dropping the rename's only real motivator. Rejected alternatives: (a) re-confirm OBSERVING-as-is (kept fuzzy trigger), (b) friction-count trigger (no clear threshold; operational surface too small), (c) Chris-flagged trigger (no expected motivator on the horizon), (d) ship the rename (~150-ref churn without operational signal). Closing as (e) drops the rename candidate entirely. **Companion substrate enrichment landed** (Chris-audio "doesn't really matter, but if anything, I'd lean toward expert" → generalizable principle): the existing [CONTEXT.md § Sub-skill tiers + Master Coordinator + Chris's vocabulary aliases](CONTEXT.md) entry extended with a closing paragraph naming the broader principle — **substrate naming optimizes for the machine layer; human-operator legibility is a non-goal; aliases bridge where Chris needs to reference substrate by name**. The principle generalizes the existing [ARBITER.md § Tool description writing convention](ARBITER.md) "Opening principle" (Tool descriptions are claude.ai-facing, not human-readable docs — Group 4 / PR [#237](https://github.com/chrismccann-dev/latent-coffee/pull/237)) from Tool descriptions to substrate naming broadly (sub-skill names, Resource URIs, cluster file paths, propose_doc_changes target_doc identifiers, etc.). If a third substrate-naming case surfaces with the same shape, promote the principle to a standalone CONTEXT.md entry; for now keeps it co-located with the alias mechanism it supports. Source PR: TBD (this Group 5 drain).
- **Item 6 — Substrate-practice gap audit (Group 5)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 6). **Audio-ratified by Chris.** Window scanned: post-architecture-arc close (Wave 4 PR 4b, 2026-05-21) through 2026-05-24; PRs #213-#242; feedback log Rounds 9-14. Step 4 grill Groups 1-4 drained essentially everything actionable from the recent dog-food window. Three candidate gaps surfaced; Chris ratified the recommendation (close A + B with substrate edits, defer C). **Gap A closed** — grilling-vs-executing rule now surfaced in [CLAUDE.md § Sprint cadence checkpoint 7](CLAUDE.md) (sub-paragraph naming the distinction + pointing at the memory file + naming the Ratification queue recovery mechanism). The rule previously lived only in `feedback_grilling_vs_executing_distinction.md` (memory) + the Ratification queue subsection intro. **Gap B closed** — Ratification queue arbitration surfaced as the 5th queue type in [ARBITER.md § Ratification queue arbitration](ARBITER.md) (R1-R4 steps mirroring the existing observing-list pattern + explicit dual-surfacing note for grill-time-first vs arbiter-walk-also). **Gap C deferred** — Round 14 architecture-validation observations (items 14-17 of Round 14: drop-rules card validated; cluster-doc routing post-Wave-4-PR-4b; cross-session resumption; design-intent/execution-reality two-table model) stay in the feedback log; consolidating them into PRODUCT.md § Lessons Learned deferred to a future architecture-arc retro sprint as part of a deliberate retrospective rather than a one-off entry. Source PR: TBD (this Group 5 drain).
- **Item 2a — Workflow-sub-skill rename promotion trigger tightening** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 5 grill, Item 2a). **Audio-ratified by Chris.** Alternative (b) locked — re-confirm OBSERVING with tightened count-based promotion trigger: `≥2 additional roasting-stage cluster docs` (candidates: cupping-protocol.md / iteration-protocol.md / close-protocol.md). Single-stage doc today gives the umbrella nothing to umbrella over; second additional stage doc is the structural threshold where the umbrella becomes load-bearing. Rejected: (a) re-confirm-as-is (current trigger wording was fuzzy and would re-litigate at every grill); (c) promote rename now (namespace reservation without lived content is overhead); (d) close as "no action" (lived-speech signal is real, rename remains legitimate future option). Lived-speech-based trigger rejected in favor of count-based — count is observable in substrate at grill time, lived-speech requires cross-session dictation tracking. Landed as: (a) [docs/grilling-queue.md](docs/grilling-queue.md) Item 2a Outstanding entry — tightened trigger language + Group 5 source-tag added; (b) [CONTEXT.md § Operator-guide / operational-guide / onboarding-protocol / operational-reference](CONTEXT.md) — Open rename question paragraph extended with the locked Promotion trigger sub-block + rationale (single-stage umbrella has nothing to umbrella over) + the count-vs-lived-speech rejection note. Source PR: TBD (this Group 5 drain).
- **Item 36 — push_roast_profile + push_roast_recipe + push_experiment silently decoupled (Tool coordination gap)** — resolved 2026-05-24 (Sprint R Phase 4 Step 4 Group 4 grill, Phase 2 Item 36). **Audio-ratified by Chris. No substrate action.** Alternative 1 (defer as transition artifact) locked — same disposition as Item 10. Chris's framing: "*That was the same architectural transition. Again, if we see it after this, after all this new architecture set, and we use all these new prompts and everything and this comes out again, we can tackle it at this point. But for this specific one, Gesha Clouds has been going on for quite a while. So it inherits all the changes and everything. I wouldn't read too much into this one either.*" The Gesha Clouds STAGE 1(b) backfill case is architectural-transition-era — the lot's lifecycle spanned the period when the roasting-side architecture (Sub Pages 6.1 `roast_recipes` entity + Sprint R prompt rewrites + Wave 1-4 sub-skills decomposition) was being built. Post-stabilization prompts (`start-lot.md` STAGE 4 + `log-cupping.md` STAGE 5 V_(n+1) design) now correctly mandate the triplet (`push_experiment` + `push_roast_recipe` × N + `push_roast_profile` × N) at V-set design moments. The "structurally recurring" queue framing was misread on the limited evidence — the lived case was an artifact, not a structural Tool-coordination gap. **Promotion trigger** if recurrence justifies real Tool architectural work: see this decoupling on a net-new lot that started AFTER architecture stabilization (post-current Wave 4 PR 4b state). Until then: no new Tool, no transactional coupling, no bundled Tool. If recurrence happens, the 3 fix options (transactional fields on `push_roast_profile` / prompt discipline reinforcement / bundled `push_v_set_design` Tool) are scoped in the original queue entry framing; the bundled Tool (Alternative 4 from this grill) is the cleanest semantic choice because it preserves the existing 3 Tools for legitimate decoupling cases (hardware tests / same-green roast layering / calibration roasts outside any V-set frame).
