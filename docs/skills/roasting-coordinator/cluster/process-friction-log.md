# Process-friction log (append-only)

The cross-lot, cross-session friction log for the Coordinator / V-Set Assistant workflow itself - the meta-layer mirror of the Roasting Brief. The Brief holds what we learned about the COFFEE; this log holds what we learned about the PROCESS (role boundaries that chafed, packets that carried too little or too much, reconstruct steps that failed, gates that fired wrong, anything the architecture made harder than it should be).

Why a file and not a meta-thread: sessions are transient by design (Model B, [ADR-0024 § 2](docs/adr/0024-lot-coordinator-claude-code-native.md)) - a standing feedback thread would rebuild the exact long-lived-session bloat this architecture escaped. Persistent artifact, transient sessions, same trick as the Brief. Instituted as a standing mechanism (not dogfood-only) at dogfood kickoff, 2026-06-11, Chris-ratified: "always good to record any friction points at each turn so each session can improve itself over time."

## Discipline

- **Who writes:** Coordinator sessions append directly, at every natural break (same write-discipline as the Brief). V-Set Assistants do NOT edit this file (role discipline - no doc edits); they report friction in the optional `process friction` line of the Results Packet (see [packet-shapes.md](docs/skills/roasting-coordinator/cluster/packet-shapes.md)), and the consuming Coordinator transcribes it here with attribution.
- **What qualifies:** friction about the WORKFLOW, not the coffee. "The Handoff Packet didn't tell me X" belongs here; "this natural needs less early energy" belongs in the Brief / DB.
- **Entry shape:** one dated bullet - `- **YYYY-MM-DD · <lot-slug> · <role>** - <friction, 1-3 sentences>. <optional: proposed fix>.` Append at the bottom of § Entries. Never edit or remove prior entries.
- **Graduation:** at each lot-close retro the Coordinator scans this log; a friction recurring across lots graduates into a cluster primitive / SKILL.md edit at the N=3 threshold ([ADR-0022](docs/adr/0022-formalization-tax-and-self-improvement-counterbalance.md)); the anti-lawyer-redline safeguard applies (make the skill better, not bigger - [ADR-0023](docs/adr/0023-self-improving-skill-loop.md)). Graduated entries get a trailing `→ graduated: <where>` annotation rather than deletion.

## Entries

- **2026-06-11 · (pre-lot) · Operator+builder session** - Phase B venue question surfaced by Chris: the Phase A build session offered to run lot intake inline at 31% context; the architecturally correct move (fresh session, reconstruct from substrate) had to be caught by the operator rather than self-suggested. Possible signal that the Coordinator SKILL.md "fresh session" entry surface should be stated more loudly wherever a warm session is tempted to adopt the role. N=1, watch.
- **2026-06-11 · (pre-lot) · Operator+builder session** - Process feedback had no durable cross-session home (Brief is per-lot lot-knowledge; retro is at lot-close, weeks away; Assistant can't edit docs). Fixed same-day by instituting this log + the Results Packet `process friction` line. → graduated: this file.
- **2026-06-12 · rwa-nova-an10-rb-2026 · Coordinator (intake + V1 design)** - First Claude-Code-native write session hit the MCP schema-publication bug: every `.optional().nullable()` tool field publishes untyped, the Claude Code client serializes those as strings, server zod rejects. `push_green_bean` + `push_roast_recipe` fully blocked; `push_inventory` reduced to strings-only. Worked around by invoking the same canonical persist functions in-process ([lib/roast-import.ts](lib/roast-import.ts)) with the worktree node_modules symlink. claude.ai sessions never hit this, so it survived 35 tools' worth of dogfooding undetected - the Claude-Code-native assumption of ADR-0024 is exactly what exposed it. Fix spun off as a background task at intake; until it ships, Coordinator sessions need the in-process fallback for any numeric/object write. → resolved 2026-06-13: [lib/mcp/schema-compat.ts](/lib/mcp/schema-compat.ts) — root cause was Claude Code's client dropping `anyOf:[T, null]` property unions (claude-code#5844), not the server publishing untyped schemas; nullable fields now publish as `type: [T, "null"]` type arrays, `npm run check:mcp` gates the catalog. Fresh session (catalog re-handshake) required; in-process fallback then retires. Residual Roest 9962 structured-field patch executed 2026-06-13 via `patch_inventory` from an MCP-connected session — fully closed. Session-cache nuance worth keeping: a session can hold TWO connections to the same server whose cached catalogs differ (one stripped `anyOf` → string-coerced writes that zod rejects; one preserved it → numeric writes succeed); when a write fails -32602 mid-session, try the sibling connection before falling back in-process.
- **2026-06-12 · rwa-nova-an10-rb-2026 · Coordinator (intake + V1 design)** - Sibling-lot intake FK near-miss: findOrCreateTerroir silently resolved Rwanda/Central Plateau Highlands to a Southern Province row (two provinces share the macro; lookup keyed on country+macro), which would have split the Bukure A/B pair across terroir rows and broken the sibling aggregation. Caught only because the Coordinator happened to diff the new row against Lot 21's; repointed via meso-keyed re-resolve, orphan deleted, provenance flipped back to canonical manually. Proposed fix, two layers: find-or-create should prefer an admin_region match when the caller supplies one (registry bug, grilling-queue candidate), and the Coordinator intake step for a declared sibling lot should diff shared FKs (terroir_id / cultivar_id) against the sibling row before proceeding - cheap check, catches the whole class.
- **2026-06-21 · rwa-nova-an10-rb-2026 · Coordinator (V1 Results consume)** - The architecture never specified where V-set Handoff/Results Packets live, and the V1 session surfaced the ambiguity directly ("drop this into a file, or is the paste block enough?"). Resolved: **packets are ephemeral couriers, not artifacts - no file home.** Their durable content already has homes - numbers + the Assistant's interpretation (`key_insight` / `what_changes_going_forward` / `delta_from_cup_*`) land in the experiment row reachable via `get_bean_pipeline`, and the Coordinator's narrative lands in the Brief (whose git history is the audit trail). Confirmed empirically this session: the entire V1 close reconstructed from `get_bean_pipeline` alone; the pasted Results Packet held nothing the DB didn't. Filing packets would rebuild a parallel un-curated history a future session feels obliged to read - the exact Model-B bloat ADR-0024 escaped. The consume step is: fold packet → Brief (+ friction here) → discard raw. Candidate one-liner for [packet-shapes.md](docs/skills/roasting-coordinator/cluster/packet-shapes.md) at the next lot-close graduation (held now per the formalization tax - log first, graduate on recurrence).
