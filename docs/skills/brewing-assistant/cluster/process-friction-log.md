# Brewing process-friction log (append-only)

The cross-brew, cross-session friction log for the **brewing workflow itself**, the brewing analog
of the roasting [process-friction-log](docs/skills/roasting-coordinator/cluster/process-friction-log.md),
instituted with the brewing to Claude Code migration ([roadmap.md Active queue #4](docs/product/roadmap.md), 2026-06-15).
The DB row + `propose_doc_changes` hold what we learned about the COFFEE; this log holds what we
learned about the PROCESS: the entry that didn't trigger, the running tasting-arc block that still
lost the arc across an idle gap, a knowledge-cluster fetch that failed, a gate that fired wrong,
anything the Claude-Code surface made harder than the claude.ai path did.

Why a file and not a meta-thread: a standing feedback thread would rebuild the long-lived-session
bloat the whole architecture avoids. Persistent artifact, transient sessions, the same trick the
roasting Brief uses, scaled down to brewing's short single session.

**Brewing has no Coordinator/Assistant split** (it is one short session, not roasting's multi-week
lot, [ADR-0024 § Context](docs/adr/0024-lot-coordinator-claude-code-native.md)). So unlike the
roasting log there is no Assistant-reports / Coordinator-transcribes step: the brew session appends
here directly at the close retro.

## Discipline

- **Who writes:** the brew session itself, at the close retro (see [`.claude/skills/brew/SKILL.md` § Close retro](.claude/skills/brew/SKILL.md), or the equivalent step when entering via [`start-brew.md`](docs/prompts/start-brew.md)). If the session can't write the repo file (e.g. a constrained mobile context), surface the friction line for Chris to land.
- **What qualifies:** friction about the WORKFLOW, not the coffee. "The entry skill didn't surface on a fresh mobile session" belongs here; "this anaerobic natural needed Suppression" belongs in the DB / the historian cluster via `propose_doc_changes`.
- **Entry shape:** one dated bullet, `- **YYYY-MM-DD · <coffee-slug> · brew** - <friction, 1-3 sentences>. <optional: proposed fix>.` Append at the bottom of § Entries. Never edit or remove prior entries.
- **Graduation:** a friction recurring across brews graduates into a SKILL.md / operational-guide edit at the N=3 threshold ([ADR-0022](docs/adr/0022-formalization-tax-and-self-improvement-counterbalance.md)); the anti-lawyer-redline safeguard applies (make the skill better, not bigger, [ADR-0023](docs/adr/0023-self-improving-skill-loop.md)). Operational-guide edits route through `propose_doc_changes` (the arbiter applies them); the `.claude/skills/brew/` SKILL.md is edited directly. Graduated entries get a trailing `→ graduated: <where>` annotation rather than deletion.

## Entries

- **2026-06-15 · (migration build) · build session** - Log instituted with the brewing to Claude Code entry surface. The two mobile-runtime gates the build could not verify from a desktop worktree, (1) does a fresh mobile CC session surface the `.claude/skills/brew/` skill, and (2) does the long-multi-fact audio-dictation tasting turn work on mobile, are deferred to the first live dogfood brew (4c). Capture both outcomes here on the first real brew; a hard audio gap is the one finding that can block the migration and should be surfaced to Chris immediately rather than logged silently.
- **2026-06-18 · 4c dogfood (El Pilón + El Mango) · brew** - First live mobile-CC dogfood, landed in a fresh desktop review session (the mobile sessions could not write this repo file, so the friction was surfaced verbally and transcribed here per the § Discipline mobile-can't-write path). Two real brews ran end-to-end on mobile CC and pushed clean: El Pilón (Moonwake, 06-17) + El Mango (Snite, 06-18) each landed a brew row + canonical-queue entries + `propose_doc_changes` proposals, all well-formed, none errored or orphaned. **Gate 4a (skill-loading):** resolved-pass with a nuance - the `/brew` slash invocation surfaces the skill reliably on mobile; natural-language auto-trigger ("brew a coffee") was inconsistent across sessions. The reliable mobile entry is the explicit `/brew` command. *Watch-item (N=1, not yet graduated): if NL auto-trigger keeps missing on mobile, the `brew` skill description is the place to tune - graduate at N=3 per [ADR-0022](docs/adr/0022-formalization-tax-and-self-improvement-counterbalance.md).* **Gate 4b (audio parity):** resolved-pass-with-workaround, NOT a blocker - the Claude **mobile app's** audio-dictation button is unstable under Claude Code; operator workaround is to dictate an audio note to self, then paste the transcribed text into the thread. Operator-managed, low-friction. The fix is **Claude-mobile-app-side; nothing actionable on the Latent substrate** (logged here only because the log captures anything the CC surface made harder, external fix or not). **Overall:** operator found the CC brewing flow straightforward and prefers it over the claude.ai path. → graduated: Active queue #4 flipped to ✅ GRADUATED (dogfood closed), [roadmap.md](docs/product/roadmap.md) + [shipped.md](docs/sprints/shipped.md), 2026-06-18.
- **2026-06-25 · redplum-cas-2026 (El Paraiso Red Plum) · brew** - `push_brew` from the **default `mcp__latent-coffee__*` connector failed on every numeric/array field** (`dose_g`/`water_g`/`temp_c`/`pours`/`modifiers`/`flavors`/`structure_tags`/`key_takeaways` all rejected as `expected number/array, received string`). Root cause: that connector publishes an **untyped** input schema (those fields are `{}` with no `"type"`), so the Claude Code harness passes them as raw strings; only `terroir`/`cultivar` carry `type:object` and parsed. Server zod then rejects the strings. Same MCP string-coercion class as the `push_roast_recipe` bezier fix (commit 4cb1f6c). **Workaround that worked:** a *second* connector registration of the identical Latent server, `mcp__eeaa2042-…__*`, publishes a **fully-typed** schema, so the harness serializes types correctly - re-issuing the identical payload through it pushed clean (verified same backend DB by reading the same green_bean row through both). The same applied to `patch_brew` + `propose_doc_changes` (typed variant used). **Proposed fix (out of scope, spawned as a task):** publish the typed JSON schema on the canonical `latent-coffee` connector (or coerce stringified scalars/arrays server-side in `push_brew`/`patch_brew` as was done for `push_roast_recipe`), so the default connector works from Claude Code without the operator/agent needing to know the typed-connector trick. *Watch-item (N=1): every CC brew/roast push hits this on the default connector until fixed.*
- **2026-06-26 · hachi-yunnan-fanhua (Syrina, Coffee with Dongze/Dongzhe) · brew** - A clean 3-brew office Extraction-Push dial-in (recorded as brew `9ca79dbf`), but **completion was blocked ~25 min on a code deploy because the variety was net-new.** Syrina is strict-canonical with no override path, so `push_brew` could not resolve it until a registry edit (`docs/taxonomies/varieties.md` + `lib/cultivar-registry.ts`) was merged to `main` AND a Vercel **prod** deploy propagated it to the MCP server - two merge→deploy cycles mid-brew. **The first registry edit failed the prod build:** the provisional entry used `family: 'Unresolved (provisional)'`, which is not a member of the strict `GeneticFamily` union (compiled fine, failed at type-check); fixed by parking Syrina in `Modern Hybrids` per the Mandela precedent and carrying provisional status in the free-text `lineage` + notes. *Lessons:* (a) when adding a provisional cultivar, `family` MUST be a valid `GeneticFamily` member - park unclassified varieties in `Modern Hybrids` (Mandela precedent) and run `npx tsc` before merging; (b) recording a brew of a net-new variety is inherently a deploy-gated round-trip today (the strict-no-override cultivar axis is deliberate, but the cost lands mid-brew) - *watch-item (N=1): if net-new-cultivar brews recur, consider an arbiter discussion on a cultivar override+queue path, or a faster registry-promotion route.* **Second friction:** the canonical home for an Extraction-Push data point, `by-strategy/extraction-push.md`, is **H1-only (no `##`/`###` anchors)**, so `propose_doc_changes` cannot target it; the confirmation was routed to `cross-coffee-insights.md` Pattern #2 instead. *Proposed fix: give the `by-strategy/*.md` docs a `## Confirmed brews` (or similar) section anchor so per-strategy data points can land in their natural home.* **Minor:** `operational-guide.md` Step 1d still tags Extraction Push "Empty in this archive ... awaiting first confirmed brew" - stale (Sidra confirmed it earlier; this is the 2nd). **Connector note (N=2 confirm of 2026-06-25):** the typed `mcp__eeaa2042-…__*` connector was used throughout - `push_brew` / `patch_brew` / `propose_doc_changes` all serialized + landed clean, reconfirming the typed connector is the working CC path until the default connector's schema is fixed. → resolved (2026-06-26): the net-new-cultivar deploy round-trip is closed by the `cultivar_override` write path (mirrors `producer_override`; provisional genetics + arbiter queue, no deploy) + a brew-SKILL build-hygiene guardrail (never registry-edit/deploy mid-brew, never merge a red build), shipped PR #517. The `by-strategy/*.md` H1-only anchor gap + stale operational-guide Extraction-Push note remain open.
- **2026-06-30 · helm-el-burro-lot16 (Helm, Panama Gesha Natural) · brew** - Clean 3-brew home Extraction-Push dial-in (reference recorded as brew `24e05434`); pushed + patched + 3 `propose_doc_changes` all landed clean via the `mcp__Latent_Coffee__*` connector (typed scalars/arrays serialized fine - the working CC path). **Two of the 06-26 open items surfaced again; one now actionable, one refuted:** (1) **operational-guide.md Axis-1 table's stale "Empty in this archive (awaiting first confirmed brew)" on Extraction Push** actively misled the brief into labeling this brew the *first-ever* Extraction Push, when extraction-push.md already carried the Pepe Jijón Sidra (this is the 2nd overall, 1st on a Gesha). Caught at completion, brew record patched, and **proposed a fix this session** (`propose_doc_changes` 12199dae - point the cell at the live by-strategy doc instead of asserting a count) → closes the 06-26 "stale operational-guide note remains open." (2) **The 06-26 "by-strategy/*.md is H1-only, cannot be targeted" claim is REFUTED:** an `append` to the H1 anchor `Coffees That Confirmed Extraction Push` resolved cleanly (`anchor_resolved: true`) - per-strategy data points CAN land in their natural home via the H1-anchor append, no `##` section needed. The 06-26 session routed around a non-problem. *Net: the by-strategy data point landed where it belongs this time.* **Minor:** the Step-2 recipe named a vague filter ("Cafec flat-bottom paper") that has no 1:1 canonical - resolved to `CAFEC Wave Paper Filter 250` at completion. *Watch-item (N=1): when the brief picks a filter, name a canonical-resolvable one (read_canonical("filters")) so completion isn't an interpretation step.*
- **2026-07-01 · hachi-yunnan-yinxue (Syrina, Dongzhe Enzyflow Honey) · brew** - Third variant of the Dongzhe × Hachi × Terroir Maximus set (recorded brew `2729194b`; unresolved — roast-development ceiling confirmed on 2 of 3 variants, note filed to the Dongzhe roaster card). Two whole-doc `read_doc` calls **overflowed the MCP token ceiling** and had to be salvaged with a save-to-file + `jq` workaround a real mobile session couldn't do: (a) `read_doc("docs://brewing/freezer-stock.md")` — the freezer-stock table has grown past the whole-doc read limit, but the `/brew` SKILL.md Step 1a still instructs a plain `read_doc` of it for the purchased-coffee lookup; (b) `list_recent_brews(limit:25)` — pulled wide to find the sibling FanHua/LiuGuang arc, then jq-filtered by roaster/coffee_name. *Proposed fixes: (a) freezer-stock lookup should move to `read_doc_section` by `##` heading (each bag is already an h2), or SKILL.md Step 1a should enumerate via `list_doc_sections` + read the single matching `##` section rather than whole-doc read — otherwise the freezer lookup hard-blocks on mobile as the table keeps growing; (b) reconfirms the standing "filtered fetches belong on a future `query_brews` Tool" note — sibling-variant lookup (same roaster + coffee family, prior brews) is the recurring shape. Watch-item (N=1 for the freezer-stock overflow; the query_brews need is long-standing N≥2).* → graduated (freezer-stock facet): [SKILL.md Step 1a](.claude/skills/brew/SKILL.md) + [start-brew.md](docs/prompts/start-brew.md) enumerate-then-read-section, 2026-07-27; query_brews facet → graduated: `query_brews` MCP Tool shipped 2026-07-27 (filters: roaster / coffee_name / extraction_strategy / base_process / cultivar / since_date / green_bean_id + `fields` projection). **No-block note:** the MCP connector dropped mid-session during Brew 1→2 iteration and reconnected a turn later; because brewing iteration is in-thread by design, nothing was lost — positive confirmation of the running-tasting-arc discipline surviving a live disconnect.
- **2026-07-10 · helm-nogales-mystic (Helm, Colombia Los Nogales Yellow Bourbon Anaerobic Washed) · brew** - Clean home dial-in, reference recorded as brew `ac916f61` (Hybrid ICS on the Hario Switch — a strategy pivot from the Clarity-First Brew 1 plus a built-water triple); `push_brew` + a 3-citation `propose_doc_changes` (`bf01676b`) landed clean via the `mcp__latent-coffee__*` connector once it was live. **Three frictions:** (1) **Connector reachability blocked the terminal write.** This was a *remote cloud* CC session; the `latent-coffee` MCP is a "Desktop · Local dev" connector and was unreachable / needed re-authorization for a stretch right at the completion step, so `push_brew` + `propose_doc_changes` hard-blocked until Chris re-attached it and it appeared in this session's tool catalog. Because brewing iteration is in-thread, **zero tasting data was lost** across the drops (again confirming the running-arc discipline) — but unlike the 2026-07-01 mid-iteration drop, this one hit the *deliverable write* and did temporarily block it. *Watch-item (N=2 connector instability): the completion write needs the connector live; a remote CC session cannot reach a local-dev desktop connector, so brew completion may have to wait for / prompt a reconnect.* (2) **Freezer-stock whole-doc read overflow — N=2 confirm of 2026-07-01.** The freezer-stock table (now 103 coffees) still overflows a whole-doc read (truncated ~491/754 lines at the 25k-token cap); I only avoided a block because the Mystic entry sat on page 1. Reconfirms the standing fix: SKILL.md Step 1a should read the single matching `##` bag section via `list_doc_sections` + `read_doc_section`, not a whole-doc read — the freezer lookup hard-blocks on mobile as the table grows. *Approaching N=3 graduation.* → graduated: [SKILL.md Step 1a](.claude/skills/brew/SKILL.md) + [start-brew.md](docs/prompts/start-brew.md) enumerate-then-read-section, 2026-07-27. (3) **No water-concentrate inventory (Chris-flagged).** The Step-2 home water suggestion has no record of which salt stocks Chris keeps on hand (hydrate forms, stock concentrations), so every build requires asking — and a hydrate-form mix-up (anhydrous vs hexahydrate) changes the dose ~2x. Chris flagged it explicitly to log for feedback. *Proposed fix: a small concentrate-inventory doc (a freezer-stock analog for water salts/stocks) that the `/brew` Step-2 water suggestion consults, or a "current stocks" block in water.md — so the build is tailored to what Chris actually has and the dose math is unambiguous.* **Positive:** the built-water triple ran cleanly on the fixed reference recipe and gave the water library its controlled 2nd row (Mystic flipped toward the MgCl₂+MgSO₄ blend vs the Pink Bourbon's MgCl₂-solo); and the Brew-4 flat-cup traced cleanly to re-boiled stale kettle water (a water-freshness variable now logged to water.md § 5).
- **2026-07-22 · no5-classic-crema (NO°5, Tanzania Kifaru Bourbon Washed) · brew** - Mobile CC session fully resolved the brew but **could not land the completion write: the Latent MCP connection dropped mid-session and the session concluded "server down" when it was partly a wrong-name lookup.** Two stacked causes: (1) the session's `ToolSearch select:mcp__latent-coffee__push_brew` searched a tool name that does not exist on the account-level connector — since the 2026-07-10 connector-canon flip, the sole connector surfaces tools under the connector-UUID prefix (`mcp__eeaa2042-…__*`), so the friendly-name select was a guaranteed miss, not evidence of an outage; (2) after the genuine mid-session drop, the deferred-tool catalog did not repopulate in-session (the standing MCP-catalog-cache-needs-fresh-session behavior), so keyword retries searched a stale catalog. The session did the right thing operationally: froze the complete payload in-thread and stopped retrying. A fresh desktop session landed it next day (brew `91b546bb` + 3-citation `propose_doc_changes` `1d456cfa`, zero warnings, ~10 min including canonical lookups — the frozen-payload discipline again proved zero-loss across a disconnect). **This is the N=3 occurrence of connector-unreachable-at-the-completion-write (2026-06-25 untyped-schema block, 2026-07-10 remote-session reachability block, this) → graduation due per [ADR-0022](docs/adr/0022-formalization-tax-and-self-improvement-counterbalance.md):** the `/brew` SKILL completion step should carry the recovery recipe — if the Latent connector is unreachable at completion: (a) search tools by keyword or the UUID prefix, never `mcp__latent-coffee__*`; (b) one fresh-session retry before declaring the server down; (c) freeze the full payload in-thread and land it in the next session. → graduated: [.claude/skills/brew/SKILL.md § Completion](.claude/skills/brew/SKILL.md) connector-recovery recipe, 2026-07-22.
- **2026-07-27 · untold-fazenda-um-pink-bourbon (Untold Coffee Lab, Brazil Pink Bourbon Dark Room Natural) · brew** - Clean 3-brew home dial-in resolved to Hybrid ICS (brew `91a014fd`); `push_brew` + 3 split `propose_doc_changes` (`c6ad5939` / `9f844879` / `fa6c84c4`) all landed first try with zero warnings via the typed connector. **Two standing frictions both hit N=3 this session — graduation due on both:** (1) **Freezer-stock whole-doc read overflow — N=3 CONFIRM** (N=1 2026-07-01, N=2 2026-07-10). The table is now 107 coffees / 786 lines and a whole-doc read truncated at 487 lines; this session only recovered by falling back to `grep`, which a mobile session cannot do, and the target bag sat at line 585 — *past the truncation point*, so on mobile this lookup would have hard-blocked outright rather than degrading. The fix has been specified identically three times and should now land: `/brew` SKILL.md Step 1a must enumerate via `list_doc_sections` and read the single matching `##` bag section via `read_doc_section`, never a whole-doc `read_doc`. → **graduated:** [SKILL.md Step 1a](.claude/skills/brew/SKILL.md) + [start-brew.md](docs/prompts/start-brew.md) enumerate-then-read-section (`list_doc_sections` → `read_doc_section`), 2026-07-27, via the route-feedback → plan-feedback pass on the retro's kickoff brief. (2) **`list_recent_brews` has no filter/projection — N=3 for the standing `query_brews` need** (logged N≥2 2026-07-01). Called twice here and blew the token ceiling both times (112K then 239K chars, both routed to file + `jq`): once to check whether this coffee had been brewed before, once to pull the sibling Fazenda Um lot's terroir/producer so the same farm would resolve to the same canonical FKs. Both are the same recurring shape the 07-01 entry named — *sibling-variant lookup by roaster + coffee family*. A `roaster` / `coffee_name` filter arg, or a `fields` projection, would have made each call a few hundred bytes. → **graduated:** `query_brews` MCP Tool shipped 2026-07-27 (option (b) per the kickoff brief's recommendation — new Tool, 38→39): filter axes roaster (canonical-resolved via ROASTER_LOOKUP, substring fallback) / coffee_name / extraction_strategy / base_process / cultivar / since_date / green_bean_id, trimmed default projection (identity + core recipe + FK-joined terroir/cultivar/green_bean) with `fields` as opt-in widener; the Untold sibling-lot question now costs a few KB instead of 239K. Lookup path added to [operational-guide.md § Archive lookup paths for 1c](docs/skills/brewing-assistant/cluster/operational-guide.md). **Minor:** `by-strategy/hybrid.md` is H1-only, and the H1-anchor `append` resolved cleanly — third confirmation that the 2026-06-26 "cannot target H1-only by-strategy docs" claim stays refuted; no `##` section needed. **Positive:** the deliberate diagnostic Brew 1 (single-mode on the Switch with the valve held open, so the pivot changed only valve behavior and not brewer geometry) is worth keeping as a technique — it established that this lot had no roast tail, which prevented committing to the roaster card's prior and starving a clean back half. Operator overrode two assistant calls (1:45 steep over 2:00; a late cut the assistant had advised against) and was right on both, which is the substantive record in the brew row rather than process friction.
- **2026-07-27 · untold-finca-deborah-interstellar (Untold, Panama Finca Deborah Geisha Natural) · brew** - Resolved in 4 brews (reference recorded as brew `c818dfcb`, 5-citation `propose_doc_changes` `0327dc8f`, both clean, zero warnings), but **three doses were spent on a Coffee-Brief hardware miss that the archive already contained the answer to.** (1) **The brief carried a precedent's STRATEGY forward without carrying its VEHICLE.** Step 1d correctly routed to Hybrid / Intensity-Clarity Split by citing the two Untold roast-tail resolutions (WWNAT, Janson Pacamara) — then specified the April Switch, when *both* cited precedents ran a cone switch + fast paper, and *all five* April Switch data points in the corpus are light roasts. The brief even reasoned explicitly about the CONE-FAST "starves a light-roast back half" warning and mis-applied it to a developed-roast lot, which is the case the fast rinse is *for*. Because the ICS open phase is defined as a true fast rinse and the April Switch cannot drain fast enough to produce one, the architecture never actually ran on Brews 1-3; three brews of grind / temperature / cut tuning moved nothing, and Brew 4 (vehicle-only change, all else identical to Brew 1) resolved it in one move. *Proposed fix: when Step 1d leans on a precedent lot to pick a strategy, the brief must carry that precedent's **brewer + filter** forward alongside it, and state explicitly + justify any deviation. A strategy citation that silently swaps the hardware is not the same citation. Watch-item (N=1), but note the failure was **cheap to prevent and expensive to discover** — the corroborating pattern (5/5 April Switch data points are light roasts) was sitting unremarked in the corpus the whole time.* (2) **The Temperature-Staged Phase-2 spec was ambiguous in a way that invalidated its own test.** Brew 2's recipe said "open valve, pour the cooled water to 250g" without flagging that pouring cool water *into a still-draining hot bed* blends the two temperatures (~91°C actual vs 88°C spec) rather than staging them — so the brew was not a clean test of the hypothesis it existed to test. Chris caught it unprompted from the bench. The archive does contain both execution shapes (pour-through on the April Switch data points; drain-then-**re-close** on Gesha Clouds / Component Terra) but never names the distinction or says when each applies. *Proposed fix: document the two Phase-2 shapes in [hybrid.md](docs/skills/brewing-historian/cluster/patterns/by-strategy/hybrid.md) — pour-through (blends, acceptable when the temp delta is not the variable) vs drain-then-re-close (true staging, required when the split IS the hypothesis) — and have the Step-2 recipe name which one it wants.* (3) **Meta-friction worth naming: both misses were caught by the operator, not by the process.** The vehicle miss and the staging-execution miss were each surfaced by Chris from bench observation (he measured the two switches' drain rates bare, over a sink), and the session's own diagnostic loop was converging on a *different* and weaker hypothesis (brewer "integration" as the culprit) when he redirected it. The brief's archive reads were thorough on strategy priors and thin on equipment priors — worth watching whether that asymmetry recurs. **Positives:** the graduated 2026-07-22 connector-recovery recipe worked first try — `mcp__latent-coffee__*` was dead in this session's catalog (the friendly-name prefix again), the UUID-prefixed connector was used directly with no retry loop, and both writes landed clean. The running tasting-arc block survived four iterations and a strategy re-zone with no loss. Chris's own bench experiment (a pinch of canning salt in the finished carafe) produced the single largest cup improvement of the arc and independently reproduced the archive's Gesha NaCl row — logged to water.md § 3 with a scope refinement (bitterness *masking* is a distinct use case from phase *revealing*, and is legitimate post-brew). **Freezer-stock note:** the N=2 whole-doc-read overflow did NOT recur here only because this was a desktop session with local repo access (grep, not `read_doc`) — the mobile path is unchanged and still blocks. **Superseded same-day by the sibling `untold-fazenda-um-pink-bourbon` entry directly above, which drove this to N=3 and a hard mobile block (target bag past the truncation point) — graduation is now DUE, not pending.** That sibling session is also adjacent corroboration for finding (1) here: same roaster, same developed-roast band, same day, resolved to ICS on the V60/Hario Switch + fast paper without the three-brew detour this session took on the April Switch.
