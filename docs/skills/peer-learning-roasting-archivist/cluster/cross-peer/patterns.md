# Cross-Peer Patterns

**Status:** Placeholder. **N<3** (only Dongzhe profile authored). Activate when ≥3 peer-roaster profiles exist and converge on a shared directional principle that isn't already in the per-peer entries.

---

## Promotion criteria

A pattern earns a cross-peer entry here when:

1. **≥3 peer profiles in cluster/per-peer/** carry the same finding (machine-conditioned or process-conditioned)
2. The finding is **stronger when aggregated** than when read per-peer (i.e., the convergence itself is the signal — not the individual claims)
3. The finding is **not already** in Roasting Historian § Cross-Coffee Insight Layer (which aggregates Chris's own lots, not peer lots)

Patterns failing all three stay in their per-peer profile only.

---

## Current state

Today: one peer profile authored (Dongzhe). No cross-peer pattern qualifies. This doc exists as a forward investment per [Peer-Learning Roasting Archivist SKILL.md](../../SKILL.md) cluster spec — it's read first when scoping "do any peer roasters converge on X?" but returns the empty-state pointer until N≥3 lands.

When the second peer profile lands (next operator-initiated Pattern I session), revisit this doc to scope whether any directional principles overlap between profiles. When the third lands, gate-check + populate.

---

## Empty-state pointer

For peer-derived directional principles today, read [cluster/per-peer/dongzhe.md](../per-peer/dongzhe.md) directly. The "Core principles" + "Five operational deltas" sections are where Dongzhe's transferable framing lives.
